You are hexe lane **P4-fix1** for tinytop. Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is `c98ca1c` = v0.4.1 + T10-fix2 + T11 OpenTelemetry push export incl. its fix round). Fix ONLY the findings below — the Phase-4 deep dual-blind review (hexe runs 633/634, validated at source by Fable: Fabulous `docs/fleet/tinytop/2026-08-29-ari-dual-blind-phase4-review.md`). Files you may edit are named per finding; NOTHING else. **Git is read-only for you — no commit, no branch, no stash, no `cargo fmt` on the tree (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` only and fix by hand); the orchestrator commits.** No edit to `agent/Cargo.toml`, `agent/Cargo.lock`, any `crates/*/Cargo.toml`, `src/`, `legacy/`, `VERSION`, `CHANGELOG.md` (Fable writes the fix-round line at merge). Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; never set `TINYTOP_OTEL_HEADERS`, `OTEL_EXPORTER_OTLP_HEADERS` or `OTEL_EXPORTER_OTLP_METRICS_HEADERS` in your own process (`std::env::set_var` is forbidden — child-process `Command::env` is the only way to set a variable in a test). **Known sandbox limits — not failures:** every `serve_*` test binds a port your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); Bun's `tests/server.test.ts` likewise; `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. If a finding's contract is wrong against the code you read, or you would be guessing, STOP and reply `ESCALATE: <what, what you tried, where you stopped>` — escalating early beats a wrong fix.

**Read first:** `briefs/T11.md` + `briefs/T11-fix1.md` (the design and the fix contracts you are extending), ADR 0016 (`docs/adr/0016-configuration-export-import-document.md`; the document never carries a secret), ADR 0015, spec §12 (`docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md:205-212`), `agent/crates/tinytop-store/src/otel_settings.rs`, `agent/crates/tinytop-store/src/lib.rs` (`get_settings_on`, `put_settings`), `agent/crates/tinytop-store/src/settings_transfer.rs` (`plan_import`, `apply_import`), `agent/crates/tinytop-agent/src/writer.rs` (`update_settings`, `spawn_otel_export_loop`, its `cfg(test)` loop tests), `agent/crates/tinytop-agent/src/otel.rs` (`preflight_standard_header_env`, `build_pipeline`, the tests), `agent/crates/tinytop-agent/tests/cli_db.rs` (`TempDatabase`, `db_stats_reports_otel_presence_only`), `agent/assets/dashboard/ladder-rules.js` (`validateOtelSettings`, `validResourceAttributes`, `describeOtelCoverage`, `describeImportPlan`), `tests/dashboard-ladder.test.ts`. Work RED → GREEN per finding and paste each RED line and GREEN summary in your final report.

**F1 — P1: `otel.endpoint` may carry credentials or no host (code + tests, both sides).** `valid_http_endpoint` (`otel_settings.rs:81`) and the JS mirror (`ladder-rules.js:216`) accept any non-empty authority, so `http://:4318/v1/metrics` (no host) and `https://user:sekrit@collector/v1/metrics` (userinfo) pass. `reqwest` 0.12 (`src/async_impl/request.rs` `extract_authority`) strips URL userinfo and sends it as a Basic `Authorization` header — a working credential path that lives in settings, the export document, coverage JSON, `db stats` and the `otel export failed: <endpoint>` line, contradicting ADR 0016. Contract: after the scheme, the authority (up to the first `/`, `?` or `#`) must contain **no `@`**, and its host part (the authority minus an optional `:port` suffix — treat a leading `[` as an IPv6 literal and take up to the closing `]`) must be **non-empty**; whitespace/control checks stay. ONE message on both sides, byte-identical, replacing the current endpoint message everywhere it appears (Rust: `otel_settings.rs` + its tests; JS: `OTEL_ENDPOINT_ERROR` + the mirror test): `otel.endpoint must be an http:// or https:// URL with a host and without credentials`. Tests: Rust `validate` cases for `http://:4318/v1/metrics`, `https://user:sekrit@collector/v1/metrics`, `https://@collector/v1/metrics` (all refused with that message) and `http://[::1]:4318/v1/metrics`, `https://collector.example/v1/metrics`, `http://collector:4318` (accepted); the JS mirror test gets the same six. **Also:** promote the nine-word secret list that lives in the test `export_document_carries_the_otel_block_and_still_no_secret_shaped_key` (`agent/crates/tinytop-store/tests/settings_transfer.rs:199-209`: `secret, token, password, passwd, apikey, api_key, authorization, bearer, credential`) to `pub const SECRET_SHAPED_KEY_WORDS: [&str; 9]` in `otel_settings.rs`, make that test use the constant, and refuse a `resourceAttributes` key when any `.`- or `_`-separated segment of it equals one of the words (so `deployment.environment` passes, `auth.token` and `api_key` are refused). Message, both sides, byte-identical: `otel.resourceAttributes keys must not be secret-shaped (no segment may be secret, token, password, passwd, apikey, api_key, authorization, bearer or credential)`. JS: mirror the list as an exported `SECRET_SHAPED_KEY_WORDS` array in `ladder-rules.js` and check it in `validResourceAttributes` / `validateOtelSettings`; tests both sides (one refused, one accepted). Validation order: keep the existing order and check the secret-shaped rule right after the existing key/value rule so the existing attributes message is unchanged.

**F2 — P2: the settings merge happens outside the write transaction (store + routes).** `update_settings` (`writer.rs:427-429`) and `apply_import` → `plan_import` (`settings_transfer.rs:97-108`) call `DashboardSettings::from_document(document, Some(&previous))` with a `previous` read BEFORE `put_settings` opens `BEGIN IMMEDIATE`; `put_settings` re-reads `previous` inside the transaction only for the ladder check and writes the stale candidate. A concurrent writer's `otel` (or any) change committed in that window is silently reverted by a document that never carried the block. Contract: add to `SqliteHistoryStore` (`lib.rs`) `pub struct SettingsWrite { pub previous: DashboardSettings, pub saved: DashboardSettings }` and `pub async fn put_settings_document(&self, document: &JsonValue) -> Result<SettingsWrite, StoreError>` which, INSIDE the `BEGIN IMMEDIATE` transaction, reads `previous` with `get_settings_on`, decodes `DashboardSettings::from_document(document.clone(), Some(&previous))`, normalizes legacy mirrors, runs exactly the validation `put_settings` runs today (settings + ladder against the in-transaction `diskPressure` and `previous.retention_ladder`), writes, commits, and returns both. Extract the shared in-transaction body (validate + write) into one private helper used by BOTH `put_settings(&DashboardSettings)` (kept — tests and struct callers) and `put_settings_document` — one validation path, never two. Callers: `update_settings` → `put_settings_document(&payload)`; marker `changed` from `DashboardSettings::changed_keys(&write.previous, &write.saved)`; response = `write.saved`. `apply_import` → keep `plan_import` for the refusal/preview semantics (`valid`, `errors`, `would_delete`), then `store.put_settings_document(envelope["settings"])` and recompute `changed_keys` from the write's `previous`/`saved` with the same function `plan_import` uses (so the marker reflects the state actually replaced); `would_delete` stays the preview's. After this, `from_document(` must not appear in `writer.rs` production code and appears in `settings_transfer.rs` only inside `plan_import` (the dry-run preview is a preview by definition — say so in a one-line comment there). Tests (`tinytop-store/tests/settings_transfer.rs` or a new `tests/settings_write.rs`): (a) `put_settings_document_merges_against_the_in_transaction_previous` — enable `otel` via `put_settings_document`, then write a 0.4.1-shaped document (no `otel`) via `put_settings_document`: `saved.otel.enabled == true`, `previous.otel.enabled == true`, `changed_keys(previous, saved)` does not contain `otel`; (b) `put_settings_document_refuses_an_invalid_document_and_writes_nothing` (bad `otel.endpoint` → `StoreError::Validation`, stored settings unchanged); (c) an agent route test proving the PUT marker's `changed` list comes from the in-transaction pair (a PUT that changes only `otel.enabled` records exactly `["otel"]`). RED first: (a) written against the old API shape (decode outside with a stale `previous`, then `put_settings`) must show the reverted block — paste it — then the new API makes that shape impossible by construction.

**F3 — P2 (both legs): the standard-variable preflight is not fail-closed.** `preflight_standard_header_env` (`otel.rs:191-200`) allows `OTEL_EXPORTER_OTLP_HEADERS` / `OTEL_EXPORTER_OTLP_METRICS_HEADERS` when one of them is the selected `headersEnvVar`; the SDK merges them from the process environment unconditionally (`opentelemetry-otlp-0.32.0/src/exporter/http/mod.rs:287`), so the custom parse path is not exclusive. Contract: (1) validation refuses the two reserved names for `otel.headersEnvVar` on both sides, checked right after the existing regex rule, message byte-identical: `otel.headersEnvVar must not be OTEL_EXPORTER_OTLP_HEADERS or OTEL_EXPORTER_OTLP_METRICS_HEADERS; tinytop reads headers only from its own variable` (Rust `otel_settings.rs` + tests; JS `ladder-rules.js` new `OTEL_HEADERS_RESERVED_ERROR` + mirror test); (2) `preflight_standard_header_env(metrics_present: bool, general_present: bool)` drops the settings parameter and refuses whenever EITHER is present, message unchanged in shape but without the "selected" clause: `<NAME> is present; tinytop reads OTLP headers only from otel.headersEnvVar` — never a value; rewrite `standard_header_environment_is_fail_closed` accordingly (both present → the metrics name is reported first; neither → Ok). (3) README `## OpenTelemetry export`: one sentence stating that the exporter refuses to start while either standard variable is set and why (one parser, one source), and that the two names cannot be selected as `headersEnvVar`.

**F4 — P3: the interval clamp guards a state the store never hands out (comment only).** `get_settings_on` (`lib.rs:637-640`) validates after decoding, so an out-of-range stored `intervalSec` errors into the 60 s settings-error branch and never reaches `used_otel_interval_sec` (`writer.rs:79`, `:710`). Keep the clamp and its test; replace the comment above it with the truth: the store validates `5..=3600` on every read, the clamp is a belt-and-braces invariant so the sleep arithmetic can never see a value outside the range, and the `eprintln!` is reachable only if that invariant is broken by a future change. No behaviour change.

**F5 — P2: no test proves collection stays live beside a HUNG receiver.** `export_failure_increments_the_counter_and_never_stalls_collection` (`otel.rs:982`) uses `127.0.0.1:1` (an immediate refusal) and a plain `tokio::join!`, which proves neither interleaving nor a hang. Add `serve_otel_hung_receiver_never_stalls_collection` in `otel.rs` tests (the `serve_` prefix keeps it out of the sandbox gate; it runs in the outside gate): bind a `tokio::net::TcpListener` on `127.0.0.1:0`, spawn an acceptor that accepts every connection and holds each socket open without reading or writing until the test ends (keep the handles; abort the acceptor at the end), `build_pipeline` with `Duration::from_secs(2)` pointed at it, record a snapshot, then run `collect_and_export()` concurrently with two `collect_and_store` calls on a `test_state` — record `Instant::now()` when the second collection completes and when the export future resolves; assert both collections completed BEFORE the export resolved, the export took ≥ 2 s and returned `Err`, `stats().sample_count == 2`, and `last_error` (via `record_failure`) contains neither `sekrit-value` nor a header name. Abort the acceptor, drop the listener. Keep the existing port-1 test but rename it to what it proves: `export_failure_increments_the_counter_and_sanitizes_the_error`.

**F6 — P3: `otel_loop_applies_a_disable_within_one_tick` (`writer.rs:2252`) allows 1 s on a 50 ms tick.** Rename to `otel_loop_applies_a_disable_promptly_and_stops_exporting`, keep the 1 s CI tolerance and the six-tick no-new-failures assertion, and add a comment that the one-tick bound is measured outside the suite by the acceptance driver (Fabulous `docs/fleet/tinytop/tools/`; 2026-08-29: disable applied in 8.9 s under a hung 10 s receiver, coverage 9 ms while disabled). Update every reference to the old name (the brief-derived comments included).

**F7 — P3: `db_stats_reports_otel_presence_only` (`cli_db.rs:244`) proves only the `false` branch.** Add `fn run_with_env(&self, args: &[&str], env: &[(&str, &str)]) -> Output` to `TempDatabase` (same `Command` as `run`, plus `.env(k, v)` per pair — child process only), and extend the test: a second invocation with `("TINYTOP_TEST_HEADER_THAT_IS_SET", "authorization=sekrit-value")` after pointing `headersEnvVar` at that name → `headersEnvVarSet == true`, `headersEnvVar` equals the name, and the WHOLE stdout does not contain `sekrit-value` or `authorization=`.

**F8 — P3: the 64-character key cap has no 65-character case.** Rust (`otel_settings.rs` tests): a valid-syntax key of exactly 64 lowercase characters is accepted and 65 is refused with the existing attributes message; JS (`tests/dashboard-ladder.test.ts` mirror test): the same two cases through `validateOtelSettings`.

**F9 — P3: locale-bound expectations.** `tests/dashboard-ladder.test.ts:527` expects `last success 10:11:12` (holds only because `bun test` pins `TZ=UTC`; `formatOtelTime` uses `toLocaleTimeString`) and `:699-` expects `1,234` (`count.toLocaleString()`, locale-dependent). Compute both expectations with the same formatter the production code uses (`new Date(ms).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit",second:"2-digit",hour12:false})` and `(1234).toLocaleString()`) so the suite is TZ- and locale-agnostic; no production change.

**F10 — P2: GUIDE.md:220 "It does not send telemetry to an external service."** contradicts GUIDE:99. Rewrite the Privacy paragraph: nothing leaves the box by default; the only outbound path is the OpenTelemetry export, off by default, which sends the §12 system metrics (never process names, never settings, never header values) to the endpoint the operator configures, and only after the operator enables it via the dialog or `config import`.

**F11 — P3: prerequisite and feature over-claims (docs only).** `aws-lc-sys 0.40.0` (`builder/main.rs:533-576`) tries its `cc` builder FIRST whenever pregenerated bindings exist for the target (x86_64/aarch64 Linux, macOS, Windows MSVC) and falls back to CMake only when that fails, when `AWS_LC_SYS_CMAKE_BUILDER=1`, for no-asm/sanitizer/FIPS builds, or for targets without pregenerated bindings; both cached host builds report `Building with: CC`. Reword INSTALL.md `:372` (the prerequisite paragraph), README.md `:106` and the vetting report `:149-153`: a **C compiler** is required (`build-essential` / Xcode Command Line Tools / VS Build Tools C++ workload); CMake is used only on the fallback paths named above and is harmless to install (keep the package names); replace "If CMake is missing, Cargo fails…" with what actually fails when no C compiler is present (the `cc` crate's `error occurred in cc-rs: failed to find tool` shape — quote the real first line from `cc` 1.x). Vetting report `:122-126`: correct the "unrelated signals" sentence — `opentelemetry-otlp`'s `http-proto` feature enables `trace` and `metrics` (`Cargo.toml:88-95`); the trace code is compiled and never invoked; `logs` stays off.

**F12 — P3: governing documents (docs only; append, never rewrite history).** Spec §12: (a) `:207` "read … at daemon start and on settings change" → "read from the environment only when the exporter pipeline is (re)built — on enable and on a change of the `otel` block — never while the exporter is disabled"; (b) `:211` → the SDK's `ManualReader` behind `experimental_metrics_custom_reader` driven by the daemon's own task (the SDK's `PeriodicReader` hosts a thread), exact pins `opentelemetry 0.32.0` / `opentelemetry_sdk 0.32.1` / `opentelemetry-otlp 0.32.0`; (c) the metric list: `system.filesystem.usage` [By] (attrs `mountpoint`, `type`, `state=used|free`); add a third dated "Amended 2026-08-29" note at the end of §12 naming (a)–(c). ADR 0015: do NOT edit existing lines; append a dated section `## Amendment 2026-08-29 (Phase 4 close)` recording the pinned versions (the SDK's patch release), the `ManualReader` choice and its feature gate with the reason, and that headers are read at pipeline build only — status stays Accepted. Plan Task 11 (`docs/plans/2026-08-28-tiered-history-ladder-plan.md:291`): a one-line dated note under the existing 2026-08-29 amendment pointing at the ADR amendment.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green (paste the summaries; expect store ≥ 137 + F1/F2 units, agent ≥ 87 + F5-renamed/F7/F8); `bun test tests/dashboard-ladder.test.ts tests/dashboard-settings.test.ts` green (paste); `git status --short` and `git diff --stat` pasted (files ⊆ the F-lists: `otel_settings.rs`, `lib.rs`, `settings_transfer.rs` (+ its tests / a new store test file), `writer.rs`, `otel.rs`, `tests/cli_db.rs`, a route test file if F2(c) needs one, `ladder-rules.js`, `tests/dashboard-ladder.test.ts`, README.md, GUIDE.md, INSTALL.md, the vetting report, the spec, ADR 0015, the plan). Final report: per finding RED line(s) → GREEN summary, the six new/renamed test names, and anything you could not do with the exact reason.
