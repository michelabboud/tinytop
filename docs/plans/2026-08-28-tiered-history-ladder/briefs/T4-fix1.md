You are hexe lane **T4-fix1** for tinytop: a FIX ROUND on Task 4 ("Read API — four-tier `auto`, coverage, detail endpoints") after its blind review (ari-luna run 561, validated by Fable). Your branch starts from the T4 lane branch (`tinytop/ladder-t4-read-api` @ `80c1c33`, everything below already exists). Do ONLY F1–F4. Lane **T5-fix1** (dashboard) runs concurrently on another branch — do not touch `agent/assets/`, `GUIDE.md`, Bun's `src/`/`tests/`, or `tinytop-store`'s `retention_ladder.rs`/`ladder.rs`/`maintenance.rs`/`migration.rs`/`disk.rs`.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §10 — the amended `auto` rule; its sentence on `limit`: *"a direct store caller that passes `None` counts as 10,000"* — the resolver honours it, the reader does not (F1).
2. The plan's **Global Constraints**, **Context & Rationale**, **Task 4**.
3. Code (line numbers = `80c1c33`): `agent/crates/tinytop-store/src/lib.rs` — `read_history` :1096 (raw samples, legacy default 120 — NOT yours), `read_history_points` :1126, the two detail readers with `unwrap_or(120)` at :1157 and :1202 (detail rows — NOT yours: the writer always passes `Some`), `read_history_markers` :1285 (default 250 — NOT yours), `read_tier_history_points` :1580 (**`unwrap_or(120)` at :1585 — yours**), `resolve_history_point_source` :1886 / `_with_poll` :1899 (**`limit` at :1913-1916 = `map(clamp).unwrap_or(10_000)` — the rule**); `agent/crates/tinytop-agent/src/writer.rs` — `detail_history_query` :630-642 (the pure clamp for the detail endpoints), `#[cfg(test)] mod tests` (`test_state` :943, `coverage_reports_every_tier_and_json_horizon` :1255, `filesystems_endpoint_filters_by_mount_and_clamps_limit` :1291, `processes_endpoint_groups_by_capture_time` :1310); `agent/crates/tinytop-store/tests/sqlite_history_store.rs` (store-level tests; how fixtures insert samples/rollups).

Fixes (each: failing test first, paste RED, implement, paste GREEN):
- **F1 — one effective limit for `auto` points (luna 561 P1, confirmed).** Add `pub fn effective_points_limit(query: &HistoryPointsQuery) -> i64` in `lib.rs` = `query.limit.map(|l| l.clamp(1, 10_000)).unwrap_or(10_000)`, and use it in BOTH `resolve_history_point_source_with_poll` (:1913-1916) and `read_tier_history_points` (:1585) — and in any other reader reachable from `read_history_points` (map them; report the list). `read_history` (samples), the two detail readers and markers keep their legacy defaults — they are not `auto` consumers. Test (RED first, store-level in `tests/sqlite_history_store.rs`): insert 130 one-minute rollup buckets ending now, `read_history_points(HistoryPointsQuery { since_ms: Some(now − 2 d), until_ms: Some(now), limit: None, source: Auto })` → 130 points, `source == "rollup"` (2 d of raw at 1.5 s = 115,200 > 10,000 → L2 by rule (c)). RED on `80c1c33` = 120 points.
- **F2 — detail `limit` clamp tests (luna 561 P2, confirmed).** Unit-test `detail_history_query` in `writer.rs` `#[cfg(test)]`: `limit: Some(0)` → `Some(1)`; `Some(99_999)` → `Some(10_000)`; `None` → `Some(DEFAULT_HISTORY_LIMIT)`. In `filesystems_endpoint_filters_by_mount_and_clamps_limit` add a second request with `limit=99999` → `200` and every fixture row for that mount (the clamp must accept, never reject). Same `limit=99999` acceptance case in `processes_endpoint_groups_by_capture_time`.
- **F3 — coverage exact shape (luna 561 P2, confirmed).** In `coverage_reports_every_tier_and_json_horizon`, assert key SETS (sorted `Vec<&str>` equality): each tier item == `["bucketCount","enabled","keepDays","newestMs","oldestMs","resolutionMs","tier"]`; `disk` == `["freeBytes","lastCheckMs","minFreeBytes","pressure"]`; `archive.queryable` == `["bucketCount","enabled","newestMs","oldestMs","path"]`; `archive.cold` == `["bytes","directory","enabled","exportedUntilMonth","fileCount"]`; `migration` is `null` on a fresh DB. RED by construction: first write the assertion with a deliberately wrong expected set, see it fail, then correct the set (paste both).
- **F4 — docs.** `docs/sqlite-history-architecture.md` §Read Path: one sentence — for `/api/history/points` the effective page limit is `limit` clamped 1–10,000, and a store caller passing `None` gets 10,000 (the resolver and the reader agree); `/api/history` raw samples keep their 120 default. CHANGELOG under `## Unreleased`: one line per fix.

NOT yours (recorded for T6): axum's `Query` rejection names the parameter but not the observed value — repo-wide, pre-existing on every endpoint.

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only.
- No new dependencies. No edits outside: `agent/crates/tinytop-store/src/lib.rs`, `agent/crates/tinytop-store/tests/sqlite_history_store.rs`, `agent/crates/tinytop-agent/src/writer.rs`, `docs/sqlite-history-architecture.md`, `CHANGELOG.md`.
- Every error message names the field and the observed value where the code already does; do not widen scope.

Gate to run and paste in full: `bun run check` (from the repo root). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests bind a local port your sandbox denies (`PermissionDenied: should reserve a local port`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours. Do not ESCALATE on a sandbox-denied bind.

Report (your final message), in this order: files changed with line ranges · tests added/rewritten (names) · RED output per fix · GREEN output · full gate output · `git diff --stat` · docs updated · the list of readers you routed through `effective_points_limit` · anything the brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
