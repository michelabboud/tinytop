You are hexe lane **T14-fix1** for tinytop — the fix round after the orchestrator's real-file gate of lane T14 (the store half of Task 14; ADR 0024 decision 2 and its **Amendment 2026-08-30** at the end of `docs/adr/0024-schema-v3-typed-history-identity-interning-filesystems-on-change.md` — read both). Base: branch `tinytop/phase5-t14-schema-v3` at **`e29468d`** (your worktree was created from it; `git rev-parse HEAD` first and quote it). Every `file:line` below was read at `e29468d`; `sed -n` / `grep -n` each before you build on it. Build ONLY F1–F3. Files you may edit: `agent/crates/tinytop-store/src/migration.rs`, `agent/crates/tinytop-store/tests/migration_v3.rs`, `INSTALL.md`, `docs/sqlite-history-architecture.md`. NOTHING else — no `agent/crates/tinytop-types` (the type stays strict: a negative count is not a count, and the migration is the only place the Rust daemon decodes legacy JSON), no `tinytop-store/src/lib.rs`, no `tinytop-agent`, no `Cargo.toml`/`Cargo.lock`, no `agent/assets/`, no `tests/*.ts`, no `src/`, no `legacy/`, no `VERSION`, no `CHANGELOG.md` (Fable writes the line at merge), no `docs/adr/*` (the amendment is on main only — your base branch does not carry it; its full text is in the finding paragraph below), no `README.md`, no plan, no spec. **Git is read-only for you — no commit, no branch, no stash, no bare `cargo fmt` (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` and fix by hand); the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; `std::env::set_var` is forbidden. **Known sandbox limits — not failures:** every `serve_*` test binds a port your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); there is no crates.io in the sandbox — `--offline` works. If a contract below contradicts the code you read, STOP and reply `ESCALATE: <the line, what the code says, what you tried>`.

**Why this round exists (the finding, measured by the orchestrator on a fresh `sqlite3.backup()` of the live v1 database — 42,893 `metric_samples` rows, 2,451 with `snapshot_json`):** `migrate_v2_to_v3` refused the whole file: `metric_samples row 41076 holds snapshot JSON that does not decode: invalid value: integer -999001, expected u64 at line 1 column 1954`. 25 rows (`sample_id` 41076–41124, one 36-second window) were written by the legacy Bun collector (`legacy/bun-collector.ts` — NOT updated, Michel's ruling 2026-08-29), whose filesystem enumeration computes `inodeUsed = inodeTotal − inodeFree` unclamped; on a filesystem whose statfs reports more free inodes than total (WSL drvfs `/usr/lib/wsl/drivers`: `f_files 999`, `f_ffree 1,000,000`) that is `-999001`, next to `"inodeUsedPercent":null` in the same object. The Rust collector clamps (it writes `inodeUsed 0, inodeTotal 999` for the same mount — nothing to change there). `FilesystemSnapshot.inode_used` / `inode_total` are `Option<u64>` (`agent/crates/tinytop-types/src/lib.rs:169-171`), so serde refuses the negative integer and the guard refuses the file — and the daemon then exits 1 on every start until an operator edits rows by hand. The guard is right for JSON this version does not know; it is wrong for a known quirk of our own legacy writer. The v3 backfill stores NOTHING from `inodeUsed`/`inodeTotal` — it reads a JSON row for the identity strings, `uptime_seconds`, `memory_available_bytes`, `swap_free_bytes`, `last_pid` and `filesystemsCapturedAtMs` (`agent/crates/tinytop-store/src/migration.rs:929-1020`) — so normalising the two fields to absent loses nothing and invents nothing.

**F1 — normalise, count, keep the guard (`migration.rs:929-937`, the decode inside `for (sample_id, captured_at_ms, snapshot_json) in json_rows`).** Replace the direct `serde_json::from_str::<SystemSnapshot>(&snapshot_json)` with: `serde_json::from_str::<serde_json::Value>(&snapshot_json)` → `normalise_legacy_snapshot(&mut value)` → `serde_json::from_value::<SystemSnapshot>(value)`, both fallible steps mapped to the SAME `StoreError::Migration` reason text as today (`metric_samples row {sample_id} holds snapshot JSON that does not decode: {error}`). New `fn normalise_legacy_snapshot(value: &mut serde_json::Value) -> u32` (private to `migration.rs`, doc-commented with the Bun `inodeTotal − inodeFree` cause): if `value["filesystems"]` is an array, for every element that is an object, set `inodeUsed` and `inodeTotal` to `Value::Null` when the existing value is an integer `< 0` (`as_i64().is_some_and(|n| n < 0)`); leave every other key, type and value untouched; return the number of FIELDS changed. A row whose count is > 0 increments a new `legacy_inode_rows_normalised: i64` (ROWS, not fields). Change the remedy of that `map_err` to exactly: `a row this version cannot decode — back up the database, then clear that row's payload (UPDATE metric_samples SET snapshot_json = NULL WHERE sample_id = <n>; see INSTALL.md, Upgrade) and start again; the database was not modified` (the current `inspect it with `db check`` is misleading: `db check` is `PRAGMA integrity_check` — `agent/crates/tinytop-agent/src/main.rs:341-352` — and shows no row). The count goes (a) into the audit document at `migration.rs:1099-1100` as `"legacyInodeRowsNormalised": legacy_inode_rows_normalised` next to `jsonRowsDecoded`, (b) into the info line at `:1120` as `, {legacy_inode_rows_normalised} rows with legacy negative inode counts normalised` before the closing parenthesis, (c) nowhere else — the `assembleable_rows != json_rows_decoded` guard at `:1027` still holds because a normalised row IS decoded and assembleable. No `String::contains` heuristics on the raw JSON; no third parse; no change to `tinytop-types`.

**F2 — tests (`agent/crates/tinytop-store/tests/migration_v3.rs`; RED first — paste the RED run, then the GREEN).** (a) `v2_fixture_with_legacy_negative_inode_counts_migrates_and_counts`: `seed_v2_schema` (`:449`), `insert_v2_metric` (`:461`) row 41 whose payload is a `legacy_snapshot(...)` (`:504`; its `"filesystems": []` is at `:552` — give the builder a `filesystems: serde_json::Value` parameter or add a sibling builder; update every caller mechanically and report the count) carrying ONE filesystem object in the exact Bun shape: `{"filesystem":"drivers","type":"9p","sizeBytes":1,"usedBytes":1,"availableBytes":0,"usedPercent":100.0,"mount":"/usr/lib/wsl/drivers","inodeUsedPercent":null,"inodeUsed":-999001,"inodeTotal":999}`, plus a healthy row 42 (a positive `inodeUsed`), plus one `insert_filesystem` (`:488`) row for that mount at row 41's stamp → `SqliteHistoryStore::connect` succeeds; `user_version` 3; BOTH rows have `identity_id IS NOT NULL`; the audit document (read it the way `v2_fixture_with_json_rows_migrates_to_v3` at `:63` reads it) has `legacyInodeRowsNormalised == 1` (the healthy row is not counted) and `jsonRowsDecoded == 2`; the `fs_samples` row count is unchanged by the migration (the backfill writes no inode value). RED before F1 = the refusal. (b) `v2_fixture_with_undecodable_json_refuses_and_leaves_the_file_untouched` (`:334`) stays and must still pass; extend its remedy assertion to `remedy.contains("snapshot_json = NULL")`. (c) a `#[cfg(test)]` unit module in `migration.rs` (create it if absent) for `normalise_legacy_snapshot`: negative `inodeUsed` only → 1; both negative → 2 and both `Null`; `0`, positive, `null`, missing, `-1.5`, `"x"`, `true` → 0 and the value byte-identical; `filesystems` missing, `null`, an object, an array of non-objects → 0 and untouched; two elements each with one negative field → 2. (d) `real_v1_file_copy_migrates_to_v3` (`:390-447`) unchanged — the orchestrator runs it on the real copy that still holds the 25 quirk rows and expects the info line to say `25 rows with legacy negative inode counts normalised`.

**F3 — docs.** `INSTALL.md` §Upgrade (`:316`; add after the pre-image paragraphs, before the `db pre-image` code block at `~:337`): a "Refused schema migration" paragraph — what `holds snapshot JSON that does not decode` means (a payload this version cannot decode; the database is untouched; the daemon exits 1); that payloads from the legacy Bun collector with negative inode counts are normalised automatically since 0.5.3 and counted in the `history migration info` line and the audit; the manual remedy verbatim (back up; `UPDATE metric_samples SET snapshot_json = NULL WHERE sample_id = <n>;`; start again — that row keeps its scalar history and becomes non-assembleable). `docs/sqlite-history-architecture.md` (`:372-385`, the v3 migration paragraphs): one paragraph on the normaliser (which two fields, why — the Bun writer's unclamped subtraction on `f_ffree > f_files` filesystems — where the count lands) and the guard that remains for everything else.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-store` green (paste EVERY `test result:` line; `migration_v3` counted 3 passed + 1 ignored at `e29468d` — expect 4 + 1, plus the new unit tests in the lib target); `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green; `git status --short` and `git diff --stat` ⊆ the four files above; paste `sed -n` of the changed decode loop, of `normalise_legacy_snapshot`, and of the new info line; `grep -n legacyInodeRowsNormalised agent/crates` shows the audit key and the test assertion only.

**Report shape:** per finding what you changed (`file:line` before → after), the new test names with their RED then GREEN output, the base commit, the gate output; anything outside this brief under `## Outside this lane` — never fixed silently.
