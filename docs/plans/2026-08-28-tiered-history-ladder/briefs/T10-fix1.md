You are hexe lane **T10-fix1** for tinytop (Phase 3). Base = branch `tinytop/ladder-t10-settings-transfer` at `5228090` (T10, hexe run 614, gated outside the sandbox: Rust 210/0, Bun 162/162). Fix ONLY the items below — F1–F3 are Fable's re-read findings; F4+ are the blind review's confirmed findings (luna, hexe run `617`), each validated at source. Read `briefs/T10.md` first (the contract T10 was built to) and the files you touch at the base sha; the spec/ADR/plan are unchanged.

**F1 — The save path must not prompt for non-retention changes (P2, a regression the T10 brief caused).** `saveDaemonSettings` (`app.js`) reuses `describeImportPlan(plan, settings.retentionLadder, state.settingsBaseline)` for the shrink confirm, and `describeImportPlan` appends `also changes: <keys>` for every non-`retentionLadder` changed key plus the `warnings`. Result: changing ONLY the theme in the dialog now opens a confirmation reading "Saving these settings changes history retention: also changes: defaultTheme" — on `main` a theme change saves silently, and the sentence is false. Fix in `ladder-rules.js`: `describeImportPlan(plan, candidateLadder, previousSettings, { includeOtherChanges = true } = {})` — the `also changes:` line and the `warnings` are appended only when `includeOtherChanges` is true. The IMPORT flow keeps the default (a user importing a file must see everything the document changes); the SAVE flow passes `{ includeOtherChanges: false }` so its lines are exactly the retention consequences (deletion counts, retained-tier lines, kept-archive lines) and a save with none of them still saves without a dialog, as on `main`. Keep the save dialog's title/labels as T10 left them ("Confirm settings changes?" / "Save changes"; `tone: "danger"` iff a deletion count > 0). Bun: extend `tests/dashboard-ladder.test.ts` with one test — a plan whose only change is `defaultTheme` yields `[]` with `includeOtherChanges: false` and `["also changes: defaultTheme"]` by default; `tests/dashboard-settings.test.ts` untouched.

**F2 — Directory fsync after publishing the export (P3, T8 parity).** `publish_settings_export_no_clobber` (`main.rs`) hard-links the synced temp file to the target and unlinks the temp, but never fsyncs the directory, so a power cut right after `config export --out` can lose the directory entry while the CLI already printed `"status":"ok"`. After the successful `hard_link` + `remove_file`, open the target's parent directory (`File::open(parent)` — `parent` = `target.parent()` or `.` when empty) and `sync_all()` it (on Windows `sync_all` on a directory handle is not supported: guard with `#[cfg(unix)]` exactly as T8's cold-export chain does — read `archive.rs`'s directory-fsync helper and REUSE it if it is reachable from the agent crate (`pub` in `tinytop_store::archive`), otherwise copy its shape; report which). A failed directory fsync is an error naming the directory (the file IS published; say so in the message: "settings export was published at <path> but its directory could not be synced: <error>"). Test: extend `config_export_out_writes_atomically_and_refuses_to_overwrite` with nothing (the fsync is not observable) — instead add a unit test in `main.rs`'s `#[cfg(test)]` that the helper returns `Ok` on a temp dir and returns the "could not be synced" error when the parent is unreadable... if that cannot be provoked portably, say so and keep the unit test to the `Ok` path; do not weaken the existing tests.

**F3 — `document` shadowing (P3).** In `importSettingsFile(file)` (`app.js`) `let document;` shadows the DOM `document` for the whole function body — harmless today (no DOM access inside) but a trap for the next edit. Rename to `importedDocument` (and the helper parameter `candidateLadderFromDocument(importedDocument)`), nothing else.

**F4 — A failed `--out` write must not strand `<FILE>.tmp` (P2; luna's P0, re-ranked).** In `config()`'s export arm (`main.rs` ≈`:440-460`) `write_all` and `sync_all` return through `?` with the temporary file left behind, so the next `config export --out FILE` fails at `create_new` with "could not create temporary settings export". On either failure, best-effort `std::fs::remove_file(&temporary)` BEFORE returning the error; if that removal itself fails, the returned message names both errors and the path that remains (`…; temporary file <path> could not be removed: <error>`). The post-publication unlink failure (`publish_settings_export_no_clobber`'s `Ok` arm) already reports the stranded name — keep it. Test: in `main.rs`'s `#[cfg(test)]`, a unit test that drives the write helper with a target directory made read-only after the temp is created is not portable — instead extract the temp write into `fn write_settings_export_temp(temporary: &Path, contents: &str) -> Result<(), Box<dyn Error>>` that owns the cleanup, and test it with a `temporary` path whose parent does not exist (create fails → no file) AND with a `temporary` that is a directory (write fails after… no — `create_new` fails first). Simplest honest test: point `temporary` at a path inside a directory that is removed between `create_new` and `write_all`… not deterministic either. Ruling: unit-test the cleanup helper directly — `fn remove_temp_after_failure(temporary: &Path, error: Box<dyn Error>) -> Box<dyn Error>` returns an error whose message contains the original error and, when `remove_file` fails (`temporary` = a non-empty directory), also names the path; when removal succeeds the file is gone. Two `#[test]`s, both portable.

**F5 — Hard-link publish needs a `rename` fallback where links are unsupported (P2).** `publish_settings_export_no_clobber` (`main.rs` ≈`:590-630`) makes `--out` fail outright on filesystems without `link(2)` (FAT/exFAT sticks, some shares) with "could not publish … without overwriting". Keep the hard link as the primary (it is the only atomic no-clobber publish std offers). On an error whose kind is `Unsupported`, or `PermissionDenied`/`Other` with raw OS error `EPERM`/`EOPNOTSUPP`/`ENOTSUP` (Linux returns `EPERM` for `link` on FAT; check `error.raw_os_error()`), fall back to: `symlink_metadata(target)` must be `NotFound` (else the existing refusal, temp removed) → `std::fs::rename(temporary, target)`; on any other error keep today's behaviour (remove the temp, report). Document in the arch doc's `## Settings transfer` and in README's CLI row that on filesystems without hard links the publish is a re-checked rename with a window of a few microseconds in which a file created by another process could be replaced. Test: a unit test that calls the fallback path directly (`fn publish_by_rename_no_clobber(temporary, target)`) — target absent → published, temp gone; target present → refused, temp removed, target's bytes intact (the existing race test's shape).

**F6 — Test gaps (P2/P3; luna).** (a) `newer_config_version_is_refused_naming_the_max` (`tinytop-store/tests/settings_transfer.rs`): add `"1"` (string) and `1.5` → each yields `valid:false` with the "required and must be an integer" message. (b) `unknown_top_level_key_is_refused_and_nothing_is_written` and `newer_config_version_is_refused_naming_the_max`: after `apply_import` returns `Err`, also assert zero `app_events` rows and that `history_state`'s `l3Enabled`/`l4Enabled` rows are unchanged (read them before and after). (c) `cli_config.rs`: NEW `config_import_refuses_an_invalid_document_with_one_refused_object` — a version-2 document through a REAL `config import FILE` (no `--dry-run`) → exit 1, stdout is exactly ONE JSON object (parse the whole stdout; a second object or trailing text fails) with `status:"refused"`, `reason` starting with `settings document invalid:`, `details.valid == false`, `details.errors[0]` naming `maximum supported 1`; settings unchanged (`config export` before/after equal modulo `exportedAtMs`); no marker (open with `connect_for_inspection` as the round-trip test does).

**F7 — Docs (P3; luna).** `docs/sqlite-history-architecture.md` `## Settings transfer`: qualify "it never deletes more than that preview" with the instant — the counts describe the rows matching the predicates at preview time; rows that cross the candidate cutoff between the preview and the maintenance tick are pruned as well (that is the ladder working, not a preview error). One sentence.

Rules (non-negotiable): git READ-ONLY (no `add`/`commit`/`checkout`/`stash`); leave the tree dirty; report `git diff --stat`. Never bare `cargo fmt` (`cargo fmt --manifest-path agent/Cargo.toml --all -- --check`, then `rustfmt --edition 2024 <file>` for a file you changed only). Never open `~/.local/share/tinytop/`. `Cargo.lock` unchanged. No edits outside the files each F names. Gate: `bun run check` from the repo root — paste it in full; the known sandbox limits (port-binding `serve_contract` tests; Bun `tests/server.test.ts`; `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`) are stated verbatim and are not yours; everything else is.

Report: files changed with line ranges · tests added/changed (names) · RED before / GREEN after per F · full gate output · `git diff --stat` · anything the brief was silent on — listed, never improvised around. You are on the lowest model expected to handle this; if the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`.
