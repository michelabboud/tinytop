# Brief — lane T17b: the thermals panel, the settings group, and the Bun-runtime hide

You are hexe lane **T17b** for tinytop — the dashboard half of the first slice of plan `docs/plans/2026-08-16-sensors-recording-plan.md` (**read its 2026-08-30 amendment bullets (a)–(j) at the end of the file first**), ruled by **ADR 0026 decisions 2, 3, 4, 6 and 8**. Michel's order, verbatim: **"make it opt in option"** — the panel and its settings group must make "off" the obvious, quiet default. **Lane T17 runs in parallel on the Rust side and its files are disjoint from yours — never edit a `.rs` file, and never wait on T17.** Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it). Every `file:line` below was read at `80694ae`; run `sed -n` / `grep -n` on each before you build on it. Build ONLY the findings below.

**Files you may edit:** `agent/assets/dashboard/index.html`, `agent/assets/dashboard/app.js`, `agent/assets/dashboard/ladder-rules.js`, `agent/assets/dashboard/styles.css`; the NEW `tests/dashboard-thermal.test.ts`; `tests/dashboard-settings.test.ts` (only to add cases — never to change an existing one); `GUIDE.md`. NOTHING else — **no `agent/crates/**` at all** (that is lane T17; touching it conflicts), no `src/`, no `legacy/`, no `README.md`/`ARCHITECTURE.md`/`docs/guides/API.md` (T17 writes those), no `VERSION`, no `CHANGELOG.md` (Fable writes the line at merge), no `docs/adr/*`, no plan edit. **No new dependency; `package.json` and `bun.lock` MUST NOT change.** **Git is read-only for you — no commit, no branch, no stash; the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, and **never start a server on `:4274`** (that is Michel's live daemon) — your tests are pure-function and DOM-level, exactly like `tests/dashboard-gpu.test.ts`.

**The contract you are rendering (produced by lane T17, ADR 0026 decision 3):** `snapshot.sensors` is an array of `{ chip: string, kind: "temp", label: string, value: number, max?: number, crit?: number }` — **the key is absent entirely when there are no sensors**, `max`/`crit` are **absent whenever the kernel's value was missing or nonsense**, and the value is degrees Celsius. `settings.thermal` is `{ enabled: boolean, extraChips: string[] }` with `enabled` **false by default**. `coverage.thermal` is `{ enabled, sensorCount, oldestCapturedAtMs, newestCapturedAtMs }` or absent.

**Read first:** ADR 0026 decisions 4 (the threshold sanity band — **an absent threshold is not a zero and must never become a gauge maximum**), 6 (this lane's ruling: an own panel, not a fifth overview gauge, not a drawer) and 8 (absence, not zero); `index.html:179` (`<section class="panel gpu-panel" id="gpu-panel" data-section="overview" aria-label="GPU" hidden>` — **the exact placement pattern your panel copies, and it goes immediately after this section**), `:274` (`history-otel-status`, the coverage-status shape), `:639-664` (the whole `otel-settings-group` fieldset — the template for your settings group, including the `hidden` attribute that the capability check clears); `ladder-rules.js:102-131` (`formatGpuPercent`, `formatGpuMemory`, `formatGpuTemperature`, `describeGpuAdapter`, `gpuColumnVisible` — **the pure-function style you copy; note they each return a string and never touch the DOM**), `:222-238` (`ladderCapabilityFrom` / `otelCapabilityFrom` — the capability probe shape), `:240-311` (`settingsPutPayload`, whose signature is `(settings, retentionLadderAvailable, otelAvailable = retentionLadderAvailable)` — **you add a fourth parameter, defaulted, exactly as `otelAvailable` was added; the T10-fix2 rule: a new parameter never changes an existing caller's behaviour**), `:312-352` (`validateOtelSettings` — the client-side validation shape); `tests/dashboard-gpu.test.ts` whole (the T15b lane's tests — your file is its twin); `tests/dashboard-settings.test.ts` (where the settings-group cases live); `app.js` — find the GPU panel's render function and its `hidden` toggle (`grep -n "gpu-panel\|gpus" app.js`) and follow that exact shape.

**F1 — pure functions in `ladder-rules.js`, RED first.** `export function thermalCapabilityFrom(settingsOrNull)` — `true` only when `settingsOrNull.thermal` is a non-null, non-array object (`otelCapabilityFrom:230-238` line for line; **the Bun runtime returns settings without a `thermal` key, and that is how the whole feature stays invisible there**). `export function formatSensorValue(value)` — `"—"` for `null`/`undefined`/non-finite, else one decimal place plus `" °C"` (`54.0 °C`). `export function formatSensorThreshold(max, crit)` — `""` when both are absent (**absent means no text at all, never `0 °C` and never `max —`**), else the present ones joined as `max 105 °C` / `crit 105 °C` / `max 91 °C · crit 105 °C`, integers, no decimals. `export function sensorBarPercent(value, max, crit)` — the fraction of the bar to fill, `null` when **no usable ceiling exists** (both thresholds absent) so the renderer draws a plain value with no bar; otherwise `value / ceiling` clamped to `0..1` and returned as a 0–100 number, where `ceiling` is `crit` when present else `max`. `export function sensorSeverity(value, max, crit)` — `"critical"` when `crit` is present and `value >= crit`, `"warn"` when `max` is present and `value >= max`, else `"normal"`; **`"normal"` whenever the relevant threshold is absent** — a missing threshold never raises an alarm. `export function groupSensorsByChip(sensors)` — returns `[{ chip, readings }]` in first-appearance order, readings in array order, `[]` for a missing/empty input (**it must accept `undefined`** — the key is absent on every sensorless host). `export function validateThermalSettings(thermal)` — mirrors T17's server rule (`validateOtelSettings:312` is the shape): each `extraChips` entry matches `/^[a-z0-9_]{1,32}$/`, at most 16, no duplicates; returns the same error-list shape the existing validator returns. Extend `settingsPutPayload` with a fourth parameter `thermalAvailable = false`: when false the payload **omits `thermal` entirely** (never sends `{enabled:false}`, which would overwrite a persisted block — the absent-key rule is the server's, and the client must not defeat it); when true it sends `{ enabled, extraChips }` only.

**F2 — the panel in `index.html` + `app.js` (ADR 0026 decision 6).** A `<section class="panel thermal-panel" id="thermal-panel" data-section="overview" aria-label="Thermals" hidden>` placed **immediately after the GPU panel at `:179`** — **not a fifth gauge in the overview grid** (the grid is `repeat(4, …)`; a fifth wraps onto its own row — ADR 0025 decision 6 measured this) and **not a collapsible drawer**. Inside: one group per chip, its `chip` name rendered **verbatim** as a heading, then one row per sensor: the label, the formatted value, the threshold text (empty when absent), and a bar whose width is `sensorBarPercent` and whose class carries `sensorSeverity` — **when `sensorBarPercent` returns `null`, render the row with the value and no bar element at all**. In `app.js`, the render runs where the GPU panel's does, and the panel is `hidden` **whenever `snapshot.sensors` is absent or empty** — which is the Bun runtime always, WSL2 always, and any host with thermals switched off. Never render a placeholder, a zero, an empty state or the words "no sensors": absence is silence (ADR 0026 decision 8). `styles.css` gains only the classes this panel uses, following the GPU panel's existing rules; the bar's `warn`/`critical` colours must come from the existing theme variables (both themes) and **the severity must not be signalled by colour alone** — the row also carries the threshold text and an `aria-label` naming the state.

**F3 — the settings group.** A `<fieldset class="settings-group thermal-settings-group" id="thermal-settings-group" hidden>` in the `otel-settings-group` shape (`:639-664`), placed after it: a checkbox `daemon-thermal-enabled` labelled so that **off is plainly the default**, and a text input `daemon-thermal-extra-chips` (comma- or newline-separated, the `parseResourceAttributes`/`formatResourceAttributes` round-trip pattern at `:353-407` is the shape for parsing and re-rendering it). The group's `hidden` is cleared only when `thermalCapabilityFrom(settings)` is true — **so the Bun runtime never shows it**. Client-side validation uses `validateThermalSettings` and blocks the save with the same message shape the OTel group uses. The save sends the block through the extended `settingsPutPayload`.

**F4 — the coverage line.** Where the OTel status line lives (`:274`), add `thermal-status` showing, only when `coverage.thermal` is present and `enabled` is true, the sensor count and the covered range (`formatCount` at `ladder-rules.js:98` for the count). **A sensor's value never appears here** (ADR 0026 decision 7). Hidden when the block is absent or `enabled` is false.

**F5 — tests, RED first, in the new `tests/dashboard-thermal.test.ts`** (`tests/dashboard-gpu.test.ts` is the twin): every F1 function, including `formatSensorThreshold(undefined, undefined) === ""`, `sensorBarPercent(54, undefined, undefined) === null`, `sensorSeverity(54, undefined, undefined) === "normal"`, `sensorSeverity(106, 105, 105) === "critical"`, `sensorSeverity(95, 91, 105) === "warn"` (the trashcan thresholds), `groupSensorsByChip(undefined)` → `[]`, and the sheep fixture (5 `coretemp` readings) grouping into one chip with five readings **in order**; `thermalCapabilityFrom` false for `null`, `{}`, `{thermal: null}`, `{thermal: []}` and true for `{thermal: {}}`; `settingsPutPayload` **omits `thermal` when `thermalAvailable` is false** and both existing callers keep their current payload byte-for-byte (the T10-fix2 rule — assert the OTel and ladder payloads are unchanged); `validateThermalSettings` for a bad character, a 17th entry and a duplicate. DOM-level cases in the same file or in `tests/dashboard-settings.test.ts` (added cases only): the panel is `hidden` for a snapshot **with no `sensors` key** and for `sensors: []`, visible with readings; the settings group is hidden when settings carry no `thermal`; a row whose thresholds are absent renders **no bar element and no threshold text**. Also assert the asset test still passes (`tests/dashboard-assets.test.ts` checks the shipped asset set — if it enumerates files, your new ids must not break it).

**Gate before you report:** `bun test tests/dashboard-thermal.test.ts` green; `bun run check:bun` green — **paste EVERY `test result` / summary line and count them yourself**: the 0.5.4 release gate counted **209 passed / 0 failed across 20 files**, so your total must be that plus your new cases and the file count must rise by exactly one (**do not trust the orchestrator's arithmetic — report your own sum**); `git diff --stat package.json bun.lock` prints nothing; `git status --short` and `git diff --stat` pasted (⊆ the allow-list; **no `.rs` file, no `src/`, no `legacy/`**). **You do not run the Rust gate and you do not rebuild the agent** — the dashboard assets are embedded in the Rust binary, so the orchestrator rebuilds after the merge (the stale-binary trap); say in your report that you did not.

**Report shape:** per finding the RED line(s) → the GREEN summary; the test names; the base commit; the exact strings `formatSensorValue` / `formatSensorThreshold` produce for the sheep (105/105) and trashcan (91/105) fixtures; how a row with no thresholds renders, quoted from the DOM assertion; confirmation that both existing `settingsPutPayload` callers are byte-identical; the theme variables you used for `warn`/`critical` in both light and dark; anything outside this brief under `## Outside this lane` — never fixed silently. **Contingencies:** if `app.js` turns out to render the GPU panel somewhere other than where you expect, paste the real function and follow it; if `dashboard-assets.test.ts` enumerates asset contents in a way your ids break, **fix your ids, not the test**, and say so; if this brief and ADR 0026 disagree, the ADR wins. **Default:** anything not covered here — STOP and reply `ESCALATE: <what, what you tried, where you stopped>`.

**The orchestrator's acceptance (after the merge, both lanes in):** `./tinytop rust build`, then a temp daemon on a scratch DB and a free port — **never `:4274`** — plus the legacy Bun server on a second free port: with thermals off the panel and the settings group are absent on both; enabled on sheep and trashcan the panel shows the five real `coretemp` sensors with 105/105 and 91/105 thresholds and no `amdgpu` row; **one rendered-page check in a real browser in both themes** (the law that cost five blank tinytop releases — a green gate says nothing about what the user sees), recorded with the T17 results in `docs/fleet/tinytop/2026-08-30-t17-acceptance-checklist.md`.
