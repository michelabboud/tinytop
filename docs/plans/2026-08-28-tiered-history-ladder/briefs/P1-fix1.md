You are hexe lane **P1-fix1** for tinytop: the RUST fix round after the Phase 1 deep dual-blind review (ari-sol-deep run 568 + ari-luna run 569, every finding validated by Fable at source — Fabulous `docs/fleet/tinytop/2026-08-29-ari-dual-blind-phase1-review.md`). Your branch starts from `main` @ `9c1b8ab` (T1–T6 merged). Do ONLY F1–F12 below (there is no F4 or F9 — they belong to lane P1-fix2). Lane **P1-fix2** (dashboard + docs) runs CONCURRENTLY on another branch — do not touch `agent/assets/`, `GUIDE.md`, `ARCHITECTURE.md`, `INSTALL.md`, `PROGRESS.md`, `README.md`, `docs/`, Bun's `src/`/`tests/`.

Read, in this order, before touching anything: `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §7 (migration), §9 (maintenance), §10 (read API), §14 (CLI), §15 (errors: every refusal names the field, the rule, the observed value and the remedy); the plan's Global Constraints; then the code at the lines below (numbers = `9c1b8ab`).

Fixes (each: failing test first, paste RED, implement, paste GREEN):
- **F1 — same-timestamp detail replacement removes omitted members (P2).** `agent/crates/tinytop-store/src/lib.rs` `write_detail_rows` `:1005-1060`: inside the existing transaction, before the member upserts, `DELETE FROM fs_samples WHERE captured_at_ms = ?` and `DELETE FROM process_samples WHERE captured_at_ms = ?` (bound). Test (store, `tests/sqlite_history_store.rs` or `tests/ladder_maintenance.rs`, where detail fixtures live): write a snapshot with two filesystems + two processes at `T`, write again at the SAME `T` with one of each → `read_history_filesystems`/`…_processes` for `T` return exactly one member each. RED = two.
- **F2 — headroom and audit bytes include the WAL (P2).** `agent/crates/tinytop-store/src/migration.rs`: add `fn database_bytes_with_wal(canonical_db_path: &Path) -> Result<u64, StoreError>` = main file length + the `-wal` sidecar's length when it exists (`symlink_metadata`; `NotFound` ⇒ 0); use it at `:364` (headroom, `required_pre_image_bytes`) and at `:618` (`bytes_after`) and wherever `bytesBefore` is measured. Unit test the helper with a temp main file + a temp `-wal` file (sizes 4096 + 8192 → 12288; no wal → 4096). Report the audit fields that now include the WAL.
- **F3 — replay guard + documented limit (P2).** `lib.rs` `insert_snapshot` / the merge at `:817`: when the raw upsert for `captured_at_ms` hit an EXISTING row (use the upsert's `rows_affected()`/a `changes()` check or a pre-`SELECT 1`), do NOT merge into a partial/frozen bucket (the sample was already counted). Test (`tests/ladder_maintenance.rs`): freeze a minute of 40 samples, prune its raw rows, replay one represented timestamp TWICE → `sample_count` 41 after the first replay (indistinguishable from a late write — the documented limit) and STILL 41 after the second. RED on `9c1b8ab` = 42. Add a doc comment at the merge naming the limit.
- **F5 — CLI diagnostics never migrate an existing database (P2, ruling).** `agent/crates/tinytop-agent/src/main.rs` `db()` arms `:208-260`: for `stats`, `check`, `vacuum` — if `inspect_database_path(&url)?` is `Some(_)` open with `SqliteHistoryStore::connect_for_inspection` (no migration, no pragmas beyond what it sets); if `None` keep today's `connect()` (creates a fresh v1 — the tests rely on it; a fresh file has nothing to lose). `stats` on an existing database with `user_version < 1` → stdout `{ "status": "refused", "reason": "user_version check observed 0; the schema v1 migration has not run — stop every TinyTop writer and start the daemon once (it takes the pre-image first; see INSTALL.md)" }`, exit 1, nothing written (reuse the `PreImageRemovalRefused`-style refusal path; rename it to a general `Refused` if cleaner — report it). `check` and `vacuum` work on any schema version and never migrate. Black-box tests in `tests/cli_db.rs` using the existing header-byte `user_version = 0` fixture (`set_sqlite_user_version`): `db_check_never_migrates_a_v0_database` (exit 0, `integrityCheck` ok, afterwards the header still says 0 and no `.pre-v0.sqlite` exists), `db_vacuum_never_migrates_a_v0_database` (same), `db_stats_refuses_a_v0_database` (exit 1, `status:"refused"`, reason contains `user_version` and `0`, no pre-image, header still 0). RED on `9c1b8ab` = the pre-image appears and `user_version` becomes 1.
- **F6 — an authentic post-commit crash test (P2, test).** `migration.rs`: split `migrate_populated_v0` so the schema phase (pre-image → transaction → `user_version = 1` + pending audit) and the post-commit phase (VACUUM → audit completion → `schemaMigrated` marker) are separately callable behind `pub(crate)` (or a `#[cfg(test)]`-visible seam). New test in `tests/migration_v1.rs` `crash_after_schema_commit_is_recovered_on_next_connect`: seed v0, run ONLY the schema phase, assert `user_version == 1`, the pending audit has no `vacuumedAtMs`, and NO `schemaMigrated` marker exists yet; then `SqliteHistoryStore::connect` → VACUUM ran, audit completed, exactly ONE `schemaMigrated` marker. Keep the existing `reconnect_completes_an_interrupted_post_schema_vacuum` as is.
- **F7 — partial maintenance report survives a later step's failure (P3).** `agent/crates/tinytop-store/src/maintenance.rs` `:216-220`: return the counts with the error — e.g. `Err(MaintenanceError { report, error })` or `Result<MaintenanceReport, (MaintenanceReport, StoreError)>` — and in `writer.rs` `:552-556` log the partial counts at the same place the error is printed. Test: make the prune step fail (a store method injected/forced to error in a test fixture — say how) after promotion succeeded → the returned error carries `promoted_l3 > 0`.
- **F8 — query-parameter rejections name the parameter and the observed value (P2, spec §15).** `agent/crates/tinytop-agent/src/writer.rs` `:348`, `:385` and the processes handler: replace bare `Query<T>` with an extractor whose rejection is TinyTop's error shape (the same JSON the handlers already return for bad input) with `query parameter <name>: invalid value "<observed>"` — e.g. read the raw query string, parse with `serde_urlencoded`/`serde_html_form` (already in the lock? check — NO new dependency; if neither is available, parse the pairs by hand for these three handlers and report it). Tests in `writer.rs` `#[cfg(test)]` via `oneshot`: `limit=abc` and `sinceMs=x` on `/api/history/points`, `/api/history/filesystems`, `/api/history/processes` → 400 with body naming the parameter and `abc`/`x`.
- **F10 — URL normalisation creates no directories (P3).** `main.rs` `normalize_sqlite_url` `:482-492`: remove `create_dir_all`; create the parent directory only immediately before a `connect()` that may create a database (`serve`/`collect` and the missing-database `db` path). Extend `tests/cli_db.rs`: `db pre-image status` with NO `--sqlite` and `HOME`/`USERPROFILE`/`LOCALAPPDATA`/`XDG_STATE_HOME` pointed at a non-existent `<tmp>/home` → exit 0, `databaseExists:false`, and `<tmp>/home` still does not exist.
- **F11 — a late write never overwrites a retained coarse bucket from pruned sources (P1 — luna 569, confirmed).** `agent/crates/tinytop-store/src/maintenance.rs` `refold_ancestors_for_late_write` `:273-299` / `refold_one` `:301-315`: today a late write below the watermark refolds the L3 (from L2 rows) and L4 (from L3 or L2 rows) bucket of that timestamp FROM THE SOURCE TIER'S ROWS. When those source rows are already past their own horizon (L2 keeps 30 d, L3 90 d — L4 keeps 730 d), `read_tier_buckets` returns only the just-written minute and `upsert_tier_bucket` REPLACES a retained bucket (count 200 → 1). Rule: refold from the source tier ONLY when the source tier still holds the whole target range (`target_start_ms ≥ now − source.keepDays`, using the effective ladder — pass `now_ms` and the ladder in, do not read the clock inside; also treat a source bucket count of zero as "not held"); otherwise MERGE: `fold(target_start_ms, &[existing_target_bucket, new_sample_as_bucket])` — the same rule `insert_snapshot` applies to a partial-raw L2 minute (`lib.rs:817-834`). Tests (`tests/ladder_maintenance.rs`): (a) retain an L4 bucket with `sample_count 200`, prune its L2 and L3 sources, insert one late sample inside that hour → L4 `sample_count` 201 (RED on `9c1b8ab` = 1); (b) the same one rung down: retained L3 bucket, pruned L2 source, late sample → L3 count +1, never 1; (c) the existing `late_write_refolds_ancestors` still passes when the sources ARE held (refold path unchanged).
- **F12 — `auto` counts the inclusive endpoint (P3 — luna 569).** `lib.rs` resolver `:2027`: a range of exactly `k × resolution_ms` holds `k + 1` points (the reader `:1644-1647` is inclusive on both ends); use `range_ms / fixed_resolution_ms + 1 <= limit`. Unit test: two raw points one poll interval apart with `limit = 1` → not raw (falls to the next tier that fits or the coarsest holding the start); re-run the 12-row `auto` table (unchanged expectations — say so). Spec §10 (c) gets the `+ 1` in a one-line note (this is the ONE spec edit allowed to this lane).
- **CHANGELOG.md:** APPEND one line per fix under the existing `## Unreleased` — do NOT rewrite or reorder earlier entries.

Rules (non-negotiable): worktree only; **git is READ-ONLY** (no add/commit/push/checkout/stash; leave the tree dirty, report `git diff --stat`); never bare `cargo fmt` (`cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; `rustfmt --edition 2024 <file>` only for a file you changed); **never open, read, copy or write `~/.local/share/tinytop/`** — temp fixtures only, every file a test deletes is one it created; NO new dependencies; no edits outside `agent/crates/tinytop-store/src/{lib.rs,migration.rs,maintenance.rs,ladder.rs}`, `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` (F12's one-line note ONLY), `agent/crates/tinytop-store/tests/`, `agent/crates/tinytop-agent/src/{main.rs,writer.rs}`, `agent/crates/tinytop-agent/tests/cli_db.rs`, `CHANGELOG.md`. Every refusal names the check and the observed value.

Gate to run and paste in full: `bun run check` (repo root). Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures: (1) the 10 `serve_contract` tests bind a local port (`PermissionDenied`); (2) Bun's `tests/server.test.ts` the same on `Bun.serve({ port: 0 })`; (3) `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the gate outside the sandbox before merge. Do not ESCALATE on a sandbox-denied bind.

Report (final message), in order: files changed with line ranges · tests added (names) · RED per fix · GREEN · full gate · `git diff --stat` · the seam you chose for F6 and the extractor approach for F8 · anything the brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
