You are hexe lane **T3-fix1** for tinytop: a FIX ROUND on Task 3 ("`retentionLadder` settings, validation, legacy aliases") after its blind review (ari-luna run 548, validated by Fable). Your branch starts from the T3 lane branch (`tinytop/ladder-t3-retention-settings` @ `b38e52e`, everything below already exists). Do ONLY F1–F5.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §5 — the disk-pressure rule and its EXACT message: `disk pressure active: free X < minFreeBytes Y; shrink first or free disk`; "errors name the field and the rule, never just invalid"; the legacy aliases are DERIVED mirrors of `l1`/`l2` (the ladder is authoritative).
2. The plan's **Global Constraints**, **Context & Rationale**, and **Task 3** (its Interfaces line was corrected today: `validate(&self, disk_pressure: Option<&DiskPressureState>, previous: Option<&RetentionLadder>)`).
3. `agent/crates/tinytop-store/src/retention_ladder.rs` (`validate` :195-272 — the pressure branch at :265-270 emits a NON-spec message and is dead in production; `grows_from` :314-334; `enabled_horizon_grew` :346-364; `apply_legacy_aliases` :280; `from_legacy` :274); `src/lib.rs` (`DiskPressureState` :379-383 — private, `#[serde]` attributes and JSON keys must stay byte-identical; `DashboardSettings::validate` :138 and the call at :229; `get_settings` :406-433; `put_settings` :474-548 — the by-value inference at :479-486 and the duplicated pressure check at :492-503); `agent/crates/tinytop-agent/src/writer.rs` (`update_settings` :245-265, `dashboard_settings_from_payload` :267-282, its unit test `settings_payload_without_ladder_derives_it_from_legacy_aliases` :873); `tests/retention_settings.rs` (`settings_save_round_trip_writes_legacy_mirrors` :315, `disk_pressure_refuses_growth_but_allows_shrink` :548, `disk_pressure_refuses_enabling_a_tier_or_archive` :580, `set_disk_pressure` :715, `consecutive_legacy_edits_work_and_explicit_ladder_reset_wins`, `legacy_alias_edit_preserves_non_legacy_ladder_settings`).

Fixes (each: failing test first, paste RED, implement, paste GREEN):
- **F1 — one source for the disk-pressure rule.** Move `DiskPressureState` to `retention_ladder.rs` as `pub struct DiskPressureState { pub active: bool, pub free_bytes: i64, pub min_free_bytes: i64 }` (same derives/serde attributes/JSON keys; re-export from `lib.rs`). Change `RetentionLadder::validate(&self, disk_pressure: Option<&DiskPressureState>, previous: Option<&RetentionLadder>)`: when `disk_pressure` is `Some(p)` with `p.active` and `previous` is `Some(prev)` and `self.grows_from(prev)`, return `StoreError::Validation(format!("disk pressure active: free {} < minFreeBytes {}; shrink first or free disk", p.free_bytes, p.min_free_bytes))` — the spec text, nothing else. Delete the old bool branch and its message. `DashboardSettings::validate()` keeps calling `validate(None, None)`. `put_settings` DELETES its own pressure block (:492-503) and instead calls `normalized.retention_ladder.validate(Some(&disk_pressure), Some(&previous.retention_ladder))?` after `normalized.validate()?`. Test (pure, no DB, in `tests/retention_settings.rs`): `validate_emits_the_spec_disk_pressure_message` — grow `l2` by one day, `validate(Some(&DiskPressureState { active: true, free_bytes: 1_000, min_free_bytes: 5_000 }), Some(&previous))` → error text EXACTLY `disk pressure active: free 1000 < minFreeBytes 5000; shrink first or free disk`; and `active: false` → `Ok`. The existing DB-backed pressure tests must still pass unchanged.
- **F2 — one decoder for external settings documents; the ladder is authoritative.** Add `pub fn DashboardSettings::from_document(document: serde_json::Value, persisted_ladder: Option<&RetentionLadder>) -> Result<DashboardSettings, StoreError>` in `lib.rs`: if the document has a `retentionLadder` key → plain deserialize; if it lacks one → deserialize, then `retention_ladder = match persisted_ladder { Some(p) => { let mut l = p.clone(); l.apply_legacy_aliases(retention_hours, rollup_retention_days); l } None => RetentionLadder::from_legacy(retention_hours, rollup_retention_days) }`. Decode errors → `StoreError::Validation("settings document could not be decoded: {error}")`. `get_settings` uses `from_document(doc, None)`; `writer.rs::update_settings` uses `DashboardSettings::from_document(payload, Some(&previous.retention_ladder))` (delete `dashboard_settings_from_payload`, move its unit test's intent into a store test `from_document_without_ladder_merges_legacy_aliases_onto_the_persisted_ladder` + keep a thin writer test only if something writer-specific remains). `put_settings` DELETES the by-value inference (:479-486): it always sets `retention_hours = l1.keep_days * 24` and `rollup_retention_days = l2.keep_days` from the ladder it was given — a typed caller who edits only the legacy fields sees them overwritten (spec §5: derived mirrors). Rewrite `consecutive_legacy_edits_work_and_explicit_ladder_reset_wins` and `legacy_alias_edit_preserves_non_legacy_ladder_settings` to go through `from_document` with a legacy-only JSON document (the real path), asserting the same outcomes. Add the trap test: `from_document_with_persisted_ladder_never_resets_customised_toggles` — persisted ladder has `l3.enabled=false, l4.keep_days=0, archive.directory="/x"`; a document WITHOUT `retentionLadder` and with `retentionHours: 96` → result keeps those three and has `l1.keep_days == 4`.
- **F3 — table-driven pressure coverage.** One test `disk_pressure_rule_table` over the pure `validate(Some(&active_pressure), Some(&prev))` with rows: `l1 +1` refused · `l2 +1` refused · `l3` enabled `keepDays +1` refused · `l3` enable (was disabled) refused · `l3` disabled `keepDays +1` allowed · `l4` enable refused · `l4` finite → `0` (forever) refused · `l4` `0` → finite allowed (shrink) · `l4` finite `+1` refused · `snapshotJsonKeepMinutes +1` refused · `archive.queryable` enable refused · `archive.cold` enable (queryable already on in prev and self) refused · `detailIntervalSec` change allowed · `diskCheck.*` change allowed · every shrink of `l1`/`l2`/`l3`/`l4` allowed · same table with `active: false` → all allowed. Every refused row asserts the exact F1 message.
- **F4 — the plan's 72/30 row.** In `settings_save_round_trip_writes_legacy_mirrors`, keep the 96/31 case and ADD the default case: `l1 3 / l2 30` → the raw `app_settings.value_json` contains `"retentionHours":72` and `"rollupRetentionDays":30`.
- **F5 — docs.** `docs/sqlite-history-architecture.md` §Retention: the exact disk-pressure message; `DashboardSettings::from_document` is the ONLY decoder for documents that may lack `retentionLadder` (the import endpoint of Task 10 must use it); legacy fields are derived mirrors — a typed save that edits only them is overwritten from the ladder. README settings section: one sentence on the mirrors. CHANGELOG: under `## 0.2.9`, two lines (pressure message single-sourced; single decoder + ladder-authoritative saves).

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only.
- No new dependencies. No edits outside: `retention_ladder.rs`, `lib.rs`, `writer.rs`, `tests/retention_settings.rs`, the three docs. Do not touch `maintenance.rs`, `ladder.rs`, `migration.rs`, `disk.rs`, `agent/assets/`, Bun's `src/`.
- Every error message names the field and the observed value; the pressure message is the spec's, byte for byte.

Gate to run and paste in full: `bun run check` (from the repo root). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests bind a local port your sandbox denies; (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours. Do not ESCALATE on a sandbox-denied bind.

Report (your final message), in this order: files changed with line ranges · tests added/rewritten (names) · RED output per fix · GREEN output · full gate output · `git diff --stat` · docs updated · anything the brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
