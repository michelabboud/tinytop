You are hexe lane **T7** for tinytop (Phase 2 opens here; Phase 1 shipped as `v0.3.0` = `ee457c8`, your base). Execute ONLY **"Task 7: Queryable archive (move expired L4 rows into `history-archive.sqlite`)"** from `docs/plans/2026-08-28-tiered-history-ladder-plan.md`. No other lane runs beside you, but the scope rules below still bind: do not touch `agent/assets/`, `GUIDE.md`, Bun's `src/`/`tests/`, `main.rs` (the `db archive` CLI is **Task 8**), `migration.rs`, `retention_ladder.rs` (the `archive` settings already exist), `disk.rs`, `ladder.rs`.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` — the whole file; your contract lines are **§6 `:129`** (archive DB: `history-archive.sqlite` beside the main DB unless `archive.directory`; `user_version 1`; `metric_rollups_1h` identical shape; `archive_manifest` DDL verbatim; attached only for the duration of a move or a read), **§6 `:107-109`** (`history_state` key `archiveMovedUntilMs`), **§9 `:172`** (expire L4: `archive.queryable ? move_to_archive : DELETE`; ATTACH · INSERT OR IGNORE · verify count · DELETE · DETACH; one transaction per batch ≤ 1,000 rows; `archiveMovedUntilMs` advances), **§10 `:192-193`** (`auto` falls through to `Archive` when no tier holds the start and `archive.queryable`; the coverage `archive` block), **§15 `:220-222`** (`StoreError::Archive { step, source }`; nothing swallows errors; a failed step does not advance a watermark).
2. The plan's **Global Constraints** (`:13-23`) and **Task 7** (`:249-263`) — Files, Interfaces, Tests, Steps. Tasks 1–6 tell you what already exists.
3. `CLAUDE.md` at the repo root (two-runtime invariant, gates).
4. The code, at `ee457c8` line numbers: `agent/crates/tinytop-store/src/lib.rs` — `pub mod` list `:1-5` (add `pub mod archive;`), `HistoryArchiveCoverage`/`HistoryQueryableArchiveCoverage` `:310-324`, `HistoryPointSource` `:382-390` and `HistoryPointMode` `:405-413` (both already have `Archive`), `history_state_get/set` `:610/:625`, `read_tier_buckets` `:871`, `upsert_tier_bucket` `:938`, **`read_history_points` `:1202-1223` — the `Archive => Ok(Vec::new())` stub at `:1220` is what you replace**, `history_coverage` `:1423` with **the hard-coded archive block `:1510-1560`** (`bucket_count: 0, oldest_ms: None, newest_ms: None` are stubs you replace with real values), `read_tier_history_points` `:1660` (reuse its `TierBucket → HistoryPoint` mapping; do NOT add an archive branch there), `resolve_history_point_source_with_poll` `:2007` (already returns `Archive` at `:2067-2069` — do not change), `StoreError` `:2081-2087`; `src/maintenance.rs` — `MaintenanceReport` `:23-30`, the **"expire L4" step `:232-243`** (today a hard `prune_rollups(Tier::L4, cutoff)`), the `record_step_error` pattern `:412-417` and `first_error` resolution `:245-249`; `src/retention_ladder.rs` — `ArchiveSettings` `:43-48` (`queryable`, `cold`, `cold_after_months`, `directory`), `RetentionLadder.archive` `:24`, L4 `keep_days == 0` = forever (`:248-259`); `src/migration.rs` — `metric_rollups_1h` DDL `:126-149` (copy it verbatim for the archive table, same index), `history_state` DDL `:151-155`; `src/ladder.rs` — `TierBucket` `:55-64`, `Tier::L4.resolution_ms()`; `agent/crates/tinytop-agent/src/writer.rs` — `history_points` `available: source != HistoryPointMode::Archive` `:367`, `maintain_history` `:550-575`; tests — `agent/crates/tinytop-store/tests/ladder_maintenance.rs:21-59` (`TempDatabase` with `.store()`/`.pool()`/`Drop`: copy this pattern into your `tests/archive.rs`; there is no `tests/common/` and creating one is OUT of scope).

**Plan corrections (RULINGS, 2026-08-29 — the plan's Files line was written before T4/T6 landed):**
- "Modify `lib.rs` (`read_tier_history_points` for `Archive`)" → the dispatch is in **`read_history_points` `:1215-1222`**; replace the `Archive` arm there. `read_tier_history_points` stays as it is.
- "Modify `writer.rs` (coverage `archive.queryable` block)" → the whole coverage document, archive block included, is built in **`lib.rs::history_coverage`**; `writer.rs::history_coverage` only serialises it. Fill the real numbers in `lib.rs`. The only `writer.rs` edits: (a) `:367` becomes `available: source != HistoryPointMode::Archive || ladder.archive.queryable` (an explicit `source=archive` against a non-queryable archive keeps today's empty `available:false` page; a queryable archive answers with `available:true`), (b) the maintenance info line `:568-573` also prints `archived L4 rows {}`.
- Acceptance's "`db archive status`" is **Task 8's** command (spec §14) — do not add it. Your acceptance transcript is `cargo run -p tinytop-agent -- db stats --json --sqlite <temp db>` showing `archive.queryable.bucketCount > 0` after a forced move (see Step 6).

**Design rulings you implement as written (do not redesign; escalate if one cannot work):**
- `archive.rs` public surface exactly as the plan's Interfaces block: `ArchivePaths { db, directory }`, `archive_paths(main_db, &ArchiveSettings)` (`directory` empty → the main DB's parent; else that absolute path; `db = directory.join("history-archive.sqlite")` — the same path `history_coverage` already computes at `:1510-1518`: make `history_coverage` call `archive_paths` so there is ONE source of that path), `ensure_archive_schema(&ArchivePaths)`, `move_expired_l4(store, &paths, cutoff_ms, batch)`, `read_archive_points(&paths, since_ms, until_ms, limit)`. Add `pub const ARCHIVE_BATCH_ROWS: usize = 1_000;` and `pub const MAX_ARCHIVE_BATCHES_PER_TICK: usize = 10;` in `archive.rs`.
- **`move_expired_l4` moves ONE batch per call** (the oldest ≤ `batch` expired rows: `WHERE bucket_start_ms + 3_600_000 <= cutoff_ms ORDER BY bucket_start_ms LIMIT ?`) and returns the rows moved (0 = nothing left). The maintenance step loops it at most `MAX_ARCHIVE_BATCHES_PER_TICK` times (bounded, convergent — the rest moves next tick). Mechanics per call, all on ONE connection from `pool.acquire()`: `ensure_archive_schema` (standalone connection to the archive file, `create_if_missing`, `PRAGMA user_version = 1`, the two tables + the index) → `ATTACH DATABASE ?1 AS archive` (bound path; ATTACH/DETACH cannot run inside a transaction, so they bracket it) → `BEGIN IMMEDIATE` → select the batch's `MIN/MAX(bucket_start_ms)` and its row count → `INSERT OR IGNORE INTO archive.metric_rollups_1h SELECT … FROM main.metric_rollups_1h WHERE bucket_start_ms BETWEEN ?min AND ?max` → **verify**: `SELECT COUNT(*) FROM archive.metric_rollups_1h WHERE bucket_start_ms BETWEEN ?min AND ?max` must equal the batch row count, else `ROLLBACK` and `Err(StoreError::Archive { step: "verify" … })` → `DELETE FROM main.metric_rollups_1h WHERE bucket_start_ms BETWEEN ?min AND ?max` → `COMMIT` → `history_state_set("archiveMovedUntilMs", max + 3_600_000)` → `DETACH DATABASE archive`. **DETACH runs on every exit path** (after `ROLLBACK` too) so a connection never returns to the pool attached — the plan's `archive_is_never_attached_while_idle` test asserts `PRAGMA database_list` on the pool shows only `main`. Why `INSERT OR IGNORE` + verify: the main DB is WAL, and SQLite does not guarantee atomicity ACROSS attached databases in WAL mode, so a crash between the two file commits can leave a batch present in both; the next call finds those PKs already archived (IGNORE), verifies the count, deletes them from main — idempotent and convergent. Put that sentence in a code comment.
- **Reads never attach and never create.** `read_archive_points` and the coverage numbers open the archive file with a dedicated **read-only** `SqliteConnectOptions` (`read_only(true)`, `create_if_missing(false)`); if the file does not exist → `Ok(Vec::new())` / `bucket_count 0, oldest/newest None` (inspection never creates a database — the T6-fix1 law). Add one sentence to spec §6 `:129` recording this ruling ("moves ATTACH; reads use a read-only connection").
- `StoreError::Archive { step: &'static str, source: ArchiveErrorSource }` with `pub enum ArchiveErrorSource { Sqlx(sqlx::Error), Io(std::io::Error) }`; `Display` = `"archive step {step} failed: {source}; remedy: keep history-archive.sqlite and the main database unchanged, check the archive directory is writable, and retry — nothing was deleted from the main database"`; `std::error::Error::source` returns the inner error. Never map an archive error into `Sqlx` — the `step` is the point.
- `MaintenanceReport` gains `pub archived_l4: i64` (rows moved this tick); `expired_l4` keeps its meaning = rows removed from the main L4 table this tick (moved or deleted). The "expire L4" step: `if ladder.archive.queryable { loop ≤ MAX_ARCHIVE_BATCHES_PER_TICK: move_expired_l4(…, ARCHIVE_BATCH_ROWS) until 0 } else { prune_rollups as today }`. `LadderConfig` gains whatever it needs to carry `ArchiveSettings` + the main DB path (`SqliteHistoryStore` already knows `database_path` — prefer reading it from the store). A failed move records through `record_step_error("expire L4", …)` exactly like the other steps; the tick continues.
- `read_history_points` `Archive` arm: `archive_paths` from the settings (`self.get_settings()` is already fetched in that fn or fetch it once) → `read_archive_points(since, until, limit)` → map through the same `TierBucket → HistoryPoint` mapping the tier arms use; `resolutionMs` for `Archive` = `Tier::L4.resolution_ms()`.
- Coverage: `bucket_count = COUNT(*)`, `oldest_ms = MIN(bucket_start_ms)`, `newest_ms = MAX(bucket_start_ms)` from the archive over the read-only connection; `enabled`/`path` as today.

Tests (`agent/crates/tinytop-store/tests/archive.rs`, TDD — failing first, paste RED, implement, paste GREEN), the plan's four plus two:
1. `expired_l4_rows_move_and_main_rows_vanish_only_after_verified_insert` — seed 3 expired L4 buckets (and 1 unexpired), `archive.queryable = true`; call `move_expired_l4(…, batch = 2)` → 2 moved, main has 2 left (1 expired + 1 unexpired), archive has 2, `archiveMovedUntilMs` = second bucket's end; **inject the failure**: `#[cfg(unix)]` `chmod 0o444` the archive file (SQLite then opens it read-only and the INSERT fails) → the second call returns `Err(StoreError::Archive { step: "insert" | "attach", .. })` (assert the variant and that `step` is non-empty), main still has its 2 rows, archive still 2, the watermark unchanged, and `PRAGMA database_list` on the pool shows only `main`; restore `0o644` and a third call moves the last expired row (convergence).
2. `archive_directory_setting_relocates_the_file` — `archive.directory = <temp subdir>` → the file is created there and nothing named `history-archive.sqlite` appears beside the main DB.
3. `auto_falls_through_to_archive_for_ranges_older_than_l4` — after a move, `read_history_points` with `source: Auto` and `since_ms` older than the L4 horizon returns `source == Archive`, the archived points, `resolution 3_600_000`; through the writer test module (`writer.rs` `#[cfg(test)]`, `tower::ServiceExt::oneshot` as T4 did) one HTTP assertion that `/api/history/points?source=auto&since_ms=…` answers `"source":"archive","available":true` with points.
4. `archive_is_never_attached_while_idle` — after a successful move, every pool connection answers `PRAGMA database_list` with `main` only (acquire ≥ 2 connections and check each).
5. `expire_l4_deletes_when_archive_not_queryable` — `queryable = false`: the tick deletes as before, `expired_l4 > 0`, `archived_l4 == 0`, and no `history-archive.sqlite` exists afterwards.
6. `coverage_reports_archive_counts_and_reads_never_create_the_file` — with `queryable = true` and no archive file: coverage says `bucketCount 0`, `oldestMs/newestMs null`, and the file still does not exist after coverage + `read_history_points(source: Archive)`; after one move the counts and min/max match the archive.
Step 6 — acceptance transcript (outside your sandbox this may need a temp dir only; no sockets): `cargo run -p tinytop-agent -- db stats --json --sqlite <temp db>` after a forced move (drive the move from a test binary or a tiny `#[ignore]`d test that prints the path; do not add a CLI command) — paste the JSON `archive.queryable` block.

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`, `branch`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only.
- No new dependencies (`sqlx` already speaks `ATTACH`, `PRAGMA`, `SqliteConnectOptions::read_only`). No edits outside Task 7's Files list + the two `writer.rs` lines above + one sentence in spec §6.
- Additive only, names unchanged (spec §13): every existing coverage field keeps its name; `"rollup"` still means 1 m; Bun untouched.
- CHANGELOG: Task 7's entries under a new `## Unreleased` heading at the top (Fable assigns 0.3.1 at merge). Docs: `docs/sqlite-history-architecture.md` new `## Archive` section (file, schema, move mechanics incl. the WAL/idempotency note, read path, watermark, what a failed batch leaves behind), `README.md` one paragraph (what `retentionLadder.archive.queryable` does and where the file lives).

Gate to run and paste in full: `bun run check` (repo root). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests in `tinytop-agent` bind a local port (`PermissionDenied: should reserve a local port`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours.

Report (your final message), in this order: files changed with line ranges · tests added (names) · the RED output of your new tests before implementation · the GREEN output after · the acceptance JSON block · full gate output · `git diff --stat` · docs updated · anything the plan or this brief was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
