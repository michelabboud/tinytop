You are hexe lane **T14b** for tinytop — the dashboard half of Task 14 (the store half is lane T14, running in parallel on its own branch; you never touch its files and you do not need its changes: every item below is a pure-JS or docs change that is correct against today's daemon and required by tomorrow's). Plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` Task 14 **including its two amendment bullets (2026-08-29 and 2026-08-30 — items (f), (g), (m))**; ADR 0024 (decisions 5, 6, 8 — read them). Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is v0.5.2 `b13f89c` + the briefs commit). Every `file:line` below was read at `b13f89c`; `sed -n` each before you build on it. Build ONLY the findings below. Files you may edit: `agent/assets/dashboard/{app.js,ladder-rules.js,index.html}`, `tests/dashboard-ladder.test.ts`, `tests/dashboard-settings.test.ts`, `GUIDE.md`. NOTHING else — no Rust, no `README.md`/`INSTALL.md`/`ARCHITECTURE.md`/`docs/**` (lane T14 owns them), no `src/`, `legacy/`, `package.json`, `bun.lock`, `VERSION`, `CHANGELOG.md`, no ADR, not the plan. **Git is read-only for you — no commit, no branch, no stash; the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`. **Known sandbox limits — not failures:** Bun's `tests/server.test.ts` binds a port your sandbox denies; `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`; run `bun test tests/dashboard-ladder.test.ts tests/dashboard-settings.test.ts` for your loop and the full `bun test` once at the end (paste the summary; the sandbox-denied files are known). If a contract below contradicts the code you read, or you would be guessing, STOP and reply `ESCALATE: <the line, what the code says, what you tried>`.

**Read first:** `ladder-rules.js:12-20` (`WOULD_DELETE_FIELDS` — `snapshotJsonRows` at `:17`), `:55-57` (the raw windows `live`/`15m`/`1h`), `:109-125` (the raw-preset gate on `snapshotJsonOldestMs` at `:114-119` and the tier gate at `:121`), `:342-349` (`describeFilesystemFreshness` — the shape of a pure exported helper with a test), `:373` (the would-delete validator that iterates `WOULD_DELETE_FIELDS`), `:406` (`addCount("snapshotJsonRows", "snapshot JSON blobs", " stripped")`), `:467` (the growing-horizon comparison on `snapshotJsonKeepMinutes`), `:480` (its validation row); `app.js:133` (the default), `:390` (`daemonSnapshotJsonKeepMinutes` selector), `:955-969` (`normalizedHistorySamples` — builds `{ capturedAt, snapshot }` and DROPS `source`), `:1013` (how the points path carries `source: point.source ?? responseSource` — the pattern), `:1723-1735` (`renderSelectedSample` — returns early unless `sample.source === "raw"`; unchanged), `:1815-1817` (`pressureValue` — `?? 0`), `:1946-1948` (the pressure list rows), `:2188` (`pressureMax`), `:2233-2238` (the pressure gauge card), `:2358-2359` (the threads / runnable cards), `:2679` and `:2773` (the control's load/save), `:3396-3410` (the raw-window fetch: a `/api/history` response has NO `source` on its samples — `HistorySample` is `{capturedAtMs, snapshot}`; `windowConfig.source` is `"raw"` for these windows); `index.html:595` (the control and its label row — `sed -n '585,600p'`); `tests/dashboard-ladder.test.ts:114`, `:234-250`, `:355-365`, `:744`, `:768`, `:811`, `:831`, `:849`, `:873`, `:887`, `:954`, `:968`, `:982`; `tests/dashboard-settings.test.ts:101`; `GUIDE.md:95`, `:169`.

**F1 — dashboard replay renders again (plan amendment (g); fact: dead since the raw windows were added).** `normalizedHistorySamples` (`app.js:955-969`) moves into `ladder-rules.js` as `export function normalizeHistorySamples(samples, source)` — same de-duplication by timestamp, same sort, and every returned entry carries `source` (the `source` argument; a sample that already has a string `source` keeps it). `app.js` imports it and calls it with `windowConfig.source` (`"raw"` for `live`/`15m`/`1h`) at every call site (`grep -n normalizedHistorySamples app.js` — replace all; delete the old function). `renderSelectedSample` (`:1723-1735`) is unchanged — it now sees `source === "raw"` and calls `renderSnapshotDetails`. **Test** (`tests/dashboard-ladder.test.ts`): `normalizeHistorySamples` stamps `source` on every entry, keeps an explicit one, de-duplicates by `capturedAtMs` and sorts ascending. RED first: the current function has no `source` — paste the failing assertion before you move the code.

**F2 — pressure renders `—`, never a fabricated `0` (plan amendment (f); ADR 0024 decision 6).** History snapshots from schema v3 carry no `pressure.*.some`/`full` lines (absent keys). `pressureValue` (`:1815-1817`) returns `null` when `snapshot.pressure?.[key]?.some?.avg10` is not a finite number; add `export function formatPressureValue(value)` to `ladder-rules.js` → `"—"` for `null`/`undefined`/non-finite, else the existing two-decimal `avg10` text (find the current formatting in the pressure list at `:1946-1948` and the gauge at `:2233-2238` and route both through it); the pressure list rows render `—`; `pressureMax` (`:2188`) is the max over the FINITE values only, `null` when none — the gauge card then shows `—`, `status` neutral (whatever `metricStatus` returns for a null/no-data value today — `grep -n 'function metricStatus' app.js` and read it; if it cannot express "no data", add the smallest honest branch and say so), never `0.00 avg10`. **Tests:** `formatPressureValue(null) === "—"`, `formatPressureValue(0.42) === "0.42"` (match the existing text); the max helper if you extract one (`export function pressureMaximum(values)` → `null` for `[null, null, null]`, the max otherwise) — RED first: paste `0` where `—` is expected.

**F3 — threads and runnable render `—` when absent (plan Task 14 amendment 2026-08-29; ADR 0024 decision 5).** `LoadSnapshot.runnable`, `totalThreads`, `lastPid` become optional on the wire (absent keys from the sysinfo collectors and from v3 history where no source existed). `app.js:2358-2359`: `String(snapshot.load.totalThreads)` → `—` when not a finite number; `` `${snapshot.load.runnable} runnable` `` → `— runnable` when not finite (today `null` would render `null runnable`). Add `export function formatCount(value)` to `ladder-rules.js` (`"—"` for non-finite, else `String(value)`) and use it for both. `emptySnapshot` (`:991-997`) is the pre-first-poll placeholder — leave it. **Tests:** `formatCount(undefined) === "—"`, `formatCount(0) === "0"`, `formatCount(12) === "12"`.

**F4 — the snapshot-JSON control and the JSON horizon leave the dashboard (ADR 0024 decision 8; plan amendment (m)).** Remove: `index.html:595` and its label/row (the whole form row for `#daemon-snapshot-json-keep-minutes` — `sed -n '585,600p'`, take the row's markup, nothing beside it); `app.js:133` (the default), `:390` (the selector), `:2679` (load), `:2773` (save — the saved `retentionLadder` object no longer carries `snapshotJsonKeepMinutes`); `ladder-rules.js:17` (`"snapshotJsonRows"` out of `WOULD_DELETE_FIELDS` — the validator at `:373` then accepts a server plan without it and REJECTS one with it? No: the validator must accept BOTH shapes during the rollout — read `:373-380`: if it requires every listed field it stays exact (the list is the contract) and a plan carrying an extra `snapshotJsonRows` from a 0.5.2 daemon must still be accepted — check what it does with extra keys and make the smallest honest change; say what you found); `:406` (the `snapshotJsonRows` count line — the deletion preview no longer mentions JSON blobs); `:114-119` (the raw presets are NO LONGER disabled by a JSON horizon — a raw window is available whenever L1 retains: keep the `:121` tier gate for non-raw windows and let `:114-119` go entirely; with v3 there is no JSON horizon and the coverage has no `snapshotJsonOldestMs`); `:467` (the growing comparison — drop the `snapshotJsonKeepMinutes` term); `:480` (the validation row — the dashboard validator no longer knows the key; an old settings document loaded into the dialog simply does not populate a control that no longer exists). **Tests:** `tests/dashboard-ladder.test.ts:114` (fixture), `:234-250` (the "disables raw presets when coverage reports no retained snapshot JSON" test becomes "raw presets stay enabled without a JSON horizon" — a coverage object WITHOUT `snapshotJsonOldestMs` leaves `live`/`15m`/`1h` enabled), `:355-365` (the "snapshot JSON range" row leaves the validation table), the would-delete fixtures at `:744`, `:768`, `:811`, `:831`, `:849`, `:873`, `:954`, `:968`, `:982` (drop `snapshotJsonRows`; add ONE test that a plan carrying an extra `snapshotJsonRows` from an older daemon is still accepted, per what you found at `:373`), `:887` (`"90 snapshot JSON blobs stripped"` expectation goes); `tests/dashboard-settings.test.ts:101`. RED first: after the JS change and before the test edits, paste the failing tests, then the GREEN.

**F5 — `GUIDE.md`.** `:95` (the dialog's ranges no longer list the JSON window), `:169` ("full snapshot JSON for UI hydration" → history detail is assembled from typed tables since schema v3; the pressure card and the threads/runnable cards show `—` when a value was not recorded; a point selected in `live`/`15m`/`1h` renders its processes and filesystems). Nothing else in GUIDE.

**Gate before you report:** `bun test tests/dashboard-ladder.test.ts tests/dashboard-settings.test.ts` green (paste the summary — the 0.5.2 full run counted 192 across 19 files; your two files gain the F1–F4 tests and lose the JSON ones); the full `bun test` summary; `bun run src/server.ts --check` and `bun run legacy/bun-collector.ts --check` clean (you did not touch them; prove it); `git status --short` and `git diff --stat` ⊆ the allow-list; `grep -n 'snapshotJson\|snapshot JSON' agent/assets/dashboard/*.js agent/assets/dashboard/index.html tests/dashboard-*.test.ts GUIDE.md` shows ONLY the F4 compatibility test's fixture line — paste it.

**Report shape:** per finding RED → GREEN (pasted); the new test names; the base commit; what the would-delete validator does with extra keys; anything outside this brief under `## Outside this lane` — never fixed silently. The orchestrator's own acceptance (outside your sandbox, after T14 and T14b both merge): Playwright on a temp daemon at `:4290` — the `1h` window, a point 30 minutes back renders processes and filesystems, the pressure card shows `—` for that point, the threads card shows a number (Linux) — and `bun run check` on main.
