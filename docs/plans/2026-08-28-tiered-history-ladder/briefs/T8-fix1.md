You are hexe lane **T8-fix1b** for tinytop, continuing run 593 (T8-fix1), which escalated correctly on F2 after finishing F1. You work IN PLACE in the fix1 worktree on branch `tinytop/ladder-t8-cold-export-fix1`, whose HEAD Fable committed: `e20190f` = F1 DONE (hermetic `TINYTOP_SYSTEMD_UNIT_DIR`; do not redo it — only keep its tests green), then the F2 lock commit (`flate2 =1.1.10`; `Cargo.toml`/`Cargo.lock` are FINAL — do not touch them; `cargo fetch` was run outside the sandbox, so the crates are in the local registry). Base of the branch = `tinytop/ladder-t8-cold-export` at `a6eec4b` (cold export, run 584). Fix ONLY the findings below (F2 report part, F3–F6); do not revisit the design. Scope rules: do not touch `archive.rs`'s export chain order, `maintenance.rs`, `migration.rs`, `retention_ladder.rs`, `disk.rs`, `ladder.rs`, `agent/assets/`, Bun's `src/`, `PROGRESS.md`, any ADR.

Read first: `CLAUDE.md`; `docs/plans/2026-08-28-tiered-history-ladder/briefs/T8.md` (the contract you are amending); the T8 commit message (`git log -1`) — it records what Fable verified and what remains.

**F1 — P1 (operational hazard): `bun run check` stops the live tinytop service.** `tests/tinytop-script.test.ts` "stop does not kill unrelated installed tinytop-agent processes" runs the real `./tinytop stop`, which calls `unit_installed` = `[[ -f "$HOME/.config/systemd/user/<unit>" ]]` (`tinytop` `unit_path()`), finds the box's REAL `tinytop.service`, and runs `systemctl --user stop tinytop.service` — on 2026-08-29 it stopped Michel's running daemon (journal: "Stopping tinytop.service" at 05:29:51Z during the gate). Ruling, hermetic and minimal: (a) in `tinytop`, `unit_path()` (and every `mkdir -p "$unit_dir"` / `unit_dir` in `cmd_systemd_install`/`cmd_systemd_uninstall`/`cmd_systemd_render` comments) resolves the unit directory from ONE new override `TINYTOP_SYSTEMD_UNIT_DIR` (default `$HOME/.config/systemd/user`; document it in `print_help`'s environment section beside `TINYTOP_HISTORY_DB`, and in `README.md`'s command-center environment table if one exists — say which); (b) in `tests/tinytop-script.test.ts`, `runTinytop`'s DEFAULT env sets `TINYTOP_SYSTEMD_UNIT_DIR` to a fresh empty `mkdtempSync` directory (created once per test file or per call — per call is fine) so NO test in that file can ever observe or act on the real user units; a caller may still override it. (c) Add one test: "stop with an installed unit under the override runs systemctl on that unit only" — write a fake `systemctl` stub into the PATH dir that records its argv to a file and exits 0, create `<override>/tinytop.service`, run `stop`, assert the stub was called with `--user stop tinytop.service` and that stdout contains no "foreground" wording. This proves the override is honoured by the systemd branch too. `bun test tests/tinytop-script.test.ts` must pass WITH a real `~/.config/systemd/user/tinytop.service` present on the box (Fable will run it that way; your sandbox has none — say so). Windows wrapper `tinytop.ps1` has no systemd — untouched.

**F2 — rule 5, RULED by Fable after run 593's escalation (the lock is already committed; your part is the report + CHANGELOG + verification).** `flate2 =1.1.10` (released 2026-08-28) is pinned and locked. The offline update added `zlib-rs 0.6.7` to `Cargo.lock` — Fable's ruling at source: `zlib-rs` is an OPTIONAL dependency of flate2 behind its `zlib-rs` backend feature, which tinytop does not enable; flate2 1.1.10's new default feature `runtime_detection = ["zlib-rs?/std", "crc32fast?/std"]` references it as a WEAK feature (`?/`), which makes Cargo record the package in the lock without activating it. Verify and paste: `cargo tree --manifest-path agent/Cargo.toml --target all -p tinytop-store -e features -i zlib-rs --offline` (expect `warning: nothing to print`) and `cargo tree --manifest-path agent/Cargo.toml -p tinytop-store -e features -i flate2 --offline` (expect `rust_backend` → `miniz_oxide`, plus `runtime_detection`, no `zlib-rs`/`zlib`/`zlib-ng` feature). Then `cargo test -p tinytop-store --test archive --offline` (the decoder change is exercised by the verify tests) and paste. Update `docs/reports/2026-08-29-dependency-vetting-flate2-sha2.md`: (a) version 1.1.10, release date, and the two fixes that matter here — "Fix infinite loop in writing gzip header/footer" (rust-lang/flate2-rs#547) on the `GzEncoder` writer path and "Reject incomplete deflate streams at EOF" (#556) on the `GzDecoder` verify path — plus `miniz_oxide` 0.9.1 (`simd`); (b) a subsection "Lock-only entry: zlib-rs 0.6.7" stating the weak-feature mechanism above, that it is never compiled on any target (paste the `--target all` line), and a short vetting of it regardless (so the record holds if a future feature activates it): pure-Rust zlib port by the Trifecta Tech Foundation, licence Zlib, MSRV 1.75, no non-optional dependencies, crates.io 0.6.7 published 2026-08-03, ~119.7 M downloads, repo `trifectatechfoundation/zlib-rs` pushed 2026-08-23, not archived; (c) a subsection "Outside-sandbox verification (Fable, 2026-08-29)": crates.io `max_stable_version` flate2 1.1.10 (2026-08-28), sha2 0.11.0 (2026-03-25; the 0.11.0-rc.x line ended 2026-02-02); live `cargo audit` on the final lock: 0 vulnerabilities, 3 pre-existing allowed warnings (`event-listener` 5.4.1 RUSTSEC-2026-0221 unsound, `chacha20` 0.10.1 yanked, `spin` 0.9.8 yanked — none on the flate2/sha2 path); and name `const-oid 0.10.2` as the one transitive addition from `sha2` (via `digest`'s default `oid` feature — sqlx already carried `sha2 0.11.0` without it). `CHANGELOG.md` `## Unreleased`: the pin line says 1.1.10 and mentions the inert `zlib-rs` lock entry in half a sentence.

**F3 — P3: `history_coverage` opens the archive twice** (`archive_coverage` then `read_archive_manifest`, each a read-only connection) on every coverage call (dashboard refresh, `db stats`). Either fold the manifest read into one helper that returns `(count, oldest, newest, manifest)` over one connection, or leave it and say why in one sentence in the T8 commit's follow-up (a second short-lived read-only open of a small file). Fable accepts either; do not widen.

**F4 — P3: dead tuple element.** `connect_for_db_diagnostic` now refuses a missing database, so its `bool` (`database_existed`) is always `true` and every caller binds `_`. Remove the bool from the signature and the callers (`stats`, `check`, `vacuum`, `db_archive`, `pre-image` if it uses it) — `main.rs` only; behaviour unchanged.

**F5 — docs truth.** `README.md` `retentionLadder.diskCheck` row still says "Disk checking lands in the disk phase" — correct, leave it. `GUIDE.md:99` is complete — leave it. `docs/sqlite-history-architecture.md` `### Cold export` — verify the per-step table still matches the code after F2 (it does not change the chain; confirm).

**F6 — luna's findings (hexe run 589), validated by Fable — Fabulous `docs/fleet/tinytop/2026-08-29-ari-luna-t8-review.md`; implement each as written:**
- **L1 (P3, contract):** `read_months_on` lists months with `strftime('%Y-%m', bucket_start_ms / 1000, 'unixepoch')` — integer division truncates toward zero, so `bucket_start_ms = -1` lists as `1970-01` while `month_bounds` puts it in `1969-12`. Change the divisor to `1000.0` (SQLite's `unixepoch` accepts fractional seconds) in BOTH places in that query. Test `cold_export_month_listing_agrees_with_month_bounds` in `tests/archive.rs`: seed L4 buckets at `-1`, `0`, `1706745599999` (2024-01-31T23:59:59.999Z) and `1706745600000` (2024-02-01T00:00:00Z), move them with cutoff `i64::MAX`, export with a ladder whose `coldAfterMonths = 1`, `l4.keepDays = 30`, `now` = `AUG_29_2026_MS` → exactly four manifest rows for `1969-12`, `1970-01`, `2024-01`, `2024-02`, each `rowCount 1` (this proves the SQL month and the Rust bounds agree at the boundaries, including the negative one, with no new `pub` surface).
- **L2 (P3):** an archive with `user_version = 1` but fewer than the 3 required objects is `accepted()` but not `current()`: `read_archive_manifest` returns `Ok(vec![])` while `export_cold_months` refuses it, and `db archive status` shows `manifest: []` with no explanation. Add `pub enum ArchiveSchemaState { Absent, Incomplete { user_version: i64, required_objects: i64 }, Current }` and `pub async fn archive_schema_state(paths: &ArchivePaths) -> Result<ArchiveSchemaState, StoreError>` (read-only, never creates, reuses `inspect_archive_schema`; a NOT-accepted file still returns the existing refusal error). `db archive status`: when `Incomplete`, set `reason` = `history-archive.sqlite at <path> has user_version 1 but <n> of 3 required objects; the next L4 move completes the schema; cold export refuses until then` and omit `nextExportableMonths` (keep the L4-disabled/forever reasons as they are; precedence: incomplete archive first). Reads stay lenient (coverage must never fail because of this). Test in `tests/archive.rs`: after a move creates a full archive, `DROP TABLE archive_manifest` through a raw sqlx connection, then `archive_schema_state` = `Incomplete { user_version: 1, required_objects: 2 }`, `read_archive_manifest` = `Ok(vec![])`, `export_cold_months` = `Err(Archive { step: "cold read", .. })`. If `tinytop-agent`'s tests can reach sqlx (check its `Cargo.toml` dev-dependencies — say which), also assert the `reason` through `db archive status` in `cli_db.rs`; if not, say so and stop there.
- **L3 (P2, error naming + doc):** the step name `cold fsync` covers two failure points on opposite sides of the rename — `sync_all` on the `.tmp` (before) and `sync_directory` (after). Rename the directory step to `cold directory fsync` (`sync_directory` only); `archive_remedy`'s `cold *` phase text is unchanged. `docs/sqlite-history-architecture.md` `### Cold export` failure list: keep `cold fsync` in the `.tmp` bullet; add a bullet `cold directory fsync` = the verified target exists with no sidecar, manifest row, or watermark; retrying re-exports and replaces it (the same convergent class as sidecar/manifest). `archive_error_remedy_is_step_aware` covers the new name.
- **L4 (P3, test gap):** `db_archive_export_now_writes_seeded_month` asserts on the `status` manifest row: `month == "2023-01"`, `file == "tinytop-1h-2023-01.csv.gz"`, `rowCount == 1`, `bytes > 0` and equal to the file's size on disk, `sha256` = 64 lowercase hex chars, and `value.cold.exportedUntilMonth == "2023-01"`.
- **L5 (P3, verify robustness + test gap):** `verify_cold_file` also rejects any data record whose field count differs from the header's (step `cold verify`; message names the record index and both widths). Test `corrupted_record_width_fails_verification`: export one month, decompress the finished file, append `,1` to the first data record, recompress with `flate2::write::GzEncoder` to the same path (flate2 is available to the store's integration tests), and assert `verify_cold_file` returns `Err(Archive { step: "cold verify", .. })`. `csv_round_trip_is_row_exact` asserts `records.len() == archived.len() + 1` (not a literal) and every record's `len() == 19`.
- **L6 (P3, test gap):** after the pass in `cold_export_writes_verified_month_files`, assert `store.attached_database_names()` on the store pool equals `["main"]` (the export never attaches; reuse T7's helper).
- **L7 (P3):** a comment in `tests/archive.rs` at the top of the cold tests: `// cold_requires_queryable: covered by tests/retention_settings.rs::retention_ladder_validation_rejects_every_invalid_shape_with_exact_error (cold=true, queryable=false).`
- **L8 (doc):** one sentence in `### Cold export` after the late-row sentence: a month with no archived rows is not exportable and is skipped; because L4 hours expire and move in ascending order and eligibility requires the month's last hour to have expired at least a day earlier, a later month cannot become exportable while an earlier one still has unmoved rows — a row that appears for a skipped month afterwards is a late row and stays queryable-only.
- Rejected by Fable (no action): root re-exports in `lib.rs`; the extended `archive_error_remedy_is_step_aware` test.

Rules (non-negotiable): git READ-ONLY (no add/commit/push/checkout/stash/branch; leave the tree dirty; report `git diff --stat`) · never bare `cargo fmt` (`cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; `rustfmt --edition 2024 <file>` for a file you changed) · never open `~/.local/share/tinytop/` · tests in `std::env::temp_dir()` only · `Cargo.toml`/`Cargo.lock` are FINAL at HEAD — any diff in them is a finding against you.

Gate: `bun run check` at the repo root, pasted in full. Known sandbox limits (say so verbatim; treat as passed-for-you if these are the ONLY failures): (1) the 10 `serve_contract` tests bind a port (`PermissionDenied`); (2) Bun's `tests/server.test.ts` `Bun.serve({ port: 0 })`; (3) `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Anything else is yours.

Report (final message, in order): files changed with line ranges · the two `cargo tree` transcripts (F2) · `cargo test -p tinytop-store --test archive` summary · test names added for F6 with RED/GREEN · full gate output · `git diff --stat HEAD` (uncommitted work only — Fable commits) · anything this brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
