You are hexe lane **T14** for tinytop — the third lane of Phase 5 and the store half of Task 14 (the dashboard half is lane T14b, running in parallel on its own branch; you never touch its files). Plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` Task 14 **including its two amendment bullets (2026-08-29 and 2026-08-30)**, decisions 2/4/7/8 in §2, and §4; **ADR 0024** (read it whole — it is the design; ADR 0021 and 0023 are its parents). Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is v0.5.2 `b13f89c` + this brief's commit). Every `file:line` below was read at `b13f89c`; run `sed -n` / `grep -n` on each before you build on it. Build ONLY the findings below. Files you may edit: `agent/crates/tinytop-types/src/lib.rs`; `agent/crates/tinytop-collectors/src/{lib,linux,common}.rs`; `agent/crates/tinytop-store/src/{migration,lib,maintenance,retention_ladder,settings_transfer}.rs`; `agent/crates/tinytop-store/tests/*.rs` (new `migration_v3.rs`, `history_assembly.rs`; the existing files named under F5); `agent/crates/tinytop-agent/src/{main,writer,otel}.rs` and `agent/crates/tinytop-agent/tests/cli_db.rs`; docs: `docs/sqlite-history-architecture.md`, `docs/guides/API.md`, `README.md`, `INSTALL.md`, `ARCHITECTURE.md`. NOTHING else — in particular no `agent/Cargo.toml`, `agent/Cargo.lock`, any `crates/*/Cargo.toml`, `agent/assets/dashboard/*`, `tests/*.ts`, `GUIDE.md`, `src/`, `legacy/`, `VERSION`, `CHANGELOG.md` (Fable writes the line at merge), `docs/adr/*`, the plan, the spec, `archive.rs`, `disk_check.rs`, `vendor/`. **Git is read-only for you — no commit, no branch, no stash, no bare `cargo fmt` (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` and fix by hand); the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; `std::env::set_var` is forbidden. **Known sandbox limits — not failures:** every `serve_*` test binds a port your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); Bun's `tests/server.test.ts` likewise; there is no crates.io in the sandbox — `--offline` works, everything you need is in the lock. If a contract below contradicts the code you read, or you would be guessing, STOP and reply `ESCALATE: <the line, what the code says, what you tried>` — escalating early beats a wrong build. Plan §4 binds you: a migration slower than 60 s on the orchestrator's real-file copy is batched, never skipped; an in-transaction failure refuses with the underlying error and rolls back, never a silent fallback.

**Read first:** ADR 0024 (all); `migration.rs:39-70` (`CREATE_SCHEMA_V1_SQL`'s `metric_samples` — the v3 shape keeps every column but `snapshot_json`), `:152-175` (`history_state`, `fs_samples` — unchanged tables), `:344-366` (the v2 process tables — unchanged), `:587-680` (`migrate_v1_to_v2`: the one-transaction shape you mirror — version check before `begin`, guard, marker, `PRAGMA user_version`, commit; and the fix1 `map_err` at `:641-650` that keeps SQLite's own error), `:716-790` (the v0 phases — the chain v0→v1→v2→v3 must still run), `:848-877` (`rebuild_v0_schema`: create-copy-drop-rename, the pattern for the v3 rebuild); `lib.rs:36-39` (`HistorySample { captured_at_ms, snapshot }` — the wire shape, unchanged; `tests/history_json_contract.rs` pins it), `:325-350` (`HistoryCoverage`; the JSON fields at `:340-342` go), `:464-468` (`HistoryQuery`), `:952-1100` (`insert_snapshot`: the metric upsert is autocommit today; `root_used_percent` derivation; the rollup fold — unchanged), `:1270-1380` (`write_detail_rows_if_due`: filesystems and minute process rows on the detail cadence; the filesystem part moves out), `:1450-1494` (`latest_snapshot` — deleted; `read_history` — becomes the assembler's entry), `:1554-1599` (`read_history_filesystems` — unchanged code, changed meaning: stored rows), `:1603-1700` (`read_history_processes` — the fast/minute rule you reuse for assembly), `:1776-1782` + `:1871-1872` (coverage JSON counters — go), `:1944` (`count_snapshot_json_older_than` — goes), `:1985` (`strip_snapshot_json` — goes), `:2011` (the `fs_samples` prune — gets the floor rule), `:2624-2638` (`row_to_sample` — goes); `maintenance.rs:9`, `:19`, `:29` (`JSON_STRIP_BATCH`, `LadderConfig.snapshot_json_keep_ms`, `MaintenanceReport.json_stripped`), `:195-202` (the strip step); `retention_ladder.rs:22`, `:81`, `:224`, `:335`, `:359` (the `snapshot_json_keep_minutes` field, default, validation row, `to_ladder_config`, the growing comparison); `settings_transfer.rs:53-59` (`WouldDelete`), `:232-249` (`collect_unknown_settings_keys` — the generic `unknown key ignored` warning), `:282-294` (the would-delete counts); `main.rs:78-86` (`DbStatsOutput` — `user_version` exists since T13; `snapshot_json_sample_count` goes), `:330`; `writer.rs:113`, `:281-290`, `:332`, `:340`, `:446` (the memory-backed `/api/snapshot`), `:463-468` (`/api/history`, `history_coverage`), `:660-668` (`read_history_with_backfill`), `:680-687` (`collect()` then `insert_snapshot(now_ms()?, …)` — `captured_at_ms` is taken AFTER the collector returns, so a snapshot's `filesystemsCapturedAtMs` never exceeds it); `tinytop-types/src/lib.rs` (`grep -n 'pub struct'`: `RuntimeDetection { kind: RuntimeKind, confidence: RuntimeConfidence, reason }`, `IdentitySnapshot`, `LoadSnapshot` (its `total_threads` doc comment already says "Schema v3 makes this optional"), `PressureSnapshot { some: Option<PressureLine>, full: Option<PressureLine> }` with `skip_serializing_if`, `SystemSnapshot.filesystems_captured_at_ms: Option<i64>`; the `with_optional_fields` test near `:264-290` is the pattern for absent-key round trips); `tinytop-collectors/src/common.rs` (`process_totals` — T12-fix1's stopgap, goes), `linux.rs` (`/proc/loadavg` → `runnable`/`total_threads`/`last_pid` stay `Some`); `tests/migration_v2.rs` (`TempDatabase`, `seed_v1_processes`, `marker_count`, the rollback test from T13-fix1 — the styles you mirror); `tests/process_history.rs`; `tests/ladder_maintenance.rs` (`maintain_with_config` usage).

**F1 — `LoadSnapshot.runnable`, `total_threads`, `last_pid` become `Option<u64>` (plan Task 14 amendment 2026-08-29 + ADR 0024 decision 5).** `#[serde(default, skip_serializing_if = "Option::is_none")]` on all three; `None` = no source. Linux (`linux.rs`, `/proc/loadavg`) keeps `Some` for all three; the sysinfo collectors (`common.rs`) report `None` for all three and `process_totals` (T12-fix1) is deleted with its unit test and the API.md sentence that documented it (grep `process_totals` and `totalThreads` in `docs/guides/API.md`). Every producer and consumer compiles without `unwrap` on these: the store writes NULL for `None` (F3), the assembler reads `Option<i64>` (F4), the OTel exporter (`otel.rs` — `grep -n 'total_threads\|runnable\|last_pid'`) SKIPS a gauge whose value is `None` (never records 0; if no gauge reads them, say so in the report). Types tests: a document without the three keys deserializes to `None` (mirror `with_optional_fields`), `None` serializes as an ABSENT key (assert `get("runnable").is_none()`), `Some` round-trips. RED first: the absent-key deserialization fails on the current `u64` fields — paste it.

**F2 — schema v3: `migrate_v2_to_v3` in ONE transaction, no pre-image (ADR 0024 decisions 1–4, 9; ADR 0023's shape).** DDL (a new `CREATE_SCHEMA_V3_SQL` group array mirroring `CREATE_SCHEMA_V2_SQL` `:405` — its last group sets `PRAGMA user_version = 3` the way v2's does at `:402` — applied on fresh files by `apply_schema_groups` `:570`; `migrate_v2_to_v3` uses its pieces):
```sql
CREATE TABLE IF NOT EXISTS host_identity (
  identity_id INTEGER PRIMARY KEY,
  first_seen_ms INTEGER NOT NULL,
  hostname TEXT NOT NULL, platform TEXT NOT NULL, arch TEXT NOT NULL, distro TEXT NOT NULL, kernel TEXT NOT NULL,
  runtime_kind TEXT NOT NULL, runtime_confidence TEXT NOT NULL, runtime_reason TEXT NOT NULL,
  UNIQUE (hostname, platform, arch, distro, kernel, runtime_kind, runtime_confidence, runtime_reason)
);
CREATE TABLE IF NOT EXISTS fs_mount_events (
  captured_at_ms INTEGER NOT NULL,
  mount TEXT NOT NULL,
  present INTEGER NOT NULL CHECK (present IN (0, 1)),
  PRIMARY KEY (mount, captured_at_ms)
) WITHOUT ROWID;
```
and `metric_samples` v3 = every `CREATE_SCHEMA_V1_SQL` column in the same order EXCEPT `snapshot_json`, with `runnable_threads INTEGER` and `total_threads INTEGER` (nullable), then `identity_id INTEGER REFERENCES host_identity(identity_id)`, `uptime_seconds INTEGER`, `memory_available_bytes INTEGER`, `swap_free_bytes INTEGER`, `last_pid INTEGER`, `filesystems_captured_at_ms INTEGER`, and the table constraint `CHECK (identity_id IS NULL OR (uptime_seconds IS NOT NULL AND memory_available_bytes IS NOT NULL AND swap_free_bytes IS NOT NULL))`; both `idx_metric_samples_*` indexes recreated on it (`runtime_kind`/`hostname` columns stay — the archive and rollups do not touch them, but the index exists). `runtime_kind` / `runtime_confidence` are stored as their serde string forms (`RuntimeKind::as_str()` exists — `grep -n 'fn as_str' tinytop-types`; give `RuntimeConfidence` the same if it lacks one). **Migration steps, all inside ONE `pool.begin()` after `PRAGMA user_version` was read as 2 (mirror `:592-598`):** (1) `CREATE TABLE metric_samples_v3 (…)` (the v3 shape under a temporary name) + `host_identity` + `fs_mount_events`; (2) `INSERT INTO metric_samples_v3 (<every v1 column but snapshot_json>) SELECT <same> FROM metric_samples` — `sample_id` preserved; (3) `SELECT sample_id, snapshot_json FROM metric_samples WHERE snapshot_json IS NOT NULL ORDER BY sample_id` — for each row decode `SystemSnapshot` (a decode failure REFUSES the migration: `StoreError::Migration { reason: "metric_samples row <sample_id> holds snapshot JSON that does not decode: <serde error>", remedy: "the row was written by an unknown writer; inspect it with `db check`; the database was not modified" }` and the `?` rolls back), intern its identity into `host_identity` (`INSERT OR IGNORE` on the eight strings, `first_seen_ms = captured_at_ms` of the first row that carried it, then `SELECT identity_id`), and `UPDATE metric_samples_v3 SET identity_id = ?, uptime_seconds = ?, memory_available_bytes = ?, swap_free_bytes = ?, last_pid = ?, filesystems_captured_at_ms = ? WHERE sample_id = ?` (`last_pid` / stamp NULL when absent in the JSON — a 0.3.1 daemon never wrote `filesystemsCapturedAtMs`); (4) **guard:** `SELECT COUNT(*) FROM metric_samples_v3 WHERE identity_id IS NOT NULL` must equal the JSON row count, else refuse (`StoreError::Migration`, rollback) — the ADR 0023 in-flight guard, v3 edition; (5) `DROP TABLE metric_samples`; `ALTER TABLE metric_samples_v3 RENAME TO metric_samples`; recreate the two indexes; (6) backfill presence: `INSERT INTO fs_mount_events (captured_at_ms, mount, present) SELECT MIN(captured_at_ms), mount, 1 FROM fs_samples GROUP BY mount` and `INSERT INTO fs_mount_events (captured_at_ms, mount, present) SELECT MAX(captured_at_ms) + 1, mount, 0 FROM fs_samples GROUP BY mount HAVING MAX(captured_at_ms) < (SELECT MAX(captured_at_ms) FROM fs_samples)` (a mount absent from the newest detail tick is gone); (7) one `schemaMigrated` app-event marker whose details carry `from: 2, to: 3`, the row count, the JSON rows decoded, the identities interned, the events written, the elapsed ms (mirror v2's marker at `:668`); (8) `PRAGMA user_version = 3`; commit. `migrate` dispatch: 0 → the v0 phases → 1 → v2 → v3; the v0→v1 rebuild's JSON cutoff uses a new `const V0_JSON_KEEP_MS: i64 = 60 * 60 * 1000` with a doc comment (the setting is gone — plan amendment (n)); a fresh file is created directly at v3 (`user_version` 3, no marker — the `:570` path with the v3 groups). `require_sqlite_at_least` is NOT called by v3 (no `DROP COLUMN`); it stays for v2. **Tests `tests/migration_v3.rs`** (mirror `migration_v2.rs`'s fixtures; `CREATE_SCHEMA_V2_SQL` is a private five-group array at `migration.rs:405` applied by `apply_schema_groups` at `:570` — export it `#[doc(hidden)] pub` for the v2 fixture, the way `CREATE_SCHEMA_V1_SQL` is): (a) `v2_fixture_with_json_rows_migrates_to_v3` — a real-v2-DDL fixture with 3 JSON-bearing rows (two distinct identities: the third differs in `kernel`), 2 stripped rows (`snapshot_json NULL`), `fs_samples` at two detail ticks where mount `/data` exists only at the first → after `connect`: `user_version` 3; `pragma_table_info('metric_samples')` has no `snapshot_json`, has the six new columns, and `runnable_threads`/`total_threads` report `notnull = 0`; the 3 JSON rows have `identity_id NOT NULL` with `uptime_seconds`/`memory_available_bytes`/`swap_free_bytes`/`last_pid`/`filesystems_captured_at_ms` equal to their JSON (assert each, per row); the 2 stripped rows have all six NULL; `host_identity` = 2 rows with `first_seen_ms` = the first carrying row's `captured_at_ms`; `fs_mount_events` = `present 1` at each mount's first tick and `present 0` at `tick1 + 1` for `/data` only; `sample_id` values unchanged; exactly one marker with `from 2 / to 3`; (b) `fresh_database_is_created_at_v3` — `user_version` 3, 0 markers, `PRAGMA integrity_check` = `ok`, `host_identity` and `fs_mount_events` exist; (c) `v2_fixture_with_undecodable_json_refuses_and_leaves_the_file_untouched` — one row's `snapshot_json` = `{"not":"a snapshot"}` → `connect` fails with `StoreError::Migration` whose `reason` names the `sample_id`; reopen raw: `user_version` 2, `snapshot_json` column present, no `host_identity`, 0 markers (the T13-fix1 rollback style — RED first: without the guard the assertion cannot fail; paste your RED); (d) the ignored real-file test moves from `migration_v2.rs` to here as `#[ignore] real_v1_file_copy_migrates_to_v3` — same `TINYTOP_V1_FIXTURE=<path>` contract as T13's (`migration_v2.rs:388`), the chain v1→v2→v3 on the copy, prints the elapsed ms per step, asserts `integrity_check` = `ok` and `user_version` 3 (the orchestrator runs it outside your sandbox on a `sqlite3.backup()` copy of the live file — you do NOT run it); (e) `migration_v1.rs`: `bun_created_database_migrates_the_same_way` and `v0_database_is_migrated_with_pre_image_and_json_window` now end at v3 — the "JSON window" assertions become "assembleable rows (`identity_id IS NOT NULL`) are exactly the rows that were inside the 60-minute window"; the fresh-file version test (`grep -n 'fn fresh_database' tests/migration_v1.rs tests/migration_v2.rs` — T13 may have moved or renamed it) asserts `user_version` 3 and is named `fresh_database_is_created_at_schema_version_3`; `migrated_history_reads_only_rows_that_keep_snapshot_json` → `migrated_history_reads_only_assembleable_rows`. Every nullable column is read as `Option<i64>` — never `try_get::<i64>` on a nullable column (sqlx-sqlite decodes NULL as 0 silently).

**F3 — the writer: identity interning, per-row scalars, filesystems on change with presence events (ADR 0024 decisions 3–4; plan §4's separate-transaction note).** `insert_snapshot(captured_at_ms, snapshot)`: in ONE `pool.begin()` transaction — (i) intern the identity: the store keeps `Mutex<Option<CurrentIdentity { identity_id, strings: [String; 8] }>>` primed at `connect` from the newest `metric_samples` row's `identity_id` (joined to `host_identity`; `None` on an empty file); when the snapshot's eight strings differ (or nothing is cached) → `INSERT OR IGNORE` + `SELECT identity_id`, `first_seen_ms = captured_at_ms`, cache it; (ii) the metric upsert gains `identity_id`, `uptime_seconds`, `memory_available_bytes`, `swap_free_bytes`, `last_pid` (NULL for `None`), `filesystems_captured_at_ms` (the snapshot's stamp or NULL), nullable `runnable_threads`/`total_threads`, and loses `snapshot_json` (no `serde_json::to_string` on the hot path any more — delete it); (iii) filesystems on change: `fs_key_ms = snapshot.filesystems_captured_at_ms.unwrap_or(captured_at_ms)`; the store keeps `Mutex<FilesystemState { last_key_ms: Option<i64>, last_rows: HashMap<String, FsRow>, present: HashSet<String> }>` primed at `connect` (newest `fs_samples` row per mount via `idx_fs_samples_mount_time`; `present` = mounts whose newest `fs_mount_events` row has `present = 1`); when `fs_key_ms` differs from `last_key_ms` (a new enumeration) → for each mount in the snapshot: not in `present` → `fs_mount_events (fs_key_ms, mount, 1)` + a `fs_samples` row at `fs_key_ms`; in `present` and differing from `last_rows[mount]` in `filesystem`, `fs_type`, `size_bytes`, `used_bytes`, `available_bytes`, `inode_used` or `inode_total` → a `fs_samples` row at `fs_key_ms` (the `ON CONFLICT` upsert at `:1302-1316` handles a repeated key); for each mount in `present` but absent from the snapshot → `fs_mount_events (fs_key_ms, mount, 0)`; update the state; when `fs_key_ms` equals `last_key_ms` → no filesystem write at all (the collector served its cache); (iv) commit. The rollup fold and `root_used_percent` are unchanged. `write_detail_rows_if_due` keeps ONLY the minute process rows (delete its `fs_samples` loop and the `DELETE FROM fs_samples WHERE captured_at_ms = ?`); `detail_rows` (rows written since the last pass) now counts filesystem rows written on change + minute process rows — keep `MaintenanceReport.detail_rows`'s meaning (`maintenance.rs:102` per the T13 brief). The process rows (fast + minute) keep their own T13 transaction and run AFTER the metric transaction committed; a failure there is logged (`warn`, once per minute — mirror the OTel once-a-minute pattern in `writer.rs`/`otel.rs`) and does NOT fail the tick (plan §4, luna note (e)) — `insert_snapshot` returns the `HistorySample` of the committed metric row; make the returned `HistorySample.snapshot` the assembled snapshot of that row (F4's assembler on the freshly written row), NOT the input clone, so the writer's own return value proves assembly (the writer route tests compare it). **Prune floor** (`lib.rs:2009-2020` `prune_detail_history`): `fs_samples` rows older than the horizon are deleted EXCEPT each mount's newest row; `fs_mount_events` older than the horizon are deleted EXCEPT each mount's newest event; then, for every mount whose newest event is `present = 0` AND older than the horizon, delete that mount's remaining row and event (the mount is gone and beyond the window). **Tests** (`tests/history_assembly.rs`, the store-level half): `filesystem_rows_are_written_only_on_change_and_events_on_presence` — five ticks at 1.5 s with the SAME stamp across ticks 1–2 (cache served → 1 row per mount, 1 appear event per mount, nothing on tick 2), a new stamp at tick 3 with `/` unchanged and `/data` grown (`used_bytes` +1) → exactly one new `fs_samples` row (`/data`), no event; tick 4 new stamp without `/data` → one `present 0` event, no row; tick 5 new stamp with `/data` back → one `present 1` event + one row; assert every count by SQL after each tick and that `metric_samples.filesystems_captured_at_ms` equals the stamp per row; `unstamped_snapshot_keys_filesystems_by_captured_at` (`filesystems_captured_at_ms: None` → `fs_key_ms = captured_at_ms`, a row written on every tick that changes a value, none otherwise); `identity_is_interned_once_until_a_string_changes` (three ticks same identity → 1 `host_identity` row, all rows point to it; a fourth with a new `kernel` → a second row, `first_seen_ms` = tick 4); `state_is_primed_at_connect` (close, reopen, tick with the same stamp/values → zero new rows/events; a `HashMap`/`HashSet` that was not primed would write duplicates — RED first by connecting without priming in your head is not evidence: paste the run before and after the priming code exists); `prune_keeps_the_carry_forward_floor` (one mount unchanged for longer than the L2 horizon keeps its newest row and appear event after `maintain_with_config`; a mount whose `present 0` event is older than the horizon loses both); `a_process_row_failure_does_not_fail_the_tick` (drop `process_commands` via a raw pool before the tick → `insert_snapshot` returns `Ok`, the metric row exists, exactly one warning emitted — capture via a tracing subscriber the way `writer.rs` tests capture logs, `grep -n 'tracing_subscriber\|with_default' agent/crates/tinytop-agent` — if the agent never captures logs, assert the returned `Ok` and the metric row only and say so).

**F4 — `/api/history` assembled from typed tables (ADR 0024 decision 8; plan amendment (l)).** New `assemble_history_snapshots(rows) -> Vec<HistorySample>` on the store, batched by window — NEVER one query per row: (1) the metric rows of the window (`read_history`'s SQL with `WHERE identity_id IS NOT NULL` instead of the JSON predicate, same `since`/`until`/`limit`/order); (2) one `host_identity` map for the distinct `identity_id`s; (3) filesystems: for the distinct `fs_key = COALESCE(filesystems_captured_at_ms, captured_at_ms)` values of the window, the present mounts and their values — `present` = mounts whose newest `fs_mount_events` row with `captured_at_ms <= key` has `present = 1`; values = the newest `fs_samples` row per present mount with `captured_at_ms <= key` (one query per distinct key is acceptable — a stamp changes once a minute, so a 1 h window has ≈ 60 keys; a per-row query is not); `filesystems` ordered by `mount`; `filesystems_captured_at_ms` = the stored stamp (None when NULL); (4) processes: the fast rows whose `captured_at_ms` equals a row's `captured_at_ms`, grouped, `rank` order, joined to `process_commands` (one query for the window's range); a row with no fast rows takes the newest `process_samples` capture at or before it that is within `2 × detailIntervalSec` (the CURRENT setting, read once per call like `read_history_processes` does) — else `processes: []`; `started_at` and `parent_pid` as stored, `gpu_percent` ignored until T15; (5) `cpu.times: None`; `pressure: PressureGroup` with every `some`/`full` `None`; `identity.uptime_seconds` = the stored `uptime_seconds` (a NULL here on an assembleable row is a CHECK violation — read as `Option<i64>` and return `StoreError::Validation` naming the row, never 0); `memory.available_bytes`, `swap.free_bytes` from their columns; `load.runnable/total_threads/last_pid` from their nullable columns; `timestamp` = `snapshot_timestamp`. `read_history` returns the assembled samples oldest-first as today (`:1490-1493`). Delete `latest_snapshot()` (`:1450`, its only agent caller is gone since T12 — `grep -rn latest_snapshot agent/crates/tinytop-agent/src` must show only the watch and the handler) and `row_to_sample` (`:2624`). The wire shape `{capturedAtMs, snapshot}` is unchanged (`tests/history_json_contract.rs`). **Tests** (`tests/history_assembly.rs`, the read half): `assembled_snapshot_round_trips_every_stored_field` — insert one full snapshot (two mounts, three processes, `Some` for the three load fields, a stamp) then `read_history` it back and assert field-by-field equality on `identity` (all eight strings + `uptime_seconds`), `cpu.usage_percent`/`cores`, `memory` (all four), `swap` (all four), `load` (all six), `filesystems` (every field, both mounts), `processes` (every field, rank order), `filesystems_captured_at_ms`, `timestamp`; and `cpu.times.is_none()`, every pressure line `None`; `filesystems_carry_forward_until_the_mount_disappears` — a stamp at T0 with `/data`, unchanged through T0+5 min, gone at the T0+10 min stamp: a sample at T0+7 min lists `/data` with T0's values, a sample at T0+12 min does not; `processes_fall_back_to_the_minute_tier_within_two_intervals` — a sample whose fast rows were pruned takes the minute capture ≤ 2 × interval before it, and `[]` beyond; `rows_without_identity_are_not_read` — a raw-inserted row with `identity_id NULL` never appears; `history_window_of_2400_rows_assembles_under_budget` — 2,400 synthetic ticks (one stamp change per 40 ticks, 8 processes each) → `read_history(limit 2400)` returns 2,400 samples and the elapsed ms is printed; assert < 2,000 ms in the debug build (the orchestrator measures the release daemon against the 500 ms budget). Writer route tests (`writer.rs` `#[cfg(test)]` or `tests/` — `grep -n 'fn history_' agent/crates/tinytop-agent`): `/api/history` for a 15 m window returns assembled snapshots equal to what `insert_snapshot` returned; `/api/history/coverage` has no `snapshotJsonOldestMs` key.

**F5 — every JSON path goes; `snapshotJsonKeepMinutes` is accepted and ignored (ADR 0024 decision 8).** Delete: `strip_snapshot_json` (`lib.rs:1985`), `count_snapshot_json_older_than` (`:1944`), the coverage query + fields (`:1776-1782`, `:1871-1872`, `HistoryCoverage.snapshot_json_oldest_ms` / `snapshot_json_sample_count` `:340-342`); `maintenance.rs` `JSON_STRIP_BATCH`, `LadderConfig.snapshot_json_keep_ms`, `MaintenanceReport.json_stripped`, the strip step (`:189-202`); `retention_ladder.rs` `snapshot_json_keep_minutes` (field `:22`, default `:81`, validation row `:224`, `to_ladder_config` `:335`, the growing comparison `:359`) — `tests/retention_settings.rs` loses every `snapshot_json` line (`:25` fixture, `:118-124` validation rows, `:229`, `:335-336` the growing-table row, `:562`/`:570` the `to_ladder_config` mapping, `:692`/`:714-715` — `grep -n snapshot_json` there); `settings_transfer.rs` `WouldDelete.snapshot_json_rows` (`:58`) and its count (`:282-284`, `:293`); `main.rs` `DbStatsOutput.snapshot_json_sample_count` (`:83`, `:330`); `writer.rs:2137` (a coverage route test's expected-key list — drop `snapshotJsonOldestMs`). **Ignore + warn:** `DashboardSettings::from_document` (the ONLY decoder) accepts a `retentionLadder.snapshotJsonKeepMinutes` key on BOTH write paths (PUT and import) and ignores it — never a 400 (a browser holding the 0.5.2 dashboard still sends it); the import path's warning for this key reads exactly `snapshotJsonKeepMinutes is no longer used and was ignored` (a special case beside `collect_unknown_settings_keys` `:232-249`, which would otherwise say `settings.retentionLadder.snapshotJsonKeepMinutes: unknown key ignored`); report what the PUT path does with unknown keys today and what you changed. Tests: `settings_transfer.rs` — the raw seed at `:131` drops `snapshot_json` from its column list; `:100`, `:244`, `:374` set the field that no longer exists → rewrite those tests around a DOCUMENT carrying `snapshotJsonKeepMinutes` (the exact warning, once, and the rest of the document applied); the would-delete tests `:236-262` and `:364-402` (no `snapshot_json_rows`); the unknown-key test at `:620-621` gains the special line beside the generic ones; a PUT with the key on the writer's settings route → 200 and the key absent from the stored document; `ladder_maintenance.rs` — the config fixture at `:68` loses `snapshot_json_keep_ms`, delete `json_is_stripped_outside_the_keep_window` (`:837-860`), rename/adjust `history_coverage_reports_every_tier_and_json_horizon` (`:1143`; its `:1165` assertion goes); `cli_db.rs` `db_stats_json_reports_the_ladder` (`:177-192`) — no `snapshotJsonSampleCount`, `userVersion` 3.

**F6 — docs (yours: the store/API half).** `docs/sqlite-history-architecture.md`: the `## Schema Versions And Migration` section gains schema v3 (the DDL above, the rebuild in one transaction, the guard, the backfill rule, the marker, "a row is assembleable iff `identity_id IS NOT NULL`"), a `### Filesystems on change` subsection (the key, the change rule, the events, the floor), `### Identity interning`, `### Assembling a history snapshot` (the five steps, what is absent and why); every sentence that still describes `snapshot_json`, the 60-minute window, the strip step or `snapshotJsonKeepMinutes` (`grep -n 'snapshot_json\|snapshot JSON\|snapshotJson' docs/sqlite-history-architecture.md` — at `b13f89c`: `:14-15`, `:86`, `:112`, `:319`, `:343`, `:454`, `:468`, `:475`, `:502`) is rewritten to v3 truth; `docs/guides/API.md`: `/api/history` (`:296-303`) is "assembled from typed tables; snapshots carry no `cpu.times` and no `pressure.*.{some,full}` lines, and `load.runnable`/`totalThreads`/`lastPid` are absent where the collector has no source — absent keys, not `null`"; `/api/history/filesystems` gains "rows are stored on change since schema v3"; the settings example (`:218`) drops the key; the coverage example (`:450`) drops `snapshotJsonOldestMs`; `db stats` field list; `README.md`: the persistence-table row (`:403`) goes, `/api/history` (`:466`) and the storage paragraph (`:479`) are rewritten, and a **two-runtime note** under the persistence section: from schema v3 the Rust daemon and the Bun runtime MUST NOT share a database path (Bun reads and writes `metric_samples.snapshot_json`, which v3 drops; the daemon never refuses on Bun's behalf — plan amendment (j)), plus one sentence that a v1 file's page slack is reclaimed by `db vacuum` only; `INSTALL.md:257`, `ARCHITECTURE.md:213` and `:265` to v3 truth. Not yours: `GUIDE.md`, the dashboard, `CHANGELOG.md`.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green (paste every `test result:` line; the 0.5.2 gate counted 277 passing + 1 ignored across 23 suites — expect two new suites); `cargo check --manifest-path agent/Cargo.toml --workspace --offline --target x86_64-pc-windows-msvc` and `--target aarch64-apple-darwin` clean (the collectors change; proven possible at T12); `bun test tests/dashboard-ladder.test.ts tests/dashboard-settings.test.ts` still green (you changed no JS); `grep -rn 'snapshot_json' agent/crates --include=*.rs` shows only the v0/v1 historical DDL constants, the v0→v1 copy and the v3 migration's read of the old column — paste it; `git status --short` and `git diff --stat` pasted (⊆ the allow-list).

**Report shape:** per finding the RED line(s) you observed → the GREEN summary; the test names; the base commit; the elapsed ms printed by `history_window_of_2400_rows_assembles_under_budget`; what the PUT path did with unknown keys before F5; anything outside this brief you noticed under `## Outside this lane` — never fixed silently. The orchestrator's own acceptance (outside your sandbox): the ignored real-file test on a fresh copy of the live v1 file (v1→v2→v3 with timings, `integrity_check`); a temp daemon on `:4290` with a scratch DB for 5 minutes → `db stats --json` `userVersion 3`, `dbstat` unused share of `metric_samples` < 10 %, `/api/history` for a 1 h window timed against 500 ms; the dashboard replay on a point 30 minutes back after T14b merges.
