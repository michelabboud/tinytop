You are hexe lane **T13** for tinytop — the second lane of Phase 5. **Re-dispatch after run 649's ESCALATE (which was correct): the first version of F3 claimed `MaintenanceReport.detail_rows` was never assigned — it IS (`maintenance.rs:102`, from `pendingDetailRows` = detail rows WRITTEN since the last pass, a meaning `ladder_maintenance.rs:953` and `retention_settings.rs:1110` pin). F3 and F7 (j) below are corrected: `detail_rows` keeps its meaning; a NEW field `detail_rows_pruned` carries the prune count.** Phase 5 (plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md`, Task 13; ADR 0021 Accepted; ADR 0023 Accepted — read both). Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is v0.5.1 = T12 merged + this brief). Every file:line below was read at `de5f951` (the commit before T12 merged); T12 touched `retention_ladder.rs` (a doc-comment at `:23`), `writer.rs`, `app.js`, `ladder-rules.js`, `index.html`, `README.md`, `GUIDE.md`, `docs/guides/API.md` — in those files anchor on the NAMES quoted here, the numbers may be off by a few lines; the store files (`migration.rs`, `lib.rs`, `maintenance.rs`, `settings_transfer.rs`, `main.rs`) are exact. Build ONLY the findings below. Files you may edit are named per finding; NOTHING else. **Git is read-only for you — no commit, no branch, no stash, no `cargo fmt` on the tree (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` only and fix by hand); the orchestrator commits.** No edit to `agent/Cargo.toml`, `agent/Cargo.lock`, any `crates/*/Cargo.toml`, `src/`, `legacy/`, `VERSION`, `CHANGELOG.md` (Fable writes the line at merge), `docs/adr/*`, the plan, the spec, `tinytop-collectors/*`, `tinytop-types/*`, `otel.rs`, `disk_check.rs`, `archive.rs`, `vendor/`. Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; `std::env::set_var` is forbidden (child-process `Command::env` is the only way to set a variable in a test). **Known sandbox limits — not failures:** every `serve_*` test binds a port your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); Bun's `tests/server.test.ts` likewise; `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`; there is no crates.io in the sandbox — everything you need is in the lock and the registry cache (`--offline` works). If a contract below is wrong against the code you read, or you would be guessing, STOP and reply `ESCALATE: <what, what you tried, where you stopped>` — escalating early beats a wrong build. Plan §4 contingencies bind you: `ALTER TABLE … DROP COLUMN` failing is a refusal with the version message, never a silent copy-table fallback; a backfill slower than 60 s on the orchestrator's real-file copy is batched by `captured_at_ms` ranges, never skipped.

**Read first:** plan §2 decisions 1–3 and Task 13; ADR 0021 §"processes" and ADR 0023 (`docs/adr/0023-schema-v2-migration-one-transaction-no-pre-image.md` — why there is NO pre-image for v1→v2 and why both process tables index `command_id`); `agent/crates/tinytop-store/src/migration.rs:14` (`SCHEMA_VERSION`), `:39-203` (`CREATE_SCHEMA_V1_SQL` — `fs_samples` `:157-170`, `process_samples` `:175-186` + its index `:188-189` on `captured_at_ms DESC` only, `app_events` `:191-197`, `PRAGMA user_version = 1` at `:202`), `:307-351` (`ensure_schema` — the three arms: `SCHEMA_VERSION` re-applies the DDL idempotently + `complete_pending_migration`; `0` = fresh / empty-v0 / populated-v0; `other` refuses with `unsupported SQLite schema version {other} at {path} (supported version is {SCHEMA_VERSION})`), `:353-373` (`migrate_populated_v0`), `:485-515` (`rebuild_v0_schema` — one `pool.begin()` transaction), `:556-608` (`pre_image_path` = `<db>.pre-v0.sqlite`, `refuse_existing_pre_image`, `create_pre_image` — you do NOT reuse these), `:610-655` (`complete_pending_migration`, `vacuum_and_complete_migration`), `:686-700` (the `app_events` marker `schemaMigrated` written once); `lib.rs:779` (`ensure_schema` is called from `connect`, the report discarded), `:830-834` (`user_version()`), `:922-1082` (`insert_snapshot` — the `metric_samples` upsert at `:948-1015` is a single autocommit statement, there is NO transaction in this range; `write_detail_rows_if_due` is called at `:1022` with `detail_interval_ms` from `:1018-1021`), `:1238-1368` (`write_detail_rows_if_due` — the `lastDetailMs` gate `:1244-1249`, its own `pool.begin()` at `:1258` … `commit` at `:1366`, the `process_samples` INSERT `:1310-1321` binding `command` at `:1326`), `:429-446` (`HistoryProcessSample`, `HistoryProcessCapture`), `:450-454` (`HistoryQuery { since_ms, until_ms, limit }`), `:1520-1578` (`read_history_processes` — the `capture_times` CTE `:1526-1540`), `:1798-1902` (the prune family: `prune_raw_history`, `count_rows_older_than`, `count_snapshot_json_older_than`, `prune_rollups`, `strip_snapshot_json` `:1864-1886` = the LIMIT-bounded pattern via `rowid IN (SELECT rowid … LIMIT ?)`, `prune_detail_history` `:1888-1902` = two unbounded DELETEs), `:2224` (`PRAGMA foreign_keys = ON` at connect — FKs are ENFORCED); `retention_ladder.rs:12-26` (`RetentionLadder`, container-level `#[serde(default)]`), `:66-84` (`Default`), `:204-283` (`validate` — `validate_range` `:363-368` formats `"{field} must be between {min} and {max}; observed {value}"`), `:308-323` (`to_ladder_config`); `maintenance.rs:9` (`JSON_STRIP_BATCH`), `:13-21` (`LadderConfig`), `:24-32` (`MaintenanceReport` — `detail_rows` at `:29` = detail rows WRITTEN since the last pass, assigned at `:102` from `pendingDetailRows`; the prune count at `:208-218` is only `eprintln!`ed today), `:93-282` (`maintain_with_archive` — the order strip JSON `:189-198` → prune L1 `:200-206` → prune detail rows at the **L2** horizon `:208-218` → rollups); `settings_transfer.rs:51-59` (`WouldDelete`), `:251-291` (`would_delete`); `agent/crates/tinytop-agent/src/main.rs:76-86` (`DbStatsOutput` — NO `user_version` field today) and `:295-330` (the `db stats` handler reads `store.user_version()` only to refuse `< 1`); `writer.rs:194-197` (`HistoryProcessesResponse { captures }`), `:532-540` (`history_processes`); `agent/assets/dashboard/index.html:562-624` (the ladder fieldset — `#daemon-detail-interval-sec` at `:598`), `app.js:388` / `:2668` / `:2761` (the `detailIntervalSec` DOM ref / write-to-form / read-from-form triple — copy that pattern), `ladder-rules.js:369-421` (`describeImportPlan`, `addCount` `:377-380`, `"snapshot JSON blobs"` `:391`), `:457-510` (`validateRetentionLadder` — byte-identical mirror of the Rust messages); tests `tinytop-store/tests/migration_v1.rs` (`TempDatabase` `:125-146`, `seed_v0_database` `:504+`, `verify_successful_migration` and every `user_version` assertion), `retention_settings.rs:127-128` (the rejection-matrix row shape), `settings_transfer.rs:261,305` (`would_delete` assertions), `:523-524` (unknown-key warnings), `ladder_maintenance.rs:981,993` (`process_samples` assertions), `tinytop-agent/tests/cli_db.rs:115-146` (`set_sqlite_user_version`), `:149` (`sqlite_user_version`), `tests/dashboard-ladder.test.ts:642-660` (`describeImportPlan` expectations), `tests/dashboard-settings.test.ts`; docs `docs/sqlite-history-architecture.md` (`## Current Schema` `:81`, `## Schema Versions And Migration` `:254`, `## Write Path` `:294`, `## Read Path` `:302`, `## Retention` `:403`, `## Future Tables` `:577`), `README.md:395-402` (the `retentionLadder.*` table), `:465` (the `/api/history/processes` row), `docs/guides/API.md:339-341` (`### GET /api/history/processes` — three lines, no example) and `:406` (the settings example), `GUIDE.md` (the ladder settings sentence T12 edited near `:93`). Work RED → GREEN per finding and paste each RED line and GREEN summary in your final report.

**F1 — Schema v2 DDL and the v1→v2 migration (`migration.rs`).** `SCHEMA_VERSION` becomes `2`. Add `const CREATE_SCHEMA_V2_SQL` = the FULL current schema (every table and index of `CREATE_SCHEMA_V1_SQL`, `IF NOT EXISTS`, in the v2 shape, ending `PRAGMA user_version = 2;`) — never re-apply the v1 DDL on a v2 database (its last line resets `user_version` to 1). v2 shape: (1) `CREATE TABLE IF NOT EXISTS process_commands (command_id INTEGER PRIMARY KEY, command TEXT NOT NULL UNIQUE);` (2) `CREATE TABLE IF NOT EXISTS process_samples_fast (captured_at_ms INTEGER NOT NULL, rank INTEGER NOT NULL, pid INTEGER NOT NULL, command_id INTEGER NOT NULL REFERENCES process_commands(command_id), cpu_percent REAL NOT NULL, memory_percent REAL NOT NULL, rss_bytes INTEGER NOT NULL, parent_pid INTEGER, started_at TEXT, gpu_percent REAL, PRIMARY KEY (captured_at_ms, rank)) WITHOUT ROWID;` + `CREATE INDEX IF NOT EXISTS idx_process_samples_fast_command ON process_samples_fast (command_id);` (`gpu_percent` is NULL until Task 15; `started_at` stays TEXT here on purpose — T14's identity table normalises it, do not intern it now); (3) `process_samples` keeps every v1 column EXCEPT `command`, and gains `command_id INTEGER REFERENCES process_commands(command_id)` as its LAST column (nullable at the DDL level because `ALTER TABLE … ADD COLUMN … REFERENCES` under `foreign_keys = ON` only allows a NULL default — the writer always sets it; the migration guard below proves it), + `CREATE INDEX IF NOT EXISTS idx_process_samples_command ON process_samples (command_id);` — the column order of the fresh DDL must equal the migrated table's order so `PRAGMA table_info(process_samples)` is identical for a fresh v2 file and a migrated v1 file (F7 test c). Keep `CREATE_SCHEMA_V1_SQL` frozen as history and make it `#[doc(hidden)] pub` (F7's v1 fixture applies it — if `migration` is not a `pub mod`, add `#[doc(hidden)] pub use migration::CREATE_SCHEMA_V1_SQL;` in `lib.rs`). **`ensure_schema` arms:** `SCHEMA_VERSION` (=2) → `raw_sql(CREATE_SCHEMA_V2_SQL)` + `complete_pending_migration` (unchanged call; harmless at v2); `1` → `complete_pending_migration(pool, db_path)` FIRST (a v0→v1 whose `VACUUM` never ran must finish before v2), then `migrate_v1_to_v2(pool, db_path, now_ms)`; `0` → the three existing branches, each followed by `migrate_v1_to_v2` (fresh file: apply `CREATE_SCHEMA_V2_SQL` directly instead and return — no v1 detour; empty v0: `rebuild_v0_schema` then v1→v2; populated v0: `migrate_populated_v0` then v1→v2 — the chain the plan names); `other` → the same refusal (its text now says `supported version is 2`). **`migrate_v1_to_v2(pool, db_path, now_ms) -> Result<(), StoreError>`:** (a) `SELECT sqlite_version()` → `require_sqlite_at_least(version: &str, (3, 35, 0)) -> Result<(), StoreError>` (a pure fn: parse `major.minor.patch`, compare lexicographically as integers; on failure `StoreError::Migration { reason: format!("schema migration requires SQLite ≥ 3.35.0 (linked: {version})"), remedy: "rebuild tinytop-agent against a bundled SQLite 3.35.0 or newer; no migration was attempted" }`) — before any write; (b) ONE `pool.begin()` transaction: `CREATE TABLE process_commands`, `CREATE TABLE process_samples_fast` + its index (the same DDL text as in `CREATE_SCHEMA_V2_SQL` — a shared `const` per table, not a second copy), `ALTER TABLE process_samples ADD COLUMN command_id INTEGER REFERENCES process_commands(command_id)`, `INSERT OR IGNORE INTO process_commands (command) SELECT DISTINCT command FROM process_samples`, `UPDATE process_samples SET command_id = (SELECT command_id FROM process_commands WHERE command = process_samples.command)`, the GUARD `SELECT COUNT(*) FROM process_samples WHERE command_id IS NULL` — if `> 0` return `StoreError::Migration { reason: "schema v2 backfill left {n} process_samples rows without a command_id", remedy: "report this with `tinytop-agent db stats --json`; the database was not modified" }` (the transaction drops → rollback), `CREATE INDEX idx_process_samples_command`, `ALTER TABLE process_samples DROP COLUMN command`, the `app_events` marker in the style of `:689-697`: `marker_type 'schemaMigrated'`, `label 'SQLite schema migrated from v1 to v2'`, `details_json` = `{"fromVersion":1,"toVersion":2,"commandsInterned":<count>,"processRows":<count>,"durationMs":<n>}` (no `WHERE NOT EXISTS` — the v0→v1 marker may legitimately precede it), `PRAGMA user_version = 2`, commit. No `VACUUM` (plan §5), no pre-image (ADR 0023), no `history_state.schemaMigration` record (that key's shape is the v0→v1 audit and `complete_pending_migration` would misread it). One `eprintln!("history migration info: schema v1 → v2 in {duration} ms ({commands} commands interned over {rows} process rows)")` after the commit, matching the store's existing info-line style. `pre_image_path` and friends are untouched. Test placement: F7. RED today: `tests/migration_v2.rs` fails to compile on `CREATE_SCHEMA_V1_SQL` visibility / `SCHEMA_VERSION == 1` — paste the first error.

**F2 — Fast rows every tick, `command_id` everywhere (`lib.rs`).** `async fn intern_command<'e>(executor: impl sqlx::SqliteExecutor<'e> + Copy, command: &str) -> Result<i64, StoreError>`: `INSERT OR IGNORE INTO process_commands (command) VALUES (?)` then `SELECT command_id FROM process_commands WHERE command = ?` (`fetch_one`) — always called on a transaction's connection (`&mut *transaction`), never on the pool. **`insert_snapshot`:** after the `metric_samples` upsert (`:1015`) and BEFORE `write_detail_rows_if_due` (`:1022`), call `self.write_process_fast_rows(captured_at_ms, &snapshot.processes).await?` — a new private method: ONE `pool.begin()` transaction (the same shape as `:1258`), `DELETE FROM process_samples_fast WHERE captured_at_ms = ?` (a re-insert at the same stamp replaces, like the detail path), then per process in order `intern_command` + `INSERT INTO process_samples_fast (captured_at_ms, rank, pid, command_id, cpu_percent, memory_percent, rss_bytes, parent_pid, started_at, gpu_percent) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, NULL)` (`rank` = 1-based position exactly as `write_detail_rows_if_due` derives it — read `:1310-1330` and reuse the same expression), commit; returns the row count. A separate transaction from the upsert is deliberate: the metric row and the fast rows have independent readers, and the upsert is autocommit today — do not wrap the upsert. **`write_detail_rows_if_due`:** the `process_samples` INSERT (`:1310-1321`) binds `command_id` (via `intern_command` on `&mut *transaction`) in place of `command`; nothing else in that function changes. **`read_history_processes`:** returns `HistoryProcessesRead { source: ProcessHistorySource, captures: Vec<HistoryProcessCapture> }` with `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)] #[serde(rename_all = "lowercase")] pub enum ProcessHistorySource { Fast, Minute }`. Selection rule (document it on the fn): read `process_fast_keep_hours` from `self.get_settings().await?.retention_ladder`; `fast_floor_ms = now_ms − hours × 3_600_000` where `now_ms` is the wall clock at the call; source is `Fast` iff `query.since_ms` is `Some(since)` with `since >= fast_floor_ms` (an open-ended or older window is `Minute`; `until_ms` needs no check — it never exceeds now by contract of the handler); the SQL is the existing CTE with the table name substituted and `JOIN process_commands c ON c.command_id = p.command_id` selecting `c.command AS command` — the `HistoryProcessSample` shape is unchanged (`command` stays a string; `gpuPercent` is NOT surfaced in T13). The two table names are `const`s; never format a table name from user input. **Prunes:** `pub(crate) async fn prune_process_fast_history(&self, cutoff_ms: i64) -> Result<u64, StoreError>` — LIMIT-bounded like `strip_snapshot_json`, but `process_samples_fast` is `WITHOUT ROWID` (no `rowid`): loop `DELETE FROM process_samples_fast WHERE (captured_at_ms, rank) IN (SELECT captured_at_ms, rank FROM process_samples_fast WHERE captured_at_ms < ?1 ORDER BY captured_at_ms, rank LIMIT ?2)` with `const PROCESS_FAST_PRUNE_BATCH: i64 = 5_000` until a batch deletes fewer than the limit; each batch is its own autocommit statement so writers interleave; return the total. `pub(crate) async fn prune_orphan_commands(&self, limit: i64) -> Result<u64, StoreError>` — `DELETE FROM process_commands WHERE command_id IN (SELECT c.command_id FROM process_commands c WHERE NOT EXISTS (SELECT 1 FROM process_samples p WHERE p.command_id = c.command_id) AND NOT EXISTS (SELECT 1 FROM process_samples_fast f WHERE f.command_id = c.command_id) LIMIT ?)` (both `NOT EXISTS` are index probes thanks to F1's two indexes — say so in a comment). `prune_detail_history` is unchanged. `pub async fn count_process_fast_rows_older_than(&self, cutoff_ms: i64) -> Result<i64, StoreError>` next to `count_snapshot_json_older_than` (for F5). RED: the compile errors on `intern_command` / `HistoryProcessesRead` from F7's tests — paste the first.

**F3 — `processFastKeepHours` (`retention_ladder.rs`, `maintenance.rs`).** `RetentionLadder` gains `pub process_fast_keep_hours: i64` (serde `processFastKeepHours` via the container `rename_all`; the container `#[serde(default)]` makes a 0.5.x export import cleanly — F7 proves it), `Default` = `24`, doc-comment: hours of per-tick process rows (`process_samples_fast`) kept; older windows fall back to the minute rows (`process_samples`, kept through the L2 horizon). `validate` adds `validate_range(&mut errors, "retentionLadder.processFastKeepHours", self.process_fast_keep_hours, 1, 72)` right after the `detailIntervalSec` check — the message is the helper's `retentionLadder.processFastKeepHours must be between 1 and 72; observed {value}` (the plan quotes the prefix; the existing helper's suffix is kept — consistency with every other ladder message wins). `LadderConfig` gains `process_fast_keep_ms: i64`; `to_ladder_config` sets `self.process_fast_keep_hours.saturating_mul(3_600_000)`; every other constructor of `LadderConfig` (grep the tests) gets the field. `MaintenanceReport` gains `detail_rows_pruned: u64` (the count `prune_detail_history` returns — today only `eprintln!`ed at `:212-215`; `detail_rows` at `:29` KEEPS its meaning, detail rows written since the last pass, assigned at `:102` — do not touch it), `process_fast_rows: u64` and `orphan_commands: u64`. `maintain_with_archive` order becomes: strip JSON → prune L1 → prune detail rows (L2 horizon, unchanged; its count now stored in `detail_rows_pruned`) → **`prune_process_fast_history(now_ms − config.process_fast_keep_ms)`** → **`prune_orphan_commands(ORPHAN_COMMAND_PRUNE_BATCH = 1_000)` only when `detail_rows_pruned + process_fast_rows > 0`** (a command can only become orphaned by a deletion — comment it) → rollups as today; each new step uses `record_step_error` like its neighbours and logs `history maintenance info: deleted {n} expired fast process rows` / `… {n} orphaned process commands` only when `n > 0`.

**F4 — `db stats --json` reports the schema version (`main.rs`).** `DbStatsOutput` gains `user_version: i64` (→ `userVersion`, the plan's acceptance reads it); the handler already holds the value from `store.user_version()` (`:298-307`) — pass it through. Test in `cli_db.rs`: `db_stats_json_reports_user_version_2` — a fresh database → `db stats --json` → `userVersion == 2`; RED today: the key is absent — paste the assertion.

**F5 — settings transfer dry-run counts fast rows (`settings_transfer.rs`, `ladder-rules.js`).** `WouldDelete` gains `process_fast_rows: i64` (→ `processFastRows`); `would_delete` computes it via `count_process_fast_rows_older_than(now_ms − candidate.process_fast_keep_ms)` only when the candidate horizon is SHORTER than the current one (a growing horizon deletes nothing — the same rule the L1 count follows; read `:259-261` and mirror). `describeImportPlan` (`ladder-rules.js:369-421`): `addCount("processFastRows", "fast process rows", " deleted")` right after the `"snapshot JSON blobs"` line, so the import dialog names the loss. Tests: `settings_transfer.rs` — `import_plan_counts_fast_process_rows_for_a_shrinking_horizon` (rows 2 h old; 24 → 1 counts them; 1 → 24 counts 0; and a 0.5.0 document WITHOUT `processFastKeepHours` imports with `24` and NO unknown-key warning); `tests/dashboard-ladder.test.ts` — extend the `describeImportPlan` expectation at `:642-660` with `processFastRows: 3` → the line `"3 fast process rows deleted"` (match the exact phrasing `addCount` produces for the other rows; paste the RED `toEqual` diff).

**F6 — dashboard ladder field (`index.html`, `app.js`, `ladder-rules.js`).** `index.html`: after the `#daemon-detail-interval-sec` control (`:598`) the same markup with `id="daemon-process-fast-keep-hours"`, `type="number" min="1" max="72" step="1"`, label `Per-tick process history (hours)`, help text `Per-tick process rows are kept this long; older windows use the once-a-minute rows.` `app.js`: the `detailIntervalSec` triple copied — `elements.daemonProcessFastKeepHours = document.querySelector("#daemon-process-fast-keep-hours")` next to `:388`, `setControlValue(elements.daemonProcessFastKeepHours, nextSettings.retentionLadder.processFastKeepHours)` next to `:2668`, `processFastKeepHours: numberControlValue(elements.daemonProcessFastKeepHours, 24)` next to `:2761`; the default `24` wherever `app.js` holds the ladder defaults (grep `detailIntervalSec: 60`). `ladder-rules.js` `validateRetentionLadder`: `validateRange(errors, "retentionLadder.processFastKeepHours", ladder.processFastKeepHours, 1, 72)` right after the `detailIntervalSec` line, producing `retentionLadder.processFastKeepHours must be between 1 and 72; observed 0` byte-for-byte equal to F3's Rust message (the existing test that compares the JS and Rust messages — find it in `tests/dashboard-ladder.test.ts` — gains this row). Tests: `tests/dashboard-ladder.test.ts` — `0` and `73` rejected with the exact message, `1`/`24`/`72` accepted; `tests/dashboard-settings.test.ts` — `html` contains `id="daemon-process-fast-keep-hours"`, `app` contains `daemonProcessFastKeepHours: document.querySelector("#daemon-process-fast-keep-hours")` (or the exact assignment form the file uses — quote it) and `processFastKeepHours: numberControlValue(`. RED = the failing `toContain` — paste one.

**F7 — Rust tests.** New `agent/crates/tinytop-store/tests/migration_v2.rs` (its own `TempDatabase`-style helper; do not `mod` another test file): (a) `fresh_database_is_created_at_v2` — connect → `user_version() == 2`; `PRAGMA table_info(process_samples)` has `command_id` and no `command`; `process_commands` and `process_samples_fast` exist; `PRAGMA index_list(process_samples_fast)` shows `idx_process_samples_fast_command`; (b) `v1_fixture_with_three_commands_migrates_to_v2` — seed a v1 file by executing `CREATE_SCHEMA_V1_SQL` on a raw sqlx pool (the fixture is the REAL v1 DDL, not a copy), insert 5 `process_samples` rows across 2 captures with 3 distinct commands, then connect through the store: `user_version() == 2`; `SELECT COUNT(*) FROM process_commands == 3`; `command` column gone; `COUNT(*) WHERE command_id IS NULL == 0`; the joined read returns the original command text on every row; `PRAGMA integrity_check == ok`; `PRAGMA foreign_key_check` empty; exactly one `app_events` row with `label 'SQLite schema migrated from v1 to v2'`; connecting a second time changes nothing (`user_version` 2, still one v1→v2 marker); (c) `migrated_and_fresh_process_samples_have_identical_shape` — `PRAGMA table_info(process_samples)` of (a) equals (b) row for row; (d) `sqlite_version_requirement_is_checked` — `require_sqlite_at_least("3.34.1", (3,35,0))` errs with reason exactly `schema migration requires SQLite ≥ 3.35.0 (linked: 3.34.1)`; `"3.35.0"` and `"3.51.3"` pass; and the LIVE `SELECT sqlite_version()` on a pool passes the check (the gate proves the linked library); (e) `newer_schema_version_is_refused` — write `user_version = 3` into a fresh v2 file's header (copy `cli_db.rs:115-146`'s technique) → `connect` errs with a message containing `unsupported SQLite schema version 3` and `supported version is 2`; (f) `#[ignore = "needs TINYTOP_V1_FIXTURE=<path to a v1 history.sqlite>"] real_v1_file_copy_migrates_to_v2` — reads the env var (absent or unreadable → PANIC naming the variable; never a silent pass), COPIES the file into the temp dir (the fixture is never opened for writing), records `COUNT(DISTINCT command)` and `COUNT(*)` from `process_samples` via a raw pool, connects through the store, asserts `user_version 2`, `process_commands` count == the distinct count, no NULL `command_id`, `integrity_check ok`, and PRINTS `migration of {rows} process rows took {ms} ms` (the orchestrator runs this one at gate time with `-- --ignored` on a copy of the live v1 file; > 60 s is plan §4's contingency). **`migration_v1.rs`:** the v0 fixture now lands at v2 through the chain — update every `user_version == 1` expectation to `2`, the `process_samples` shape assertions (`:182`, `:845`) to the v2 shape, and add one assertion that `process_commands` exists after a v0 migration; nothing else in that file changes (its pre-image/VACUUM/crash tests are about v0→v1 and stay). New `tests/process_history.rs`: (g) `fast_rows_every_tick_and_minute_rows_every_interval` — three `insert_snapshot` calls at `t`, `t + 1_500`, `t + 3_000` with `detailIntervalSec 60` and 4 processes (2 distinct commands, one shared across all ticks) → `process_samples_fast` has 3 captures × 4 rows, `process_samples` has 1 capture, `process_commands` has exactly 2 rows, and the shared command resolves to the SAME `command_id` in both tables; (h) `read_history_processes_picks_fast_inside_the_keep_window_and_minute_outside` — `processFastKeepHours 1`: `since = now − 30 min` → `source == Fast` and the fast captures; `since = now − 2 h` → `Minute`; `since = None` → `Minute`; (i) `prune_process_fast_history_is_limit_bounded_and_leaves_no_orphans` — 12,000 fast rows older than the cutoff plus 10 newer → returns 12,000 (three batches — assert via the count, and assert the 10 survive); then `prune_orphan_commands(1_000)` deletes exactly the commands referenced by neither table and keeps one still referenced only by `process_samples`; `PRAGMA foreign_key_check` empty afterwards. `ladder_maintenance.rs`: (j) `maintenance_reports_fast_and_detail_prunes` — after a `maintain_with_archive` run over aged rows the report's `detail_rows_pruned`, `process_fast_rows`, `orphan_commands` are the counts actually deleted, and `detail_rows` still reports the rows written (the existing assertions at `:953` keep passing untouched). `retention_settings.rs`: the matrix gains `processFastKeepHours` `0` → `retentionLadder.processFastKeepHours must be between 1 and 72; observed 0`, `73` → `… observed 73`, and `1`/`72` accepted. Every existing test that builds a `RetentionLadder` or `LadderConfig` literal gains the new field (mechanical). Paste every RED line and every GREEN `test result:` line.

**F8 — docs (no behaviour).** `docs/sqlite-history-architecture.md`: `## Current Schema` — the v2 DDL of `process_commands`, `process_samples_fast`, and the changed `process_samples` (with the two `command_id` indexes and why `started_at` is still TEXT); `## Schema Versions And Migration` — the v1→v2 step (the ≥ 3.35 check, the one transaction, the guard, the marker, NO pre-image + the ADR 0023 pointer, the v0 chain, the refusal text now naming 2); `## Write Path` — fast rows per tick in their own transaction, the dictionary interning; `## Read Path` — the `source` rule verbatim from F2; `## Retention` — the `processFastKeepHours` horizon, the LIMIT-bounded fast prune (row-value `IN` because `WITHOUT ROWID`), the orphan prune gated on deletions; `## Future Tables` — drop the sentence that says v1 "provides" the typed detail rows (stale) and point at T14. `README.md`: the `retentionLadder.*` table gains `| retentionLadder.processFastKeepHours | 24 | Per-tick process rows kept before falling back to the once-a-minute rows; 1–72 hours |` after `detailIntervalSec`; the `/api/history/processes` row (`:465`) adds `; the response names its `source` (`fast` for windows inside `processFastKeepHours`, else `minute`)`; under Implementation notes one bullet: per-tick process history in `process_samples_fast` with a `process_commands` dictionary (schema v2, migrated on first start, `db stats --json` → `userVersion`). `docs/guides/API.md`: `### GET /api/history/processes` gains a `curl` + JSON example in the shape of its neighbours with `"source": "fast"` and one capture, and a sentence on the rule; the settings example (`:406`) gains `"processFastKeepHours": 24,`. `GUIDE.md`: one sentence next to the ladder settings sentence naming the new field. No CHANGELOG (Fable writes the line at merge), no ADR, no spec edit.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green (paste every `test result:` line — the v0.5.1 gate counted 261 passing across 21 suites; expect 23 suites and 261 + F4's one + F5's one + F7's ten); `bun test tests/dashboard-ladder.test.ts tests/dashboard-settings.test.ts` green (paste; 0.5.1 counted 188 Bun tests in the full run); `grep -n "process_samples" agent/crates/tinytop-store/src/lib.rs | grep -n "command\b"` shows no production INSERT binding a command string; `git status --short` and `git diff --stat` pasted (files ⊆ the F-lists: `tinytop-store/src/{migration,lib,retention_ladder,maintenance,settings_transfer}.rs`; `tinytop-store/tests/{migration_v1,migration_v2,process_history,retention_settings,settings_transfer,ladder_maintenance}.rs` + any store test whose `RetentionLadder`/`LadderConfig` literal needed the field; `tinytop-agent/src/{main,writer}.rs`; `tinytop-agent/tests/cli_db.rs`; `agent/assets/dashboard/{index.html,app.js,ladder-rules.js}`; `tests/dashboard-ladder.test.ts`, `tests/dashboard-settings.test.ts`; `README.md`, `GUIDE.md`, `docs/sqlite-history-architecture.md`, `docs/guides/API.md`). Final report: per finding RED line(s) → GREEN summary; the twelve new test names; the base commit you quoted; the `sqlite_version()` your gate observed; anything you could not do with the exact reason. The orchestrator's own acceptance (outside your sandbox): the ignored real-file test on a copy of the live v1 database with its timing; a temp daemon on `:4290` with a scratch DB for 5 minutes → `SELECT COUNT(*) FROM process_commands` ≤ distinct commands seen, `process_samples_fast` ≈ 200 captures × 8, `dbstat` bytes per row for `process_samples_fast` pasted against the plan's ≤ 60 B target (the table alone; its index separately), `db stats --json` → `userVersion 2`.
