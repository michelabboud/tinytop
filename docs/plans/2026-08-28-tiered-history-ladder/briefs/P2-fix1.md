You are hexe lane **P2-fix1** for tinytop — the Phase 2 close fix round, from `main` at `8496b6e` (`v0.3.3`). Fix ONLY F1–F8 below (the deep dual-blind review's findings as validated by Fable); do not revisit the designs (ADRs 0014, 0018, 0019, 0020 stand). Scope: `agent/crates/tinytop-store/src/archive.rs`, `agent/crates/tinytop-agent/src/main.rs` (F1 status helper only), `agent/crates/tinytop-agent/src/writer.rs` (F7 only), `agent/crates/tinytop-store/src/maintenance.rs` (F7 allow-attributes ONLY — no logic), `agent/crates/tinytop-store/src/disk.rs` (F7 only), `agent/crates/tinytop-store/tests/archive.rs`, `agent/crates/tinytop-agent/tests/cli_db.rs`, `tests/tinytop-script.test.ts`, `package.json` (F7: ONE script line), `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` (§9 sentence), `docs/sqlite-history-architecture.md`, `GUIDE.md`, `INSTALL.md`, `CHANGELOG.md`, `agent/crates/tinytop-store/src/lib.rs` (F8: `put_settings` ONLY), `agent/crates/tinytop-store/tests/retention_settings.rs` (F8 test). Untouchable: `Cargo.toml`/`Cargo.lock`, `migration.rs`, `retention_ladder.rs`, `ladder.rs`, `agent/assets/`, Bun `src/`, `PROGRESS.md`, every ADR.

Read first: `CLAUDE.md`; the Phase-2 deep brief the reviewers were given (Fabulous `docs/fleet/tinytop/2026-08-29-phase2-deep-review-brief.md` — Fable pastes the relevant claims below); `briefs/T8.md` F1/F4 and `briefs/T9.md` (the contracts you are amending); `docs/sqlite-history-architecture.md` `### Cold export`.

**F1 — P0: cold export can seal an incomplete month.** `exportable_months` proves a month's hours have EXPIRED from main by time (rule (b)), not that the bounded — and possibly failing — archive move has COPIED them. If the move is behind or was failing for days (archive directory unwritable, then fixed), an eligible month can be exported while some of its rows are still in main; when the move catches up, those rows land in the archive after the watermark passed the month and never reach the CSV. Reproduced by the reviewer with two manual moves. Ruling — a fourth condition **(d): main holds no rows for M** — checked in `export_cold_months` per candidate month, IN ORDER, against the MAIN database through `store` (one `SELECT COUNT(*) FROM metric_rollups_1h WHERE bucket_start_ms >= ?1 AND bucket_start_ms < ?2` on the store pool); the pass STOPS at the first month that still has main rows (it and every later month wait — the watermark must never jump past a month that is still filling), logging nothing (this is the normal state while the move catches up). `exportable_months` stays pure and unchanged; add `pub async fn months_ready_to_export(store, months: &[String]) -> Result<Vec<String>, StoreError>` (the prefix of `months` whose main row count is 0) used by `export_cold_months` AND by `db archive status`'s `nextExportableMonths` (so status never promises a month the pass will skip). Spec §9 cold-export paragraph: add "(d) main holds no rows for M" to the eligibility sentence. `docs/sqlite-history-architecture.md` `### Cold export`: the eligibility paragraph gains (d) and states the stop-at-first-incomplete rule and why; `:512`'s "complete UTC calendar months" becomes "complete, fully moved UTC calendar months"; `:426`'s stale "cold file export remains a later phase" is removed. `GUIDE.md`'s cold paragraph: one clause ("a month is exported only once every one of its rows has left the main database"). Tests in `tests/archive.rs`: `cold_export_waits_until_main_holds_no_rows_for_the_month` — seed TWO L4 buckets for one old month in main, `move_expired_l4(.., i64::MAX, 1)` moves ONE, `export_cold_months` writes NOTHING (no file, no manifest row, watermark `None`), then move the second, export → one file with `rowCount == 2`; `cold_export_stops_at_the_first_month_still_in_main` — months A < B both eligible, A fully moved, B has one row still in main → only A exports; move B's row → B exports next pass. In `tests/cli_db.rs`, extend `db_archive_status_on_fresh_v1_is_read_only` or add one test asserting `nextExportableMonths` excludes a month that still has main rows. RED/GREEN pasted.

**F2 — P0: the command-centre harness still touches the host.** `tests/tinytop-script.test.ts` `runTinytop` inherits `HOME` and the XDG variables; `./tinytop start` (three cases) writes its PID file to `runtime_state_dir()` = `$XDG_RUNTIME_DIR/tinytop` or `${XDG_STATE_HOME:-$HOME/.local/state}/tinytop` — the REAL directory — and `stop`'s foreground path `kill`s the PID that file names (PID reuse ⇒ an unrelated host process). `doctor` uses the real `curl`/`ss`/`systemctl --user show-environment`. Ruling: `runTinytop` builds a fresh temp ROOT per call (`mkdtempSync(join(tmpdir(), "tinytop-home-"))`) and sets, by default, `HOME`, `XDG_CONFIG_HOME`, `XDG_DATA_HOME`, `XDG_STATE_HOME`, `XDG_CACHE_HOME`, `XDG_RUNTIME_DIR` and `TINYTOP_SYSTEMD_UNIT_DIR` to subdirectories of that root (created), and prepends a per-call STUB directory to `PATH` holding executable stubs `systemctl` (exit 1), `ss` (prints nothing, exit 0), `curl` (exit 7), `pgrep` (prints nothing, exit 1) — then `/usr/bin:/bin`; per-test `env` overrides still win (`...env` last), so existing cases that pass their own `PATH`/`HOME`/`XDG_RUNTIME_DIR` keep working. Add `start_writes_its_pid_file_only_under_the_test_runtime_dir`: run a `start` case with a fake agent, then assert the PID file exists under the harness's `XDG_RUNTIME_DIR/tinytop/` and that NOTHING was created under the harness `HOME/.local/state` — and, by construction, nothing under the real `$HOME` (assert the real `~/.local/state/tinytop` mtime is unchanged if it exists; skip that assertion if absent). Every existing case must still pass. `tinytop` itself needs no change (say so after checking `runtime_state_dir`, `unit_path`, `default_history_db` all derive from env you now control).

**F3 — P3 (robustness): the cold verifier accepts non-RFC 4180 input.** `parse_rfc4180` accepts a `"` inside an unquoted non-empty field and any characters after a closing quote. Make it strict: a `"` in an unquoted field that is not at the field start → `InvalidData` naming the record; after a closing `"` only `,` or CR may follow → `InvalidData`. Tests through `verify_cold_file` on a hand-built gz (as `corrupted_record_width_fails_verification` does): `verify_rejects_a_quote_inside_an_unquoted_field`, `verify_rejects_junk_after_a_closing_quote`; `csv_field_quotes_rfc4180_special_characters` still passes.

**F4 — P3: `read_archive_points` skips the schema inspection.** It opens read-only and queries `metric_rollups_1h` directly, so a `user_version 1` archive missing that table fails with "no such table" while coverage/manifest reads (T8-fix1b) return empty. The second reviewer sharpened it: a `user_version = 2` (newer/foreign) file that happens to contain the table is SERVED as archive points instead of refused, while every other path refuses it. Make it consistent: inspect; foreign/newer → the existing refusal (`StoreError::Archive { step: "schema", .. }`); accepted-but-incomplete → `Ok(Vec::new())`. Extend `incomplete_archive_schema_is_reported_while_reads_stay_lenient` to assert `read_archive_points` returns empty after `DROP TABLE metric_rollups_1h`, and add `archive_points_refuse_a_newer_user_version`: set `PRAGMA user_version = 2` on a full archive → `read_archive_points` is `Err(Archive { step: "schema", .. })` and so are `archive_coverage`-backed coverage and `read_archive_manifest` (assert all three).

**F5 — P3 (from the reviewer's P1, downgraded — hourly rows make one pass small, but symmetry with the move's caps is right): bound one export pass.** `pub const MAX_COLD_MONTHS_PER_PASS: usize = 12`; `export_cold_months` exports at most that many months per call, oldest first (the rest next pass — the hourly scheduler and `export-now` both call one pass). Doc sentence in `### Cold export`. Test `cold_export_exports_at_most_twelve_months_per_pass`: seed 13 eligible fully-moved months (one row each) → first pass 12 manifest rows, watermark = the 12th; second pass 1. `db archive status` stays unbounded in its manifest listing (months, not rows) — say so in the doc.

**F6 — P3 (docs):** covered by F1's doc edits plus: `INSTALL.md` — its environment table (~`:208`) gains `TINYTOP_SYSTEMD_UNIT_DIR` (the same row README carries) and its operations section (~`:251`) gains one paragraph each for the queryable archive (`retentionLadder.archive.queryable`, where `history-archive.sqlite` lives, read-only reads), the cold export (`cold`, the files and `sha256sum -c`, `db archive status|export-now`) and the disk check (`diskCheck`, what pressure refuses, the two markers) — pointing at GUIDE/README for detail, no duplication of tables; `CHANGELOG.md` `## Unreleased` entries for F1–F5, F7, F8 (Fable assigns 0.4.0 at merge).

**F7 — P3 (lint): clippy is not clean — 17 warnings — and the gate does not run it.** Fix mechanically: `manual_clamp` (`archive.rs:330,351`), `needless_lifetimes` (`archive.rs:1024`), `explicit_auto_deref` (`archive.rs:1238`; `disk.rs:123,152,153,157,167`), `io_other_error` (`writer.rs:617` → `io::Error::other`), `needless_borrow` (`tests/archive.rs:372`), `field_reassign_with_default` (`tests/archive.rs:990,1059`). For `result_large_err` (`maintenance.rs:65,83,92`) and `too_many_arguments` (`maintenance.rs:387`) add `#[allow(clippy::result_large_err)]` / `#[allow(clippy::too_many_arguments)]` on those items with a one-line comment ("`StoreError` boxing is a Phase-3 refactor; the struct-of-args refactor touches the T2 fold contract") — NO logic change in `maintenance.rs`. Then make the gate enforce it: in `package.json`, `check:rust` becomes `cargo fmt --manifest-path agent/Cargo.toml --all -- --check && cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings && cargo test --manifest-path agent/Cargo.toml --workspace` (ONE line changed; paste `cargo clippy … -- -D warnings` exit 0).

**F8 — P1 (from the second reviewer, validated): `put_settings` has a TOCTOU against the disk check.** `lib.rs` `put_settings` (~`:725-760`) reads `diskPressure` via `history_state_get` (`:732`) and `get_settings` for `previous`, validates, and only THEN opens its transaction (`:742`); with the store's single-connection pool, `apply_disk_measurement`'s `BEGIN IMMEDIATE` can commit a breach between that read and the settings write, so a growth commits while persisted pressure is active — the exact rule §5 exists to enforce. Ruling: make `put_settings` one atomic read-validate-write: acquire one connection, `BEGIN IMMEDIATE` (raw, as `disk.rs` does), read `previous` (the settings document — move the SELECT of `get_settings` into a `get_settings_on(&mut SqliteConnection)` helper that `get_settings` wraps, same pattern as `history_state_get_on`) and `diskPressure` (`history_state_get_on`) INSIDE it, validate, write `app_settings` and the `l3Enabled`/`l4Enabled` state rows, `COMMIT`; `ROLLBACK` on any error path before the connection returns to the pool (mirror `rollback_disk_transaction`). Behaviour on the happy path and every refusal message are unchanged (`tests/retention_settings.rs` 20/20 must stay green untouched except the one test below). Test in `tests/retention_settings.rs`: `settings_growth_and_a_breach_serialize_on_one_transaction` — from one store with an inactive state, run `tokio::join!(put_settings(grown), apply_disk_measurement(.., Ok(100), ladder(min 200), now))` and assert the outcome is one of exactly two consistent worlds: (i) the growth committed AND the breach committed after it (the stored settings are grown, pressure active) or (ii) the breach committed first AND the growth was refused with the §5 message (settings unchanged, pressure active) — never "grown while pressure was already active when the settings transaction began"; run the pair 20 times in a loop to exercise both orders and print which orders were observed. Say plainly that a single-connection pool cannot force the interleave deterministically — the structural guarantee (read inside `BEGIN IMMEDIATE`) is the fix, the test is the guard.

Rejected by Fable (no action): "daemon logs persisted setting values" — `minFreeBytes` and the clamped interval are operational thresholds, not secrets or PII; the Phase-2 brief's wording was over-strict.

Rules (non-negotiable): git READ-ONLY (no add/commit/push/checkout/stash/branch; leave the tree dirty; report `git diff --stat`) · never bare `cargo fmt` (`cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; `rustfmt --edition 2024 <file>` for a file you changed) · never open `~/.local/share/tinytop/` or `~/.config/systemd/user/` or `~/.local/state/tinytop/` · tests in `std::env::temp_dir()` only · `Cargo.toml`/`Cargo.lock` unchanged · the move (`move_expired_l4` and its phases) untouched.

Gate: `bun run check` at the repo root (WITH the new clippy step), pasted in full. Known sandbox limits (say so verbatim; treat as passed-for-you if these are the ONLY failures): (1) the 10 `serve_contract` tests bind a port (`PermissionDenied`); (2) Bun's `tests/server.test.ts` `Bun.serve({ port: 0 })`. `tests/tinytop-script.test.ts` must now pass WITHOUT any `XDG_RUNTIME_DIR` you set by hand (F2 makes the harness self-contained) — if it does not, that is yours. Anything else is yours.

Report (final message, in order): files changed with line ranges · F1 and F2 RED/GREEN transcripts · `cargo clippy … -- -D warnings` output · full gate output · `git diff --stat` · anything this brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
