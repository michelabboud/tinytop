You are hexe lane **T11-fix1** for tinytop. Base: branch `tinytop/ladder-t11-otel` at `__LANE_SHA__` (T11 = runs 625/627 + `main` merged in; your worktree branches from it). Fix ONLY the findings below — Fable's acceptance pass (Fabulous `docs/fleet/tinytop/2026-08-29-t11-acceptance-checklist.md`) and luna's blind review (run `__LUNA_RUN__`, validated by Fable). Files you may edit are named per finding; NOTHING else (no `Cargo.toml`/`Cargo.lock`, no `settings_transfer.rs`, no Bun `src/`, no `vendor/`). Git is read-only for you; the orchestrator commits. TDD where a finding has behaviour: paste RED, implement, paste GREEN. Escalate (`ESCALATE: …`) rather than improvise.

**Read first:** the T11 brief (`briefs/T11.md`, F1–F8 + its continuation preamble), spec §12 (`docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md:205-212`), the vetting report `docs/reports/2026-08-29-dependency-vetting-opentelemetry.md`, `agent/crates/tinytop-agent/src/otel.rs` (`Instruments::new`, the metric units), `agent/crates/tinytop-agent/src/writer.rs` (`spawn_otel_export_loop`; and `spawn_disk_check_loop`'s defensive clamp + its comment, the pattern to copy), `INSTALL.md` (prerequisites section; `:370` mentions SQLite's C toolchain), `README.md` (the build/install section and the `## OpenTelemetry export` section).

**F1 — P2: document the new build prerequisite (docs only).** The OTel subtree compiles `aws-lc-sys` (rustls's default crypto provider, reached through `reqwest` ← `opentelemetry-http` ← `opentelemetry-otlp`) from C via `cmake`. Files: `INSTALL.md` (the prerequisites/toolchain paragraph near `:370`: `cmake` + a C compiler are required to build the Rust daemon since 0.5.0, on Linux/WSL, macOS and Windows alike — name the package on Debian/Ubuntu (`cmake build-essential`), Homebrew (`cmake`), and Windows (Visual Studio Build Tools' C++ workload + CMake); a missing `cmake` fails the build inside `aws-lc-sys`'s build script — say what the error looks like), `README.md` (one sentence in the build section pointing at INSTALL.md), the vetting report (a "Build prerequisites" paragraph under the measured-cost section: `aws-lc-sys` + `cmake`; `quinn`/`jni`/`wasm-bindgen`/`schannel`/`security-framework` are lock-only entries that are not compiled on Linux — state that `cargo tree -i quinn` is empty; note that a `ring`-only rustls provider is not reachable through `opentelemetry-otlp` 0.32's features and would need a direct `reqwest`/`rustls` dependency with `with_http_client` — deferred, listed in PROGRESS Backlog by the orchestrator). No code change.

**F2 — P3: units for percent-valued gauges (code + test + spec).** `tinytop.load.percent` and `tinytop.pressure.some` / `tinytop.pressure.full` carry 0–100 values; their unit is declared `1` (a 0–1 ratio — correct for `system.*.utilization`, wrong here). Files: `otel.rs` — `with_unit("%")` on those three instruments; the unit assertion in `metric_request_carries_the_spec_names_units_and_resource` (RED with `1`, GREEN with `%`); `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §12 — change `tinytop.load.percent` [1] → [%] and `tinytop.pressure.some / .full` [1] → [%] with an "amended 2026-08-29 (T11-fix1)" note; README's metric table if it lists units. Nothing else about the metric map changes.

**F3 — P3: defensive interval clamp in the export loop (code + test).** `spawn_otel_export_loop` uses `settings.interval_sec` as stored; a value written outside the validated settings API (a hand-edited `app_settings` row) of `0` would export on every 5 s tick, a huge value would never export. Files: `writer.rs` — clamp to `OTEL_INTERVAL_SEC_RANGE` (5..=3600, import the constant from `tinytop_store::otel_settings`) once per tick where `interval_sec` is read, with one `eprintln!` naming the stored and the used value only when they differ (copy the disk loop's comment style: "Keep this defensive clamp aligned with OtelSettings::validate's 5..=3600 range; the persisted document may have been edited outside the validated settings API"); the status/coverage `intervalSec` shows the USED value. Test: a unit test on the schedule helper (`OtelSchedule::is_due` with a clamped interval) or a writer test that seeds `intervalSec: 0` through a direct `history_state`/`app_settings` write (whichever the existing test fixtures allow — say which) and asserts the loop's effective interval is 5 (RED: 0 → due every tick; GREEN: 5).

__LUNA_FINDINGS__

Acceptance (paste): RED/GREEN per F2/F3; `cargo test -p tinytop-agent -- --skip serve_` summary; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check` clean; `git diff --stat` = only the files named above.
