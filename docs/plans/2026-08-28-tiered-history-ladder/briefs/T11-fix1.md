You are hexe lane **T11-fix1** for tinytop. Base: branch `tinytop/ladder-t11-otel` at `efc31fe` (T11 = runs 625/627 + `main` merged in; your worktree branches from it). Fix ONLY the findings below — Fable's acceptance pass (Fabulous `docs/fleet/tinytop/2026-08-29-t11-acceptance-checklist.md`) and luna's blind review (run `630`, validated by Fable). Files you may edit are named per finding; NOTHING else (no `Cargo.toml`/`Cargo.lock`, no `settings_transfer.rs`, no Bun `src/`, no `vendor/`). Git is read-only for you; the orchestrator commits. TDD where a finding has behaviour: paste RED, implement, paste GREEN. Escalate (`ESCALATE: …`) rather than improvise.

**Read first:** the T11 brief (`briefs/T11.md`, F1–F8 + its continuation preamble), spec §12 (`docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md:205-212`), the vetting report `docs/reports/2026-08-29-dependency-vetting-opentelemetry.md`, `agent/crates/tinytop-agent/src/otel.rs` (`Instruments::new`, the metric units), `agent/crates/tinytop-agent/src/writer.rs` (`spawn_otel_export_loop`; and `spawn_disk_check_loop`'s defensive clamp + its comment, the pattern to copy), `INSTALL.md` (prerequisites section; `:370` mentions SQLite's C toolchain), `README.md` (the build/install section and the `## OpenTelemetry export` section).

**F1 — P2: document the new build prerequisite (docs only).** The OTel subtree compiles `aws-lc-sys` (rustls's default crypto provider, reached through `reqwest` ← `opentelemetry-http` ← `opentelemetry-otlp`) from C via `cmake`. Files: `INSTALL.md` (the prerequisites/toolchain paragraph near `:370`: `cmake` + a C compiler are required to build the Rust daemon since 0.5.0, on Linux/WSL, macOS and Windows alike — name the package on Debian/Ubuntu (`cmake build-essential`), Homebrew (`cmake`), and Windows (Visual Studio Build Tools' C++ workload + CMake); a missing `cmake` fails the build inside `aws-lc-sys`'s build script — say what the error looks like), `README.md` (one sentence in the build section pointing at INSTALL.md), the vetting report (a "Build prerequisites" paragraph under the measured-cost section: `aws-lc-sys` + `cmake`; `quinn`/`jni`/`wasm-bindgen`/`schannel`/`security-framework` are lock-only entries that are not compiled on Linux — state that `cargo tree -i quinn` is empty; note that a `ring`-only rustls provider is not reachable through `opentelemetry-otlp` 0.32's features and would need a direct `reqwest`/`rustls` dependency with `with_http_client` — deferred, listed in PROGRESS Backlog by the orchestrator). No code change.

**F2 — P3: units for percent-valued gauges (code + test + spec).** `tinytop.load.percent` and `tinytop.pressure.some` / `tinytop.pressure.full` carry 0–100 values; their unit is declared `1` (a 0–1 ratio — correct for `system.*.utilization`, wrong here). Files: `otel.rs` — `with_unit("%")` on those three instruments; the unit assertion in `metric_request_carries_the_spec_names_units_and_resource` (RED with `1`, GREEN with `%`); `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §12 — change `tinytop.load.percent` [1] → [%] and `tinytop.pressure.some / .full` [1] → [%] with an "amended 2026-08-29 (T11-fix1)" note; README's metric table if it lists units. Nothing else about the metric map changes.

**F3 — P3: defensive interval clamp in the export loop (code + test).** `spawn_otel_export_loop` uses `settings.interval_sec` as stored; a value written outside the validated settings API (a hand-edited `app_settings` row) of `0` would export on every 5 s tick, a huge value would never export. Files: `writer.rs` — clamp to `OTEL_INTERVAL_SEC_RANGE` (5..=3600, import the constant from `tinytop_store::otel_settings`) once per tick where `interval_sec` is read, with one `eprintln!` naming the stored and the used value only when they differ (copy the disk loop's comment style: "Keep this defensive clamp aligned with OtelSettings::validate's 5..=3600 range; the persisted document may have been edited outside the validated settings API"); the status/coverage `intervalSec` shows the USED value. Test: a unit test on the schedule helper (`OtelSchedule::is_due` with a clamped interval) or a writer test that seeds `intervalSec: 0` through a direct `history_state`/`app_settings` write (whichever the existing test fixtures allow — say which) and asserts the loop's effective interval is 5 (RED: 0 → due every tick; GREEN: 5).

**F4 — P0 (luna #1, confirmed at severity): the export loop holds the `state.otel` lock across its sleep while disabled.** `writer.rs:704` takes `state.otel.lock().await` inside the `if !settings.enabled` block; the guard lives through `disable_pipeline(…)` (`:705–710`, which calls the synchronous `shutdown_best_effort` — up to 2 × 10 s when a pipeline exists) and through the `tokio::time::sleep(5 s).await` at `:711`, and is dropped only at the `continue`. `history_coverage` (`:460`) awaits the same tokio `Mutex`, so every `/api/history/coverage` request waits up to ~5 s **while OTel is disabled — the default state of every install**. Files: `writer.rs` (the loop), `otel.rs` (`disable_pipeline`, `:105–114`, if you change its shape). Fix: in the disabled branch, take the pipeline out and shut it down FIRST, with no guard alive (`if let Some(existing) = pipeline.take() { existing.shutdown_best_effort(…) }`), then lock `state.otel` in its own block for `mark_disabled(&mut status, &settings)` only, then sleep after the block ends. Either drop the `pipeline` parameter from `disable_pipeline` (it becomes the lock-scoped half) or keep it and call the two halves from the loop — keep `disabled_block_tears_the_pipeline_down` (`otel.rs:1061`) green with whichever shape you choose. Invariant to state in a comment above the loop and to hold at every lock site (`:704`, `:716`, `:761`, `:783`, `:789`): **no `.await`, no `shutdown_best_effort`, no store call while a `state.otel` guard is alive.** Test (`writer.rs` `cfg(test)`, uses `tests::test_state`): `otel_loop_never_holds_the_status_lock_across_its_sleep` — default settings (disabled), spawn the loop with the REAL 5 s tick (see F6 for the parameter), `tokio::time::sleep(200 ms).await` so the loop reaches its sleep, then `tokio::time::timeout(Duration::from_millis(500), state.otel.lock()).await` must be `Ok` (assert `enabled == false` on the guard); abort the handle at the end. RED today (the guard is held for the whole 5 s → `Elapsed`), GREEN after — paste both.

**F5 — P2 (luna #2, re-ranked from P1): a settings change waits behind an in-flight export plus a full tick.** The loop awaits the export at `writer.rs:779` (bounded by the 10 s `OTEL_EXPORT_TIMEOUT_SECS`) and then sleeps the full tick at `:797` before re-reading settings, so with a hanging receiver a disable / endpoint / header change is applied after up to ~15 s. Files: `writer.rs`, `README.md:453`, `GUIDE.md:99`. Fix: take `let iteration_started = Instant::now()` at the top of each iteration (before `get_settings`); replace the sleep at `:797` with `tokio::time::sleep(next_tick_delay(tick, iteration_started.elapsed())).await` where `fn next_tick_delay(tick: Duration, elapsed: Duration) -> Duration { tick.saturating_sub(elapsed) }` is a free function with a unit test (elapsed 0 → tick; elapsed == tick → 0; elapsed > tick → 0; 2 s of 5 s → 3 s). The `:711` and `:726` sleeps (no export in that path) stay a plain `tick`. The contract this defines — write it as ONE sentence in both README (`:453`, after "Export runs in its own daemon task at the configured interval.") and GUIDE (`:99`, after the rotation sentence): *"Settings changes are picked up on the exporter's next 5-second tick; an export already in flight (bounded by its 10-second timeout) can delay that tick, so a change is applied within 10 seconds at worst and within 5 seconds when the receiver answers promptly."* Do NOT restructure the export into a spawned task: its distinguishing behaviour (a receiver that hangs) is only observable with a port-bound test your sandbox cannot run; Fable's acceptance pass covers the hung-receiver bound outside.

**F6 — P2 (luna #3, re-ranked from P1): nothing exercises `spawn_otel_export_loop` → `OtelStatus`.** `otel.rs:990` (`export_failure_increments_the_counter_and_never_stalls_collection`) drives `collect_and_export` and `record_failure` by hand; no test spawns the loop — which is why F4 reached the gate green. Files: `writer.rs`. Fix: make the tick a parameter — `pub(crate) fn spawn_otel_export_loop(state: AppState, tick: Duration) -> JoinHandle<()>`; `serve` (`:298`) passes `Duration::from_secs(OTEL_TICK_SECS)`; the three sleeps use `tick` (with F5's helper at `:797`). Tests (`writer.rs` `cfg(test)`, `tests::test_state`; enable OTel through the store exactly as the PUT handler does — read `put_settings`'s signature at `tinytop-store/src/lib.rs:809` and pass what it needs; endpoint `http://127.0.0.1:1/v1/metrics`, `intervalSec` 5 = the validated minimum, the default `headersEnvVar` (leave the environment alone — no `set_var`)): (a) `otel_loop_counts_failures_through_the_status_and_collection_continues` — spawn with a 50 ms tick, `collect_and_store` once (publishes the snapshot the build needs — F7), poll `state.otel` every 50 ms for ≤ 3 s until `failures ≥ 1`; assert `enabled`, `endpoint` = the fixture, `last_failure_ms.is_some()`, `last_error.is_some()`; then `collect_and_store` twice more and assert `test_store(&state).stats().sample_count == 3`; abort the handle. (b) `otel_loop_applies_a_disable_within_one_tick` — same setup; once `failures ≥ 1`, `put_settings` with `enabled: false`; poll ≤ 1 s until `state.otel.enabled == false`; read `failures`, sleep 300 ms (six ticks), assert `failures` unchanged. Honesty: (a) and (b) are new coverage of wiring that has no failing state today — say so instead of inventing a RED; the RED of this round is F4's test. Each test aborts its `JoinHandle` before returning (the `TempDatabase` drop must not race a live loop).

**F7 — P3 (luna #4, re-ranked from P2): the build falls back to `host.name = "unknown"` when no snapshot has been published.** `writer.rs:736–745`. Unreachable in production (`serve` collects at `:281` before spawning at `:298`) but a real deviation from the brief; and the brief's fallback (the daemon's hostname source) does not exist — `daemon_metadata.bind.host` is the bind address, not a hostname (luna's ruling (e), agreed). Files: `writer.rs`. Fix: never build without a snapshot — gate the build branch on `let Some(snapshot) = latest_snapshot.borrow().clone()` (clone the `Arc` out of the `watch` `Ref` before anything else, exactly as the export path does at `:768`); `None` → skip this tick without `mark_attempt` and without a failure (mirrors the export path's `None` rule); take `hostname` from that snapshot; delete the `"unknown"` literal (`grep -n '"unknown"' writer.rs` must be empty afterwards). No in-sandbox RED is possible (the fallback is only observable at a receiver) — state that; the outside `serve_otel_receiver_decodes_one_request` and Fable's acceptance check `host.name` at the receiver.

**F8 — P3 (Fable): the 64-character resource-attribute key cap is enforced on both sides and named in neither message.** `tinytop-store/src/otel_settings.rs:112` (`key.chars().count() <= 64`) and `ladder-rules.js:194` (`characters.length <= 64`) both refuse a longer key; the refusal text (`otel_settings.rs:69–72`, `ladder-rules.js:186`) says only "keys matching ^[a-z][a-z0-9._]*$", so an operator with a 65-character key that matches the regex is told nothing true. Files: `otel_settings.rs`, `ladder-rules.js`, the tests that assert the message text on each side (grep both suites for `keys matching`), spec §12 (the resourceAttributes sentence — add "keys ≤ 64 characters" beside F2's units amendment, same dated note). Fix: both messages become exactly *"otel.resourceAttributes must hold at most 32 entries with keys of at most 64 characters matching ^[a-z][a-z0-9._]*$ and values of at most 256 characters"*; keep the rule. RED = the message assertions on both sides (paste one of each).

Acceptance (paste): RED/GREEN per F2, F3, F4 and F8 (F6's two loop tests and F7 are new coverage — say so, do not invent a RED); `grep -n '"unknown"' agent/crates/tinytop-agent/src/writer.rs` empty; `cargo test -p tinytop-store` and `cargo test -p tinytop-agent -- --skip serve_` summaries; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check` clean; `bun test tests/dashboard-ladder.test.ts` (F8's JS message); `git diff --stat` = only the files named above. Read-first list for this round adds: `writer.rs:681–800` (the loop), `:455–470` (`history_coverage`), `:1455–1510` (`tests::test_state`), `otel.rs:98–114` (`mark_disabled`/`disable_pipeline`), `:1050–1075` (`disabled_block_tears_the_pipeline_down`), `tinytop-store/src/lib.rs:809` (`put_settings`).
