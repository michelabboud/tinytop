You are hexe lane **T7-fix1** for tinytop. Your base is branch `tinytop/ladder-t7-queryable-archive` (lane T7 = hexe run 573, committed as "T7 — queryable archive as briefed"); you are on `tinytop/ladder-t7-queryable-archive-fix1`. T7 implemented `docs/plans/2026-08-28-tiered-history-ladder/briefs/T7.md` exactly and then **escalated, correctly**: the brief's single cross-database move transaction is not crash-safe. The reviewer found the same defect at SQLite source while the lane ran and wrote the decision as **ADR 0018** (`docs/adr/0018-archive-move-commits-copy-before-delete.md` — read it FIRST, in full; it is the contract for F1). This lane applies ADR 0018 plus the lane's own three self-findings and two reviewer findings. No redesign beyond the ADR; escalate if the ADR cannot be implemented as written.

Read, in this order: (1) ADR 0018; (2) `briefs/T7.md` (the surface you keep — names, files, the six tests); (3) `agent/crates/tinytop-store/src/archive.rs` end-to-end (404 lines); (4) the T7 hunks in `agent/crates/tinytop-store/src/lib.rs` (`git diff main..HEAD -- agent/crates/tinytop-store/src/lib.rs`), `src/maintenance.rs` `:245-268`, `agent/crates/tinytop-agent/src/writer.rs` `:367-368`; (5) `agent/crates/tinytop-store/tests/archive.rs`; (6) `CLAUDE.md` at the repo root.

**F1 — ADR 0018: two transactions, copy committed before delete** (`archive.rs::move_attached_batch` `:263-387`, `move_expired_l4` `:110-142`). Replace the single `BEGIN IMMEDIATE … COMMIT` with, on the same attached pool connection:
1. `BEGIN` (deferred — a read snapshot of `main`, a write lock on `archive` only; step `"begin copy"`) → the batch `SELECT MIN/MAX/COUNT` exactly as today `:273-295` (step `"select"`) → `row_count == 0` ⇒ `COMMIT` and `Ok(0)` → **`INSERT OR REPLACE`** (not `OR IGNORE`) `INTO archive.metric_rollups_1h (cols) SELECT cols FROM main.metric_rollups_1h WHERE bucket_start_ms BETWEEN ?min AND ?max` (step `"insert"`) → `COMMIT` (step `"commit copy"`; on failure `ROLLBACK` + `Err`). Only the archive's WAL receives frames — `main` is a read transaction here (assert this in the crash-order test below via `PRAGMA main.data_version` before/after phase A on a second pool connection: unchanged).
2. **Verify the committed copy** outside any transaction: `SELECT COUNT(*) FROM archive.metric_rollups_1h WHERE bucket_start_ms BETWEEN ?min AND ?max` must equal `row_count` (step `"verify"`; on mismatch `Err`, nothing deleted, watermark untouched). `bucket_start_ms` is the primary key on both tables, so the range IS the batch.
3. `BEGIN IMMEDIATE` (step `"begin delete"`) → `DELETE FROM main.metric_rollups_1h WHERE bucket_start_ms BETWEEN ?min AND ?max AND EXISTS (SELECT 1 FROM archive.metric_rollups_1h a WHERE a.bucket_start_ms = metric_rollups_1h.bucket_start_ms AND a.sample_count = metric_rollups_1h.sample_count AND a.newest_captured_at_ms = metric_rollups_1h.newest_captured_at_ms)` (step `"delete"`) → `COMMIT` (step `"commit delete"`; on failure `ROLLBACK` + `Err` — rows stay in main, copies stay in the archive, the next call converges). **Delete today's `deleted != row_count` check `:358-363`** — a row that changed between phases 1 and 3 is *meant* to stay (its archive copy is stale) and is re-copied by the next call's `OR REPLACE`; `deleted <= row_count` always holds. Return `Ok(deleted)`.
4. Watermark: **only when `deleted == row_count`**, `store.history_state_set("archiveMovedUntilMs", &(max_ms + Tier::L4.resolution_ms()))` (step `"watermark"`) — through the store's ONE `history_state` writer (`lib.rs:626`), NOT the hand-rolled `INSERT … ON CONFLICT` at `:370-385`, which you delete. The watermark is advisory (ADR 0018 "Consequences"); a partial batch leaves it where it was.
5. `move_expired_l4` keeps the `ATTACH`/`DETACH` bracket `:124-141`. Change the DETACH-failure path: if the operation already produced an error, RETURN THAT error (it is the actionable one) and print the detach failure to stderr with its step; if the operation succeeded and only DETACH failed, return `Err(step "detach")`. `close_on_drop()` in both cases stays.
6. Rewrite the comment `:315-316` to the truth: "SQLite commits attached databases file-by-file in ATTACH order when `main` is WAL (`aMJNeeded[WAL] = 0`, no super-journal), so a single cross-file transaction makes main's DELETE durable before the archive's INSERT and a crash between them loses the batch. Hence: copy + commit (archive only) → verify the committed copy → delete + commit (main only). `OR REPLACE` keeps a stale copy from freezing; the content-matched DELETE keeps a changed row until it is re-copied. ADR 0018."
7. Step names are distinct per failure point: `create directory`, `open`, `schema`, `close`, `acquire`, `attach`, `begin copy`, `select`, `select decode`, `insert`, `commit copy`, `verify`, `begin delete`, `delete`, `commit delete`, `watermark`, `detach`, plus the read/coverage steps already there.

**F2 — Step-aware remedy** (`lib.rs` `StoreError::Archive` `Display` `:2139-2142`). Today every archive error ends "nothing was deleted from the main database" — false for a failure at `watermark` or `detach` after `commit delete` succeeded (the lane's own finding). Add `pub(crate) fn archive_remedy(step: &'static str) -> &'static str` in `archive.rs`: for `"watermark"` and `"detach"` → `"the batch is committed in history-archive.sqlite and removed from the main database; only the {step} bookkeeping failed — retrying is safe, nothing is duplicated or lost"` (format the step in); for every other step → today's sentence unchanged. `Display` uses it. Test: construct `StoreError::Archive { step: "watermark", source: … }` and `{ step: "insert", … }` and assert each remedy text; keep test 1's existing substring assertion (`:153-157`) valid for `attach`/`insert`.

**F3 — Revert the `read_tier_history_points` refactor** (`lib.rs` `:1724-1758` in this tree; `git diff main..HEAD` hunks 4 and 5 of `lib.rs`). T7.md said `read_tier_history_points` stays as it is; the lane routed it through `tier_bucket_from_row`, which decodes `min_*`/`max_*` as non-null `f64`. Legacy L2 rows (v0 → v1 migration) hold NULL there — `read_tier_buckets` `:911-924` `COALESCE`s them for exactly that reason — so `source=rollup` reads fail on every migrated database. Restore the SELECT (7 columns) and the per-row `HistoryPoint` mapping byte-for-byte from `main` (`git show main:agent/crates/tinytop-store/src/lib.rs` `:1708-1755`); keep `history_point_from_bucket` `:2238-2250` for the `Archive` arm only. **Regression test** in `tests/migration_v1.rs` (the v0 fixture there has legacy rollup rows): after migration, `read_history_points(HistoryPointsQuery { source: HistoryPointMode::Rollup, since_ms: None, until_ms: None, limit: Some(100) })` returns the fixture's rollup rows — RED on this tree with a NULL-decode error (paste it), GREEN after the revert. If the fixture's rollup rows do not carry NULL `min_*`, insert one legacy-shaped row through the pool (`INSERT INTO metric_rollups (…) VALUES (…)` with the `min_*`/`max_*` columns omitted) and say so.

**F4 — `ensure_archive_schema` never stamps a foreign or newer file** (`archive.rs` `:83-108`, `archive_schema_is_current` `:214-237`). Today any existing file with `user_version != 1` is opened read-write, gets the tables `IF NOT EXISTS` and `PRAGMA user_version = 1` — a silent downgrade of a future archive or an overwrite of an unrelated SQLite file that happens to be named `history-archive.sqlite`. Rule: `user_version == 1` → ensure the three objects exist (as today); `user_version == 0` **and** `sqlite_master` is empty (a fresh or just-created file) → initialise; anything else (`user_version` ∉ {0, 1}, or 0 with any object present) → `Err(StoreError::Archive { step: "schema", source: ArchiveErrorSource::Io(std::io::Error::new(InvalidData, "history-archive.sqlite at <path> has user_version <n> with <k> objects; expected a tinytop archive (user_version 1) — move the file away or point retentionLadder.archive.directory elsewhere")) })`, nothing written. Test `archive_schema_refuses_foreign_or_newer_files`: (a) create the file with `PRAGMA user_version = 2` → `move_expired_l4` fails with step `"schema"`, the file's `user_version` is still 2, main unchanged; (b) a `user_version 0` file containing `CREATE TABLE stranger (x)` → same refusal, the table survives.

**F5 — One settings fetch in `read_history_points`** (`lib.rs` `:1206-1245`): the `Auto` branch fetches settings and the `Archive` arm fetches them again. Fetch once before the `match` when `query.source` is `Auto` or `Archive` (an `Option<DashboardSettings>`), and pass the same document to both the resolver and the arm. Behaviour unchanged for the other sources.

**F6 — Docs tell the truth.** (a) spec `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §9 `:177` — rewrite the `move_to_archive` parenthesis: `(ATTACH; BEGIN; INSERT OR REPLACE INTO archive.metric_rollups_1h; COMMIT — archive only; verify the committed count; BEGIN IMMEDIATE; content-matched DELETE from main; COMMIT — main only; DETACH; ≤ 1,000 rows per batch; archiveMovedUntilMs advances only for a fully deleted batch — ADR 0018: a single cross-file transaction commits main first under WAL)`. (b) `docs/sqlite-history-architecture.md` `## Archive` `:435-473`: rewrite paragraphs 2 and 3 (the mechanics and the WAL/crash paragraph) to the two-commit order and the correct crash matrix (before phase 1 commits → nothing; between the commits → the batch is in both files and the next call converges through `OR REPLACE`; after → done; "absent from both" unreachable), name ADR 0018, and state what a failed step leaves behind per step. (c) `CHANGELOG.md` `## Unreleased`: amend bullet 1 to name the two-commit order and ADR 0018; add a bullet for F3's regression fix and one for F4's refusal. (d) `docs/adr/README.md` is already updated on `main` — do not touch it.

**F7 — Crash-order test** `archive_copy_is_committed_before_main_delete` in `tests/archive.rs`. Expose phase 1 for tests as `#[doc(hidden)] pub async fn copy_expired_l4_batch(store, paths, cutoff_ms, batch) -> Result<Option<ArchiveBatch>, StoreError>` where `pub struct ArchiveBatch { pub min_ms: i64, pub max_ms: i64, pub row_count: i64 }` (`move_expired_l4` calls it internally — ONE implementation of phase 1; doc comment: "phase 1 of `move_expired_l4`, public only so the crash-order test can stop between the two commits"). Test: seed 3 expired L4 buckets; `copy_expired_l4_batch(…, batch 3)` → returns `Some(min 0, max 2h, count 3)`; the archive holds 3 (read-only read) AND main still holds 3 (the state a crash between the commits leaves); `PRAGMA main.data_version` on a second pool connection is unchanged across the copy; then mutate one main row (`UPDATE metric_rollups_1h SET sample_count = 61 WHERE bucket_start_ms = 3600000`); `move_expired_l4(…, batch 3)` → returns 3, main holds 0, the archive holds exactly 3 rows (no duplicates) and the mutated row's archive copy has `sample_count 61` (`OR REPLACE` refreshed it), watermark = `2h + 1h`.

Keep the six T7 tests passing; test 1's step assertion `["attach", "insert"]` may need `"commit copy"` added — say which step the `0o444` injection actually fails at (paste the error text) and keep the failure in place: main still 2, archive still 2, watermark unchanged, `PRAGMA database_list` = `main` only.

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`, `branch`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only. Leave nothing under `/tmp` outside `std::env::temp_dir()` fixtures that your tests remove.
- No new dependencies (`Cargo.lock` unchanged). No edits outside: `archive.rs`, `lib.rs`, `tests/archive.rs`, `tests/migration_v1.rs`, the spec line, `docs/sqlite-history-architecture.md`, `CHANGELOG.md`. Not `maintenance.rs`, `writer.rs`, `main.rs`, `migration.rs`, `retention_ladder.rs`, `agent/assets/`, Bun.
- Additive only, names unchanged (spec §13); the public surface of `archive.rs` from T7.md stays, plus `copy_expired_l4_batch`/`ArchiveBatch`/`archive_remedy` from this brief.

Gate to run and paste in full: `bun run check` (repo root). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests in `tinytop-agent` bind a local port (`PermissionDenied`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours.

Report (your final message), in this order: files changed with line ranges · the RED output of F3's regression test and F7's test before your changes · GREEN after · the step the `0o444` injection fails at · full gate output · `git diff --stat` · docs updated · anything ADR 0018 or this brief was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
