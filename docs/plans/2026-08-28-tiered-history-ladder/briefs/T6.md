You are hexe lane **T6** for tinytop. Execute ONLY **"Task 6: CLI + docs + Phase 1 close-out material"** from `docs/plans/2026-08-28-tiered-history-ladder-plan.md`. You start from `main` @ `06c23cb` (v0.2.11: T1–T5 landed). No other lane runs beside you.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` — **§14 (CLI additions: ONLY `db stats` and `db pre-image status|remove` are yours; `db archive …` is T8's and `config export/import` is T10's — do not add them)**, §7 (the pre-image: `<db>.pre-v0.sqlite`, taken before the first migration, never overwritten), §10 (the coverage shape — `db stats` reuses it), §6 (`user_version` 1).
2. The plan's **Global Constraints**, **Context & Rationale**, **Task 6**, and the T1–T5 "Landed" lines (what already exists).
3. Code (line numbers = `06c23cb`): `agent/crates/tinytop-agent/src/main.rs` (409 lines: dispatch :28-33, `db()` :122-171 — subcommands `stats|check|vacuum`, `--sqlite`, the `DbStatus { status, value }` JSON envelope; `print_help` :346-364), `agent/crates/tinytop-store/src/lib.rs` (`StoreStats` :34, `stats()` :1326, `history_coverage()` — the single source for tiers/JSON horizon/disk/archive, `integrity_check()`), `agent/crates/tinytop-store/src/migration.rs` (`pre_image_path(db_path)` :521 — private today; `user_version` read :313; `MigrationReport.pre_image_path` :22), `agent/crates/tinytop-store/src/disk.rs` (`required_pre_image_bytes` = db + db/5, ADR 0017), `agent/crates/tinytop-agent/tests/help_contract.rs` (the binary-spawning test pattern: `Command::new(env!("CARGO_BIN_EXE_tinytop-agent"))`), `INSTALL.md`, `README.md` (CLI section), `ARCHITECTURE.md`, `PROGRESS.md` (`## Current Version` :3, `## Completed` :29 — newest first), `CHANGELOG.md`.

Steps (TDD: failing tests first, paste RED, implement, paste GREEN, then docs):
- **Step 1 — tests** in a NEW `agent/crates/tinytop-agent/tests/cli_db.rs` (spawn the binary with `--sqlite sqlite://<std::env::temp_dir()>/…/h.sqlite`; a fresh DB is created at schema v1 by `connect`):
  - `db_stats_json_reports_the_ladder`: `db stats --json` → stdout JSON with `status:"ok"` and `value.tiers` = four entries `l1..l4` each with `tier, enabled, keepDays, resolutionMs, bucketCount, oldestMs, newestMs`; `value.snapshotJsonSampleCount` (integer), `value.archive` and `value.disk` objects (spec §10 shapes), plus every existing `StoreStats` field unchanged.
  - `pre_image_status_reports_absence_on_a_fresh_db`: `db pre-image status` → `value: { path: "<db>.pre-v0.sqlite", exists: false, bytes: null, userVersion: 1, integrityCheck: "ok" }`.
  - `pre_image_remove_refuses_without_yes`: create an empty file at the pre-image path → `db pre-image remove` → exit 1, stdout JSON `status:"refused"`, `reason` mentions `--yes`; the file still exists.
  - `pre_image_remove_refuses_when_absent`: `db pre-image remove --yes` on a fresh DB → exit 1, `status:"refused"`, reason names the path; nothing deleted.
  - `pre_image_remove_refuses_when_user_version_is_below_1`: open the temp DB with sqlx in the test, `PRAGMA user_version = 0`, create the pre-image file → `remove --yes` → exit 1, `status:"refused"`, reason contains `user_version`; file still exists.
  - `pre_image_remove_deletes_after_checks`: v1 DB + pre-image file → `remove --yes` → exit 0, `value: { action: "remove", path, bytes }`, file gone, DB untouched (`db check` still `ok`).
  - The `integrity_check != "ok"` refusal: implement it in the same pure predicate as the `user_version` check (`fn pre_image_remove_allowed(user_version: i64, integrity: &str, exists: bool, yes: bool) -> Result<(), String>` in `main.rs`) and unit-test that predicate for all four refusals in `main.rs`'s `#[cfg(test)]` — corrupting a real SQLite file in a spawned-binary test is not required; say so in the report.
- **Step 2 — run** → FAIL (paste). **Step 3 — implement:** `db stats [--json]` (`--json` accepted as a no-op for symmetry with `collect --json`; the output is already JSON) — extend the printed `value` with `tiers`, `snapshotJsonSampleCount`, `archive`, `disk` taken from `store.history_coverage()` (single source — do not recompute; keep `StoreStats` as is and compose a `DbStatsOutput` that flattens both). `db pre-image status` and `db pre-image remove [--yes]`: expose `pub fn pre_image_path(db_path: &Path) -> PathBuf` from `tinytop-store` (re-export from `migration.rs`; the canonical path logic must stay the ONE implementation), read `PRAGMA user_version` and `integrity_check` through the store, print the JSON envelopes above; on refusal print `{ "status": "refused", "reason": "…" }` to stdout and exit 1; delete ONLY that exact path with `std::fs::remove_file`, never a glob, never a directory. `print_help`: add the two lines and one example. **Step 4 — run** → PASS; workspace PASS. **Step 5 — docs:** `INSTALL.md` migration note (first start after upgrading to 0.3.0 takes a complete pre-image `<db>.pre-v0.sqlite` BEFORE touching a row — minutes on a large DB; refuses without free space ≥ 1.2 × the DB; the pre-image is never deleted automatically: `tinytop-agent db pre-image status|remove --yes`); `README.md` CLI table (`db stats --json`, `db pre-image status|remove --yes`) + one paragraph on the ladder pointing at `docs/sqlite-history-architecture.md`; `ARCHITECTURE.md` (one section: the four-tier ladder, fold/freeze/promote-before-prune, the pre-imaged migration, the read API — pointers, not duplication); `PROGRESS.md` (`## Current Version` → 0.2.11 with "0.3.0 = Phase 1 close pending"; under `## Completed` add `### 0.3.0 - Tiered history ladder, Phase 1 (T1–T6)` summarising T1–T6 with the per-version pointers 0.2.7 → 0.2.11 and this lane); `CHANGELOG.md`: your entries under a new `## Unreleased` heading at the top — do NOT rewrite the 0.2.7–0.2.11 sections (Fable retitles at the phase close and writes the 0.3.0 consolidation paragraph).
- **Acceptance:** `bun run check` green; paste a real `tinytop-agent db stats --json` sample from a temp DB (no port needed — this runs in your sandbox).

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` (the `--all` is required); if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only. `db pre-image remove` in tests targets temp files only.
- No new dependencies. No edits outside Task 6's Files list: `main.rs`, the new `tests/cli_db.rs`, `tinytop-store/src/lib.rs` + `migration.rs` ONLY for the `pub` re-export of `pre_image_path` (and a `user_version()` accessor if none exists — report it), `INSTALL.md`, `README.md`, `ARCHITECTURE.md`, `PROGRESS.md`, `CHANGELOG.md`. Do not touch `writer.rs`, `agent/assets/`, Bun's `src/`/`tests/`, `ladder.rs`, `maintenance.rs`, `disk.rs`, `retention_ladder.rs`.
- Every refusal names the check and the observed value (e.g. `user_version is 0; the migration has not run`).

Gate to run and paste in full: `bun run check` (from the repo root). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests bind a local port your sandbox denies (`PermissionDenied: should reserve a local port`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours. Do not ESCALATE on a sandbox-denied bind.

Report (your final message), in this order: files changed with line ranges · tests added (names) · the RED output before implementation · the GREEN output after · full gate output · the `db stats --json` sample · `git diff --stat` · docs updated · anything the plan was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
