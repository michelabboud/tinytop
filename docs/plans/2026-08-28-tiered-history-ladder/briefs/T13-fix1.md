You are hexe lane **T13-fix1** for tinytop — the fix round after the T13 lane (`tinytop/phase5-t13-schema-v2` at `ad22df8`, hexe run 654) and the luna blind review of it (run 655; F1 is the orchestrator's own finding, F2–F4 are hers, re-ranked and verified at source) (Fabulous `docs/fleet/tinytop/2026-08-29-ari-luna-t13-review.md`; plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` Task 13 + §4; ADR 0023). Base: `tinytop/phase5-t13-schema-v2` at `ad22df8` (`git rev-parse HEAD` first and quote it). Every `file:line` below was read at `ad22df8`. Build ONLY the items below; touch ONLY `agent/crates/tinytop-store/src/migration.rs`, `agent/crates/tinytop-store/tests/migration_v2.rs`, `agent/crates/tinytop-store/tests/settings_transfer.rs`, `agent/crates/tinytop-store/tests/process_history.rs`, `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` (§4 only), `docs/adr/0023-schema-v2-migration-one-transaction-no-pre-image.md` (an appended amendment line only — never rewrite an ADR sentence), `docs/sqlite-history-architecture.md` (`## Schema Versions And Migration` only) plus whatever a later F-item names explicitly. NOTHING in `Cargo.*`, `CHANGELOG.md`, `VERSION`, the spec, `tinytop-types`, `tinytop-collectors`. **Git is read-only for you** (no commit, no stash, no branch; the orchestrator commits); never run bare `cargo fmt` — `--check` only, fix by hand. If any line below contradicts the code you read, STOP and reply `ESCALATE: <the line, what the code says>` — do not invent a bridge.

**Read first:** `migration.rs:587-680` (`migrate_v1_to_v2` — the version check at `:592-595` BEFORE `pool.begin()` at `:598`; the one transaction through `PRAGMA user_version = 2` at `:670` and `commit` at `:673`), `:641-644` (the `DROP COLUMN` statement and its `.map_err(|_| sqlite_version_refusal(&linked_version))?`), `:682-709` (`require_sqlite_at_least` and `sqlite_version_refusal` — its text `schema migration requires SQLite ≥ 3.35.0 (linked: {version})` is asserted by `tests/migration_v2.rs:262-285` `sqlite_version_requirement_is_checked` and MUST NOT change); `tests/migration_v2.rs:15-58` (`TempDatabase::new(label)`, `.url`, `.raw_pool()`), `:61-76` (`process_sample_shape`), `:78-103` (`seed_v1_processes` — executes the REAL `CREATE_SCHEMA_V1_SQL` then inserts 5 rows / 3 commands), `:105-113` (`marker_count`), `:153-232` (`v1_fixture_with_three_commands_migrates_to_v2` — the assertion style to mirror); ADR 0023 `:22-29` (the decision this round finally puts under test) and `:52-53`, `:78-79`; plan `:71` (§4, the sentence being corrected).

**F1 — a `DROP COLUMN` failure inside the v1→v2 transaction keeps its real cause, and the rollback is TESTED (P2; the misattribution is the plan's wording — plan §4 `:71` — not the lane's).** Today `migration.rs:641-644` maps ANY error from `ALTER TABLE process_samples DROP COLUMN command` to `sqlite_version_refusal(&linked_version)`. But `:595` has already proven the linked version ≥ 3.35.0 before any write, so a failure here is by construction NOT a version problem — SQLite's own message names the cause (measured identically on SQLite 3.45.1 and 3.50.6 — one minor version below the bundled 3.51.3; quote what 3.51.3 actually says in your RED evidence: an index on the column ⇒ `error in index <name> after drop column: no such column: command`; a view ⇒ `error in view <name> after drop column: no such column: command`), and the current mapping throws it away and blames the version. Change `:641-644` to `.map_err(|error| StoreError::Migration { reason: format!("ALTER TABLE process_samples DROP COLUMN command failed inside the v1→v2 transaction: {error} (linked SQLite {linked_version})"), remedy: "remove the index, trigger or view that references process_samples.command and restart; the database was not modified".to_string() })?;` — the `?` inside the transaction still drops it (rollback), exactly as the NULL guard at `:622-634` does. `sqlite_version_refusal` stays for the pre-write check at `:698` only (text unchanged). **Test (the RED proof first, paste it):** add `tests/migration_v2.rs` `v1_fixture_with_an_index_on_command_refuses_and_leaves_the_file_untouched`: `TempDatabase::new("indexed-command")` → `seed_v1_processes(&fixture)` → on `fixture.raw_pool()` execute `CREATE INDEX idx_probe_command ON process_samples (command)` and close that pool → `SqliteHistoryStore::connect(&fixture.url).await.expect_err(…)` → the error is `StoreError::Migration { reason, remedy }` and `reason` contains BOTH `DROP COLUMN command failed` and `idx_probe_command` (on the current code this assertion is RED: the reason says `requires SQLite ≥ 3.35.0` — paste that RED line) → reopen `fixture.raw_pool()` and assert the file is untouched: `PRAGMA user_version` = 1; `process_sample_shape` still lists `command` and NO `command_id`; `SELECT count(*) FROM sqlite_master WHERE type='table' AND name IN ('process_commands','process_samples_fast')` = 0; `marker_count` = 0; `SELECT count(*) FROM process_samples` = 5 (the seeded rows). Then drop the probe index on the raw pool, connect again, and assert the migration now succeeds (`user_version` 2, `marker_count` 1) — proving the refusal was about the index, not the file. **Docs:** plan §4 `:71` → *"If `sqlite_version()` < 3.35 → refuse BEFORE any write with `schema migration requires SQLite ≥ 3.35.0 (linked: <version>)`. If `ALTER TABLE … DROP COLUMN` itself fails after that check (an index, trigger or view still references the column — none exist in the shipped schema) → the migration refuses with SQLite's own error text, the transaction rolls back and the file is untouched; never a silent copy-table fallback. On this fleet the linked library is 3.51.x (`libsqlite3-sys` bundled)."*; ADR 0023: append ONE line under Consequences after `:79`: *"Amendment 2026-08-29 (T13-fix1): the in-transaction rollback is now exercised by `v1_fixture_with_an_index_on_command_refuses_and_leaves_the_file_untouched` (a probe index makes `DROP COLUMN` fail with SQLite's own error, which is reported verbatim; the version refusal stays a pre-write check)."*; `docs/sqlite-history-architecture.md` `## Schema Versions And Migration`: the sentence describing the refusal gains the same distinction (version refusal before any write; in-transaction failures reported with SQLite's message, file untouched).

**F2 — the growing-horizon dry-run test proves WHY the count is zero (P3; luna run 655 P1.2, re-ranked).** `tests/settings_transfer.rs:270-309` (fixture label `fast-process-horizon`): the only fast row is `now_ms − 120 × MINUTE_MS`; shrinking to 1 h counts 1 (keep), then `put_settings(1 h)` and the growing plan (back to the 24 h default) asserts 0 — true only because a 2 h-old row is not older than 24 h; no maintenance ever ran, so the brief's "already pruned ⇒ 0" is not what this test proves. Extend the SAME test after the shrink assertion: insert a second fast row at `now_ms − 30 × 60 × MINUTE_MS` (same `command_id`, a different `captured_at_ms`/`rank`) → with the 1 h horizon saved and NO maintenance run, the growing plan must count **1** (the 30 h row is older than the 24 h candidate — the count is unconditional, `settings_transfer.rs` `process_fast_rows` line) → then run ONE maintenance pass at the saved 1 h horizon, exactly the way `tests/ladder_maintenance.rs` `maintenance_reports_fast_and_detail_prunes` runs it (same entry point, `now_ms` as the clock; read that test first and mirror its call) → assert the pass reports `process_fast_rows == 2` and the growing plan now counts **0** (already pruned). Keep every existing assertion and the 0.5.0-document part of the test byte-identical. RED first: the intermediate `1` is what the current test never asserts — paste the run that shows the new assertions failing/passing before and after your edit.

**F3 — the interning test asserts the SAME `command_id` in both tables (P3; luna P1.3, re-ranked).** `tests/process_history.rs:59-100` `fast_rows_every_tick_and_minute_rows_every_interval`: the check at `:92-98` (`SELECT DISTINCT command_id FROM (… UNION ALL …) WHERE …` ⇒ `len() == 1`) is satisfied by `UNIQUE(command)` alone and cannot see a minute row bound to a different command's id. Replace it with per-table reads for the capture and rank the test already knows: `SELECT command_id FROM process_samples_fast WHERE captured_at_ms = ? AND rank = ?` and `SELECT command_id FROM process_samples WHERE captured_at_ms = ? AND rank = ?` — assert the two are equal AND equal to `SELECT command_id FROM process_commands WHERE command = ?` for that process's command text (bind the text from the same `snapshot(...)` fixture the test inserts, `:248`). Keep the `COUNT(*) FROM process_commands == 2` assertion. RED: temporarily reading the wrong table in your head is not evidence — paste the new assertion's output.

**F4 — the source-selection test can tell the two tables apart (P2; luna P1.4, re-ranked).** `tests/process_history.rs:103-150` `read_history_processes_picks_fast_inside_the_keep_window_and_minute_outside`: one `insert_snapshot` at `now − 10 min` writes the SAME capture into both tables, and `source` is decided BEFORE the SQL runs (`lib.rs:1616-1628`), so a swapped table `const` (`:1607-1608`) passes every current assertion. After the `insert_snapshot`, insert ONE extra capture directly into `process_samples_fast` only (raw SQL on `fixture.pool()`: `captured_at_ms = now_ms − 5 × 60_000`, `rank 0`, `pid 424242`, the `command_id` of the fixture's first command looked up from `process_commands`, `gpu_percent NULL`) — a row the minute table does not have. Then assert: the fast read (`since now − 30 min`) returns **2** captures and one of them holds `pid 424242`; the minute read (`since now − 2 h`) returns **1** capture with NO `pid 424242`; the open read (`since None`) returns **1** capture with NO `pid 424242`; and on every returned process `command` equals the fixture's command text (the `JOIN process_commands`). RED first: swap nothing — the new `2`/`424242` assertions are the proof; paste them.


**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-store --test migration_v2` (5 → 6 passed, 1 ignored — do NOT run the ignored real-file test), `--test process_history` (3 passed, strengthened), `--test settings_transfer` (20 passed, one extended) and `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` green; `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green (paste every `test result:` line); `git diff --stat` ⊆ the allow-list above.

**Report shape:** for each F: the RED evidence you observed before the change (the exact failing assertion / message), the GREEN after; then `git rev-parse HEAD`, `git status --short`, `git diff --stat`, every `test result:` line, the fmt/clippy lines; anything outside this brief you noticed, under `## Outside this lane` — never fixed silently.
