You are hexe lane **T15b** for tinytop — the dashboard half of Phase 5 Task 15 (the store/collector half is lane T15, running in parallel on its own branch; you never touch its files and you cannot run its daemon). Plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` Task 15 **including its two amendment bullets (2026-08-30)** — the dashboard sentences and amendment (g)/(h)/(k) — and §5; **ADR 0025** decisions 3 and 6 (read the whole ADR once). Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is v0.5.3 `b5e9186` + this brief's commit). Every `file:line` below was read at `b5e9186`; run `sed -n` / `grep -n` on each before you build on it. Build ONLY the findings below. Files you may edit: `agent/assets/dashboard/index.html`, `agent/assets/dashboard/app.js`, `agent/assets/dashboard/ladder-rules.js`, `agent/assets/dashboard/styles.css`, `tests/dashboard-gpu.test.ts` (NEW), `tests/dashboard-ladder.test.ts`, `tests/dashboard-process-filesystem.test.ts`, `GUIDE.md`. NOTHING else — in particular no `agent/crates/**`, `agent/Cargo.*`, `README.md`, `docs/**`, `INSTALL.md`, `ARCHITECTURE.md`, `src/`, `legacy/`, `VERSION`, `CHANGELOG.md` (Fable writes the line at merge), `tests/server.test.ts`, `tests/fixtures.ts`, `vendor/`. **Git is read-only for you — no commit, no branch, no stash; the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`. **Known sandbox limits — not failures:** `tests/server.test.ts` binds a port your sandbox denies — run `bun test` on the other files (`bun test $(ls tests/*.test.ts | grep -v server.test.ts)`); there is no Rust daemon to point a browser at — the dashboard tests are string-contract tests on the HTML/JS plus unit tests on the pure `ladder-rules.js` exports, exactly as `tests/dashboard-process-filesystem.test.ts:1-45` and `tests/dashboard-ladder.test.ts:758-870` do today; **the Bun runtime never sends `gpus` — the dashboard must hide everything GPU on Bun by absence, never by a runtime check.** If a contract below contradicts the code you read, or you would be guessing, STOP and reply `ESCALATE: <the line, what the code says, what you tried>`.

**The wire shape you render (produced by lane T15 — treat it as the contract):** `/api/snapshot` and every assembled `/api/history` sample carry `gpus: [{ id, vendor, name, driver, busyPercent?, memoryUsedBytes?, memoryTotalBytes?, temperatureC? }]` — the key is ABSENT when no adapter is detected (Bun, WSL2, a box without a GPU) and every metric key is absent when unknown (`busyPercent` on the first tick, on NVIDIA-proprietary adapters, where the kernel exposes no source); `processes[].gpuPercent` is a number or absent; the import dry-run's `wouldDelete` gains `gpuSampleRows` (absent from a 0.5.3 daemon — treat absent as 0, never as invalid). `vendor` is `amd` | `nvidia` | `intel` | `apple` | `microsoft` | a `0x1a03`-style id.

**Read first:** `index.html:121-178` (the four-gauge `.overview-grid` — the GPU panel goes AFTER this section as its own `section.panel`, never as a fifth gauge; `data-section="overview"` so the existing section toggle governs it), `:269-291` (`.metric-band` — a compact strip whose look you borrow), `:335-372` (the process table: `<th>` row at `:360-365`, `data-process-sort` buttons, `#process-rows`), the process detail dialog (`grep -n 'process-detail-' index.html` — "Parent PID" and "Started" rows); `app.js:67` (`PROCESS_SORT_KEYS`), `:297-346` (`elements` — `loadPanel`, `processCount`, `processRows`), `:423-454` (section elements — `data-section="overview"` is collected automatically at `:450`), `:779` (`setHidden`-style helper: `if (node) node.hidden = hidden;`), `:1961-1985` (`sortProcesses` — the nested ternaries you extend with `gpu`), `:2093-2137` (`renderProcesses` — `cell.colSpan = 6` at `:2102`, the per-row cells at `:2111-2135`), `:2319-2364` (`renderSnapshotDetails` — called for LIVE and for HISTORY points alike; `renderProcesses(snapshot.processes)` at `:2363` — `renderGpus(snapshot.gpus)` goes beside it), `:2874`, `:3636` (the two other `PROCESS_SORT_KEYS` sites), `renderProcessDetail` (`grep -n 'function renderProcessDetail'`); `ladder-rules.js:12-18` (`WOULD_DELETE_FIELDS`), `:97` (`formatCount` — the `—` convention for null), `:395-409` (`isValidImportPlan` — read what it requires of each field), `:410-460` (`describeImportPlan` — `addCount("processFastRows", "fast process rows", " deleted")` at `:431` is the line you copy); `styles.css:749-790` (`.overview-grid` `repeat(4, …)` — unchanged; `.gauge-panel`; the accent classes), `:1161` (`.mini-stat`); `tests/dashboard-process-filesystem.test.ts:1-45` (the string-contract shape), `tests/dashboard-ladder.test.ts:758-870` (the `describeImportPlan` tests — the `wouldDelete: {}` and partial-object cases), `GUIDE.md:40-62` (Live Status), `:187` (browser-local process controls), `:197-206` (Reading The Numbers).

**F1 — markup: the GPU panel and the GPU column (`index.html`, `styles.css`; ADR 0025 decision 6, amendment (k)).** After the `.overview-grid` section (`:178`): `<section class="panel gpu-panel" id="gpu-panel" data-section="overview" aria-label="GPU" hidden>` with a `.section-heading` (`<h2>GPU</h2>`, `<p>Detected adapters, sampled every poll.</p>`, `<span class="mini-stat" id="gpu-count">0 adapters</span>`) and `<ul class="gpu-adapters" id="gpu-adapters"></ul>`; each rendered `<li class="gpu-adapter">` holds `.gpu-name` (the adapter `name`, `title` = `id`), `.gpu-meta` (`vendor · driver`), `.gpu-busy` (`<strong>` `NN.N%` or `—`), `.gpu-memory` (`used / total` via the existing `formatBytes`, or `—`), `.gpu-temp` (`NN °C`, omitted entirely when absent) — `hidden` on the section whenever `gpus` is absent or empty. Process table: `<th scope="col" class="gpu-cell"><button type="button" data-process-sort="gpu">GPU</button></th>` between RSS and Details; the column's `th`/`td` carry `class="gpu-cell"` and are shown only when the table has `data-has-gpu="true"` (CSS: `.process-panel table:not([data-has-gpu="true"]) .gpu-cell { display: none; }`); the empty-state `colSpan` follows the visible column count (7 with GPU, 6 without). The process detail dialog gains a `GPU` row in the "Parent PID"/"Started" style, rendered `—` when the process has no `gpuPercent`. `styles.css`: `.gpu-panel`, `.gpu-adapters` (a responsive grid `repeat(auto-fit, minmax(240px, 1fr))` like `:966-971`), `.gpu-adapter` (the `.metric-band` look), the `.gpu-cell` rule — theme-aware through the existing custom properties; no new colours beyond the palette already in the file.

**F2 — behaviour (`app.js`).** `elements.gpuPanel`, `gpuCount`, `gpuAdapters`, `processTable` (the `<table>` in `#processes`) at `:297-346`; `function renderGpus(gpus)`: `const list = Array.isArray(gpus) ? gpus : [];` → `elements.gpuPanel.hidden = list.length === 0`; `#gpu-count` = `${n} adapter(s)`; rebuild the `<li>` rows with `replaceChildren` (the `:2109` pattern); numbers through the pure helpers of F3. Called from `renderSnapshotDetails` next to `renderProcesses` (`:2363`) — so a history point 30 minutes back renders the adapters it stored, and a Bun/WSL2 snapshot hides the panel. `renderProcesses`: set `elements.processTable.dataset.hasGpu = String(gpuColumnVisible(processes))`; each row gains a `td.gpu-cell` with `formatGpuPercent(process.gpuPercent)`; `colSpan` = 7 when the column is visible else 6. `PROCESS_SORT_KEYS` gains `"gpu"`; `sortProcesses` sorts `gpu` by `gpuPercentSortValue(process)` (F3: a missing value sorts BELOW every number in either direction — `-1`); the stored-sort restore at `:2874` and the click handler at `:3636` need no change beyond the set (verify and say so). `renderProcessDetail`: the GPU row. Nothing polls anything new; no `runtime === "rust"` check anywhere — absence hides.

**F3 — pure helpers and the dry-run line (`ladder-rules.js`) — RED first.** Export `formatGpuPercent(value)` (`finite number → "12.5%"` with one decimal, else `"—"`), `formatGpuMemory(usedBytes, totalBytes)` (`both finite → "<used> / <total>"` through the module's `formatCoverageBytes`; only `used` → `"<used>"`; else `"—"`), `formatGpuTemperature(value)` (`finite → "44 °C"` rounded to the integer, else `""`), `describeGpuAdapter(adapter)` (`{ name, meta: "amd · amdgpu", busy, memory, temperature }` — the strings above; `name` falls back to `id` when empty), `gpuColumnVisible(processes)` (`true` iff any element has a finite `gpuPercent`), `gpuPercentSortValue(process)` (`finite → the number, else -1`). `WOULD_DELETE_FIELDS` gains `"gpuSampleRows"` and `describeImportPlan` gains `addCount("gpuSampleRows", "GPU rows", " deleted")` right after the fast-process-rows line — inside the ladder block so `ladderDescribed` stays true when only GPU rows are affected; **`isValidImportPlan` must accept a plan WITHOUT `gpuSampleRows`** (read `:395-409`: if it requires every field, make the absent-field case pass as 0 for this field — a 0.5.3 daemon behind a 0.5.4 page must still import; paste what it did before). Tests (`tests/dashboard-gpu.test.ts`, NEW; the `dashboard-ladder.test.ts` additions beside `:758-870`): every helper's happy and empty path (`formatGpuPercent(undefined) === "—"`, `formatGpuMemory(6000640, 2147483648) === "5.7 MiB / 2.0 GiB"` — derive the exact strings from `formatCoverageBytes`'s rounding rule and assert them, `formatGpuTemperature(44) === "44 °C"`, `formatGpuTemperature(null) === ""`, `describeGpuAdapter` on trashcan's adapter `{ id: "pci-0000:02:00.0", vendor: "amd", name: "0x1002:0x6810", driver: "amdgpu", memoryUsedBytes: 6000640, memoryTotalBytes: 2147483648, temperatureC: 44 }` (no `busyPercent` → `busy` `"—"`), `gpuColumnVisible([])`, `gpuColumnVisible([{ gpuPercent: 0 }])` (true — zero is a value), `gpuPercentSortValue({})` → `-1`); `describeImportPlan({ wouldDelete: { gpuSampleRows: 1200 }, changedKeys: ["retentionLadder"] }, …)` → `["1,200 GPU rows deleted"]` and no "no stored history is affected" line; `isValidImportPlan` with and without the field; the string-contract test (`index.html` contains `id="gpu-panel"`, `id="gpu-adapters"`, `id="gpu-count"`, `data-process-sort="gpu"`, `class="gpu-cell"`; `app.js` contains `function renderGpus`, `gpuPanel: document.querySelector("#gpu-panel")`, `"gpu"` inside the `PROCESS_SORT_KEYS` line, `renderGpus(snapshot.gpus)`; `styles.css` contains `.gpu-panel` and the `data-has-gpu` rule); `dashboard-process-filesystem.test.ts:11-14` gains `data-process-sort="gpu"`.

**F4 — `GUIDE.md` (yours; README/API/ARCHITECTURE are lane T15's).** `## Live Status` (`:40-62`): a paragraph on the GPU panel — appears only when the daemon detects an adapter; per adapter name, busy % (the busiest engine, Task Manager's rule; `—` on the first sample, on NVIDIA-proprietary adapters and where the kernel exposes no source — on Linux the value is computed over the processes the daemon's user can see), memory used/total when the driver reports VRAM, temperature when the GPU node has a sensor; the Bun runtime and WSL2 never show it. `:187` (browser-local controls): the `GPU` sort. `## Reading The Numbers` (`:197-206`): the busy definition in one line and "per-process GPU % is shown only when at least one row has a value". `## Settings`/import: the dry-run may list `GPU rows deleted` when the L1 horizon shrinks. Nothing else in `GUIDE.md` changes.

**Gate before you report:** `bun test $(ls tests/*.test.ts | grep -v server.test.ts)` green (paste the totals; 0.5.3 counted 197 across 19 files — expect one new file), `bun run src/server.ts --check` and `bun run legacy/bun-collector.ts --check` clean (the two halves of `check:bun` that the sandbox allows), `git status --short` and `git diff --stat` pasted (⊆ the allow-list). No Rust.

**Report shape:** per finding the RED line(s) you observed → the GREEN summary; the test names; the base commit; what `isValidImportPlan` required before F3; the exact strings your helpers produce for trashcan's adapter; anything outside this brief you noticed under `## Outside this lane` — never fixed silently. The orchestrator's own acceptance (outside your sandbox): after both lanes merge, a Playwright pass on a temp `:4290` daemon — on this WSL2 box the panel and the column stay hidden on the live point and on a point 30 minutes back; through an SSH tunnel to a trashcan daemon the panel shows two `amdgpu` adapters with memory and temperature, the GPU column appears once a process carries a value, sorting by GPU works, and the detail dialog shows the row.
