You are hexe lane **T14b-fix1** for tinytop — the fix round after luna's blind review of lane T14b (dashboard half of Task 14; ADR 0024 decisions 5, 6, 8). Base: branch `tinytop/phase5-t14b-dashboard` at **`c065f91`** (your worktree was created from it; `git rev-parse HEAD` first and quote it). Every `file:line` below was read at `c065f91`; `sed -n` each before you build on it. Build ONLY F1–F3. Files you may edit: `agent/assets/dashboard/app.js`, `tests/dashboard-ladder.test.ts`. NOTHING else — no `ladder-rules.js` (it is already right), no `index.html`, no `GUIDE.md`, no Rust, no `src/`, no `legacy/`, no `package.json`/`bun.lock`, no `VERSION`, no `CHANGELOG.md` (Fable writes the line at merge), no docs. **Git is read-only for you — no commit, no branch, no stash; the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/` or `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`. **Known sandbox limits — not failures:** `tests/server.test.ts` binds a port your sandbox denies (run the two dashboard files explicitly, then the full `bun test` and report the one known failure); `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. If a contract below contradicts the code you read, STOP and reply `ESCALATE: <the line, what the code says, what you tried>`.

**Why this round exists (the finding, verified by the orchestrator on a live 0.3.1 dashboard):** `renderSelectedSample` (`app.js:1710-1722`) renders detail only when `sample.source === "raw"` (`:1716`, since T5-fix1 `9770dbf`). The raw-window FETCH path now stamps `source` through `normalizeHistorySamples` (`ladder-rules.js:66-85`, T14b F1), but the LIVE POLL path does not: `renderSnapshot` (`:2364-2368`) is `pushHistory(snapshot)` → `renderSelectedSample()` → `redrawCharts()`, and `pushHistory` (`:940-956`) stores `{ capturedAt, snapshot }` with NO `source` on both branches (`:945` replace, `:951` push). `activeHistoryIndex` (`:1595-1598`) selects the NEWEST sample when nothing is pinned, so on every poll the selected sample is the unstamped live one and `:1716` returns before `renderSnapshotDetails` (`:2317`, its only caller is `:1720`). Observed on the live page: HOST/KERNEL/DISTRO/UPTIME `-`, CPU `- cores`, 0 process rows, 0 filesystem rows, 0 pressure items, threads `-`, while the status strip and the chart keep updating. This is a regression from 0.3.1 through 0.5.2; the T14b brief named only the fetch path (a brief gap, the orchestrator's).

**F1 — the live poll entry is built by the ONE constructor and carries `source: "raw"` (P0; T14b brief F1's goal "dashboard replay renders again"; plan Task 14 amendment (g)).** In `pushHistory` (`app.js:940-956`) build the entry through the normalizer instead of two hand-written literals:
```js
function pushHistory(snapshot, capturedAtMs = snapshotCapturedAtMs(snapshot)) {
  const [entry] = normalizeHistorySamples([{ capturedAtMs, snapshot }], "raw");
  if (!entry) return;
  const existingIndex = state.snapshots.findIndex((sample) => sample.capturedAt === entry.capturedAt);
  if (existingIndex !== -1) {
    state.snapshots[existingIndex] = entry;
    …(unchanged sort / rebuildHistoryValues / return)
  }
  state.snapshots.push(entry);
  …(unchanged sort / rebuildHistoryValues / trimHistory)
}
```
`normalizeHistorySamples` is already imported at `app.js:15`. The timestamp fallback is unchanged in effect: the normalizer takes a finite `capturedAtMs`, else `Date.parse(snapshot.timestamp)`, else `Date.now()` — exactly what `capturedAt` at `:941` computed through `snapshotCapturedAtMs` (`:891-894`). A live sample IS a raw sample (full processes, filesystems, pressure), so `"raw"` is the honest stamp; the entry shape `{ capturedAt, snapshot, source }` now has ONE constructor for both the fetch and the live path (luna's ruling (b), accepted). `renderSelectedSample` stays byte-identical. RED first is not possible in `bun test` for `app.js` (browser code); the RED is the orchestrator's live-page probe quoted above — say so in your report rather than inventing one.

**F2 — the gauge's absent branch routes through the helper (luna P2, re-ranked P3 by the orchestrator: behaviour identical, exactness of T14b brief F2 "route both through it").** `app.js:2227` reads `formatted: pressureMax === null ? "—" : \`${formatPressureValue(pressureMax)} avg10\``. Mirror the pressure-list shape at `:1947-1948`: compute `const formattedPressure = formatPressureValue(pressureMax);` once above the `candidates` array (or inline) and use `formatted: formattedPressure === "—" ? formattedPressure : \`${formattedPressure} avg10\``. No literal `"—"` remains at that site (`grep -n '"—"' agent/assets/dashboard/app.js` — paste it; the only hits must be pre-existing sites unrelated to pressure, or none).

**F3 — pin the live-entry contract in the normalizer test (luna P2 test gap, the honest unit-level half; the DOM half is the orchestrator's Playwright acceptance).** In `tests/dashboard-ladder.test.ts` next to the normalizer test at `:25-44`, add `test("builds a single live sample as a raw entry the way pushHistory does", …)`: `normalizeHistorySamples([{ capturedAtMs: 1_000, snapshot }], "raw")` equals `[{ capturedAt: 1_000, snapshot, source: "raw" }]`; a second input `{ snapshot: { ...snapshot, timestamp: "2026-08-30T00:00:00.000Z" } }` with NO `capturedAtMs` yields `capturedAt === Date.parse("2026-08-30T00:00:00.000Z")` and `source: "raw"`; an input without `snapshot` yields `[]`. This test passes immediately (the normalizer already behaves) — it is a pin, say so; do not fake a RED.

**Gate before you report:** `bun test tests/dashboard-ladder.test.ts tests/dashboard-settings.test.ts` green (paste the summary; the T14b lane's run counted 92 across 2 files — expect 93); the full `bun test` summary (196 across 19 files at `c065f91` outside the sandbox; inside yours, one known `server.test.ts` port failure); `bun run src/server.ts --check` clean; `git status --short` and `git diff --stat` ⊆ {`agent/assets/dashboard/app.js`, `tests/dashboard-ladder.test.ts`}; paste `sed -n '940,958p' agent/assets/dashboard/app.js` after the change and `grep -n 'source' agent/assets/dashboard/app.js` (every `source:` write must now be a normalizer or the rollup path at `~:1000`).

**Report shape:** per finding what you changed (`file:line` before → after), the new test name, the base commit, the gate output; anything outside this brief under `## Outside this lane` — never fixed silently.
