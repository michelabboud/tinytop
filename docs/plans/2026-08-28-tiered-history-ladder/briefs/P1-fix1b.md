You are hexe lane **P1-fix1b** for tinytop: ONE fix on top of lane P1-fix1 (hexe run 570, validated by Fable at source). Your branch starts from `tinytop/phase1-fix1` @ `5bc41b0`. Do ONLY F13 below. Lane P1-fix2 (dashboard + docs) is already merged on `main` — do not touch `agent/assets/`, any `*.md` except the one CHANGELOG line named below, `docs/`, Bun files, `Cargo.lock`, or `VERSION`. Git is READ-ONLY for you: no commit, tag, push, branch, stash or reset — leave the worktree dirty and report. You are on the lowest model expected to handle this; if the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`.

Read first: `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §9 (maintenance: frozen buckets are never rebuilt; a late write folds INTO its ancestors), then the code at the lines below (numbers = `5bc41b0`).

**F13 — a replayed timestamp older than the L2 horizon must not be merged into a retained L3/L4 bucket twice (P2, found by Fable validating F11).**

The mechanism, at source:
- `agent/crates/tinytop-store/src/lib.rs:728` computes `raw_row_existed` BEFORE the raw insert (`INSERT … ON CONFLICT(captured_at_ms) DO UPDATE`, so a replay updates the row and never fails).
- `lib.rs:830-851`: when the L2 minute bucket is absent, or present but NOT partial (`ladder::raw_is_partial(existing, raw_now)` is `existing > raw_now`), the `else` branch at `:850` rebuilds L2 from raw and sets `l2_changed = true` UNCONDITIONALLY; `:853-861` then calls `maintenance::refold_ancestors_for_late_write(self, captured_at_ms, now_ms(), &settings.retention_ladder, &new_sample)`.
- `agent/crates/tinytop-store/src/maintenance.rs:356-385` `refold_one`: `source_holds_whole_range` (`:370`) is false for a target bucket older than the source tier's horizon, so it takes the MERGE path (`:373-380`): retained target bucket + `new_sample` → fold → upsert. That is F11's fix and it is correct for the FIRST write of a new timestamp.
- The hole: the SECOND insert of the same `captured_at_ms` (a replay — e.g. the Bun collector's `fetchWriterWithRetry` re-posting after a timeout) finds the raw row present, the L2 minute rebuilt with count 1 versus raw count 1 → `raw_is_partial(1, 1)` is false → `:850` rebuild → `l2_changed = true` → merge path again → the retained L3 bucket goes 121 → 122 and L4 201 → 202, and +1 on every further replay. The existing replay test `tests/ladder_maintenance.rs:566` (`replay_of_a_represented_pruned_timestamp_is_counted_only_once`) covers only the partial-L2-bucket path within the L2 horizon (`:833` guard); it does not reach the merge path.

The rule to implement: **the merge path runs only for a genuinely new raw row.** Thread `raw_row_existed` from `lib.rs:728` into `refold_ancestors_for_late_write` and `refold_one` as a `new_raw_row: bool` parameter (or an equivalent small enum — name it for what it means, not `flag`). In `refold_one`, when `!source_holds_whole_range && !new_raw_row` → return `Ok(())` without reading or writing the target: the retained bucket already counts that timestamp, and there is nothing to add. Keep the within-horizon fold-from-source path exactly as it is, replay or not — it is idempotent, and a replay carrying corrected values legitimately refreshes the ancestors from the rebuilt L2 source. Do not "fix" this by skipping the whole refold when the row existed, and do not change `raw_is_partial`, the `:833` guard, `rebuild_rollup_bucket`, or the L2 upsert logic.

Tests — failing first, paste RED, implement, paste GREEN (in `agent/crates/tinytop-store/tests/ladder_maintenance.rs`, modelled on `late_write_merges_retained_l3_when_l2_source_is_pruned` at `:442` and its L4 twin `late_write_merges_retained_l4_when_l2_and_l3_sources_are_pruned`; consts `MINUTE_MS`/`DAY_MS` at `:18-19`, `snapshot()` at `:978`):
1. `second_replay_past_l2_horizon_does_not_double_count_retained_l3`: same setup as `:442`; after the first late insert asserts `sample_count == 121`, insert the SAME `captured_at_ms` with the same snapshot again → assert L3 `sample_count == 121`; a third insert → still `121`. Also assert the L3 bucket's `cpu.avg` is unchanged by the replays.
2. `second_replay_past_l3_horizon_does_not_double_count_retained_l4`: same for the L4 twin — `201` after the first insert, `201` after two replays.
3. Confirm by running (not by editing) that `late_write_refolds_ancestors`, the two F11 tests and `replay_of_a_represented_pruned_timestamp_is_counted_only_once` still pass together with the new ones: `cargo test --manifest-path agent/Cargo.toml -p tinytop-store --test ladder_maintenance`.
**If the RED run does not fail on the un-fixed code (`122`/`202` expected as RED evidence), STOP and report `ESCALATE:` with the exact output — a test that cannot fail is not evidence.** [[failure-path-test-must-keep-the-failure]]

CHANGELOG: append ONE bullet under `## Unreleased` (after the existing F11 bullet): "Replaying an already-counted timestamp older than the L2 horizon no longer merges it into a retained L3/L4 bucket again; only a genuinely new raw row takes the merge path."

Gate to run and paste in full: `bun run check` (repo root). Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures: (1) the 10 `serve_contract` port binds (`PermissionDenied`), (2) Bun's `tests/server.test.ts` `Bun.serve({ port: 0 })` bind, (3) `tests/tinytop-script.test.ts` needing `XDG_RUNTIME_DIR=/tmp/<random>`. Also run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` (never bare `cargo fmt`) and `git diff --check`.

Report (your final message IS the report): files changed with line ranges; the RED and GREEN output for both tests verbatim; the full gate; `git diff --stat`. Files you may change: `agent/crates/tinytop-store/src/lib.rs`, `agent/crates/tinytop-store/src/maintenance.rs`, `agent/crates/tinytop-store/tests/ladder_maintenance.rs`, `CHANGELOG.md` (one line). Nothing else.
