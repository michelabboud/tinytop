**CONTINUATION — you are lane `tinytop-ladder-t11b` (fresh session) working INSIDE the worktree run 625 left behind.** Run 625 (`ari-sol-deep`) escalated correctly at 11:35Z because F1's feature list forbade what F3 requires (`ManualReader` is behind `experimental_metrics_custom_reader`); F1 is now amended. The orchestrator committed its partial work on this branch as a WIP commit so nothing is lost — `git log --oneline -3` shows it. **Verify what exists before writing anything; do not redo it:** vetted pins in `agent/Cargo.toml` (+ `tinytop-agent/Cargo.toml`, `Cargo.lock` +93 packages), `docs/reports/2026-08-29-dependency-vetting-opentelemetry.md` (update it: the SDK feature gate ruling above; re-measure the release-binary size after your build; keep the honest "no crates.io in this sandbox" note), `tinytop-store/src/otel_settings.rs` + the `otel` field + `from_document(document, persisted: Option<&DashboardSettings>)` with 8 callers updated (run 625 reported store tests GREEN: 4/4 OTel settings, 17/17 settings_transfer), docs edits in README/GUIDE/INSTALL/ARCHITECTURE/CHANGELOG/`docs/sqlite-history-architecture.md`, and dashboard edits in `index.html`/`app.js`/`ladder-rules.js`/`styles.css` + the two Bun test files that run 625 called **interrupted and unverified** — run `bun test` first and finish/fix them against F6/F7. NOT done: `tinytop-agent/src/otel.rs` (F3/F4), `writer.rs` (`AppState.otel` + `latest_snapshot` watch + `spawn_otel_export_loop` + coverage merge + tests, F3/F5), `main.rs` (`mod otel`, `db stats` block, F5), the F7 exporter tests, the gates. Add `experimental_metrics_custom_reader` to the SDK's features in `agent/Cargo.toml` as the first step. Everything else in this brief stands.

You are hexe lane **T11** for tinytop (Phase 4; Phase 3 shipped as `v0.4.1` = `f8b8b43`, your code base — the commit on top of it is docs only: this brief, ADR 0015's status, the plan's Task 11 amendment, spec §12 sentences). Execute ONLY **"Task 11: OTLP metrics push exporter"** from `docs/plans/2026-08-28-tiered-history-ladder-plan.md` (`### Task 11`, including its 2026-08-29 amendment). Scope rules: do not touch `maintenance.rs`, `archive.rs`, `disk.rs`, `migration.rs`, `ladder.rs`, `retention_ladder.rs`, `settings_transfer.rs`, `tinytop-collectors`, `tinytop-types`, Bun's `src/`, `legacy/`, `vendor/`, `styles.css` (unless F6 names a rule). **This lane MAY change `agent/Cargo.toml`, `agent/crates/tinytop-agent/Cargo.toml` and `agent/Cargo.lock` — only for the crates F1 names.** Git is read-only for you: never `add`/`commit`/`checkout`/`stash`; the orchestrator commits. Escalate (`ESCALATE: <what is beyond you, what you tried>`) instead of improvising around anything this brief does not cover.

**What already exists at `f8b8b43` — verify each before writing anything, and do not rebuild what exists:**
- `tinytop-store/src/lib.rs`: `DashboardSettings` (`:49` — fields `default_theme … retention_ladder (serde default = RetentionLadder::default_for_serde) … enabled_sections`), `from_document(document, persisted_ladder)` (`:147` — **the ONE decoder**; an absent `retentionLadder` key keeps the persisted ladder — you extend this rule to `otel`, F2), `normalize_legacy_mirrors` (`:174`), `changed_keys` (`:179`), `validate` (`:214`; helpers `validate_one_of` `:2478`, `validate_range` `:2488` — read their exact message shapes and reuse them), `HistoryCoverage` (`:312`; sub-blocks `disk: HistoryDiskCoverage` `:348`, `archive: HistoryArchiveCoverage` `:358`), `history_coverage(&settings)` (`:1583`), `put_settings` (`BEGIN IMMEDIATE` read-validate-write), `record_event`, `HistoryMarkerType::SettingsChange`, `StoreError::Validation`.
- `tinytop-store/src/retention_ladder.rs`: the settings-block pattern to copy — `pub struct RetentionLadder` (`:13`, `#[serde(rename_all = "camelCase")]`, `Default` `:66`, `default_for_serde` `:200`, `validate(disk_pressure, previous)` `:204`).
- `tinytop-store/src/settings_transfer.rs`: `export_document`, `plan_import` (its warnings walk compares the input against `serde_json::to_value(&candidate)` — a new serialised `otel` block therefore produces NO spurious warning; assert it), the secret-shaped-key walk in `tests/settings_transfer.rs::export_document_carries_the_envelope_and_no_secret_shaped_key` (word list `secret, token, password, passwd, apikey, api_key, authorization, bearer, credential` — `headersEnvVar` is an env-var NAME and must stay green).
- `tinytop-agent/src/writer.rs`: `AppState` (`:51` — `collector: Arc<Mutex<NativeCollector>>`, `store`, `dashboard_assets`, `daemon`, `embed_frame_ancestors`; derives `Clone`), `serve` (`:220` — spawns `_collection_task`/`_cold_export_task`/`_disk_check_task` at `:244-246`), `router` (`:263`), `import_settings` (`:337`), `update_settings` (`:361`; PUT marker `{"changed": …}`), `history_coverage` handler (`:405` — `get_settings` → `store.history_coverage(&settings)` → JSON), `collect_and_store` (`:601` — `collector.lock().collect()` → `insert_snapshot` → `get_settings` → `maintain_history`), `spawn_collection_loop` (`:643`), `spawn_disk_check_loop` (`:656` — **the loop pattern**: re-read settings every iteration, clamp defensively, `eprintln!` on errors, never panic), `now_ms` (`:1103`), `ServeError`, `no_store`, the `#[cfg(test)]` module (`test_state`, `request_json` via `tower::ServiceExt::oneshot`, `coverage_reports_every_tier_and_json_horizon` `:1793`, the `serve_`-prefixed tests that bind ports).
- `tinytop-agent/src/main.rs`: `DbStatsOutput` (`:77` — `#[serde(flatten)] stats`, `tiers`, `snapshot_json_sample_count`, `archive`, `disk`), the `db stats` arm, `print_help`, `Refused`. **`tinytop-agent` has no `[lib]` target** (verify: `grep -n "\[lib\]" crates/tinytop-agent/Cargo.toml`) — integration tests in `tests/` can only drive the binary; unit tests for new modules live in their `#[cfg(test)]` blocks (the plan's `tests/otel_export.rs` is therefore NOT created; F7 says where each test lives).
- `tinytop-types/src/lib.rs`: `SystemSnapshot` (`:174` — `identity: IdentitySnapshot { hostname … }`, `cpu: CpuSnapshot { usage_percent, cores, times }` `:81`, `memory: MemorySnapshot { total_bytes, available_bytes, used_bytes, used_percent }` `:89`, `swap: SwapSnapshot { total_bytes, free_bytes, used_bytes, used_percent }` `:98`, `load: LoadSnapshot { one, five, fifteen, runnable, total_threads, last_pid }` `:107`, `pressure: PressureGroup { cpu, memory, io }` each `PressureSnapshot { some: Option<PressureLine>, full: Option<PressureLine> }` with `PressureLine { avg10, avg60, avg300, total }` `:118-139`, `filesystems: Vec<FilesystemSnapshot { filesystem, fs_type, size_bytes, used_bytes, available_bytes, used_percent, mount, … }>` `:144`).
- Workspace deps (`agent/Cargo.toml` `[workspace.dependencies]`): every crate pinned `=x.y.z`; `tokio` features `macros, net, rt-multi-thread, signal, sync, time` (**`sync` is present — `tokio::sync::watch` needs nothing new**); `axum` `=0.8.9` (`http1, json, original-uri, query, tokio`); `serde`, `serde_json`, `sqlx`, `sysinfo`, `time`, `flate2`, `sha2`, `rustix`, `procfs`. `rust-version = "1.95"` (workspace); the box builds with rustc 1.98.
- Dashboard: `index.html` `#history-ladder-settings-group` (`:561-623`, the fieldset your OTel group is modelled on; `.settings-dialog-actions` `:628-638`), `app.js` `state.retentionLadderAvailable` (`:217`), `normalizeSettings` (`:509`, ladder defaults merged at `:533`), `populateDaemonSettings` (`:2587`; ladder controls `:2603-2622`), the Bun-hide block (`:2677-2680` — `setHidden(elements.historyLadderSettingsGroup, …)`, export/import buttons), the coverage rendering that prints "Archive — Queryable: off; Cold: off" (find it: `grep -n "Queryable" app.js`), `saveDaemonSettings` (`:2893`), `renderSettingsValidation`. `ladder-rules.js` exports (`ladderCapabilityFrom` `:155`, `settingsPutPayload(settings, retentionLadderAvailable)` `:163`, `validateRetentionLadder` `:269`, `describeDiskCoverage` `:36`). Bun tests you may EXTEND (new tests only): `tests/dashboard-settings.test.ts` (pins `index.html` ids, e.g. `:156-158`), `tests/dashboard-ladder.test.ts`.
- Port facts on this box: `:4317`/`:4318` are ALREADY LISTENING (someone's OTLP receiver). Never send to them from a test; tests use port 0 or `127.0.0.1:1` (F7).

Read, in this order, before touching anything:
1. Spec `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §12 (`:205-212`, the `otel` block, the metric list with units and attributes, the exporter semantics, the coverage block) and its 2026-08-29 sentences, §13 (`:213-216`, Rust-daemon-only), §15, §16 (`:228`).
2. **ADR 0015** `docs/adr/0015-opentelemetry-metrics-push-export.md` (push only; HTTP/protobuf; headers from an env var; failure counter; no read path). ADR 0016 (the document never carries a secret). Plan Global Constraints (`:13-23`), Task 11 + its amendment, the Contingencies entry about the TLS stack.
3. `CLAUDE.md` at the repo root; `docs/reports/2026-08-29-dependency-vetting-flate2-sha2.md` (the vetting report shape you copy).
4. The code listed above, at `f8b8b43`.

**Design rulings (implement as written; escalate if one cannot work):**

**F1 — Dependencies (rule 5: vet FIRST, before any code).** Write `docs/reports/2026-08-29-dependency-vetting-opentelemetry.md` BEFORE editing `Cargo.toml`: for `opentelemetry`, `opentelemetry_sdk`, `opentelemetry-otlp` (and `opentelemetry-proto` + `prost` as dev-dependencies if F7 needs them) record: the latest stable on crates.io **at execution time** (`cargo search <crate> --limit 1`; at brief time: `opentelemetry 0.32.0`, `opentelemetry_sdk 0.32.1`, `opentelemetry-otlp 0.32.0`, `opentelemetry-proto 0.32.0` — the same 0.32 minor line; patch levels may differ and that is fine), each crate's `rust-version` (MSRV must be ≤ 1.95 — read `Cargo.toml` of the downloaded crate under `~/.cargo/registry/src`; **> 1.95 → STOP**), advisories (`cargo audit` after the lock resolves — paste it; a vulnerability in the new subtree → STOP), upstream health (open-telemetry/opentelemetry-rust: release cadence, open issues touching `metrics`/`otlp` HTTP, pre-1.0 status → exact pins), alternatives weighed (Prometheus pull — rejected by ADR 0015; gRPC OTLP — rejected, `tonic` weight; hand-rolled OTLP over `prost` + `reqwest` — rejected: re-implements the SDK's aggregation and resource semantics), and the measured cost: `Cargo.lock` package count before/after and `ls -l agent/target/release/tinytop-agent` before/after a `cargo build --release` (rule 8 — numbers, not adjectives). Then pin in `[workspace.dependencies]` with `=` exact versions: `opentelemetry = { version = "=…", default-features = false, features = ["metrics"] }`, `opentelemetry_sdk = { version = "=…", default-features = false, features = ["metrics", "experimental_metrics_custom_reader"] }` (**amended 11:40Z after run 625 escalated correctly:** in 0.32 the SDK exposes `ManualReader`, the `MetricReader` trait and `Pipeline` ONLY behind `experimental_metrics_custom_reader` — verified in the cached source, `src/metrics/mod.rs:50-80`; `PeriodicReader` is a `std::thread` + `futures_executor::block_on` design that cannot host the async reqwest client inside our tokio daemon and hides failures, so the caller-driven reader is the right path and the experimental gate is accepted, pinned exactly and stated in the vetting report as an upgrade-deliberately risk), `opentelemetry-otlp = { version = "=…", default-features = false, features = ["metrics", "http-proto", "reqwest-client", "reqwest-rustls"] }` — use the exact feature names the 0.32 `Cargo.toml` of `opentelemetry-otlp` declares for the reqwest HTTP client and rustls TLS (read them; `reqwest-blocking-client` is NOT wanted — the exporter runs inside tokio). If the HTTP client feature drags a native TLS stack that fails to build → the plan's contingency: try the rustls feature set; if neither builds → STOP with the exact error. No other new dependency; `tinytop-agent/Cargo.toml` gets `opentelemetry.workspace = true` etc.; `tinytop-store` gets NOTHING new (the settings block is plain serde).

**F2 — Settings block (`tinytop-store/src/otel_settings.rs`, `pub mod otel_settings;` in `lib.rs`).**
```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)] #[serde(rename_all = "camelCase")]
pub struct OtelSettings { pub enabled: bool, pub endpoint: String, pub protocol: String, pub interval_sec: i64, pub headers_env_var: String, pub service_name: String, #[serde(default)] pub resource_attributes: BTreeMap<String, String> }
impl Default for OtelSettings { … } // spec §12: enabled false, endpoint "http://127.0.0.1:4318/v1/metrics", protocol "http/protobuf", interval_sec 60, headers_env_var "TINYTOP_OTEL_HEADERS", service_name "tinytop", resource_attributes {}
impl OtelSettings { pub fn default_for_serde() -> Self; pub fn validate(&self) -> Result<(), StoreError>; }
pub const OTEL_INTERVAL_SEC_RANGE: (i64, i64) = (5, 3_600);
```
`DashboardSettings` gains `#[serde(default = "OtelSettings::default_for_serde")] pub otel: OtelSettings` after `retention_ladder`. **`from_document`'s absent-key rule extends to `otel`:** change its signature to `from_document(document: JsonValue, persisted: Option<&DashboardSettings>) -> Result<Self, StoreError>`; an absent `retentionLadder` behaves exactly as today using `persisted.map(|p| &p.retention_ladder)`; an absent `otel` key → `settings.otel = persisted.map(|p| p.otel.clone()).unwrap_or_default()`. Update EVERY caller (`writer.rs`, `settings_transfer.rs`, tests — report the count) mechanically (`Some(&previous.retention_ladder)` → `Some(&previous)`); behaviour for the ladder byte-identical (the existing tests prove it). Consequence to state in the docs: a 0.4.1 export imported into 0.5.0 keeps the daemon's OTel block; `tinytopConfigVersion` stays 1 (additive). `validate` (called from `DashboardSettings::validate` after the existing rules): `otel.protocol` via `validate_one_of` with `["http/protobuf"]`; `otel.endpoint` — string check without a new crate: starts with `http://` or `https://`, the authority (up to the next `/`, `?` or `#`) is non-empty, contains no whitespace/control characters; message `otel.endpoint must be an http:// or https:// URL with a host`; `otel.intervalSec` via `validate_range(…, 5, 3600)`; `otel.headersEnvVar` — `^[A-Z][A-Z0-9_]*$` by chars, message `otel.headersEnvVar must match ^[A-Z][A-Z0-9_]*$`; `otel.serviceName` — 1..=128 chars, no control characters, message `otel.serviceName must be 1–128 characters without control characters`; `otel.resourceAttributes` — ≤ 32 entries, each key `^[a-z][a-z0-9._]*$` (≤ 64 chars), each value ≤ 256 chars without control characters, message `otel.resourceAttributes must hold at most 32 entries with keys matching ^[a-z][a-z0-9._]*$ and values of at most 256 characters`. Validation is exercised on PUT, dry-run and import automatically (they call `validate`) — test it once per path. `changed_keys` gains `"otel"` (after `retentionLadder`). Make `validate_one_of`/`validate_range` `pub(crate)` if needed — no other change to them.

**F3 — Exporter (`tinytop-agent/src/otel.rs`, `mod otel;` in `main.rs` beside `mod writer;`).** The exporter is driven by the daemon's own loop, NOT by the SDK's `PeriodicReader` (which spawns its own thread/runtime and hides failures): `SdkMeterProvider` + `ManualReader` + `opentelemetry_otlp::MetricExporter` (HTTP/protobuf) + synchronous gauges recorded at export time. Verify the 0.32 API on docs.rs first — `opentelemetry_sdk::metrics::ManualReader::builder().build()`, `SdkMeterProvider::builder().with_reader(reader).with_resource(resource).build()`, `MetricReader::collect(&self, &mut ResourceMetrics)`, `PushMetricExporter::export(&self, &ResourceMetrics) -> OTelSdkResult` (or the async form), `Meter::f64_gauge(name).with_unit(u).with_description(d).build()` / `u64_gauge`, `MetricExporter::builder().with_http().with_endpoint(url).with_protocol(Protocol::HttpBinary).with_timeout(Duration).with_headers(HashMap<String, String>).build()`; **if `ManualReader` cannot feed a `PushMetricExporter` in 0.32, or synchronous gauges are not available, STOP and ESCALATE with the exact API you found** — do not switch to `PeriodicReader`.
- `pub struct OtelStatus` (behind `Arc<Mutex<…>>` in `AppState` as `otel: Arc<Mutex<OtelStatus>>`): `enabled: bool, endpoint: String, interval_sec: i64, last_success_ms: Option<i64>, last_failure_ms: Option<i64>, failures: u64, last_error: Option<String>` (`last_error` = the error's `to_string()` truncated to 200 chars; F5 asserts it never holds a header value).
- `AppState` gains `latest_snapshot: Arc<tokio::sync::watch::Sender<Option<Arc<SystemSnapshot>>>>`; `collect_and_store` publishes `Some(Arc::new(snapshot.clone()))` right after `collector.collect()` succeeds (before `insert_snapshot` — the value is real whether or not persistence succeeds). Observed values = the latest published snapshot at export time; **no re-collection, no store read** in the exporter.
- `pub fn spawn_otel_export_loop(state: AppState) -> JoinHandle<()>`, spawned in `serve` next to the other three. Loop: `tick = 5 s` (`const OTEL_TICK_SECS`); each tick `get_settings()` (an error → `eprintln!` + sleep 60 s, like the disk loop) → `settings.otel`: if `!enabled` → drop any pipeline (`provider.shutdown()` best-effort), status `enabled=false`, sleep; else if the block differs from the applied one (or no pipeline) → (re)build: `headers = std::env::var(&otel.headers_env_var).ok()` → `parse_otlp_headers(headers.as_deref())` (F4) → `build_pipeline(&otel, headers, resource)` → on error: count it as a failure (F3 failure path) and retry after `interval_sec`, not every tick; resource = `service.name` = `otel.service_name`, `service.version` = `env!("CARGO_PKG_VERSION")`, `host.name` = the latest snapshot's `identity.hostname` (or the daemon's hostname source if the snapshot is `None` — find what `daemon_metadata` uses), plus `resource_attributes` (user keys never override the three fixed ones — fixed win). When due (`now − last_attempt_ms ≥ interval_sec × 1000`; first export immediately after a successful build): `latest = rx.borrow().clone()`; `None` → skip (not a failure); record every gauge (F3 metric map); `reader.collect(&mut rm)`; `exporter.export(&rm)` with the builder's timeout (10 s) → `Ok` → `last_success_ms = now`, `failures` unchanged, if the previous attempt failed print `otel export recovered: <endpoint>`; `Err(e)` → `failures += 1`, `last_failure_ms`, `last_error`, and `eprintln!("otel export failed: {endpoint}: {e}")` **at most once per 60 s** (keep `last_warn_ms`). The export awaits inside this task only — it can never block `collect_and_store` or the HTTP router.
- **Metric map (spec §12 names, units, attributes — exactly):** `system.cpu.utilization` [`1`] = `cpu.usage_percent / 100`; `system.memory.utilization` [`1`] = `memory.used_percent / 100`; `system.memory.usage` [`By`] attr `state=used` = `memory.used_bytes`; `system.memory.limit` [`By`] = `memory.total_bytes`; `system.paging.utilization` [`1`] attr `state=used` = `swap.used_percent / 100` (0 when `swap.total_bytes == 0`); `system.cpu.load_average.1m/5m/15m` [`{thread}`] = `load.one/five/fifteen`; per filesystem: `system.filesystem.utilization` [`1`] attrs `mountpoint=<mount>`, `type=<fs_type>` = `used_percent / 100`; `system.filesystem.usage` [`By`] attrs `mountpoint`, `type`, `state=used` = `used_bytes` and `state=free` = `available_bytes`; `tinytop.load.percent` [`1`] = the daemon's existing load-percent definition (find it — `grep -rn "load_percent\|loadPercent" agent/` — and call the same function; if none exists in Rust, `load.one / cores × 100` and say so in the report); `tinytop.pressure.some` / `tinytop.pressure.full` [`1`] attr `resource=cpu|memory|io` = `avg10`, **emitted only when the line is `Some`**. Instruments are created once per pipeline build; values are recorded on every export.

**F4 — Headers from the environment, never from settings.** `pub fn parse_otlp_headers(value: Option<&str>) -> Result<HashMap<String, String>, String>`: `None`/empty → empty map; else split on `,`, each entry on the FIRST `=`, trim both sides, percent-decode the value if it contains `%` (OTLP `OTEL_EXPORTER_OTLP_HEADERS` semantics; implement a small decoder — no crate), refuse an entry without `=` or with an empty key with `header entry <n> is not key=value` (a refused value = the pipeline build fails → counted failure + one warn; the header VALUE never appears in the message). The value is read at every (re)build — a rotated token applies when the operator toggles `enabled` or changes the block (document this; a restart also re-reads it). **Never log, never store, never serialise a header value**: not in `OtelStatus`, not in coverage, not in `db stats`, not in the export document.

**F5 — Surfaces.** `HistoryCoverage` gains `#[serde(default, skip_serializing_if = "Option::is_none")] pub otel: Option<HistoryOtelCoverage>` (`{ enabled, endpoint, interval_sec, last_success_ms, last_failure_ms, failures, last_error }`, camelCase) — the store leaves it `None`; the `history_coverage` handler in `writer.rs` fills it from `state.otel` after calling the store (Bun has no such block — the dashboard treats absence as "no OTel", §13). `db stats --json` (`DbStatsOutput`) gains `otel: OtelStatsOutput { enabled, endpoint, interval_sec, headers_env_var, headers_env_var_set: bool, service_name }` — presence of the env var only (`std::env::var_os(name).is_some()`), never its value. PUT/import of an `otel` change records the usual marker (`{"changed":["otel"]}` / `source: import`); the loop applies it within one tick.

**F6 — Dashboard (Bun-neutral).** `index.html`: a new `<fieldset class="settings-group otel-settings-group" id="otel-settings-group" hidden>` directly AFTER `#history-ladder-settings-group`, legend `OpenTelemetry export`, controls: `daemon-otel-enabled` (checkbox, `toggle-row`), `daemon-otel-endpoint` (text, `settings-wide-field`, placeholder the default endpoint), `daemon-otel-interval-sec` (number 5–3600), `daemon-otel-headers-env-var` (text, placeholder `TINYTOP_OTEL_HEADERS`), `daemon-otel-service-name` (text), `daemon-otel-resource-attributes` (textarea, `settings-wide-field`, placeholder `key=value, one per line`); a one-line `<p class="settings-help">` under the group: "Headers (e.g. authorization) are read from the environment variable named here — never stored in settings." (if `settings-help` does not exist, reuse whatever class the ladder group uses for its note; a NEW `styles.css` rule is allowed only for the textarea's height — name it). `ladder-rules.js` (pure, tested): `otelCapabilityFrom(settingsOrNull)` (has an `otel` object), `settingsPutPayload(settings, retentionLadderAvailable, otelAvailable = retentionLadderAvailable)` deletes `payload.otel` when `!otelAvailable` (existing two-arg calls keep their behaviour; new tests cover the third), `parseResourceAttributes(text) -> { attributes, errors }` (one `key=value` per line; blank lines ignored; the same key/value rules as F2; errors name the line number), `formatResourceAttributes(attributes) -> string` (sorted keys), `validateOtelSettings(otel) -> string[]` mirroring F2's messages (the T5 mirror pattern — the PUT's 400 stays authoritative), `describeOtelCoverage(otel)` → `OTel — off` / `OTel → <endpoint> every <n> s · last success <HH:MM:SS> · failures <n>` (+ ` · last error: <text>` when `lastError` is set and `lastFailureMs > lastSuccessMs`). `app.js`: `state.otelAvailable` set beside `retentionLadderAvailable` from `GET /api/settings`; `normalizeSettings` merges `otel` defaults like the ladder's; `populateDaemonSettings` fills the six controls; the form read used by `saveDaemonSettings` builds `otel` (interval as an integer; attributes via `parseResourceAttributes`, its errors shown through `renderSettingsValidation` and blocking the save); `settingsPutPayload(..., state.otelAvailable)` at every call site (including the shrink dry-run body); the Bun-hide block hides `#otel-settings-group` when `!state.otelAvailable`; the coverage rendering adds one status line `#history-otel-status` beneath the archive line (`describeOtelCoverage(coverage.otel)`; hidden when `coverage.otel` is absent). No change to `describeImportPlan` (T10-fix2 owns it).

**F7 — Tests (TDD — paste RED, implement, paste GREEN). Port-free unless the name starts with `serve_`.**
- `otel_settings.rs` `#[cfg(test)]`: defaults equal spec §12 verbatim; each validation rule RED→GREEN with its exact message (`protocol`, `endpoint` ×3: no scheme / empty host / whitespace, `intervalSec` 4 and 3601, `headersEnvVar` lowercase and leading digit, `serviceName` empty and 129 chars, `resourceAttributes` 33 entries and a bad key); a document without `otel` decodes to the persisted block through `from_document` and to the default with `persisted = None`; `changed_keys` reports `otel` once.
- `tests/settings_transfer.rs` (extend): `export_document_carries_the_otel_block_and_still_no_secret_shaped_key` (the existing walk extended to assert the `otel` object is present with `headersEnvVar`); `import_without_otel_keeps_the_persisted_block` (enable OTel via `put_settings`, import a document whose `settings` lacks `otel` → plan `valid`, no warning mentions `otel`, `changedKeys` excludes `otel`, the block survives); `import_with_an_invalid_otel_block_is_refused` (the message in `errors[]`, nothing written).
- `otel.rs` `#[cfg(test)]`: `parse_otlp_headers` (empty; two pairs; percent-decoded value; `a=b=c` keeps `b=c`; an entry without `=` refused naming the index — and the refusal text does not contain the value); `metric_request_carries_the_spec_names_units_and_resource` — build the provider with a `ManualReader` and NO network: record from a fixture `SystemSnapshot`, `collect`, convert with `opentelemetry_proto`'s `From<&ResourceMetrics> for ExportMetricsServiceRequest` (dev-dependency `opentelemetry-proto` at the same 0.32 version with the `gen-tonic-messages` + `metrics` features if `opentelemetry-otlp` does not re-export it — record the choice in the report), then assert the sorted metric-name list equals spec §12's, the units, the attributes of one filesystem series (`mountpoint`, `type`, `state`), that `tinytop.pressure.*` appears only for `Some` lines, and the resource `service.name`/`service.version`/`host.name` plus one user attribute; `export_failure_increments_the_counter_and_never_stalls_collection` — pipeline pointed at `http://127.0.0.1:1/v1/metrics` (nothing listens; no bind needed) with a 2 s timeout and a header value `sekrit-value`: one export → `failures == 1`, `last_error` is `Some` and does NOT contain `sekrit-value`; meanwhile `collect_and_store` on a temp store runs twice and both `insert_snapshot`s land; `warn_is_rate_limited` (two failures within a minute → one line; assert via the `last_warn_ms` state, not stderr); `disabled_block_tears_the_pipeline_down` (status `enabled=false`, no export attempted). `serve_otel_receiver_decodes_one_request` — a tokio `TcpListener` on port 0 with a minimal axum `Router` (`POST /v1/metrics` → 200, body captured through a channel) — one real export → `prost::Message::decode` (dev-dependency `prost` at the version the lock resolves) → the same name assertions + the `authorization` header seen by the receiver equals the parsed env value. **This test cannot bind in your sandbox** (the `serve_` tests fail with `PermissionDenied` there) — run it once, paste the result, and say so; the orchestrator's outside gate proves it.
- `writer.rs` `#[cfg(test)]`: `coverage_reports_the_otel_block_from_state` (default state → `otel.enabled == false`, `failures == 0`); `put_settings_rejects_a_bad_otel_endpoint_with_400` (`{"error": "otel.endpoint must be …"}`); `latest_snapshot_is_published_by_collect_and_store` (the `watch` receiver sees `Some` after one `collect_and_store`).
- `tests/help_contract.rs`: unchanged (no new verb). `tests/cli_db.rs` or `cli_config.rs`: `db_stats_reports_otel_presence_only` (`otel.headersEnvVarSet` false; no key of the `otel` object is a header value).
- Bun: `tests/dashboard-settings.test.ts` + the seven new ids pinned; `tests/dashboard-ladder.test.ts` + `parseResourceAttributes` (valid; blank lines; a bad line names its number), `formatResourceAttributes` round-trip, `validateOtelSettings` (one message per rule), `settingsPutPayload` third argument, `otelCapabilityFrom`, `describeOtelCoverage` (off / healthy / failing wording).

**F8 — Docs.** `README.md`: new `## OpenTelemetry export` section (what is exported — the §12 table with units/attributes; how to enable (dialog, `config import`); `TINYTOP_OTEL_HEADERS="authorization=Bearer <token>"` set in the service environment (a systemd `Environment=` drop-in — show the three lines), never in settings; a minimal `otelcol-contrib` YAML receiving OTLP/HTTP on `127.0.0.1:4318` with the `debug` exporter; failure semantics — counter in `/api/history/coverage`, one warn per minute, collection unaffected; Bun runtime: none), the `### History API` coverage row mentions `otel`, the `db stats` CLI row mentions `otel`. `GUIDE.md` `## Settings`: the OTel group and the headers rule. `ARCHITECTURE.md`: the two-runtime section lists OTel as daemon-only; the daemon task list names the exporter loop and the `watch` channel. `INSTALL.md`: if it documents the unit's environment, add `TINYTOP_OTEL_HEADERS` there (one paragraph). `docs/sqlite-history-architecture.md` `## Settings transfer`: one paragraph — the `otel` block is part of the document, `headersEnvVar` is a name, an absent block keeps the persisted value, `tinytopConfigVersion` stays 1. `CHANGELOG.md`: `## Unreleased` ABOVE `## 0.4.1` (Fable assigns 0.5.0 at the phase close) with one line per surface (deps, store, exporter, API/coverage, CLI, dashboard, docs). `PROGRESS.md`, spec, ADR, plan: untouched (already amended on main for this lane).

Acceptance (paste in the final message): the vetting summary (versions, MSRVs, `cargo audit` output, lock package delta, release binary size before/after); RED/GREEN per test above; the sorted metric-name list printed by `metric_request_carries_the_spec_names_units_and_resource`; the sandbox result of the `serve_` test; `bun run check` (the sandbox port boundary acknowledged, every other suite green; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo fmt --check` clean); `git diff --stat`; the `from_document` call-site count; any contract-silent detail you had to decide.
