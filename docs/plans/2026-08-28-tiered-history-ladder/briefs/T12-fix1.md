You are hexe lane **T12-fix1** for tinytop — the fix round after the luna blind review of T12 (Fabulous `docs/fleet/tinytop/2026-08-29-ari-luna-t12-review.md`; plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` Task 12; ADR 0021). Base: `tinytop/phase5-t12-cadence` at `bfdc36d` (`git rev-parse HEAD` first and quote it). Every file:line below was read at `bfdc36d`. Build ONLY the two findings below. Files you may edit: `agent/crates/tinytop-collectors/src/lib.rs`, `agent/crates/tinytop-collectors/src/common.rs`, `agent/crates/tinytop-types/src/lib.rs` (one doc-comment), `docs/guides/API.md`, `agent/assets/dashboard/index.html` (one label), `GUIDE.md` (one phrase); NOTHING else. **Git is read-only for you — no commit, no branch, no stash, no `cargo fmt` on the tree (`cargo fmt --manifest-path agent/Cargo.toml --all -- --check` only, fix by hand); the orchestrator commits.** No edit to any `Cargo.toml`/`Cargo.lock`, `src/`, `legacy/`, `VERSION`, `CHANGELOG.md`, `docs/adr/*`, the plan, `linux.rs`, `macos.rs`, `windows.rs`, the store, the agent, `vendor/`. Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; `std::env::set_var` is forbidden. **Known sandbox limits — not failures:** `common.rs` is compiled ONLY for macOS/Windows (`agent/crates/tinytop-collectors/src/lib.rs:1-5` `cfg(any(all(feature = "macos-collector", target_os = "macos"), all(feature = "windows-collector", target_os = "windows")))`) — you cannot execute it; the two offline cross-target checks are its compile proof and the pure function in F1 carries the executable test; `serve_*` tests bind ports your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); no crates.io (`--offline`). If a contract below is wrong against the code you read, or you would be guessing, STOP and reply `ESCALATE: <what, what you tried, where you stopped>`.

**Read first:** `agent/crates/tinytop-collectors/src/common.rs:56-60` (`SysinfoCollector::collect`), `:96-140` (the `SystemSnapshot` literal — `load: LoadSnapshot { …, runnable: 0, total_threads: processes.len() as u64, last_pid: processes.iter().map(|process| process.pid as u64).max().unwrap_or(0) }` at `:128-139`), `:196-227` (`native_processes(system, top_process_count)` — builds from `system.processes().values()`, sorts by CPU descending, `truncate(top_process_count)` at `:226`); `linux.rs:418-431` (how Linux derives `runnable`/`total_threads`/`last_pid` from `/proc/loadavg` — the semantics the field names carry); `agent/crates/tinytop-types/src/lib.rs:106-115` (`LoadSnapshot`); `agent/crates/tinytop-collectors/src/lib.rs:1-80` (the `cfg` gates, the `Collector` trait, `CollectorConfig`); `tests/linux_collector.rs` (style reference only); `docs/guides/API.md:165-176` (the `load` object in the `/api/snapshot` example, `"runnable": 0, "totalThreads": 0` at `:172-173`); `agent/assets/dashboard/index.html:596-600` (the `Detail interval seconds` label) and the neighbouring labels in the same fieldset (`Snapshot JSON minutes` `:594`, `Disk check minutes` `:618` — the `<Name> <unit>` style); `GUIDE.md:93` (the sentence naming `Disk check every`). Work RED → GREEN and paste each RED line and GREEN summary.

**F1 — `total_threads` and `last_pid` on the sysinfo collector come from the FULL process table, not the truncated top-N list (P0, pre-existing, made visible by T12).** Today (`common.rs:133-138`) both are computed from `processes` — the vector `native_processes` already truncated to `top_process_count` — so `totalThreads` reads as the process-list length (a constant 10 before v0.5.1, `topProcessCount` after; `topProcessCount = 1` → "1 thread" on the dashboard's Threads card, `app.js:2355`) and `lastPid` is the max pid among the top N by CPU, not the system's newest pid. Linux is unaffected (`/proc/loadavg`). **Contract:** in `agent/crates/tinytop-collectors/src/lib.rs`, OUTSIDE every `cfg` gate (so it compiles and tests on Linux), add
`#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)] pub struct ProcessTotals { pub count: u64, pub last_pid: u64 }` and `pub fn process_totals(pids: impl IntoIterator<Item = u64>) -> ProcessTotals` (count every pid, `last_pid` = the maximum, `0` for an empty iterator), with a doc-comment stating the purpose: the sysinfo collectors' `LoadSnapshot.total_threads` / `last_pid` are derived from the full process table BEFORE the top-N truncation — sysinfo exposes no thread totals on macOS/Windows, so `total_threads` is the PROCESS COUNT there (Linux reads the kernel's task total from `/proc/loadavg`); the honest `Option<u64>` waits for schema v3 (Task 14), because `metric_samples.total_threads` is a `NOT NULL` typed column today. Both items `pub` (the crate has no `pub(crate)` consumers outside `common.rs`, and hiding them behind `#[doc(hidden)]` is fine). In `common.rs:128-139`: `let totals = process_totals(self.system.processes().keys().map(|pid| pid.as_u32() as u64));` computed from `self.system` (the same `System` `native_processes` reads — no second refresh), then `total_threads: totals.count`, `last_pid: totals.last_pid`; `runnable: 0` stays (no sysinfo source; T14) — one comment on the literal pointing at `process_totals`'s doc-comment; nothing else in `collect` changes. `agent/crates/tinytop-types/src/lib.rs:113` (`total_threads`) gets a `///` doc-comment: kernel task total on Linux (`/proc/loadavg`); the process count on the sysinfo-based macOS/Windows collectors (no thread total exists there); v3 makes it optional. `docs/guides/API.md:172-173`: one sentence after the example — `runnable` is `0` and `totalThreads` is the process count on the macOS/Windows collectors (sysinfo has no thread totals); Linux reports the `/proc/loadavg` task totals. **Tests** (`agent/crates/tinytop-collectors/src/lib.rs`, a new `#[cfg(test)] mod tests` outside the gates): `process_totals_counts_every_pid_and_keeps_the_newest` — `[3, 1, 2]` → `{ count: 3, last_pid: 3 }`; `process_totals_of_nothing_is_zero` — empty → `{ 0, 0 }`; and `process_totals_ignores_ordering_and_truncation_of_the_caller` — the totals of 50 pids equal the totals of the same 50 pids reversed, and differ from the totals of their first 8 (the invariant the bug violated). RED today: the first compile error on `process_totals` (`E0425`) — paste it. Cross-target proof (the sandbox cannot run `common.rs`; both pass offline on the orchestrator's box): `cargo check --manifest-path agent/Cargo.toml -p tinytop-collectors --no-default-features --features windows-collector --target x86_64-pc-windows-msvc --offline` and `… --features macos-collector --target aarch64-apple-darwin --offline` — paste both `Finished` lines; and paste `grep -n "processes.len()\|processes.iter().map(|process| process.pid" agent/crates/tinytop-collectors/src/common.rs` showing no remaining hit inside the `LoadSnapshot` literal.

**F2 — the filesystem-interval control's label matches the docs and its new meaning (P2).** `index.html:598`: `<span>Detail interval seconds</span>` → `<span>Filesystem check seconds</span>` (the fieldset's `<Name> <unit>` style; T12 redefined the setting as the filesystem check interval, so the old name is stale, not just mismatched). `GUIDE.md:93`: `` (`Disk check every`, also the typed detail cadence) `` → `` (`Filesystem check seconds`, also the typed detail cadence) `` — nothing else in that sentence. No test (a label; `grep -rn "Detail interval" tests/ agent/assets/dashboard/*.js README.md docs/` must be empty afterwards — paste it).

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green (paste every `test result:` line — expect the T12 census 261 + 3); the two cross-target `Finished` lines; `bun test tests/dashboard-ladder.test.ts tests/dashboard-process-filesystem.test.ts tests/dashboard-settings.test.ts` green (paste); `git status --short` and `git diff --stat` pasted (files ⊆ the six named above). Final report: per finding RED → GREEN; the three new test names; the base commit you quoted; anything you could not do with the exact reason.
