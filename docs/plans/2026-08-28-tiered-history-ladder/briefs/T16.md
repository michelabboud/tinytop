# Brief — lane T16: native GPU backends — Windows (PDH + DXGI) and macOS (IOKit)

You are hexe lane **T16** for tinytop — Phase 5 Task 16 of plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` **including its amendment bullets dated 2026-08-30 under Task 16** (read them first — they correct the plan text), ADR 0022 **including its 2026-08-30 amendment** (the stable-id rule; the seven IOKit symbols; memory from DXGI only), ADR 0025 decisions 2 and 5 (the interning key; the busy rule). The measured inputs are `docs/fleet/tinytop/2026-08-30-t16-fact-sheet.md` in the Fabulous repository — every wizai fact below is quoted from it; the orchestrator pastes §A verbatim under `## Fixture truth` at the end of this brief. Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is v0.5.4 `e49e3d0` + this brief's commit). Every `file:line` below was read at `e49e3d0`; run `sed -n` / `grep -n` on each before you build on it. Build ONLY the findings below.

**Files you may edit:** `agent/Cargo.toml` (the two workspace pins in F1 — nothing else in that file); `agent/crates/tinytop-collectors/Cargo.toml` (the two target tables in F1); `agent/crates/tinytop-collectors/src/gpu/{mod,windows,macos}.rs`; the NEW `agent/crates/tinytop-collectors/src/gpu/{pdh_names,ioreg_keys}.rs`; `agent/crates/tinytop-collectors/src/common.rs`; the NEW `agent/crates/tinytop-collectors/tests/native_gpu.rs`; the NEW `docs/reports/2026-08-30-dependency-vetting-windows-core-foundation.md`; docs: `README.md` (the `## GPU` section only), `INSTALL.md` (one paragraph), `ARCHITECTURE.md` (the GPU line), `docs/guides/API.md` (the `gpus[]` / `gpuPercent` notes). NOTHING else — in particular no `gpu/linux.rs`, no `linux.rs`, no `windows.rs`/`macos.rs` collector wrappers (they wrap `SysinfoCollector` and stay 29 lines), no `tinytop-types`, no `tinytop-store`, no `tinytop-agent`, no `agent/assets/dashboard/*`, no `tests/*.ts`, `src/`, `legacy/`, `GUIDE.md`, `VERSION`, `CHANGELOG.md` (Fable writes the line at merge), `docs/adr/*`, the plan, the spec, `vendor/`. **`Cargo.lock` may change ONLY in the `tinytop-collectors` package's `dependencies` list — `git diff agent/Cargo.lock | grep -c '^+name'` MUST print 0 (no new `[[package]]`); paste the lock diff. If it prints anything else, STOP and ESCALATE.** **Git is read-only for you — no commit, no branch, no stash, no bare `cargo fmt` (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` and fix by hand); the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/` (the live database) or anything under `/tmp/tinytop*`; every test uses `std::env::temp_dir()` / `tempfile`-free scratch under the target dir as the existing tests do. No subprocess anywhere (ADR 0012): no `typeperf`, no `powershell`, no `ioreg`, no `system_profiler` — not even in tests. No vendor library. No new crate.

**You build on Linux.** The two FFI backends cannot run here; they are proven by (a) the pure parsers/aggregators in F2, which compile and are tested on every OS in this gate, RED first, and (b) the per-crate cross-target `cargo check` for `x86_64-pc-windows-msvc` and `aarch64-apple-darwin` (the gate below). Runtime acceptance on real hardware (wizai native, miniwiz when reachable) is the orchestrator's and blocks the 0.6.0 release notes' claim, not the merge. Anything you cannot verify without the hardware is stated as such in your report — never "works on Windows".

**Read first:** ADR 0022 whole (Context lists the measured sources; the 2026-08-30 amendment rules the ids); ADR 0025 decisions 2 and 5; `gpu/mod.rs:5-10` (the `mod` gating on `target_os` — your two new pure modules are NOT gated), `:12-45` (`GpuVendor::from_pci_vendor` — `0x1414` is already `Microsoft`), `:47-70` (`GpuAdapter`, `GpuSample`, `GpuScanStats` — you change none of them), `:72-84` (`trait GpuBackend: Send` — `sample()` before `process_busy()`; `last_scan()`), `:86-101` (`detect_backend` already dispatches to `windows::detect_backend()` / `macos::detect_backend()` — keep those two function names), `:103-127` (`attach_gpu`); `gpu/windows.rs` and `gpu/macos.rs` (the 6-line placeholders you replace); `gpu/linux.rs:107-119` (`GpuDetector`, the three fields), `:121-135` (`Default` wiring), `:138-170` (`without_gpu` / `with_clock` / `with_gpu_backend` / `last_gpu_scan` — the GPU-free test constructors), `:196-233` (`collect()`: re-detect in the `slow_due` branch `:205-213`, `gpu_tick` `:218-222`, `attach_gpu` `:229-231`) — **`common.rs` gets exactly this wiring**; `common.rs:22-47` (`SysinfoSlowCache`, `SysinfoCollector`, `Default`), `:56-…` (`collect()`: `slow_due` `:66-68`, the slow cache `:69-80`, `native_processes` `:86`, the `SystemSnapshot` literal from `:94` — check it already carries `gpus: Vec::new()` from T15; if not, that literal gains it); `lib.rs:1-12`; `tinytop-collectors/Cargo.toml` (the `[target.'cfg(target_os = "linux")'.dependencies]` table is the shape); `agent/Cargo.toml` `[workspace.dependencies]` (16 exact pins); the T15 brief's `## Fixture truth` idea: fixtures are the measured lines, never invented ones.

**F1 — vet-first dependencies (plan Task 16; rule 5; fact sheet §C).** Both crates are ALREADY in the lock through `sysinfo 0.39.5`: `windows 0.62.2` (`agent/Cargo.lock:2623`), `core-foundation 0.10.1` (`:255`); crates.io `max_stable_version` at 02:50Z on 2026-08-30 was exactly those (`windows` 0.62.2 of 2025-10-06; `core-foundation` 0.10.1 of 2025-05-26); the release `cargo audit` on this lock reports no advisory for either. Add to `agent/Cargo.toml` `[workspace.dependencies]`, in alphabetical position: `core-foundation = "=0.10.1"` and `windows = { version = "=0.62.2", features = ["Win32_Foundation", "Win32_Graphics_Dxgi", "Win32_System_Performance"] }` (the three feature names exist in `windows-0.62.2/Cargo.toml`; `Win32_Graphics_Dxgi` pulls `Win32_Graphics`; `DXGI_ADAPTER_DESC1` needs no `Dxgi_Common` type — do not add features you do not use). In `tinytop-collectors/Cargo.toml` add `[target.'cfg(windows)'.dependencies] windows = { workspace = true }` and `[target.'cfg(target_os = "macos")'.dependencies] core-foundation = { workspace = true }` — NOT optional, NOT behind the `*-collector` features (the `gpu` module is gated on `target_os`, never on a feature — T15's rule). Write `docs/reports/2026-08-30-dependency-vetting-windows-core-foundation.md` (the T11 vetting report `docs/reports/2026-08-29-*otel*` is the shape): version pinned + why it is the current stable line, provenance (Microsoft's `windows-rs`; servo's `core-foundation-rs`), the audit result, the alternatives weighed — `windows-sys` raw bindings (rejected: `IDXGIAdapter3` is a COM interface — hand-written vtables), `io-kit-sys` (rejected by the plan: seven hand-declared symbols suffice), a vendor SDK (forbidden) — and the lock-size delta (must be 0 packages). Paste `git diff agent/Cargo.lock` in the report.

**F2 — the pure parsers and aggregators, on every OS, RED first (plan Task 16 tests; fact sheet §A + §E).** New `gpu/pdh_names.rs` (declared `pub(crate) mod pdh_names;` in `gpu/mod.rs` WITHOUT a cfg): `#[derive(Clone, Copy, PartialEq, Eq, Hash, Debug)] pub struct Luid { pub high: u32, pub low: u32 }` with `impl Luid { pub fn parse_hex_pair(high: &str, low: &str) -> Option<Self> }` (case-insensitive, `0x` prefix required, up to 8 hex digits each); `pub struct EngineInstance { pub pid: u32, pub luid: Luid, pub phys: u32, pub eng: u32, pub engtype: String }`; `pub fn parse_engine_instance(name: &str) -> Option<EngineInstance>` over the INSTANCE NAME (the text between the parentheses — `pid_100772_luid_0x00000000_0x000120CC_phys_0_eng_0_engtype_3D`): split on the literal markers `pid_`, `_luid_`, `_phys_`, `_eng_`, `_engtype_` in that order; **`engtype` is everything after `_engtype_` to the end, verbatim, including spaces and digits** (`Compute 0`, `Security 1`, `Video JPEG 0`, `Video Codec Engine`, `Video Processor`, `Timer 0`, `3D`, `Copy`, `Compute` are the nine real wizai values); an empty engtype, a non-numeric pid/phys/eng, or a missing marker → `None`. `pub struct EngineReading { pub instance: EngineInstance, pub value: f64 }`; `pub struct PdhAggregate { pub per_adapter: HashMap<Luid, f64>, pub per_pid: HashMap<u32, f64>, pub unknown_luid_readings: usize, pub instances: usize }`; `pub fn aggregate(readings: &[EngineReading], known: &HashSet<Luid>) -> PdhAggregate` — ADR 0025 decision 5's rule at both scopes: adapter busy = max over `engtype` of Σ(values of that luid + engtype), capped at 100; pid busy = for that pid, max over `engtype` of Σ(that pid's values with that engtype) per luid, then the max across luids, capped at 100; readings whose `luid ∉ known` are dropped and counted (wizai's `0x3FC20ECC` has no DXGI adapter); NaN/negative values are treated as 0. New `gpu/ioreg_keys.rs` (also ungated): `pub enum PropValue { Number(f64), Text(String), Bytes(Vec<u8>) }`; `pub struct AcceleratorProps { pub class: String, pub model: Option<String>, pub vendor_id: Option<u16>, pub device_id: Option<u16>, pub stats: BTreeMap<String, PropValue> }`; `pub struct Selected { pub busy_percent: Option<f64>, pub memory_used_bytes: Option<u64>, pub memory_total_bytes: Option<u64> }`; `pub fn select_sample(props: &AcceleratorProps) -> Selected` — EXACT, case-sensitive key matches, precedence per ADR 0022: busy = `Device Utilization %` else `GPU Activity(%)` else `None` (clamped 0..100); used = `In use system memory` else `None`; total = `Alloc system memory` else `None`; a key whose value is not a `Number` counts as absent; **no other key is ever consulted — do not add fuzzy matching, do not guess a VRAM key for discrete GPUs (the fact sheet says the Mac was not measured; a wrong key would be an invented number)**; `pub fn vendor_of(props: &AcceleratorProps) -> GpuVendor` = `from_pci_vendor(vendor_id)` when present, else `Apple` when `class` starts with `AGX`, else `Other(0)`; `pub fn pci_id_le(bytes: &[u8]) -> Option<u16>` (4-byte little-endian `CFData` → the low 16 bits). Tests in each module (`#[cfg(test)]`, run on Linux — **the RED lines are your evidence**): the six verbatim wizai `GPU Engine` paths and all nine engine types parse; uppercase and lowercase hex produce the same `Luid`; a name with a missing `_engtype_` and a name with `pid_abc` → `None`; `aggregate` over a fixture — pid 100772 on luid `3FB5EAE5`: `3D` eng_0/1/2 = 10, 20, 5, `Copy` eng_3 = 30; pid 4242 same luid `3D` = 40; pid 7 on the unknown luid `3FC20ECC` `3D` = 99 → `per_adapter[3FB5EAE5]` = 75 (3D: 10+20+5+40), `per_pid[100772]` = 35 (3D sum, > Copy 30), `per_pid[4242]` = 40, `per_pid` has no 7, `unknown_luid_readings` = 1, `instances` = 6; a cap case (two engines summing 130 → 100); `select_sample` over three synthetic dictionaries labelled SYNTHETIC in the test names — Apple-silicon-shaped (`AGXAcceleratorG14X`, `Device Utilization %` 37, `In use system memory` 1,234,567, `Alloc system memory` 9,876,543 → all three `Some`), dGPU-shaped (`GPU Activity(%)` 12, vendor bytes `02 10 00 00` → `Amd`, no memory keys → `None`s), iGPU-shaped (`GPU Activity(%)` as `Text("12")` → busy `None`; vendor `86 80 00 00` → `Intel`); `vendor_of` for an `AGX…` class without `vendor-id` → `Apple`.

**F3 — `gpu/windows.rs`: the DXGI + PDH backend (plan Task 16; ADR 0022 amendment; fact sheet §A, §C).** `#[cfg(target_os = "windows")]` module (the `mod` line in `gpu/mod.rs:9-10` already is). `pub struct WindowsGpuBackend { factory: IDXGIFactory1, adapters: Vec<DxgiAdapter>, query: Option<PdhQuery>, primed: bool, last: Option<PdhAggregate>, last_scan: Option<GpuScanStats>, last_warn: Option<Instant>, luid_set: HashSet<Luid> }`, `struct DxgiAdapter { stable_id: String, luid: Luid, adapter: IDXGIAdapter3, desc: DXGI_ADAPTER_DESC1 }`, `struct PdhQuery { query: PDH_HQUERY, engine: PDH_HCOUNTER }` with `impl Drop` → `PdhCloseQuery`. `pub fn detect_backend() -> Option<Box<dyn GpuBackend>>`: `unsafe { CreateDXGIFactory1::<IDXGIFactory1>() }` → `Err` → `None` (no backend, one `eprintln!("gpu: DXGI factory unavailable: {e}")`); then `detect()` once — zero real adapters → `None` (the detection gate: no adapter, no backend). `detect()`: `EnumAdapters1(i)` for `i = 0..` until `Err` with `DXGI_ERROR_NOT_FOUND`; `GetDesc1()`; skip when `VendorId == 0x1414` or `Flags & DXGI_ADAPTER_FLAG_SOFTWARE.0 != 0`; `cast::<IDXGIAdapter3>()` (skip the adapter with a warn if the cast fails — pre-Windows-10 stacks); `stable_id` = `format!("dxgi-{:04x}:{:04x}:{:08x}:{:02x}-{k}", VendorId, DeviceId, SubSysId, Revision)` with `k` the 0-based count of earlier adapters with the same tuple (ADR 0022 amendment — LUIDs are per-boot and never appear in `stable_id`); `name` = the `Description` UTF-16 array up to the first NUL, trimmed; `driver` = `"wddm"`; `vendor` = `GpuVendor::from_pci_vendor(VendorId as u16)`; the new LUID set ≠ the old one → drop the `PdhQuery` (re-open on the next `sample`, `primed = false`). Returns the `GpuAdapter` list (id = `stable_id`). `sample()`: open the query if `None` — `PdhOpenQueryW(PCWSTR::null(), 0, &mut q)` then `PdhAddEnglishCounterW(q, w!("\\GPU Engine(*)\\Utilization Percentage"), 0, &mut c)` (English names — never localized; **one counter — the memory counter is not added, see memory below**); `PdhCollectQueryData(q)`; if `!primed` → `primed = true` and every adapter's `busy_percent = None` this tick (the first sample of a rate counter is discarded — the plan's rule); else the two-call protocol: `PdhGetFormattedCounterArrayW(c, PDH_FMT_DOUBLE, &mut bytes, &mut count, None)` must return `PDH_MORE_DATA`, allocate `bytes` as a `Vec<u64>` of `bytes.div_ceil(8)` elements (the item array is followed by the name strings in the SAME buffer — alignment of `PDH_FMT_COUNTERVALUE_ITEM_W` is what the u64 vector guarantees), second call with the buffer; iterate `count` items: `szName` → `String` (`PCWSTR::to_string()`), `FmtValue.CStatus` ∈ {0 `PDH_CSTATUS_VALID_DATA`, 1 `PDH_CSTATUS_NEW_DATA`} else skip, `FmtValue.Anonymous.doubleValue` → `EngineReading` via `parse_engine_instance` (unparsed names counted); `aggregate(&readings, &self.luid_set)` → per adapter `busy_percent = Some(v)` (an adapter with no reading this tick → `Some(0.0)` — PDH omits idle instances? NO: PDH lists every instance; an adapter absent from the readings while the query succeeded → `Some(0.0)`; if the collect/format call FAILED → `None` for all and a warn ≤ once per minute naming the PDH status in hex, and drop the query so the next tick re-opens it); memory per adapter: `QueryVideoMemoryInfo(0, DXGI_MEMORY_SEGMENT_GROUP_LOCAL, &mut info)` → `memory_used_bytes = Some(info.CurrentUsage)`, `memory_total_bytes = (desc.DedicatedVideoMemory > 0).then_some(desc.DedicatedVideoMemory as u64)`; a failed query → both `None`; `temperature_c = None` always (no kernel interface without a vendor library). `process_busy()` = `last.per_pid.clone()` (empty before the first primed sample). `last_scan()` = `GpuScanStats { duration: collect + format + query time, pids_scanned: distinct pids in the readings, pids_denied: 0, clients: readings.len() }`. Every `unsafe` block carries a `// SAFETY:` line naming the invariant (a valid handle from `PdhOpenQueryW`; the buffer outlives the call; `count` items were written). No `static mut`, no globals; the backend lives on the collector's thread (`Send` is required by the trait; `windows` COM wrappers are `Send + Sync`). The 32-bit wizai `AdapterRAM` (1 GiB) is NOT the truth — `DedicatedVideoMemory` is (890,077,184 on wizai's APU).

**F4 — `gpu/macos.rs`: the IOKit backend (plan Task 16; ADR 0022 amendment; fact sheet §B).** `#[cfg(target_os = "macos")]`. Exactly SEVEN symbols under `#[link(name = "IOKit", kind = "framework")] unsafe extern "C" { … }`: `IOServiceMatching(name: *const c_char) -> CFMutableDictionaryRef`, `IOServiceGetMatchingServices(main_port: u32, matching: CFDictionaryRef, existing: *mut u32) -> i32`, `IOIteratorNext(iterator: u32) -> u32`, `IORegistryEntryCreateCFProperties(entry: u32, properties: *mut CFMutableDictionaryRef, allocator: CFAllocatorRef, options: u32) -> i32`, `IORegistryEntryGetParentEntry(entry: u32, plane: *const c_char, parent: *mut u32) -> i32`, `IORegistryEntryGetRegistryEntryID(entry: u32, id: *mut u64) -> i32`, `IOObjectRelease(object: u32) -> i32` — `mach_port_t`, `io_object_t`, `io_iterator_t`, `io_registry_entry_t` are all `u32`; `kern_return_t` is `i32`; `kIOMainPortDefault` = `0` (a `const MAIN_PORT_DEFAULT: u32 = 0` with the comment "kIOMainPortDefault, formerly kIOMasterPortDefault, both 0"). **`IOServiceGetMatchingServices` consumes one reference of the matching dictionary — never `CFRelease` it after the call (the fact sheet's trap).** Release every iterator, entry and parent you obtain (`IOObjectRelease`), in every path including errors — wrap them in a small `struct IoObject(u32)` with `Drop`. `pub fn detect_backend()`: match `"IOAccelerator"` → zero services → `None`. `detect()`: for each accelerator (enumeration order `k = 0..`): `IORegistryEntryCreateCFProperties` → a `core_foundation::dictionary::CFDictionary` → `IOClass` (`CFString`), `model` (`CFData` → UTF-8 up to the first NUL, or `CFString`), `IOAccelIndex` (ignored for the id — enumeration order is the index); walk up to THREE parents in the `"IOService"` plane looking for `vendor-id` + `device-id` (`CFData`, `pci_id_le`), stop at the first hit; `stable_id` = `format!("ioaccel-{k}")` + `format!("-{:04x}:{:04x}", vendor, device)` when found; `name` = model else class; `driver` = class; `vendor` = `vendor_of`; the registry entry id (`IORegistryEntryGetRegistryEntryID`) is kept only to skip a duplicate service within one enumeration. `sample()`: one `IORegistryEntryCreateCFProperties` per accelerator per tick, `PerformanceStatistics` (a `CFDictionary` of `CFNumber`s) copied into `AcceleratorProps.stats` (numbers → `Number`, strings → `Text`, data → `Bytes`), `select_sample` → the `GpuSample`; a failed property read → all `None` for that adapter and a warn ≤ once per minute; `temperature_c = None`. `process_busy()` → empty map (the plan: no per-process busy on macOS). `last_scan()` = `{ duration, pids_scanned: 0, pids_denied: 0, clients: accelerators }`. The `CFDictionary` → `BTreeMap` conversion is the ONLY place CF types are touched; keep it in one function so the cross-target check covers it.

**F5 — the collector wiring in `common.rs` (plan Task 16 "the fast tick calls the backend when `Some`"; `gpu/linux.rs:107-233` is the template line for line).** `SysinfoCollector` gains `gpu: Option<Box<dyn GpuBackend>>`, `gpu_adapters: Vec<GpuAdapter>`, `gpu_detector: Option<GpuDetector>` (the `GpuDetector` alias moves to `gpu/mod.rs` as `pub(crate) type GpuDetector` and `linux.rs` imports it — the ONE allowed touch of `linux.rs`: the alias line `:107` becomes a `use`; nothing else in that file); `Default` → `gpu: detect_backend()`, `gpu_detector: Some(Box::new(detect_backend))`; `#[doc(hidden)] pub fn with_gpu_backend(backend)` and `pub fn last_gpu_scan()` as in `linux.rs:160-170` (GPU-free otherwise — `Default` is the only constructor that touches the OS); in `collect()`: inside the `slow_due` branch after the cache is stored, the re-detect block copied from `linux.rs:205-213` (a `first_slow_tick` bool computed before the cache is replaced, as linux.rs does); after `native_processes` (`:86`) and before the `SystemSnapshot` literal: `let gpu_tick = self.gpu.as_mut().map(|gpu| { let samples = gpu.sample(); let busy = gpu.process_busy(); (samples, busy) });`; after the literal is built: `if let Some((samples, busy)) = gpu_tick { attach_gpu(&mut snapshot, &self.gpu_adapters, &samples, &busy); }`. `windows.rs` / `macos.rs` wrappers unchanged. New `tests/native_gpu.rs`: `#![cfg(any(target_os = "windows", target_os = "macos"))]` — ONE `#[ignore]` test `live_gpu_scan_cost_on_this_host` that builds `NativeCollector::default()`, collects three times 1.5 s apart, prints `snapshot.gpus` as JSON and `last_gpu_scan()` with `--nocapture` (the orchestrator's acceptance instrument, mirroring T15's), and ONE non-ignored test `with_gpu_backend_attaches_samples_and_pid_busy` using a fake `GpuBackend` (two adapters, one pid) proving the wiring — it runs only on those OSes (the cross-target `cargo check --tests` compiles it). On Linux this file compiles to nothing — say so in the report.

**F6 — docs (yours).** `README.md` `## GPU`: two new rows/paragraphs — **Windows**: DXGI enumerates adapters (software adapters and vendor `0x1414` dropped), PDH `\GPU Engine(*)\Utilization Percentage` (English counter names, first sample discarded, Task Manager's busy rule, per-process `gpuPercent`), memory = DXGI local-segment usage / dedicated video memory, temperature absent, requires Windows 10 1709+ for the GPU counters, a PDH LUID with no DXGI adapter is ignored; **macOS**: IOKit `IOAccelerator` `PerformanceStatistics`, the three keys, absent keys → absent metrics, no per-process value, temperature absent, "runtime-verified on: —" until the orchestrator fills it. `INSTALL.md`: "Windows / macOS GPU: nothing to install; the daemon uses DXGI + PDH / IOKit in-process." `ARCHITECTURE.md`: the GPU backends line gains the two OSes. `docs/guides/API.md`: `gpus[].temperatureC` present on Linux only; `processes[].gpuPercent` present on Linux and Windows. The vetting report (F1). No ADR edit (Fable's amendment is already on main); no plan edit.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green — paste EVERY `test result:` line and COUNT them yourself: the 0.5.4 gate counted 344 passed / 2 ignored across 27 suites; your F2 tests land in the `tinytop-collectors` unit suite and `native_gpu` adds a suite with 0 tests on Linux — **do not trust the orchestrator's arithmetic (T15-fix1 escalated on exactly that); report your own sum**; **the cross-target proofs** `cargo check --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors --tests --no-default-features --features tinytop-collectors/windows-collector --target x86_64-pc-windows-msvc --offline` and `cargo check --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors --tests --no-default-features --features tinytop-collectors/macos-collector --target aarch64-apple-darwin --offline` clean (paste both; `--tests` so `native_gpu.rs` and the F5 wiring compile; if `--offline` fails because a `windows` feature module is not in the registry cache, paste the error and ESCALATE — do not go online); `bun test tests/dashboard-ladder.test.ts` still green (you changed no JS); `git diff agent/Cargo.lock | grep -c '^+name'` prints `0`; `grep -rn 'Command::new\|std::process' agent/crates/tinytop-collectors/src` empty; `git status --short` and `git diff --stat` pasted (⊆ the allow-list; the two new pure modules, the new test file and the vetting report listed).

**Report shape:** per finding the RED line(s) you observed → the GREEN summary; the test names; the base commit; the exact instance-name grammar your parser accepts and the nine engine types you tested; the seven IOKit symbols with their signatures; both cross-target outputs; the lock diff; what is verified (Linux gate + cross-check) versus what only hardware can verify (listed explicitly); anything outside this brief you noticed under `## Outside this lane` — never fixed silently. **Contingencies (plan §4 applies):** if `IDXGIAdapter3`, `QueryVideoMemoryInfo`, `PdhGetFormattedCounterArrayW` or `PDH_FMT_COUNTERVALUE_ITEM_W` is not where the fact sheet §C says in `windows 0.62.2` → paste the `grep` and ESCALATE; if `core-foundation 0.10.1` lacks a wrapper you need (`CFDictionary` iteration, `CFData` bytes, `CFNumber` → f64/i64 all exist) → ESCALATE rather than reaching for `core-foundation-sys` directly; if the plan and this brief disagree → this brief (it carries the 2026-08-30 amendments) and say so. **Default:** anything not covered here — STOP and reply `ESCALATE: <what, what you tried, where you stopped>`.

The orchestrator's own acceptance (outside your sandbox, after the merge, before the 0.6.0 notes): a native `cargo build --release -p tinytop-agent` on wizai (`C:\src\tinytop`) through `powershell.exe`; `tinytop-agent collect --json` three times → `gpus[0]` = the 8060S with `stable_id dxgi-1002:1586:…-0`, busy `null` on the first primed tick then a number, `memoryTotalBytes 890077184`, no `0x1414` adapter, no phantom for `0x3FC20ECC`; under a GPU load `busyPercent > 0` and the load pid's `gpuPercent`; `native_gpu` with `--nocapture` for the scan cost over the 1,504 instances; on miniwiz the same when it is reachable, with the `ioreg` dump captured into the fact sheet first.

## Fixture truth (fact sheet §A, verbatim, pasted 2026-08-30 02:58Z)

### A. wizai (the Windows acceptance target) — measured

| fact | value |
| --- | --- |
| OS | Windows `10.0.26200` build **25H2** |
| physical adapter (`Win32_VideoController`) | **AMD Radeon(TM) 8060S Graphics**, `PCI\VEN_1002&DEV_1586&SUBSYS_801D2014&REV_C1`, `AdapterRAM` 1,073,741,824 (WDDM's 32-bit-capped figure — not the truth), driver `32.0.31041.1004`, status OK |
| PDH counter sets present | `GPU Engine` (**1,504** instance paths), `GPU Adapter Memory` (9), `GPU Local Adapter Memory` (3), `GPU Non Local Adapter Memory` (1), `GPU Process Memory` (270) |
| LUIDs seen by PDH | **three**: `0x00000000_0x000120CC`, `0x00000000_0x3FB5EAE5`, `0x00000000_0x3FC20ECC` |
| engine types present (`engtype_…`) | `3D`, `Compute`, `Compute 0`, `Copy`, `Security 1`, `Timer 0`, `Video Codec Engine`, `Video JPEG 0`, `Video Processor` — **spaces and trailing digits are part of the type name**; a parser must take everything after `engtype_` up to the closing `)` |
| `GPU Engine` instance form (verbatim, from `PathsWithInstances`) | `\GPU Engine(pid_100772_luid_0x00000000_0x000120CC_phys_0_eng_0_engtype_3D)\Utilization Percentage` (also `eng_10`, `eng_11`, … — `eng_<k>` is a per-adapter engine index, not the type) |
| `GPU Adapter Memory` instance form | `\GPU Adapter Memory(luid_0x00000000_0x3FB5EAE5_phys_0)\Dedicated Usage` — counters `Shared Usage`, `Dedicated Usage`, `Total Committed` |
| **case of the LUID hex** | `Get-Counter -ListSet` paths print UPPERCASE hex (`0x3FB5EAE5`); `CounterSamples[].InstanceName` returns **lowercase** (`luid_0x00000000_0x3fb5eae5_phys_0`). The join must be case-insensitive. |
| the DirectX adapter database (`HKLM\SOFTWARE\Microsoft\DirectX\{guid}` — what DXGI reports, cached by the D3D runtime) | `Microsoft Basic Render Driver` luid `0x00000000000120CC`, vendor **`0x1414`**, device `0x008C`, dedicated 0, shared 108,789,186,560 · `AMD Radeon(TM) 8060S Graphics` luid `0x000000003FB5EAE5`, vendor `0x1002`, device `0x1586`, **dedicated 890,077,184** (the unified-memory carve-out — an APU), shared 108,789,186,560 · **no entry for `0x3FC20ECC`** |
| ⇒ the LUID census | one real adapter (`3FB5EAE5`), one `0x1414` software adapter (`120CC`), **one PDH LUID that DXGI does not enumerate at all** (`3FC20ECC`, zero usage, no registry entry). The plan's Task 16 line "the two `0x1414` software adapters must be filtered" was wrong by one: the join DXGI ← PDH must **drop an unknown LUID silently and count it**, not assume every PDH LUID has an adapter. |
| idle sample (1 s, 02:4xZ, no interactive session) | every `GPU Engine` utilization cooked value 0 (nothing to aggregate — the load proof is an acceptance step); `Dedicated Usage` = **792,592,384** on `3fb5eae5`, 0 on the other two; `Shared Usage` 4,561,338,368 on `3fb5eae5`, 8,192 on `120cc`, 0 on `3fc20ecc` |
| not measured | a non-zero engine sample under load (needs a GPU load on his desktop — acceptance, not a read-only probe); DXGI `QueryVideoMemoryInfo` values (need the code) |
