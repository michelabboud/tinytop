You are hexe lane **T10-fix2** for tinytop (a small dashboard-only fix on top of `v0.4.1` = `f8b8b43`; the commit above it is docs only). One finding from Fable's 0.4.1 acceptance pass (Fabulous `docs/fleet/tinytop/2026-08-29-t10-acceptance-checklist.md`, observation 1). Files you may edit: `agent/assets/dashboard/ladder-rules.js` and `tests/dashboard-ladder.test.ts` — NOTHING else (no `app.js`, no `CHANGELOG.md` — the orchestrator writes the changelog line at merge; no Rust; no `src/`). Git is read-only for you; the orchestrator commits. Escalate (`ESCALATE: …`) rather than improvise.

**What exists (verify first):** `describeImportPlan(plan, candidateLadder, previousSettings, { includeOtherChanges = true } = {})` at `ladder-rules.js:189-233` returns `string[]`: one line per non-zero `wouldDelete` field, the four disable/stop transition lines, then — only when `includeOtherChanges` — `also changes: <changedKeys minus retentionLadder>` and the plan's `warnings`. Callers: `saveDaemonSettings` (`app.js:2896`, passes `{ includeOtherChanges: false }`; shows NO dialog when the array is empty — fix1 F1, keep it) and `importSettingsFile` (`app.js:3003`, default options; ALWAYS shows the confirmation with `lines.join("; ")` as the body). Tests: `tests/dashboard-ladder.test.ts` `describe("settings transfer plan description")` (`:436-…`).

**The defect (observed on a fresh database):** a `retentionLadder` change whose counts are all zero (e.g. L2 30 → 12 d with nothing older than 12 d) produces no ladder line and `retentionLadder` is filtered out of "also changes", so the import confirmation named only the theme; an import that changes *only* the ladder with zero counts opens a confirmation with an **empty body**; an import identical to the current settings does the same.

**F1 — Two new lines, import path only (`includeOtherChanges === true`), appended in this order after the existing ones:**
1. When `Array.isArray(plan?.changedKeys) && plan.changedKeys.includes("retentionLadder")` and no ladder line was produced (no count line, no transition line) → push `retention ladder changes — no stored history is affected`. Implement by remembering `lines.length` before the count/transition section (a boolean `ladderDescribed`), not by string-matching lines.
2. When `Array.isArray(plan?.changedKeys) && plan.changedKeys.length === 0` and `lines` is still empty after everything else → push `no settings change — the document matches the current settings`.
The save path (`includeOtherChanges: false`) is untouched: it still returns `[]` for a zero-impact change so no dialog appears (assert it). `also changes:` keeps excluding `retentionLadder`.

**F2 — Tests (TDD: paste RED, then GREEN), new cases in the existing `describe` block:** `names a zero-impact ladder change on the import path` (candidate L2 12 vs previous 30, all counts 0, `changedKeys: ["retentionLadder", "defaultTheme"]` → exactly `["retention ladder changes — no stored history is affected", "also changes: defaultTheme"]`); `keeps a save preview silent for a zero-impact ladder change` (same plan with `{ includeOtherChanges: false }` → `[]`); `does not add the ladder line when a count already describes it` (`l2Buckets: 5` → `["5 L2 buckets", "also changes: defaultTheme"]`, no extra line); `does not add the ladder line when a transition describes it` (L3 enabled → disabled, counts 0 → the existing "L3 disabled — …" line only); `describes an identical document` (`changedKeys: []`, counts 0 → `["no settings change — the document matches the current settings"]`); `stays silent on the save path for an identical document` (`{ includeOtherChanges: false }` → `[]`).

Acceptance: `bun test tests/dashboard-ladder.test.ts` RED (the new cases) then GREEN with the whole file passing; `bun run check:bun` (the `port: 0` sandbox failure in `tests/server.test.ts` is a known boundary — say so; everything else green); `git diff --stat` = the two files.
