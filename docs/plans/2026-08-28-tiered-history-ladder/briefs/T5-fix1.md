You are hexe lane **T5-fix1** for tinytop: a FIX ROUND on Task 5 ("Dashboard — presets, ladder settings, shrink confirmation, coverage card") after its lane escalated on one forbidden line (hexe run 555) and its blind review (ari-luna run 558, validated by Fable). Your branch starts from the T5 lane branch (`tinytop/ladder-t5-dashboard` @ `ea8c6f9`, everything below already exists). Do ONLY F1–F7. Lane **T4** (Rust read API in `tinytop-store` + `writer.rs` API handlers) runs on another branch — do not touch `agent/crates/tinytop-store/` or any `writer.rs` line outside F1.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` — **§11 (AMENDED 2026-08-28: every preset from `6h` up is `source=auto&limit=10000`; a preset is disabled only when NO enabled tier holds its start and the archive is not queryable)**, **§10 (the `auto` rule as amended — what the server returns: `source` ∈ `raw|rollup|5m|1h|archive` plus `resolutionMs`; `archive` is an empty page with `available:false` until 0.4.0)**, **§13 (AMENDED: Bun's `settings.ts` allowed windows = the same ten as Rust; Bun has no ladder → the dashboard hides the group)**, §4 (the ladder; the "source of truth" column now reads via `auto`), §5 (message strings — source of truth `agent/crates/tinytop-store/src/retention_ladder.rs::validate`).
2. The plan's **Global Constraints**, **Context & Rationale**, **Task 5** (Step 1 amended) and **§Contingencies** (the two 2026-08-28 bullets about T5 — read them; they are the why).
3. Code (line numbers = `ea8c6f9`): `agent/assets/dashboard/ladder-rules.js` (`HISTORY_WINDOWS` :4-15, `historyWindowFor` :50, `validateRetentionLadder` :115, the pressure hedge :161-164); `agent/assets/dashboard/app.js` (import :1; `grep -n "disk.pressure ?? disk.active"` — one hedge; `historyDiskPressure` :301; the settings normaliser :496-525 with `hasProvidedLadder` :496; the ladder form :2412-2450 and :2552; `fetchHistoryWindow` — its `windowConfig.source === "rollup"` branch and the raw paging loop; `hydrateHistoryPoints`; the `sample.source === "rollup"` checks at :855 and :1560); `agent/assets/dashboard/index.html` (`#history-ladder-coverage` :261, the `history-ladder-settings-group` fieldset :560); `src/settings.ts` :63 (`allowedHistoryWindows`) — and `grep -l "defaultHistoryWindow" tests/*.test.ts` to find where Bun already tests it (there is no `tests/settings.test.ts`; report which file you used); `agent/crates/tinytop-agent/src/writer.rs` `static_relative_path` :597-606 and its `#[cfg(test)] mod tests`; `agent/crates/tinytop-agent/tests/serve_contract.rs` :83-90; `tests/dashboard-ladder.test.ts` :33-106; `tests/dashboard-settings.test.ts`; `tests/dashboard-assets.test.ts`.

Fixes (each: failing test first, paste RED, implement, paste GREEN):
- **F1 — Rust serves the module.** `writer.rs` `static_relative_path`: add `"/ladder-rules.js" => Some(Path::new("ladder-rules.js")),` after the `/vendor/echarts.min.js` arm — this ONE arm is your only `writer.rs` edit besides its unit test. Tests: (a) in `writer.rs`'s `#[cfg(test)] mod tests`: `static_relative_path_serves_ladder_rules` asserting `static_relative_path("/ladder-rules.js") == Some(Path::new("ladder-rules.js"))` — RED before, GREEN after (that is your honest RED); (b) `serve_contract.rs`: beside the `/app.js` assertion add `.and_then(|_| http_get(port, "/ladder-rules.js")).map(|response| assert!(response.contains("historyWindowFor")))` — sandbox-denied for you like its siblings; say so; Fable runs it outside.
- **F2 — one name for disk pressure.** The coverage `disk` object is spec §10's: `{freeBytes, minFreeBytes, pressure, lastCheckMs}`. `validateRetentionLadder`'s third argument IS that object: read `Boolean(diskPressure?.pressure)` only — delete the `active ??` hedge at `ladder-rules.js:161` and the `disk.pressure ?? disk.active` hedge in `app.js`. Tests (RED first, `tests/dashboard-ladder.test.ts`): `{active:false, pressure:true, freeBytes:1000, minFreeBytes:5000}` with a ladder that grows → the EXACT refusal message `disk pressure active: free 1000 < minFreeBytes 5000; shrink first or free disk`; `{pressure:false, freeBytes:1000, minFreeBytes:5000}` with the same growth → allowed.
- **F3 — an unavailable default preset falls back.** When coverage arrives and the selected window is disabled per `historyWindowFor`, select the nearest FINER available preset (walk `all → 1y → 90d → 30d → 7d → 24h → 6h → 1h → 15m → live` starting just below the requested key; first available wins; `live` is always available), refetch, persist nothing, keep the disabled tooltip on the original button. Pure helper `export function fallbackWindowKey(key, coverage)` in `ladder-rules.js`; `app.js` calls it where coverage is applied to the window buttons. Bun tests: `1y` with L4 disabled (L3 keeps 90 d, archive off) → `"90d"`; `1y` with L4 disabled AND L3 keepDays 30 → `"30d"`… (compute with the amended §11 rule); every tier disabled except L2 keep 7 → `"7d"`; a coverage without `tiers` (older runtime) → the requested key unchanged.
- **F4 — Bun has no ladder.** (a) `src/settings.ts:63`: `allowedHistoryWindows` = `live, 15m, 1h, 6h, 24h, 7d, 30d, 90d, 1y, all` (identical to `lib.rs:181`); Bun test in the file that already tests `defaultHistoryWindow`: `"90d"` accepted, `"2d"` rejected with the existing message text. (b) Dashboard: when `GET /api/settings` returns no `retentionLadder` key (`hasProvidedLadder` :496 is the flag — do NOT synthesise a ladder for the FORM from legacy keys when the runtime returned none; the internal normalised copy may keep deriving one for the confirmation maths), hide the History ladder fieldset (:560) and `#history-ladder-coverage` (:261) and show one muted line in the fieldset's place: `History ladder — Rust daemon only` (give it `id="history-ladder-unavailable"`); the PUT body then omits `retentionLadder` entirely; the legacy inputs stay editable on that runtime exactly as before T5. Tests: `tests/dashboard-settings.test.ts` — a settings document without `retentionLadder` → the PUT payload builder omits the key (through the pure helpers), and the HTML contract asserts `history-ladder-unavailable` exists.
- **F5 — presets from `6h` up use `auto` (spec §11 amended; the reason: one page is ≤ 10,000 buckets — `30d` at 1 m silently showed the newest 6.9 d; `90d` at 5 m would show 34.7 d).** `HISTORY_WINDOWS`: `6h, 24h, 7d, 30d, 90d, 1y, all` → `source: "auto"`, `pageSize: MAX_HISTORY_PAGE_SIZE`; `live/15m/1h` unchanged (raw, paged). `fetchHistoryWindow`: every non-raw preset goes through `fetchHistoryPoints({ sinceMs, untilMs, limit, source: "auto" })` — ONE page, no paging loop for `auto`. Render by the RETURNED `point.source` / response `resolutionMs`: `rollup`, `5m`, `1h` render as rollup-style points with their `sampleCount` (replace the two `=== "rollup"` checks with `!== "raw"`); `archive` with `available:false` renders the empty state `Archive not available until 0.4.0` (no error). `historyWindowFor(key, coverage)` per the amended §11: disabled iff no enabled tier in `coverage.tiers` holds the preset's start (`keepDays × 86_400_000 ≥ durationMs`; L4 `keepDays 0` = forever) AND `coverage.archive?.queryable?.enabled` is not true; reason = `retentionLadder.<coarsest disabled tier>.enabled` when a disabled tier would have held it, else `retentionLadder.<coarsest enabled tier>.keepDays`; raw presets disabled when `snapshotJsonOldestMs` is null (unchanged); `all` unchanged; a coverage without `tiers` (older runtime) → never disabled (unchanged); a tier ABSENT from `coverage.tiers` (record missing while `tiers` exists) counts as NOT holding the start (luna 558 P2 — today `ladder-rules.js:73-76` treats it as available). Rewrite the Step-1 tests (RED first): the sources table (`raw,raw,raw,auto,auto,auto,auto,auto,auto,auto`); `1y` with L4 disabled → `{disabled:true, reason:"retentionLadder.l4.enabled"}`; `90d` with L3 disabled and L4 enabled → `{disabled:false}`; `90d` with L3 keepDays 30, L4 disabled → `{disabled:true, reason:"retentionLadder.l4.enabled"}`; `30d` with every tier enabled but L2 keep 7, L3 keep 14, L4 keep 20 → `{disabled:true, reason:"retentionLadder.l4.keepDays"}`. Docs: `README.md:377`, `docs/sqlite-history-architecture.md:398`, `GUIDE.md` history section — the amended mapping with the worked resolutions at the defaults (6h → 1 m, 360 · 24h → 1 m, 1,440 · 7d → 5 m, 2,016 · 30d → 5 m, 8,640 · 90d → 1 h, 2,160 · 1y → 1 h, 8,760 · all → the coarsest tier holding the oldest data, newest ≤ 10,000 buckets ≈ 416 d at 1 h; the archive holds the rest). CHANGELOG under `## Unreleased`: one line per fix, F5's saying that before it `30d` silently showed the newest 6.9 days.
- **F6 — GUIDE timeline list (luna 558 P3, confirmed).** `GUIDE.md:136`: the timeline walkthrough lists presets only through `30d` — list all ten (`Live, 15m, 1h, 6h, 24h, 7d, 30d, 90d, 1y, All`) and say in one clause that presets from `6h` up need the Rust daemon (F7).
- **F7 — honest presets on the Bun runtime (luna 558 P1-3c, true by design — spec §13).** Bun's `src/server.ts` serves `/api/history` only; `/api/history/points`, `/coverage` and `/markers` answer 404 `API route not found` (pre-existing: the `6h`–`30d` presets never worked on Bun; the `catch` in `fetchHistoryWindow` swallows it). Do NOT add those routes to Bun. Instead: when `GET /api/history/coverage` is not 2xx, record `state.historyCoverage = { unavailable: true }` (or the equivalent flag where coverage is stored) and `historyWindowFor(key, coverage)` with `coverage.unavailable === true` disables every preset from `6h` up with `reason: "runtime"` and the tooltip text `History presets beyond 1h need the Rust daemon`; `fallbackWindowKey` then lands on `1h`/`15m`/`live`. Raw presets keep working (Bun serves `/api/history`). Pure tests: `historyWindowFor("6h", { unavailable: true })` → `{disabled:true, reason:"runtime"}`; `historyWindowFor("1h", { unavailable: true })` → enabled; `fallbackWindowKey("30d", { unavailable: true })` → `"1h"`.

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only.
- No new dependencies. No edits outside: `ladder-rules.js`, `app.js`, `index.html`, `styles.css` (only if F4's line needs a token), `src/settings.ts` (F4a only), `writer.rs` (F1's one arm + one unit test), `tests/serve_contract.rs` (F1's assertion), `tests/dashboard-*.test.ts`, the Bun settings test file you identified, `README.md`, `GUIDE.md`, `docs/sqlite-history-architecture.md`, `CHANGELOG.md`. Do not touch `agent/crates/tinytop-store/`.
- Every validation message stays byte-identical to `retention_ladder.rs`; the dashboard never invents a message.

Gate to run and paste in full: `bun run check` (from the repo root; `check:bun` for your tests, `check:rust` because the binary embeds your assets). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests bind a local port your sandbox denies (`PermissionDenied: should reserve a local port`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the full gate outside the sandbox before merge and runs the served-dashboard checklist. Any other failure is yours. Do not ESCALATE on a sandbox-denied bind.

Report (your final message), in this order: files changed with line ranges · tests added/rewritten (names) · RED output per fix · GREEN output · full gate output · `git diff --stat` · docs updated · which Bun file carries the F4a test · anything the brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
