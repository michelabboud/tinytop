You are hexe lane **T2-fix1** for tinytop — the fix round for **Task 2** after review (luna, hexe run 545, validated by Fable). Your worktree branch `tinytop/ladder-t2-fold-freeze-fix1` starts from `tinytop/ladder-t2-fold-freeze` (T2's work, commit `80ff897`).

Read first: spec `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §8 (fold) and §9 (the freeze rule: a completed bucket is never rebuilt from a partial tail); plan Task 2; then `agent/crates/tinytop-store/src/lib.rs` `insert_snapshot` (the calls at ~:556-563) and `rebuild_rollup_bucket` (~:1351-1420, it recomputes the minute `FROM metric_samples`), `src/maintenance.rs` `refold_ancestors_for_late_write`, `src/ladder.rs` `fold`/`raw_to_bucket`, `tests/ladder_maintenance.rs` (`late_write_refolds_ancestors`, `decimation_regression_completed_minute_keeps_its_sample_count`).

Rules (non-negotiable, same as T2): git is READ-ONLY (no add/commit/checkout/stash); never bare `cargo fmt` — `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`, `rustfmt --edition 2024 <file>` only on files you changed; **never open `~/.local/share/tinytop/`**; no new dependencies. **Allowed files, nothing else:** `agent/crates/tinytop-store/src/lib.rs`, `src/maintenance.rs`, `src/ladder.rs`, `tests/ladder_maintenance.rs`, `docs/sqlite-history-architecture.md`, `CHANGELOG.md`.

**F1 (P1) — the freeze rule is violated by the insert path.** `insert_snapshot` always rebuilds the sample's minute from raw rows. When that minute's raw rows were already pruned (it is older than the L1 horizon) or partially pruned (the minute containing the L1 cutoff), the rebuild replaces a frozen 1-minute bucket of `sample_count` 40 with a bucket computed from the surviving tail (1, or 17) — the decimation defect re-entering through the front door. Fix, in `insert_snapshot`: after the raw upsert, read the existing `metric_rollups_1m` row for the minute (if any) and the raw row count now present for the minute; **if an existing bucket has `sample_count` greater than that raw count, the raw set is provably partial → do NOT rebuild; instead upsert `fold(bucket_start, &[existing_bucket, raw_to_bucket(&new_sample)])`** (count +1, count-weighted averages, true min/max, `first`/`newest` extended). Otherwise (open minute, or a duplicate-timestamp upsert where counts are equal) rebuild from raw exactly as today. Then `refold_ancestors_for_late_write` runs as today. Keep the decision as a pure function in `ladder.rs` (`pub fn raw_is_partial(existing_count: i64, raw_count_now: i64) -> bool`) with unit tests on both sides (40 vs 1 → true; 40 vs 17 → true; 5 vs 6 → false; 5 vs 5 → false; no existing bucket → false).

Tests in `tests/ladder_maintenance.rs`, RED first (paste), then GREEN (paste):
- `late_write_into_a_pruned_minute_merges_instead_of_rebuilding`: 40 samples in minute 0 (1.5 s apart, cpu 10.0), then `maintain_with_config` with an `l1_keep_ms` and `now` that prune minute 0's raw rows entirely (assert raw count for minute 0 == 0 and L2 bucket 0 `sample_count == 40` before the late write); insert one late sample at minute 0 + 10 s with cpu 90.0 → L2 bucket 0 has `sample_count == 41`, `cpu.avg == (10.0*40 + 90.0)/41` within 1e-9, `cpu.max == 90.0`, `cpu.min == 10.0`; the containing 5 m and 1 h buckets gained exactly 1. On current code this yields `sample_count == 1` — that is the RED.
- `late_write_into_the_boundary_minute_merges_instead_of_rebuilding`: same, but prune so that 16 raw samples of minute 0 survive (reuse the decimation fixture's arithmetic); late write → 41, not 17.
- The existing `insert_rebuilds_its_minute_with_min_and_max` and duplicate-timestamp store tests must still pass unchanged (open minute keeps rebuilding).

**F2 (docs):** `docs/sqlite-history-architecture.md` §Rollups: state the rule ("a minute whose raw rows are partial is folded, never rebuilt"); `CHANGELOG.md` under the existing `## 0.2.8` heading: one bullet naming the review finding and the RED→GREEN pair.

**Not in scope (T3 owns it):** the one-tick staleness of the tier-enabled flags after a settings change (`lib.rs:564` area) — do not touch.

Gate to run and paste in full: `bun run check:rust`. Known sandbox limit: the 10 `serve_contract` tests in `tinytop-agent` bind a local port your sandbox denies — if those 10 are the ONLY failures, say so verbatim; Fable re-runs outside. Any other failure is yours.

Report (final message), in this order: files changed with line ranges · per item: test names, RED output, GREEN output · full gate output · `git diff --stat` · docs updated · anything this brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
