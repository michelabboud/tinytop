You are hexe lane **T9-fix1** for tinytop. Your base is the T9 lane branch `tinytop/ladder-t9-disk-check` at `413d8c2` (disk check, run 598, validated by Fable). Fix ONLY F1–F5 below; do not revisit the design (ADR 0020 stands). Scope rules: no edits outside `agent/crates/tinytop-store/src/disk.rs`, `agent/crates/tinytop-store/src/lib.rs` (one helper), `agent/crates/tinytop-agent/src/writer.rs` (F5 only), `agent/crates/tinytop-store/tests/disk_check.rs`, `README.md`, `GUIDE.md`, `docs/sqlite-history-architecture.md`, `CHANGELOG.md`. `Cargo.toml`/`Cargo.lock`, `retention_ladder.rs`, `main.rs`, `app.js`, ADR 0020 and `PROGRESS.md` are untouchable.

Read first: `CLAUDE.md`; `briefs/T9.md` (the contract you are amending); `git log -1` (what Fable verified); ADR 0020; the luna review's findings as ruled by Fable (below).

**F1 — P2 (robustness): the decision inputs are read outside the transaction.** `apply_disk_measurement` (`disk.rs` ~`:114-145`) reads `database_bytes` and the previous `diskPressure` state BEFORE `store.pool.begin()`, so two concurrent callers can both observe "inactive" and both insert a `diskPressure` marker (serialized by construction in the daemon — one scheduler task — but the function is `pub`). Ruling: make the check a single atomic read-modify-write. Add `pub(crate) async fn history_state_get_on<T: DeserializeOwned>(connection: &mut SqliteConnection, key: &str) -> Result<Option<T>, StoreError>` in `lib.rs` beside `history_state_set_on` (the SQL of `history_state_get` `:689` moved into it; `history_state_get` becomes the one-line wrapper — same pattern as `record_event`/`record_event_on`). In `apply_disk_measurement`: acquire ONE connection from the pool, run `sqlx::query("BEGIN IMMEDIATE")` on it (the pattern `archive.rs:1208` uses), THEN read the previous state with `history_state_get_on`, compute the transition, write the two `history_state` rows and the marker, `COMMIT` (raw `sqlx::query("COMMIT")`, with `ROLLBACK` on any error before returning it — mirror `archive.rs`'s handling so a failed write never leaves the connection inside an open transaction when it returns to the pool). `database_bytes` may stay outside (it is metadata for the marker details, not a decision input) — say so in a comment. The undeterminable early return stays BEFORE any pool access. Test `concurrent_checks_record_one_marker`: from one store, run two `apply_disk_measurement(.., Ok(100), ladder(min 200), now)` calls concurrently with `tokio::join!` (the `max_connections(1)` pool serializes them) and assert exactly ONE `diskPressure` marker, state `active:true`, `sinceMs == now`; then two concurrent `Ok(300)` calls → exactly ONE `diskRecovered` marker. Paste RED (two markers on the un-fixed code — if the single-connection pool already serializes the un-fixed code so the RED does not show, say so and keep the test as the guard) and GREEN.

**F2 — P3 (test gap): marker read-back.** `tests/disk_check.rs` queries `app_events` directly and never exercises `HistoryMarkerType::from_storage`; a regression rejecting `diskPressure` would go unnoticed (and would make EVERY `read_history_markers` call fail once one row exists). Add `disk_markers_read_back_through_the_markers_api`: after a breach and a recovery, `store.read_history_markers(HistoryQuery { since_ms: None, until_ms: None, limit: Some(10) }, 60_000)` returns both rows with `marker_type == HistoryMarkerType::DiskPressure` / `DiskRecovered` in time order (check `HistoryQuery`'s real field names).

**F3 — P3 (test gap): the undeterminable test compares `value_json` only.** Extend `undeterminable_free_bytes_keeps_state_and_returns_error` to snapshot and compare the FULL `history_state` rows (`state_key, value_json, updated_at_ms`) for `diskPressure` AND `lastDiskCheckMs` before and after the failing call, and the full `app_events` table (count + rows).

**F4 — P3 (docs).** `README.md` feature bullet (~`:248`, "daemon-start, settings-change, migration, and coverage-gap markers") and `GUIDE.md` "Persisted in SQLite" list (~`:160-168`, "daemon timeline events for starts and settings changes") name the two disk markers too. `docs/sqlite-history-architecture.md` `## Disk check`: the marker-rate sentence says the bound is per continuous daemon run — the immediate check after a restart may write a marker sooner than one interval after the previous run's last marker (ADR 0020's consequence, stated precisely; ADR 0020 itself is NOT edited).

**F5 — P3 (robustness): `spawn_disk_check_loop` sleeps `interval_minutes as u64 * 60`.** The document is validated on save, but a hand-edited row can hold a negative or huge value; `as u64` on a negative wraps to an eternal sleep. Clamp to the validated range (5–1_440 minutes — reuse the same constants `validate` uses if they are reachable, else literal with a comment naming `validate`) and `eprintln!` once per tick when the stored value was out of range.

`CHANGELOG.md` `## Unreleased`: one bullet for F1 (atomic read-modify-write under `BEGIN IMMEDIATE`) and one for F5.

Rules (non-negotiable): git READ-ONLY (no add/commit/push/checkout/stash/branch; leave the tree dirty; report `git diff --stat`) · never bare `cargo fmt` (`cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; `rustfmt --edition 2024 <file>` for a file you changed) · never open `~/.local/share/tinytop/` or `~/.config/systemd/user/` · tests in `std::env::temp_dir()` only · `Cargo.toml`/`Cargo.lock` unchanged.

Gate: `bun run check` at the repo root, pasted in full. Known sandbox limits (say so verbatim; treat as passed-for-you if these are the ONLY failures): (1) the 10 `serve_contract` tests bind a port (`PermissionDenied`); (2) Bun's `tests/server.test.ts` `Bun.serve({ port: 0 })`; (3) `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Anything else is yours.

Report (final message, in order): files changed with line ranges · F1 RED/GREEN transcripts · `cargo test -p tinytop-store --test disk_check` summary · full gate output · `git diff --stat` · anything this brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
