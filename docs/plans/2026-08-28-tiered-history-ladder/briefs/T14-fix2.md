You are hexe lane **T14-fix2** for tinytop — the fix round after luna's blind review (hexe run #667) of lane T14 + T14-fix1 (the store half of Task 14; ADR 0024 and its two **Amendments 2026-08-30** at the end of `docs/adr/0024-schema-v3-typed-history-identity-interning-filesystems-on-change.md` — both are on `main` only, not on your base; their text is reproduced below where it matters). Base: branch `tinytop/phase5-t14-schema-v3` at **`6038bec`** (your worktree was created from it; `git rev-parse HEAD` first and quote it). Every `file:line` below was read at `6038bec`; `sed -n` / `grep -n` each before you build on it. Build ONLY F1–F4. Files you may edit: `agent/crates/tinytop-store/src/lib.rs` (F1 only), `agent/crates/tinytop-store/src/migration.rs` (F4 only — one string), `agent/crates/tinytop-store/tests/history_assembly.rs`, `agent/crates/tinytop-store/tests/migration_v3.rs`, `docs/sqlite-history-architecture.md`, `docs/guides/API.md`. NOTHING else — no `tinytop-types`, no `tinytop-agent`, no `Cargo.*`, no `agent/assets/`, no `tests/*.ts`, no `src/`, no `legacy/`, no `README.md`, no `INSTALL.md`, no `VERSION`, no `CHANGELOG.md` (Fable writes the line at merge), no `docs/adr/*`, no plan. **Git is read-only for you — no commit, no branch, no stash, no bare `cargo fmt` (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` and fix by hand); the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; `std::env::set_var` is forbidden. **Known sandbox limits — not failures:** every `serve_*` test binds a port your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); no crates.io — `--offline` works. If a contract below contradicts the code you read, STOP and reply `ESCALATE: <the line, what the code says, what you tried>`.

**F1 — the regressing-stamp discard gets its diagnostic (`lib.rs:1580-1590`; ADR 0024 amendment #2).** Today `insert_snapshot` processes the filesystem domain only when `previous_fs_state.last_key_ms.is_none_or(|last| fs_key_ms > last)` (`:1585-1587`) — correct and pinned by `regressing_filesystem_key_cannot_reintroduce_a_mount_into_future_history` (`tests/history_assembly.rs:411`), but SILENT. Add a rate-limited warning for the REGRESSION case only (`fs_key_ms < last_key_ms`; EQUAL is the normal steady state — no new enumeration — and must never warn): mirror the process-warning mechanism exactly — a new field `filesystem_warning_last_ms: Arc<Mutex<Option<i64>>>` beside `process_warning_last_ms` (`:627`, initialised at `:1141` and `:1181`), decided inside the transaction as a `bool` but EMITTED AFTER `transaction.commit()` (`:1616`) the way the process warning is (`:1644-1652`), at most once per 60 s of `captured_at_ms`, text exactly: `history writer warning: filesystem enumeration stamp {fs_key_ms} is older than the last processed stamp {last_key_ms}; the sample's filesystems were not stored (metric row kept)`. No other behaviour changes: the metric row still commits with its stamp, no `fs_samples`/`fs_mount_events` row is written, `next_fs_state` keeps the newest stamp, the returned assembled row is unchanged. Evidence: run the pinned test with `-- --nocapture` and paste the warning line (there is no tracing subscriber; the pinned test's assertions stay as they are and must pass).

**F2 — the two test gaps (RED first where a RED is possible — paste it).** (a) `tests/migration_v3.rs`: new `migrated_and_fresh_v3_schemas_are_identical` — build the v2 fixture the way `v2_fixture_with_json_rows_migrates_to_v3` (`:63`) does (at least one JSON row and one `fs_samples` row so every table is exercised), `connect` (migrates), and a FRESH file via `connect`; for each of `metric_samples`, `host_identity`, `fs_mount_events`, `fs_samples`, `process_samples`, `process_samples_fast`, `process_commands` compare the COMPLETE ORDERED `PRAGMA table_info(<table>)` rows (`cid, name, type, notnull, dflt_value, pk`) migrated vs fresh with `assert_eq!` on the collected `Vec`s (a diff must name the table and the first differing row); also compare `PRAGMA index_list(metric_samples)` names (`idx_metric_samples_runtime_captured_at`, `idx_metric_samples_captured_at`, the autoindex) and `PRAGMA index_list(fs_samples)` names. The existing checks at `:105-135` stay. (b) `filesystems_carry_forward_until_the_mount_disappears` (`tests/history_assembly.rs:470-505`): besides membership, assert that the `/data` row assembled at 7 min is FIELD-BY-FIELD EQUAL to the `/data` `FilesystemSnapshot` in `at_zero` (`assert_eq!(carried_row, at_zero_row)` on the whole struct — every one of `filesystem, fs_type, size_bytes, used_bytes, available_bytes, used_percent, mount, inode_used_percent, inode_used, inode_total`), and that the `/` row at 7 min equals the `unchanged` snapshot's `/` row. If `FilesystemSnapshot` lacks `PartialEq`, compare field by field — do NOT edit `tinytop-types`.

**F3 — docs (every item confirmed at source by the orchestrator).** `docs/sqlite-history-architecture.md`: (i) the `fs_samples` DDL excerpt at `:236-250` names a `fs_key_ms` column that does not exist — replace the excerpt with the real DDL from `migration.rs:164-175` (`captured_at_ms INTEGER NOT NULL`, …) verbatim, plus one sentence: the value written into `captured_at_ms` is the enumeration key `fs_key_ms` (the snapshot's `filesystemsCapturedAtMs`, else the sample's `captured_at_ms`); (ii) `:388-389` "The migration guard refuses an in-progress v3 migration rather than allowing a second writer to rebuild the same table." is not true of the code — `migrate_v2_to_v3` is `pool.begin()` → `CREATE` → copy (`migration.rs:897-913`); replace with the truth: one transaction, a second writer is serialised by SQLite's write lock, any failure or crash rolls back, and the explicit guard is the post-backfill identity-count check (`:1035`); (iii) `:518` "`/api/history` still returns recent complete raw snapshots" → "`/api/history` returns recent snapshots assembled from the typed tables (no `cpu.times`, no pressure lines; `load.runnable`/`totalThreads`/`lastPid` are absent when the collector has no source)"; (iv) `### Filesystems on change` (`:398`): one paragraph stating the monotonic rule and the new warning (F1) — a stamp older than the last processed one keeps the metric row, stores no filesystem rows/events, warns once a minute; a stamp equal to the last one is the steady state. `docs/guides/API.md:304`: "`/api/history` still returns recent full raw snapshots" → the same assembled wording as (iii). Then `grep -rn -i -E 'full raw|complete raw' docs README.md` must show nothing (README.md is NOT yours to edit — if it matches, report it under `## Outside this lane`).

**F4 — one string (`migration.rs:1175`):** the u64→i64 overflow error's remedy still reads "the row was written by an unknown writer; inspect it with `db check`; the database was not modified" — `db check` is `PRAGMA integrity_check` and shows no row. Replace with the T14-fix1 wording used at `:947`: `a row this version cannot decode — back up the database, then clear that row's payload (UPDATE metric_samples SET snapshot_json = NULL WHERE sample_id = <n>; see INSTALL.md, Upgrade) and start again; the database was not modified`. `grep -rn 'unknown writer' agent/crates` must then be empty.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-store` green (paste EVERY `test result:` line; at `6038bec`: `migration_v3` 4 passed + 1 ignored → expect 5 + 1; `history_assembly` count unchanged with stronger assertions); `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green; the pinned regressing-key test run with `--nocapture` showing the F1 warning line; `git status --short` and `git diff --stat` ⊆ the six files; paste `sed -n` of the F1 block after the change; the two greps (F3, F4) pasted empty.

**Report shape:** per finding what you changed (`file:line` before → after), the new/changed test names with their RED (where possible) then GREEN output, the base commit, the gate output; anything outside this brief under `## Outside this lane` — never fixed silently.
