You are hexe lane **T15-fix1** for tinytop — the fix round after luna's blind review (hexe run #675) and the orchestrator's validation of lane T15 (`177e1ef` on `tinytop/phase5-t15-gpu-linux`; ADR 0025; `briefs/T15.md`). Base: branch `tinytop/phase5-t15-gpu-linux` at **`177e1ef`** (your worktree was created from it; `git rev-parse HEAD` first and quote it). Every `file:line` below was read at `177e1ef`; `sed -n` / `grep -n` each before you build on it. Build ONLY F1–F4. Files you may edit: `agent/crates/tinytop-collectors/src/gpu/linux.rs` (F1), `docs/sqlite-history-architecture.md` (F2, one line), `agent/crates/tinytop-store/tests/migration_v3.rs` (F3, one comment), `INSTALL.md` (F4, one phrase). NOTHING else — no Cargo files, no other source or test, no CHANGELOG/VERSION/ADR/plan. **Git is read-only for you — no commit, no branch, no stash, no bare `cargo fmt` (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` and fix by hand); the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; tests never read the real `/sys` or `/proc`; the `serve_*` tests bind ports your sandbox denies (`--skip serve_`); there is no crates.io here — `--offline` works. If a line below contradicts the code you read, STOP and reply `ESCALATE: <the line, what the code says, what you tried>`.

**F1 — `parse_fdinfo` must treat `drm-total-cycles-*` as the cycles form too (`gpu/linux.rs:468`; ADR 0025 decision 5; the kernel's `drm-usage-stats`: `drm-cycles-<keystr>` AND `drm-total-cycles-<keystr>` are the cycles-based utilisation pair).** Today the arm is `_ if key.starts_with("drm-cycles-") => saw_cycles = true,` — a file whose only engine lines are `drm-total-cycles-*` yields no engines AND no diagnostic (luna's finding; confirmed at source). Change the arm to match both prefixes (`key.starts_with("drm-cycles-") || key.starts_with("drm-total-cycles-")`) and add ONE unit test beside `xe_cycles_stats_yield_none` (`:929`): `total_cycles_only_stats_are_reported_as_cycles_only` — `parse_fdinfo("drm-driver:\txe\ndrm-total-cycles-rcs:\t200\n")` → `cycles_only == true` and `engines.is_empty()`. `mixed_cycles_and_engine_stats_are_not_reported_as_cycles_only` (`:952`) must still pass. RED first: paste the failing assertion before the one-line fix.

**F2 — `docs/sqlite-history-architecture.md:14`** says `- Current schema version: v3, with typed metric rows, …` — make it v4 and complete the list: `- Current schema version: v4, with typed metric rows, interned host identity, on-change filesystem rows and mount-presence events, one-minute/five-minute/hourly rollup tables, typed process detail tables with millisecond start times and nullable GPU percent, a per-tick process table and command dictionary, interned GPU adapters and per-tick GPU samples, migration/disk/fold state, and daemon timeline events`. Lines 48, 395, 405 and 806 describe v3 HISTORY and stay as they are.

**F3 — `agent/crates/tinytop-store/tests/migration_v3.rs:380-381`**, the comment under `migrated_v3_and_fresh_v4_schemas_are_identical`: `// Break caught: fresh-v3 DDL and the v2-to-v3 rebuild drift in any column` / `// attribute or required index while both still report user_version 4.` → `// Break caught: fresh-v4 DDL and the v2→v3→v4 rebuild chain drift in any column` / `// attribute or required index while both still report user_version 4.` Comment only; no code change.

**F4 — `INSTALL.md` §Upgrade, the v4 paragraph** (`grep -n 'adds minute-tier GPU percentages' INSTALL.md`): the migration adds a NULL column, not values — replace `adds minute-tier GPU percentages` with `adds a nullable GPU-percent column to the minute tier`. One phrase.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-collectors` green (paste every `test result:` line — the collectors unit suite gains one test: 30 passed); `git status --short` and `git diff --stat` pasted — exactly the four files, `agent/Cargo.lock` untouched. **Report shape:** per finding RED → GREEN (F1's failing line), the test name, the base commit, and `## Outside this lane` for anything you noticed — never fixed silently.
