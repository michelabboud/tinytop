You are hexe lane **T3** for tinytop. Execute ONLY **"Task 3: `retentionLadder` settings, validation, legacy aliases"** from
`docs/plans/2026-08-28-tiered-history-ladder-plan.md`.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` — the whole file; §5 (settings: the JSON shape, defaults, ranges, every validation rule and its exact message, the legacy aliases, the disk-pressure rule) is your spec; §4 and §9 tell you what the settings drive (T2 landed the ladder).
2. The plan's **Global Constraints** and **Context & Rationale** sections.
3. **Task 3** in the plan — its Files, Interfaces, Steps 1–5, Acceptance. Tasks 1–2 tell you what already exists (`history_state_get/set`, `maintenance::LadderConfig`, `maintain`).
4. `CLAUDE.md` at the repo root (the two-runtime invariant and the gates).
5. `agent/crates/tinytop-store/src/lib.rs` `DashboardSettings` :38-51, `Default` :85-101, `validate` :133-225, `put_settings` :452-473, `get_settings` :392-411, `history_state_get/set`; `src/maintenance.rs` whole (the `LadderConfig` struct and its legacy constructor :13-24 that you replace — keep the struct, keep `maintain`'s signature); `src/ladder.rs` :1-60 (Tier); `agent/crates/tinytop-agent/src/writer.rs` `changed_setting_keys` :554-593, `update_settings` :244-263; `tests/sqlite_history_store.rs` (settings round-trip tests to copy the pattern from); `src/settings.ts` (Bun — READ ONLY, do not edit: its legacy keys must keep working).

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` (the `--all` is required — without it cargo reports "Failed to find targets"); if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only.
- No new dependencies. No edits outside Task 3's Files list. Do not touch `tinytop-collectors`, `migration.rs`, `disk.rs`, `ladder.rs`, or Bun's `src/`. Read ADR 0013 and ADR 0016 (the export/import document that will carry these settings later) before Step 3.
- TDD as the steps say: write the failing tests first, run them, paste the failing output, then implement, then paste the passing output.
- **Every validation rule gets its own failing case first** (table-driven, spec §5 verbatim): run the table against the current `validate` → paste the RED (rules absent), implement, paste GREEN. Error text must name the field AND the observed value, never just "invalid". The disk-pressure rule reads `history_state.diskPressure` (absent = inactive; T9 writes it). **Also (T2 review finding, luna run 545, P2):** `insert_snapshot`'s ancestor refold reads the tier-enabled flags that `maintain` persists in `history_state`, so for one tick after a settings save a just-disabled tier can still be written. Fix it here: `put_settings` persists those same flags to `history_state` immediately on a successful save (same keys `maintenance.rs` writes), with a test: disable L3, insert before any `maintain`, assert no 5 m row is written.
- "All data is saved" is the owner's order: **a settings save can only shrink retention when the caller asked for it** — `put_settings` overwrites the legacy aliases (`retention_hours = l1.keep_days * 24`, `rollup_retention_days = l2.keep_days`) and validates BEFORE writing; a stored document without `retentionLadder` is populated from the legacy fields via `from_legacy` and NOT persisted until the next explicit save; monotonic rules (`l3 ≥ l2`, `l4 ≥ l3|l2`) are enforced server-side.

Gate to run and paste in full: `bun run check` (both runtimes — Task 3's acceptance names `check:bun` because Bun's `src/settings.ts` must keep passing with the legacy keys) (from the repo root; `cargo fmt --all -- --check` + `cargo test --workspace` under `agent/`). **Known sandbox limit:** the 10 `serve_contract` tests in `tinytop-agent` bind a local port, which your sandbox denies (`PermissionDenied: should reserve a local port`). If those 10 are the ONLY failures, say so verbatim and treat the gate as passed-for-you — Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours.

Report (your final message), in this order: files changed with line ranges · tests added (names) · the RED output of your new tests before implementation · the GREEN output after · full gate output · `git diff --stat` · docs updated · anything the plan was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
