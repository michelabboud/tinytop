You are hexe lane **T2** for tinytop. Execute ONLY **"Task 2: `fold`, tiers, frozen buckets, promote-before-prune"** from
`docs/plans/2026-08-28-tiered-history-ladder-plan.md`.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` — the whole file; §4 (the ladder), §8 (fold) and §9 (maintenance order, freeze rule, promote-before-prune, watermarks) are your spec; §6 is the schema T1 already landed.
2. The plan's **Global Constraints** and **Context & Rationale** sections.
3. **Task 2** in the plan — its Files, Interfaces (Consumes T1's tables + `history_state_get/set`), Steps 1–5, Acceptance. Task 1's section tells you what T1 built.
4. `CLAUDE.md` at the repo root (the two-runtime invariant and the gates).
5. `agent/crates/tinytop-store/src/migration.rs` (the v1 DDL: tables, columns, indexes — read it whole, 606 lines), `src/lib.rs` `insert_snapshot` :457-545, `rebuild_rollup_bucket` :946-1035, `prune_raw_history` :731-746, `prune_rollups` :748-759, `history_coverage` :690-729, `history_state_get/set` :395-431; `agent/crates/tinytop-agent/src/writer.rs` `maintain_history` :456-470; `tests/sqlite_history_store.rs` lines 1–60 (temp-dir fixture + `snapshot()` helper you must copy); `tests/migration_v1.rs` (how T1 builds fixtures).

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` (the `--all` is required — without it cargo reports "Failed to find targets"); if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only.
- No new dependencies. No edits outside Task 2's Files list. Do not touch `tinytop-collectors`, `migration.rs` or `disk.rs` (T1's, merged). Read ADRs 0013 and 0017 before Step 3.
- TDD as the steps say: write the failing tests first, run them, paste the failing output, then implement, then paste the passing output.
- **The decimation RED proof must keep the failure in place.** Build `ladder.rs`/`maintenance.rs` and the tests first; run `decimation_regression_completed_minute_keeps_its_sample_count` while `prune_raw_history` STILL calls `rebuild_rollup_bucket` (the old behaviour) → paste that RED output (bucket 1's `sample_count` collapses to 1–2). Only then delete that call (Step 3) and paste GREEN. A RED that comes from a missing module is not the proof the plan asks for.
- "All data is saved" is the owner's order: **promote before prune** — no row leaves a tier until the coarser tier's watermark (`history_state.l3FoldedUntilMs` / `l4FoldedUntilMs`) has passed it; completed buckets are never rebuilt from a partial tail; a disabled tier is neither written nor pruned.

Gate to run and paste in full: `bun run check:rust` (from the repo root; `cargo fmt --all -- --check` + `cargo test --workspace` under `agent/`). **Known sandbox limit:** the 10 `serve_contract` tests in `tinytop-agent` bind a local port, which your sandbox denies (`PermissionDenied: should reserve a local port`). If those 10 are the ONLY failures, say so verbatim and treat the gate as passed-for-you — Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours.

Report (your final message), in this order: files changed with line ranges · tests added (names) · the RED output of your new tests before implementation · the GREEN output after · full gate output · `git diff --stat` · docs updated · anything the plan was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
