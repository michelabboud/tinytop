You are hexe lane **T5** for tinytop. Execute ONLY **"Task 5: Dashboard — presets, ladder settings, shrink confirmation, coverage card"** from
`docs/plans/2026-08-28-tiered-history-ladder-plan.md`. Lane **T4** (Rust read API in `tinytop-store` + the API handlers in `writer.rs`) runs CONCURRENTLY on another branch — the ONLY `writer.rs` edits allowed to you are the two lines that serve/embed a new asset (below), and the ONLY `src/` edit is the one allow-list line in `src/server.ts` (below); do not touch `agent/crates/tinytop-store/`, `src/settings.ts`, or any Rust outside those two lines.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` — the whole file; **§11 (dashboard: the three new presets and their sources, the History-ladder settings group field by field, validation mirroring §5's exact messages, the shrink confirmation, the coverage card)** is your spec; §5 (every rule and its exact message — copy the strings from `agent/crates/tinytop-store/src/retention_ladder.rs` `validate` :195-272, never paraphrase) and §10 (the coverage payload you render; T4 is adding `tiers[]`, `disk`, `archive` to it concurrently — render what is present, degrade gracefully when a key is absent); §13 (two-runtime invariant: one asset directory serves BOTH runtimes).
2. The plan's **Global Constraints**, **Context & Rationale**, and **Contingencies** (the `app.js` one applies to you — see the test rule below).
3. **Task 5** in the plan — its Files, Steps 1–4, Acceptance, and the "Server-computed confirmation" paragraph (until T10 lands, `wouldDelete` is computed client-side from coverage tier counts and labelled "approx." — do exactly that, nothing more).
4. `CLAUDE.md` at the repo root (the two-runtime invariant; the Rust agent embeds the assets, so it must be rebuilt after any asset edit and your report must say so).
5. `agent/assets/dashboard/app.js`: `HISTORY_WINDOWS` :41-50, window use :1505/:2757/:2792, `validateDaemonSettings` :2326, `renderEffectiveSettings` :2348, `collectDaemonSettingsFromForm` :2487, the settings GET :2571 and PUT :2600, `renderHistoryCoverage` :2661 and its fetch :2694; `agent/assets/dashboard/index.html`: `#settings-dialog` :367-557, the daemon settings group :417-…, the legacy inputs `#daemon-retention-hours` :466 and `#daemon-rollup-retention-days` :470, the script tags :625-626; `agent/assets/dashboard/styles.css` (existing tokens for fieldsets/banners); `tests/dashboard-settings.test.ts` and `tests/dashboard-assets.test.ts` (how the Bun suite reads the assets — as TEXT via `readFileSync`, :4-6); `GUIDE.md` (settings walkthrough section); **also (luna run 548 P3, deferred to you):** `README.md` :377 and `docs/sqlite-history-architecture.md` :398 list the range presets only through `30d` — update both lists with `90d`, `1y`, `all` and their sources when you add the presets.

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` (the `--all` is required); if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use temp dirs only.
- **Pure functions + Bun tests (plan contingency, confirmed 2026-08-28):** `app.js` is a DOM-bound browser module with no exports, and the Bun suite reads it as text — it is NOT importable. Create `agent/assets/dashboard/ladder-rules.js` holding the pure functions `validateRetentionLadder(ladder, previous, diskPressure)` and `historyWindowFor(key, coverage)` (and the `HISTORY_WINDOWS` table if you move it there), as an ES module; `app.js` imports it (`import { … } from "./ladder-rules.js"` — `app.js` is already `type="module"`); `tests/dashboard-ladder.test.ts` imports the same file. To serve it from BOTH runtimes: in `writer.rs` add `.route("/ladder-rules.js", get(static_file))` after the `/vendor/echarts.min.js` route (:214) and a `Some("ladder-rules.js") => include_bytes!(...)` arm after the echarts arm in `embedded_response` (:393-402) — those two lines are your only Rust edits. **Bun's server is an ALLOW-LIST** (`src/server.ts` `staticFileName` :42-49 — verified 2026-08-28): add exactly one line there, `if (pathname === "/ladder-rules.js") return "ladder-rules.js";`, beside the `app.js` line — that one line is your only `src/` edit (the two-runtime invariant requires both runtimes to serve the same asset set). `tests/dashboard-assets.test.ts` :7-15 enumerates the dashboard files — add `ladder-rules.js` to that list. Optionally add one `http_get(port, "/ladder-rules.js")` assertion beside the `/app.js` one in `agent/crates/tinytop-agent/tests/serve_contract.rs` :83 (it will be sandbox-denied for you like its siblings; Fable runs it outside).
- No new dependencies. No edits outside Task 5's Files list + `ladder-rules.js` + the two `writer.rs` lines + the one `src/server.ts` line + `tests/dashboard-assets.test.ts` + `tests/dashboard-ladder.test.ts` (+ the optional `serve_contract.rs` line). Read ADR 0013 and ADR 0016 (export/import — the dialog's Export/Import buttons are T10's, NOT yours; do not add them) before Step 3.
- TDD as the steps say: failing Bun tests first, paste RED, implement, paste GREEN.
- **Legacy inputs become read-only mirrors** with the hint "derived from L1/L2"; the form still sends `retentionHours`/`rollupRetentionDays` (T3's server overwrites them) AND the full `retentionLadder`. Messages shown inline must be byte-identical to the server's. Presets whose tier is disabled or holds no data render disabled with a tooltip naming the setting (`retentionLadder.l3.enabled` etc.). Match existing CSS tokens; no new colours.
- CHANGELOG: put Task 5's entries under a new `## Unreleased` heading at the top (Fable assigns the version at merge — T4 merges beside you).

Gate to run and paste in full: `bun run check` (both runtimes; Task 5 names BOTH — `check:bun` for your tests, `check:rust` because the Rust binary embeds your assets and `tests/serve_contract.rs` asserts the served dashboard). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests in `tinytop-agent` bind a local port, which your sandbox denies (`PermissionDenied: should reserve a local port`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs a writable runtime dir — run the Bun suite with `XDG_RUNTIME_DIR=/tmp/<random>` and it passes. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours. **Acceptance's manual checklist / screenshot** needs a served dashboard, which your sandbox refuses — say so verbatim; Fable runs the checklist outside (open settings → ladder group renders → L2 = 6 → inline error names `retentionLadder.l2.keepDays` and 7 → L4 forever → save → coverage card shows four tiers). Do not ESCALATE on a sandbox-denied bind.

Report (your final message), in this order: files changed with line ranges · tests added (names) · the RED output of your new tests before implementation · the GREEN output after · full gate output (state that the Rust agent was rebuilt after the asset edit) · `git diff --stat` · docs updated · anything the plan was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
