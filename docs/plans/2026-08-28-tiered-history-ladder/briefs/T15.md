**Second dispatch (2026-08-30 ~01:05Z).** Your first dispatch (hexe run #670) escalated CORRECTLY on F4: the workspace pins `time` with only the `formatting` feature (`agent/Cargo.toml:31`), so `OffsetDateTime::parse` does not exist. RULING: add `parsing` to that existing line — `time = { version = "=0.3.51", features = ["formatting", "parsing"] }` — the ONE `agent/Cargo.toml` edit this brief allows; `parsing` pulls no crate, so `git diff --stat agent/Cargo.lock` MUST stay empty (paste it; if the lock changes, STOP and ESCALATE); no `crates/*/Cargo.toml` change. This prompt SUPERSEDES the copy of `briefs/T15.md` in your worktree (main carries the amendment). Your branch already carries the first dispatch's work as WIP `20eba84` (types GREEN; `gpu/{mod,linux,windows,macos}.rs`, `migration.rs`, `writer.rs`, `cli_db.rs`, `linux_collector.rs`, the two new store tests and the docs partially built and UNVERIFIED). Start with `git log -3` and `git diff main...HEAD --stat` (THREE dots; the merge base is `61d819d`), quote both, then CONTINUE from that WIP — re-read every file it touched against F1–F6 before you build on it (it was never gated), do not re-derive what is already right, and fix what is not. Everything else in this brief is unchanged.

You are hexe lane **T15** for tinytop — the store + collector + agent half of Phase 5 Task 15 (the dashboard half is lane T15b, running in parallel on its own branch; you never touch its files). Plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md` Task 15 **including its two amendment bullets (2026-08-30)**, decisions 3/6/7/8 in §2, and §4; **ADR 0025** (read it whole — it is the design; ADR 0022 is the source contract, ADR 0024 decision 7 is the `started_at` debt). Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is v0.5.3 `b5e9186` + this brief's commit). Every `file:line` below was read at `b5e9186`; run `sed -n` / `grep -n` on each before you build on it. Build ONLY the findings below. Files you may edit: `agent/crates/tinytop-types/src/lib.rs`; `agent/crates/tinytop-collectors/src/{lib,linux,common}.rs`, the NEW `agent/crates/tinytop-collectors/src/gpu/{mod,linux,windows,macos}.rs`, `agent/crates/tinytop-collectors/tests/linux_collector.rs`; `agent/crates/tinytop-store/src/{migration,lib,maintenance,settings_transfer}.rs`; `agent/crates/tinytop-store/tests/*.rs` (NEW `migration_v4.rs`, `gpu_history.rs`; the existing files ONLY where F1's literal census or F3's version assertions force a line — named under F1/F3); `agent/crates/tinytop-agent/src/{main,writer}.rs`, `agent/crates/tinytop-agent/tests/{cli_db,cli_config}.rs` (version assertions only); docs: `docs/sqlite-history-architecture.md`, `docs/guides/API.md`, `README.md`, `INSTALL.md`, `ARCHITECTURE.md`, `docs/INTEGRATION.md` (the `/api/snapshot` field table only). NOTHING else — in particular no `agent/Cargo.toml` beyond F4's single `time` feature line (second dispatch), no `agent/Cargo.lock` change, no `crates/*/Cargo.toml` (**no new dependency: the GPU backend is `std::fs` + `std::time` only; the store already has `time`**), `agent/crates/tinytop-agent/src/otel.rs` (no GPU instruments this phase — `grep -n gpu` on it must stay empty), `retention_ladder.rs`, `otel_settings.rs`, `ladder.rs`, `archive.rs`, `disk.rs`, `agent/assets/dashboard/*`, `tests/*.ts`, `GUIDE.md`, `src/`, `legacy/`, `VERSION`, `CHANGELOG.md` (Fable writes the line at merge), `docs/adr/*`, the plan, the spec, `vendor/`. **Git is read-only for you — no commit, no branch, no stash, no bare `cargo fmt` (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` and fix by hand); the orchestrator commits.** Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; `std::env::set_var` is forbidden; **tests never read the real `/sys/class/drm` or `/proc` — every GPU test runs on a fixture tree under `std::env::temp_dir()` through injected roots.** **Known sandbox limits — not failures:** every `serve_*` test binds a port your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); Bun's `tests/server.test.ts` likewise; there is no crates.io in the sandbox — `--offline` works, everything you need is in the lock; **this sandbox has no DRM device** (`/sys/class/drm` holds only `version`, like every WSL2 guest) — `detect_backend()` returning `None` here is the WSL2 rule working, and the live integration test must pass with `gpus` empty. If a contract below contradicts the code you read, or you would be guessing, STOP and reply `ESCALATE: <the line, what the code says, what you tried>` — escalating early beats a wrong build. Plan §4 binds you: the fdinfo format is the kernel's (`Documentation/gpu/drm-usage-stats.rst`) — if a fixture rule below cannot be expressed from that document, STOP and report the line; a migration slower than 60 s on the orchestrator's real-file copy is batched, never skipped; an in-transaction failure refuses with the underlying error and rolls back, never a silent fallback.

**Read first:** ADR 0025 (all) and ADR 0022 (all); `tinytop-types/src/lib.rs:176-186` (`ProcessSnapshot`: `parent_pid`/`started_at` carry `#[serde(default, skip_serializing_if = "Option::is_none")]` — the attribute shape you copy), `:190-202` (`SystemSnapshot`; `filesystems_captured_at_ms` shows the additive-optional pattern; `gpus` goes AFTER `processes`); `tinytop-collectors/src/lib.rs:1-12` (the cfg gating of `common`/`linux`/`macos`/`windows` — your `gpu` module is gated on `target_os`, never on a feature), `:19-45` (`CollectorConfig`, the `Collector` trait — unchanged), `:47-55`; `linux.rs:103-111` (`LinuxCollector` fields — you add the backend, the adapter list and the last scan stats), `:127-144` (`with_clock`, the `#[doc(hidden)]` accessors — the shape for `with_gpu_backend` / `last_gpu_scan`), `:146-180` (`collect()`: `slow_due` at `:154-156` is your re-detect trigger; `collect_fast_sources` at `:167`; the snapshot is built at `:178` — you attach GPU data AFTER that call and BEFORE returning), `:195` (`build_linux_snapshot_from_sources` — pure; its literal gains `gpus: Vec::new()`), `:699-735` (`parse_processes` — two literals gain `gpu_percent: None`), `:880-925` (`sysinfo_process_text` — the pid is column 0 of the tab format; the text format is unchanged); `common.rs:205-215` (the sysinfo process literal), `:239-247` (`process_started_at`: sysinfo's whole seconds → `Rfc3339` — the ONLY writer of `startedAt`, which is why v4's `strftime` conversion is exact); `migration.rs:15` (`SCHEMA_VERSION = 3`), `:357-373` (`CREATE_PROCESS_SAMPLES_FAST_V2_SQL` — `started_at TEXT` then `gpu_percent REAL`, WITHOUT ROWID, one index), `:376-395` (`CREATE_PROCESS_SAMPLES_V2_SQL` — `started_at TEXT` then `command_id`, two indexes), `:405-412` (`CREATE_SCHEMA_V2_SQL` is a `#[doc(hidden)] pub` 5-group array — the v3 array follows at `:420+`; find `CREATE_SCHEMA_V3_SQL` and its groups with `grep -n 'CREATE_SCHEMA_V3'`), `:722-780` (`ensure_schema`: the dispatcher you extend — `SCHEMA_VERSION => apply_schema_v3`, `2 => migrate_v2_to_v3`, `1 =>`, `0 =>`; note `complete_pending_migration` ordering per branch), `:783-797` (`apply_schema_v3`, `apply_schema_groups`), `:897-1160` (`migrate_v2_to_v3` — the ONE-transaction rebuild you mirror: `Instant` at start, `begin`, create the `_v3` temp table, copy, per-row work, guard, `DROP`, `RENAME`, recreate indexes, marker with `details_json`, `PRAGMA user_version = 3`, commit, `eprintln!` info line), `:1162-1195` (`normalise_legacy_snapshot`, `migration_u64_to_i64` — the `StoreError::Migration { reason, remedy }` wording); `tinytop-store/src/lib.rs:53-57` (`StoreStats` — flattened into `db stats`), `:421-433` (`HistoryFilesystemSample` — the model for `HistoryGpuSample`), `:438-447` (`HistoryProcessSample` — gains `gpu_percent`), `:471-475` (`HistoryQuery`), `:1385-1431` (`insert_snapshot` head: writer gate, settings, identity cache `current_identity`, filesystem cache `filesystem_state`, `begin` at `:1431` — the GPU adapter cache is primed and used exactly like `current_identity`), `:1605-1622` (the filesystem writes and the metric transaction's commit at `:1622` — GPU rows go inside this transaction, before the commit), `:1648-1670` (the process transaction AFTER the metric commit; `process_warning_last_ms` and the once-a-minute `eprintln!` at `:1664-1670` — the shape for the `startedAt` warn), `:1905-1975` (`write_detail_rows_if_due` — the minute rows: the INSERT at `:1930-1955` binds `started_at` text), `:1993-2036` (`write_process_fast_rows` — `gpu_percent` is the literal `NULL` at `:2013`; `.bind(&process.started_at)` at `:2029`), `:2077-2178` (`assemble_history_snapshots`: the two batched process reads at `:2128-2152` and `:2154-2178` select `p.started_at` — you add `p.started_at_ms, p.gpu_percent` there and ONE batched GPU read of the same shape), `:2308-2316` (the fast/minute fallback), `:2388-2394` (the assembled `SystemSnapshot` literal — gains `gpus`), `:2459-2503` (`read_history_filesystems` — copy its shape for `read_history_gpus`, `LIMIT` clamp `1..10_000`, DESC then `reverse()`), `:2536-2552` (the process-history SQL — `p.started_at` → `p.started_at_ms, p.gpu_percent`) and `:2570-2580` (the decode), `:2644-2662` (`stats()`), `:2801-2813` (`prune_raw_history` — the model for `prune_gpu_history`), `:2840-2852` (`count_process_fast_rows_older_than` — the model for `count_gpu_rows_older_than`), `:3572-3590` (`process_snapshot_from_row` — `started_at: row.try_get("started_at")?` becomes the `started_at_ms` → RFC 3339 formatting); `maintenance.rs:24-34` (`MaintenanceReport`), `:191-215` (the prune steps — "prune L1", "prune detail rows", "prune fast process rows": your "prune GPU rows" step sits right after "prune L1" with the fast-rows step's info-line shape); `settings_transfer.rs:53-59` (`WouldDelete`), `:253-292` (`would_delete` — `count_rows_older_than(Tier::L1, now - l1_keep_ms)` is the horizon you reuse); `tinytop-agent/src/main.rs:78-86` (`DbStatsOutput` flattens `StoreStats`); `writer.rs:331-354` (routes — `/api/history/gpus` goes after `:344`), `:522-533` (`history_filesystems`: `parse_history_filesystems_params` + `detail_history_query` — `grep -n` both and copy their shape with `adapter` in place of `mount`), `:677-694` (`collect_and_store`), `:894-905` (the loop), `:1802-1818` (the two rejection tests you mirror for `/api/history/gpus`); tests: `history_json_contract.rs:23-72` (`minimal_snapshot()`), `history_assembly.rs:58-148` (`filesystem()`, `snapshot()`, `count()` helpers — reuse them from `gpu_history.rs` by copying, not by cross-test imports), `migration_v3.rs:382` (the full ordered `table_info` equality test from T14-fix2 — its comment; mirror it for v4), `cli_db.rs:177-195`, `:256-271`, `:757`; docs: `docs/guides/API.md:122-186` (`/api/snapshot`; the field paragraph at `:186`), `:262-300` (`/api/history`), `:346-348` (`/api/history/filesystems`), `:350-384` (`/api/history/processes` — the example carries `startedAt`), `docs/sqlite-history-architecture.md:85-90` ("created directly at schema v3"), `:326-345` (Schema Versions), `:405-441` (the v3 subsections), `:728` (Future Tables), `README.md:239-265` (What It Shows), `:391-411` (Persistence), `:412` (`## OpenTelemetry export` — the `## GPU` section goes after it, before `## Verification` at `:480`), `INSTALL.md:316-355` (Upgrade), `ARCHITECTURE.md:28`, `:76`, `:189`, `:275`, `docs/INTEGRATION.md` (`grep -n filesystemsCapturedAtMs` finds the `/api/snapshot` field table).

**F1 — types: `GpuSnapshot`, `SystemSnapshot.gpus`, `ProcessSnapshot.gpu_percent` (plan Task 15; ADR 0025 amendment (h)).** In `tinytop-types`: `pub struct GpuSnapshot { pub id: String, pub vendor: String, pub name: String, pub driver: String, pub busy_percent: Option<f64>, pub memory_used_bytes: Option<u64>, pub memory_total_bytes: Option<u64>, pub temperature_c: Option<f64> }` (`#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]`, `#[serde(rename_all = "camelCase")]`, every `Option` with `#[serde(default, skip_serializing_if = "Option::is_none")]`); `SystemSnapshot.gpus: Vec<GpuSnapshot>` after `processes` with `#[serde(default, skip_serializing_if = "Vec::is_empty")]`; `ProcessSnapshot.gpu_percent: Option<f64>` after `started_at` with the same optional attributes. `vendor` is a plain string on the wire: `amd` | `nvidia` | `intel` | `apple` | `microsoft` | the four-hex-digit id `0x1a03` for anything else (the collector's `GpuVendor::as_str()` produces it — F2). **Literal census (every `SystemSnapshot { … }` / `ProcessSnapshot { … }` literal in the workspace at `b5e9186` — each gains `gpus: Vec::new()` / `gpu_percent: None`; this list is why the first T14 dispatch escalated, so sweep it with `grep -rn 'SystemSnapshot {\|ProcessSnapshot {' agent/crates --include=*.rs` and quote the count):** `tinytop-collectors/src/common.rs` (2), `linux.rs` (3), `tinytop-store/src/lib.rs` (2), `tests/history_assembly.rs` (3), `tests/history_json_contract.rs` (1), `tests/ladder_maintenance.rs` (2), `tests/migration_v1.rs` (2), `tests/process_history.rs` (2), `tests/retention_settings.rs` (2), `tests/sqlite_history_store.rs` (2). Types tests (RED first — paste the failing line): `gpus_are_absent_when_empty_and_round_trip_when_present` (serialize a snapshot with `gpus: vec![]` → `get("gpus").is_none()`; deserialize a document WITHOUT the key → empty; one adapter with every `Option` `Some` round-trips; an adapter document with only `id`/`vendor`/`name`/`driver` deserializes with every metric `None`), `process_gpu_percent_is_optional` (absent → `None`; `None` serializes absent; `Some(12.5)` round-trips). `docs/INTEGRATION.md`: the `/api/snapshot` field table gains rows for `gpus[]` (`array, optional — absent when no GPU adapter is detected`) with its eight fields and for `processes[].gpuPercent` (`number, optional — absent when the collector has no per-process GPU source`), in the wording of the `cpu.times` row; nothing else in that file changes.

**F2 — the GPU backend: `tinytop-collectors/src/gpu/` (ADR 0022; ADR 0025 decisions 5 and 7; plan Task 15 `[JUDGMENT-HEAVY]`).** `gpu/mod.rs` (platform-neutral, compiled on every target): `pub enum GpuVendor { Amd, Nvidia, Intel, Apple, Microsoft, Other(u16) }` with `pub fn from_pci_vendor(id: u16) -> Self` (`0x1002` Amd, `0x10de` Nvidia, `0x8086` Intel, `0x106b` Apple, `0x1414` Microsoft, else `Other`) and `pub fn as_str(&self) -> String` (`"amd"`, `"nvidia"`, `"intel"`, `"apple"`, `"microsoft"`, `format!("0x{:04x}", id)`); `pub struct GpuAdapter { pub id: String, pub vendor: GpuVendor, pub name: String, pub driver: String }`; `pub struct GpuSample { pub adapter_id: String, pub busy_percent: Option<f64>, pub memory_used_bytes: Option<u64>, pub memory_total_bytes: Option<u64>, pub temperature_c: Option<f64> }`; `pub struct GpuScanStats { pub duration: Duration, pub pids_scanned: usize, pub pids_denied: usize, pub clients: usize }`; `pub trait GpuBackend: Send { fn detect(&mut self) -> Vec<GpuAdapter>; fn sample(&mut self) -> Vec<GpuSample>; fn process_busy(&mut self) -> HashMap<u32, f64>; fn last_scan(&self) -> Option<GpuScanStats>; }` (`sample()` performs the tick's scan and computes BOTH the adapter and the per-pid numbers; `process_busy()` returns the map from the last `sample()` — the collector calls them in that order every fast tick, documented on the trait); `pub fn detect_backend() -> Option<Box<dyn GpuBackend>>` — `#[cfg(target_os = "linux")]` → `linux::LinuxGpuBackend::detect_default()`, `#[cfg(target_os = "windows")]` → `windows::detect_backend()`, `#[cfg(target_os = "macos")]` → `macos::detect_backend()`, else `None`; `pub fn attach_gpu(snapshot: &mut SystemSnapshot, adapters: &[GpuAdapter], samples: &[GpuSample], busy: &HashMap<u32, f64>)` — pure: `snapshot.gpus` = one `GpuSnapshot` per adapter in `adapters` order (`vendor: adapter.vendor.as_str()`; metrics from the sample with the same `adapter_id`, every metric `None` when no sample exists), `gpu_percent = busy.get(&process.pid).copied()` on every process. `gpu/windows.rs` and `gpu/macos.rs`: `pub fn detect_backend() -> Option<Box<dyn GpuBackend>> { None }` with a doc comment stating the backend is not implemented on this platform and names plan Task 16 — no `todo!()`, no fake adapter, and the cross-target checks in the gate prove they compile. `gpu/linux.rs` (`#[cfg(target_os = "linux")]`, `std::fs` only): `pub struct LinuxGpuBackend` built by `pub fn with_roots(drm_root: PathBuf, proc_root: PathBuf, nvidia_root: PathBuf, clock: Box<dyn FnMut() -> Instant + Send>) -> Self` (`#[doc(hidden)]`) and `pub fn detect_default() -> Option<Box<dyn GpuBackend>>` (roots `/sys/class/drm`, `/proc`, `/proc/driver/nvidia/gpus`; returns `None` when `detect()` finds no adapter — the WSL2 rule). **Detection (`detect()`, re-run by the collector on every slow tick — hotplug):** every entry of `drm_root` whose name matches `card` + digits ONLY (`card1`; never `card1-DP-1`, never `renderD128`) and whose `device/vendor` reads; `id = "pci-" + PCI_SLOT_NAME` from `device/uevent` (`PCI_SLOT_NAME=0000:02:00.0`), else the entry name; `driver` = `DRIVER=` from `device/uevent`, else `"unknown"`; `vendor` from `device/vendor` (`0x1002`); `name` = `device/product_name` trimmed when present and non-empty, else `"<vendor>:<device>"` from `device/vendor` + `device/device` (`0x1002:0x6810` — the plan ships no PCI table); then NVIDIA proprietary: every `nvidia_root/<slot>/information` whose `Bus Location:` slot is not already an adapter → `GpuAdapter { id: "pci-<slot>", vendor: Nvidia, name: <Model:> , driver: "nvidia" }`; a DRM card whose `driver == "nvidia"` is identity-only too. Detection resets the per-adapter busy-source verdicts and the fdinfo client state. **Sampling (`sample()`, every fast tick):** per adapter — `memory_used_bytes` / `memory_total_bytes` from `device/mem_info_vram_used` / `device/mem_info_vram_total` when both read as `u64`, else `None`; `temperature_c` = the first `device/hwmon/hwmon*/temp1_input` that reads, `/ 1000.0`, else `None`; identity-only adapters (`driver == "nvidia"`) get every metric `None` and are never scanned; `busy_percent`: the adapter's busy source starts `Unknown` → try `device/gpu_busy_percent`: reads and parses as a number → source `Sysfs` (value used; a later read failure yields `None` for that tick and keeps the source); a read error (trashcan's GCN 1 cards answer *Operation not supported*), a missing file (i915) or a parse error → source `Fdinfo`, **cached until the next `detect()`** — never re-read every tick. When any adapter's source is `Fdinfo` (or always, for the per-pid map — one scan serves both): walk `proc_root/<digits>/fd/`; a directory that cannot be listed (EACCES, ENOENT — a pid of another uid, or one that exited) counts in `pids_denied` and is skipped silently; every fd whose `read_link` target starts with `/dev/dri/` → read `proc_root/<pid>/fdinfo/<fd>`; parse lines as `key: value` split on the FIRST `:` with both sides trimmed (the kernel writes a tab after the colon) — keys `drm-driver` (mandatory; skip the file without it), `drm-pdev`, `drm-client-id`, `drm-engine-<k>` (`<uint> ns`), `drm-engine-capacity-<k>` (`<uint>`, default 1); every other key is ignored (`drm-memory-*`, `drm-total-*`, `pasid`, `pos`, …). The client key is (`drm-pdev`, `drm-client-id`); the adapter is the one whose slot equals `drm-pdev` (`pci-<pdev>`); a file without `drm-pdev` belongs to the only adapter when exactly one exists, else it is skipped and counted in the report; a file without `drm-client-id` is keyed by (`pdev`, pid, fd). A client key seen again in the same scan (a second fd, or a second pid) is NOT counted twice — it belongs to the first pid seen. Busy math (ADR 0025 decision 5, the kernel's delta rule): keep the previous scan's `{client → engine ns}` map and its `Instant`; for a client present in both scans, per engine `Δ = max(0, now − previous)` (a decrease counts 0); per adapter per engine key `Σ Δ over clients`, utilisation = `Σ Δ / (capacity × Δt_ns) × 100`; **adapter busy = the busiest engine, capped at 100**; per pid the same over that pid's clients; a client first seen this scan contributes nothing; the first scan after start or `detect()` yields `None` for every fdinfo adapter (no interval yet); clients absent from the scan are evicted. A file whose engine lines are `drm-cycles-*` only (Xe) contributes nothing and is reported once per detect cycle by an `eprintln!("gpu collector info: <driver> reports drm-cycles engine stats which this version does not read; busy is unavailable")`. `last_scan()` = the stats of the latest scan (`duration` = the scan's wall time by `Instant`). **Tests (`#[cfg(test)]` in `gpu/linux.rs`, fixture trees under `std::env::temp_dir().join(format!("tinytop-gpu-{name}-{pid}"))`, removed at the end; NEVER the real roots; the clock injected — RED first, paste the failing lines):** `amdgpu_card_with_a_busy_file_reports_sysfs_busy_vram_and_temperature` (trashcan's tree with `gpu_busy_percent` = `37`: id `pci-0000:02:00.0`, vendor `amd`, name `0x1002:0x6810`, driver `amdgpu`, busy `Some(37.0)`, memory 6000640 / 2147483648, temperature `Some(44.0)`); `an_unreadable_busy_file_is_cached_as_unsupported_until_redetect` (`gpu_busy_percent` created as a DIRECTORY → the first `sample()` selects `Fdinfo`; replace it with a file `50` → the next `sample()` still yields the fdinfo result — `None` with no clients; after `detect()` → `Some(50.0)`); `connectors_and_render_nodes_are_not_adapters` (`card1`, `card1-DP-1`, `renderD128`, `version` → exactly one adapter); `i915_card_without_busy_vram_or_hwmon_reports_none` (sheep's tree: `pci-0000:00:02.0`, `intel`, `0x8086:0x46d1`, `i915`, every metric `None` on the first scan); `nvidia_proprietary_is_identity_only` (a `nvidia_root/0000:01:00.0/information` with `Model:` and `Bus Location:` lines and no DRM card → id `pci-0000:01:00.0`, vendor `nvidia`, driver `nvidia`, every metric `None`; and a DRM card with `DRIVER=nvidia` is never scanned — a client fixture on its pdev is ignored); `an_empty_drm_class_yields_no_backend` (`drm_root` holding only `version` → `detect()` empty → `detect_default`-equivalent `None`); `fdinfo_engine_deltas_give_busy_per_adapter_and_per_pid` (pid `4242`, `fd/7` → symlink to `/dev/dri/renderD128` (dangling is fine — only `read_link` is used), `fdinfo/7` = `drm-driver: amdgpu` / `drm-pdev: 0000:02:00.0` / `drm-client-id: 4` / `drm-engine-gfx: 1000000000 ns` / `drm-engine-compute: 0 ns` / `drm-memory-vram: 5860 KiB` (ignored) — tab after each colon; second scan 2 s later by the injected clock with gfx `2000000000` → adapter busy `Some(50.0)`, `process_busy()[4242] == 50.0`; the first scan was `None`); `the_same_client_through_two_fds_counts_once` (fd 7 and fd 9 with the same pdev + client-id → 50, not 100); `two_clients_sum_and_the_busiest_engine_wins` (client A gfx 50 % + client B gfx 30 % → 80; one client with gfx 40 % and compute 40 % → 40; three clients at 50 % each → capped 100); `engine_capacity_divides_the_delta` (`drm-engine-capacity-video: 2`, video Δ = 2 s over Δt = 2 s → 50); `a_decreasing_counter_counts_as_zero`; `an_unreadable_pid_is_skipped_and_counted` (a `<pid>` dir without `fd/` → `pids_denied` 1, no error); `a_client_without_pdev_belongs_to_the_only_adapter`; `xe_cycles_stats_yield_none` (`drm-cycles-rcs` + `drm-total-cycles-rcs` only → `None`). **Collector integration (`linux.rs`):** `LinuxCollector` gains `gpu: Option<Box<dyn GpuBackend>>` (from `detect_backend()` in `Default`), `gpu_adapters: Vec<GpuAdapter>`, `#[doc(hidden)] pub fn with_gpu_backend(backend: Box<dyn GpuBackend>) -> Self` and `#[doc(hidden)] pub fn last_gpu_scan(&self) -> Option<GpuScanStats>`; in `collect()`: when `slow_due` (the same `:154-156` condition — no second timer) → `gpu_adapters = gpu.detect()`; every call → `let samples = gpu.sample(); let busy = gpu.process_busy();` after `collect_fast_sources` (`:167`) and `attach_gpu(&mut snapshot, …)` after `:178`, before returning; a backend that is `None` costs nothing. `common.rs` (the sysinfo collectors): `gpus: Vec::new()` / `gpu_percent: None` only — Task 16 wires their backends. Unit tests with a counting `FakeBackend` (in `linux.rs`'s test module, via `with_gpu_backend` + `with_clock`): `gpu_is_redetected_on_the_slow_tick_and_sampled_every_tick` (`detect` count 1 after the first collect, still 1 before `filesystems_interval`, 2 after; `sample` count equals the collect count), `attach_gpu_maps_busy_by_pid_and_fills_absent_samples_with_none` (pure, on a synthetic snapshot: pids 1 and 2, busy `{2: 12.5}` → process 2 `Some(12.5)`, process 1 `None`; two adapters, one sample → the other adapter's metrics `None`). `tests/linux_collector.rs`: `linux_fixture()`'s expectations gain `gpus` empty; add `#[ignore] live_gpu_scan_cost_on_this_host` next to `:147` — two `collect()` calls one second apart on `LinuxCollector::default()`, then `println!` the adapter count, `snapshot.gpus` as JSON and `last_gpu_scan()` (duration, pids scanned/denied, clients) — the orchestrator's rule-8 instrument on real hardware; it must PASS with zero adapters here.

**F3 — schema v4: `migrate_v3_to_v4` in ONE transaction, both process tables rebuilt, the two GPU tables (ADR 0025 decisions 1–4; ADR 0023/0024's shape).** `SCHEMA_VERSION = 4`. A new `CREATE_SCHEMA_V4_SQL` group array (mirroring the v3 array; its last group sets `PRAGMA user_version = 4`; applied on fresh files by `apply_schema_v4`); the process tables and the GPU tables in the fresh DDL are EXACTLY:
```sql
CREATE TABLE IF NOT EXISTS process_samples_fast (
  captured_at_ms INTEGER NOT NULL,
  rank INTEGER NOT NULL,
  pid INTEGER NOT NULL,
  command_id INTEGER NOT NULL REFERENCES process_commands(command_id),
  cpu_percent REAL NOT NULL,
  memory_percent REAL NOT NULL,
  rss_bytes INTEGER NOT NULL,
  parent_pid INTEGER,
  started_at_ms INTEGER,
  gpu_percent REAL,
  PRIMARY KEY (captured_at_ms, rank)
) WITHOUT ROWID;
CREATE INDEX IF NOT EXISTS idx_process_samples_fast_command ON process_samples_fast (command_id);
CREATE TABLE IF NOT EXISTS process_samples (
  captured_at_ms INTEGER NOT NULL,
  rank INTEGER NOT NULL,
  pid INTEGER NOT NULL,
  cpu_percent REAL NOT NULL,
  memory_percent REAL NOT NULL,
  rss_bytes INTEGER NOT NULL,
  parent_pid INTEGER,
  started_at_ms INTEGER,
  command_id INTEGER REFERENCES process_commands(command_id),
  gpu_percent REAL,
  PRIMARY KEY (captured_at_ms, rank)
);
CREATE INDEX IF NOT EXISTS idx_process_samples_time ON process_samples (captured_at_ms DESC);
CREATE INDEX IF NOT EXISTS idx_process_samples_command ON process_samples (command_id);
CREATE TABLE IF NOT EXISTS gpu_adapters (
  adapter_id INTEGER PRIMARY KEY,
  stable_id TEXT NOT NULL UNIQUE,
  vendor TEXT NOT NULL,
  name TEXT NOT NULL,
  driver TEXT NOT NULL,
  first_seen_ms INTEGER NOT NULL,
  last_seen_ms INTEGER NOT NULL
);
CREATE TABLE IF NOT EXISTS gpu_samples (
  captured_at_ms INTEGER NOT NULL,
  adapter_id INTEGER NOT NULL REFERENCES gpu_adapters(adapter_id),
  busy_percent REAL,
  memory_used_bytes INTEGER,
  memory_total_bytes INTEGER,
  temperature_c REAL,
  PRIMARY KEY (captured_at_ms, adapter_id)
) WITHOUT ROWID;
```
(no other table changes; no secondary index on `gpu_samples`). **`migrate_v3_to_v4(pool, now_ms)`, all inside ONE `pool.begin()` after `PRAGMA user_version` was read as 3:** (1) `CREATE TABLE process_samples_fast_v4 (…)` and `process_samples_v4 (…)` in the fresh shapes above under the temporary names, plus `gpu_adapters` and `gpu_samples`; (2) `INSERT INTO process_samples_fast_v4 (captured_at_ms, rank, pid, command_id, cpu_percent, memory_percent, rss_bytes, parent_pid, started_at_ms, gpu_percent) SELECT captured_at_ms, rank, pid, command_id, cpu_percent, memory_percent, rss_bytes, parent_pid, CAST(strftime('%s', started_at) AS INTEGER) * 1000, gpu_percent FROM process_samples_fast` and the minute tier likewise with `NULL` for `gpu_percent` (SQLite's `strftime('%s', …)` parses `2026-08-29T05:28:11Z`, `+00:00` and fractional forms and returns NULL for anything else — verified; do NOT write a Rust-side parser for the migration); (3) count `startedAtUnparsed` = rows of BOTH source tables with `started_at IS NOT NULL AND strftime('%s', started_at) IS NULL` (before the drop); **guard:** for each table `COUNT(*)` of the `_v4` copy equals the source's, else `StoreError::Migration { reason: "schema v4 rebuild copied <n> of <m> <table> rows", remedy: "inspect the database with `db check`; the database was not modified" }` (the `?` rolls back); (4) `DROP TABLE process_samples_fast; ALTER TABLE process_samples_fast_v4 RENAME TO process_samples_fast;` recreate `idx_process_samples_fast_command`; the same for `process_samples` with its two indexes; (5) the `schemaMigrated` marker (`label` `SQLite schema migrated from v3 to v4`, `details_json` `{fromVersion: 3, toVersion: 4, fastRows, minuteRows, startedAtUnparsed, durationMs}`), `PRAGMA user_version = 4`, commit, then `eprintln!("history migration info: schema v3 → v4 in {duration_ms} ms ({fast_rows} fast process rows and {minute_rows} minute process rows rebuilt with started_at_ms, {started_at_unparsed} unparsable start times stored as NULL; gpu_adapters and gpu_samples created)")`. `ensure_schema` (`:722-780`): `SCHEMA_VERSION => apply_schema_v4` (idempotent `IF NOT EXISTS`), `3 => migrate_v3_to_v4` then `complete_pending_migration`, `2 =>` v2→v3→v4, `1 =>` and `0 =>` extend their chains by one call; the "unsupported version" error text still names `SCHEMA_VERSION`. Keep `CREATE_SCHEMA_V3_SQL` (tests build v3 fixtures from it — make it `#[doc(hidden)] pub` if it is not). **Tests — `tests/migration_v4.rs` (RED first):** `fresh_database_is_created_at_v4` (`user_version` 4; `gpu_adapters`, `gpu_samples` exist; `process_samples_fast` `table_info` has `started_at_ms` INTEGER at position 8 and no `started_at`); `v3_fixture_migrates_to_v4_converting_started_at_and_creating_gpu_tables` (seed a v3 file from `CREATE_SCHEMA_V3_SQL` with a `process_commands` row and fast + minute rows whose `started_at` are `2026-08-29T05:28:11Z`, `-`, `garbage`, NULL → `1787981291000`, NULL, NULL, NULL; `startedAtUnparsed` 4 (two per table) in the marker; row counts equal; `user_version` 4; the info line is not asserted); `migrated_v4_schema_equals_a_fresh_v4_schema` (the T14-fix2 proof, `migration_v3.rs:382`'s shape: full ordered `PRAGMA table_info` equality migrated-vs-fresh for `process_samples_fast`, `process_samples`, `gpu_adapters`, `gpu_samples`, and the `sqlite_master` index-name set equality); `a_v2_fixture_chains_to_v4` and `a_v0_fixture_chains_to_v4` (extend or mirror the existing chain tests); the ignored `real_v1_file_copy_migrates_to_v3` becomes `…_to_v4` (same env fixture `TINYTOP_V1_FIXTURE`; prints the per-step timings — the orchestrator runs it on a fresh copy of the live file). **Version assertions that become 4 (each read at source first; a `toVersion` assertion on the v2→v3 marker stays 3):** `migration_v1.rs:176`, `:707`; `migration_v2.rs:120`, `:160`, `:308`; `migration_v3.rs:149`, `:346`, `:539`, `:657` (and `:382`'s comment); `cli_db.rs:192`, `:256-271` (rename `db_stats_json_reports_user_version_3` → `_4`), `:757`; `cli_config.rs` and `archive.rs` mention `user_version` — read them and change only what asserts the MAIN schema version (the archive file has its own).

**F4 — the store: GPU rows in the metric transaction, adapter interning, `started_at_ms` on both tiers, `gpu_percent` written, history assembled with `gpus`, prune at L1, would-delete, stats (ADR 0025 decisions 2–4).** (i) A `Mutex<HashMap<String, CachedAdapter { adapter_id: i64, last_seen_written_ms: i64 }>>` primed at `connect` from `gpu_adapters` (beside `prime_writer_state` `:1192`); in `insert_snapshot`, inside the metric transaction before `:1622`: for each `snapshot.gpus[i]` — unknown `stable_id` → `INSERT OR IGNORE INTO gpu_adapters (stable_id, vendor, name, driver, first_seen_ms, last_seen_ms) VALUES (?, ?, ?, ?, ?, ?)` with both stamps = `captured_at_ms`, then `SELECT adapter_id`, cache it; known → if `captured_at_ms − last_seen_written_ms >= 60_000` → `UPDATE gpu_adapters SET last_seen_ms = ? WHERE adapter_id = ?` and record it (ADR 0025: at most once a minute); then `INSERT INTO gpu_samples (captured_at_ms, adapter_id, busy_percent, memory_used_bytes, memory_total_bytes, temperature_c) VALUES (?, ?, ?, ?, ?, ?) ON CONFLICT(captured_at_ms, adapter_id) DO UPDATE SET …` (the manual collect route may repeat a stamp — the `:1999` DELETE-then-INSERT pattern or an upsert; pick the upsert); `u64 → i64` through the existing `to_i64` helper; a snapshot with no `gpus` writes nothing and touches no cache. (ii) `write_process_fast_rows` (`:1993`): bind `started_at_ms` from `started_at_ms_from_rfc3339(process.started_at.as_deref())` and `process.gpu_percent` (replacing the literal `NULL` at `:2013`); `write_detail_rows_if_due` (`:1930-1955`): the same two columns (`gpu_percent = excluded.gpu_percent`, `started_at_ms = excluded.started_at_ms` in the upsert). `fn started_at_ms_from_rfc3339(text: Option<&str>) -> Option<i64>` parses with `time::OffsetDateTime::parse(text, &Rfc3339)` (**second dispatch: enable `time`'s `parsing` feature on the existing workspace line — see the preamble; the lock stays unchanged**) → `unix_timestamp_nanos() / 1_000_000`; a text that does not parse → `None` PLUS a once-a-minute `eprintln!("history writer warning: process {pid} startedAt {text:?} is not RFC 3339; stored as NULL")` through a new `started_at_warning_last_ms` mutex in the `:1664-1670` shape (the collector is the only writer of this field — a failure here is a bug worth a line, never a swallowed one). (iii) Reads: `process_snapshot_from_row` (`:3572`) and the `HistoryProcessSample` decode (`:2570-2580`) read `started_at_ms` as `Option<i64>` (sqlx-sqlite decodes NULL into a bare `i64` as 0 SILENTLY — never `try_get::<i64>` on a nullable column) and format with `fn rfc3339_from_ms(ms: i64) -> Option<String>` (`OffsetDateTime::from_unix_timestamp_nanos(ms × 1_000_000)` → `Rfc3339`; whole seconds print as `2026-08-29T05:28:11Z` — assert the exact round trip), and read `gpu_percent` as `Option<f64>`; `HistoryProcessSample.gpu_percent: Option<f64>` (`#[serde(default, skip_serializing_if = "Option::is_none")]`); the SQL column lists at `:2132`, `:2158`, `:2546` change accordingly. (iv) `assemble_history_snapshots`: ONE batched read for the window, `SELECT s.captured_at_ms, a.stable_id, a.vendor, a.name, a.driver, s.busy_percent, s.memory_used_bytes, s.memory_total_bytes, s.temperature_c FROM gpu_samples s JOIN gpu_adapters a ON a.adapter_id = s.adapter_id WHERE s.captured_at_ms BETWEEN ? AND ? ORDER BY s.captured_at_ms, a.stable_id` (the window's min/max `captured_at_ms`, like the process reads) → `HashMap<i64, Vec<GpuSnapshot>>`; the literal at `:2388-2394` gains `gpus` (empty when the row has none). (v) `pub async fn read_history_gpus(&self, query: HistoryQuery, adapter: Option<&str>) -> Result<Vec<HistoryGpuSample>, StoreError>` — `pub struct HistoryGpuSample { captured_at_ms, id, vendor, name, driver, busy_percent: Option<f64>, memory_used_bytes: Option<i64>, memory_total_bytes: Option<i64>, temperature_c: Option<f64> }` (camelCase; the `Option`s with the skip attribute); the filesystems shape: `?3 IS NULL OR a.stable_id = ?3`, DESC + `LIMIT` clamped `1..10_000`, then `reverse()`. (vi) `pub async fn prune_gpu_history(&self, cutoff_ms: i64) -> Result<u64, StoreError>` (`DELETE FROM gpu_samples WHERE captured_at_ms < ?`) and `pub async fn count_gpu_rows_older_than(&self, cutoff_ms: i64) -> Result<i64, StoreError>`; `maintenance.rs`: `MaintenanceReport.gpu_rows: u64`, the step right after "prune L1" (`:191-196`) with the fast-rows step's shape (label `prune GPU rows`, info line `deleted {count} expired GPU rows` when > 0), horizon `now_ms − config.l1_keep_ms`; `settings_transfer.rs`: `WouldDelete.gpu_sample_rows: i64` (serde `gpuSampleRows`) counted at the L1 horizon in `would_delete` (`:253-292`). (vii) `StoreStats` (`:53-57`) gains `gpu_adapter_count: i64` and `gpu_sample_count: i64` (`db stats --json` flattens them as `gpuAdapterCount` / `gpuSampleCount`); `stats()` (`:2644`) fills them with two `COUNT(*)`. **Tests — `tests/gpu_history.rs` (RED first; copy the `snapshot()`/`count()` helper shapes from `history_assembly.rs:58-148` and add a `gpu(id, busy)` helper):** `gpu_rows_are_written_per_adapter_per_tick_and_adapters_are_interned_once` (two adapters × three ticks → 6 `gpu_samples`, 2 `gpu_adapters`, `first_seen_ms` = the first tick); `adapter_ids_survive_a_reconnect` (close, `connect` again, insert → no new `gpu_adapters` row, the same `adapter_id`); `last_seen_is_written_at_most_once_a_minute` (ticks at 0, 1 500, 59 000, 61 000 ms → `last_seen_ms` = 0 after three, 61 000 after four); `a_snapshot_without_gpus_writes_nothing` (both tables stay empty; `gpuAdapterCount` 0); `read_history_gpus_filters_by_adapter_and_orders_ascending`; `assembled_history_carries_gpus_and_process_gpu_percent` (insert with `gpus` + a process `gpu_percent: Some(12.5)` → `read_history` returns them field-by-field; a tick without `gpus` assembles `gpus: []`); `started_at_round_trips_through_ms_storage_on_both_tiers` (`2026-08-29T05:28:11Z` in → the identical string out of `read_history` (fast) and `read_history_processes` with a `minute` window; `started_at_ms` column = `1787981291000`); `an_unparsable_started_at_is_stored_as_null_and_the_row_is_kept` (`Some("-")` → NULL, the process row present; `stderr` not asserted); `gpu_rows_prune_at_the_l1_horizon` (through `maintain`/the ladder config as `ladder_maintenance.rs` drives it, or `prune_gpu_history` directly — assert `MaintenanceReport.gpu_rows`); `would_delete_counts_gpu_rows_at_the_l1_horizon` (in `tests/settings_transfer.rs` beside the existing would-delete tests — the dry-run of a shrunk `l1.keepDays` reports `gpuSampleRows`); `history_json_contract.rs`: `history_sample_with_a_gpu_serializes_with_dashboard_field_names` (`gpus[0].busyPercent`, `memoryUsedBytes`, `memoryTotalBytes`, `temperatureC`, `processes[0].gpuPercent`; the existing test still finds no `gpus` key).

**F5 — the agent: `GET /api/history/gpus`, `db stats`, nothing else.** `writer.rs`: `.route("/api/history/gpus", get(history_gpus))` after `:344`; `history_gpus` mirrors `:522-533` with `parse_history_gpus_params` (copy `parse_history_filesystems_params`; the optional exact-match parameter is `adapter`, the `stable_id`) → `no_store(Json(HistoryGpusResponse { gpus }))`. Tests (in `writer.rs`'s module, the `:1802-1818` shape): `history_gpus_query_rejections_name_parameter_and_observed_value` (`limit=abc`, `sinceMs=x`), `history_gpus_rejects_zero_limit_and_inverted_range`, `snapshot_route_omits_gpus_when_the_collector_has_none` (the memory-backed `/api/snapshot` on this sandbox — `get("gpus").is_none()`). `main.rs`: nothing beyond what `StoreStats` brings (`DbStatsOutput` flattens it); `cli_db.rs`: `db_stats_json_reports_gpu_counts` (`gpuAdapterCount` 0 and `gpuSampleCount` 0 on a fresh file). `otel.rs` untouched — paste `grep -n -i gpu agent/crates/tinytop-agent/src/otel.rs` (empty).

**F6 — docs (yours: the store/API/collector half).** `docs/sqlite-history-architecture.md`: `## Current Schema` says "created directly at schema v4" and shows the v4 process and GPU tables; `## Schema Versions And Migration` gains schema v4 (the rebuild of both process tables in one transaction, the `strftime` conversion and the NULL-and-count rule, the row-count guard, the GPU tables, the marker), a `### GPU samples` subsection (per adapter per tick, interned adapters, `last_seen_ms` once a minute, pruned at L1, `wouldDelete.gpuSampleRows`), and `### Assembling a history snapshot` gains the GPU step; `## Future Tables` loses any GPU line; `docs/guides/API.md`: `/api/snapshot` example + the `:186` paragraph gain `gpus[]` (absent when no adapter is detected; `busyPercent` null on the first tick, on identity-only adapters, and where the source is unavailable; on Linux the fdinfo-derived value covers only processes readable by the daemon's user) and `processes[].gpuPercent`; `/api/history` states that assembled samples carry `gpus`; a new `### GET /api/history/gpus` between filesystems and processes (`sinceMs`/`untilMs`/`limit` 1..10000/`adapter`; response `{ "gpus": [...] }` with the nine fields; a curl + JSON example in the `:355-380` style); `/api/history/processes` example + text gain `gpuPercent` (absent when unknown); the `db stats` field list gains `gpuAdapterCount`/`gpuSampleCount`; the import dry-run's `wouldDelete` list gains `gpuSampleRows`. `README.md`: a `## GPU` section after `## OpenTelemetry export` (`:412-479`) — sources per OS (Linux: DRM sysfs + `/proc/<pid>/fdinfo` + the GPU node's hwmon; Windows/macOS: Task 16, not yet), the detection rule ("no adapter → no rows, no `gpus`"), the busy definition in one sentence (busiest engine, Task Manager's rule), `gpu_busy_percent` preferred where the driver supports it and fdinfo otherwise, NVIDIA proprietary = identity only, WSL2 = none, **the readable-pid rule** (the daemon's user sees its own processes; run the daemon as root or grant it `CAP_SYS_PTRACE` to see all — TinyTop never asks for either), never a subprocess or vendor library (ADR 0022/0025); `## What It Shows` (`:239-265`) gains one bullet; `## Persistence` (`:391-411`) mentions v4 and the GPU tables in the storage paragraph. `INSTALL.md` `## Upgrade` (`:316-355`): a v4 paragraph — the first 0.5.4 start rebuilds both process tables in one transaction (seconds on a default 24 h window), `startedAtUnparsed` in the info line, the file is untouched on any failure. `ARCHITECTURE.md`: `:76` (the collectors row names the GPU backend), a sentence under `## Rust Daemon` (the backend is detected at start and re-detected on the slow tick; the fast tick samples it), `:189` and `:275` to v4 truth. Not yours: `GUIDE.md`, the dashboard, `CHANGELOG.md`, the ADRs.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green (paste every `test result:` line; the 0.5.3 gate counted 301 passed + 1 ignored across 26 suites — expect two new suites and one more ignored); `cargo check --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors --no-default-features --features tinytop-collectors/windows-collector --target x86_64-pc-windows-msvc --offline` and `… --features tinytop-collectors/macos-collector --target aarch64-apple-darwin --offline` clean (the `--workspace` form fails on C build scripts in this environment — that is known; the per-crate form is the proof that `gpu/windows.rs` and `gpu/macos.rs` compile); `bun test tests/dashboard-ladder.test.ts tests/dashboard-settings.test.ts` still green (you changed no JS); `grep -rn 'started_at\b' agent/crates --include=*.rs` shows only the v1/v2/v3 historical DDL constants, the v4 migration's read of the old column, and the `ProcessSnapshot`/`HistoryProcessSample` field — paste it; `grep -n -i gpu agent/crates/tinytop-agent/src/otel.rs` empty; `git status --short` and `git diff --stat` pasted (⊆ the allow-list; the four new files under `gpu/` and the two new test files listed).

**Report shape:** per finding the RED line(s) you observed → the GREEN summary; the test names; the base commit; the literal-census count; the sysfs/fdinfo key set your parser accepts and the exact fixture lines you used (the orchestrator compares them against real trashcan/sheep dumps — if the kernel's real lines differ, that is a fix lane, not your guess); what `ensure_schema` does per version now; anything outside this brief you noticed under `## Outside this lane` — never fixed silently. The orchestrator's own acceptance (outside your sandbox): the ignored real-file test on a fresh copy of the live database (v1→v2→v3→v4 with timings, `integrity_check`, `startedAtUnparsed`); a temp daemon on `:4290` with a scratch DB for ≥ 5 minutes → `db stats --json` `userVersion 4`, `dbstat` bytes per `process_samples_fast` row ≤ 60 (baseline 66.7 + 19.1 index); the release binary on trashcan (two `amdgpu`, fdinfo path) and sheep (`i915`) from a temp dir: `/api/snapshot | jq .gpus`, the ignored scan-cost instrument with `--nocapture`, a GPU load started by the orchestrator → `busyPercent > 0` and a process row with `gpuPercent`, `/api/history/gpus`; on this WSL2 box `gpus` absent and `gpuAdapterCount 0`.
