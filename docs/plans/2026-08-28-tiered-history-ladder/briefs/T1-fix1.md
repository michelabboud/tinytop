You are hexe lane **T1-fix1** for tinytop — the fix round for **Task 1** after review (luna, hexe run 542, validated by Fable). Your worktree branch `tinytop/ladder-t1-schema-migration-fix1` starts from `tinytop/ladder-t1-schema-migration` (T1's work, commit `8321584`).

Read, in this order, before touching anything:
1. `docs/plans/2026-08-28-tiered-history-ladder-plan.md` → Task 1 and §Contingencies; `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` → §6, §7, §10 (the line: `GET /api/history` returns only rows with `snapshot_json IS NOT NULL`), §13; `docs/adr/0017-store-owns-its-free-space-check.md`.
2. The code: `agent/crates/tinytop-store/src/migration.rs` (whole), `src/disk.rs` (whole), `src/lib.rs` :545-600 (`latest_snapshot`, `read_history`) and :1125-1133 (`row_to_sample`), `tests/migration_v1.rs` (whole), `src/history-store.ts` :49-53, :204-208, :228-240.

Rules (non-negotiable, same as T1): git is READ-ONLY for you (`git show`/`log`/`diff` allowed; no add/commit/checkout/stash) — leave the tree dirty and report `git diff --stat`. Never run bare `cargo fmt`; use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` and `rustfmt --edition 2024 <file>` only on files you changed. **Never open, read, copy, or write `~/.local/share/tinytop/`.** No new dependencies. **Allowed files, nothing else:** `agent/crates/tinytop-store/src/{disk.rs,migration.rs,lib.rs}`, `agent/crates/tinytop-store/tests/migration_v1.rs`, `src/history-store.ts`, ONE new Bun test `tests/history-store-null-json.test.ts`, `docs/sqlite-history-architecture.md`, `CHANGELOG.md`.

TDD per item: the failing test first (paste RED), then the fix (paste GREEN). Do the items in this order.

**F1 (P1) — NULL `snapshot_json` breaks raw reads.** `lib.rs:1127` `try_get::<String, _>("snapshot_json")` errors on every migrated row older than the keep window, so `read_history` fails for any range crossing the 60-minute boundary. Spec §10: the raw endpoint returns only rows with JSON; its horizon is the JSON window. Fix (Rust): `latest_snapshot` and `read_history` SQL add `snapshot_json IS NOT NULL` to their WHERE (keep the existing `?1`/`?2` bounds and ordering); `row_to_sample` reads `Option<String>` and maps `None` to `StoreError::Validation(format!("metric_samples row at {captured_at_ms} has no snapshot_json"))` so any future caller gets a named error, never `UnexpectedNull`. Test in `tests/migration_v1.rs`: after migrating the 10-row fixture, `read_history(None, None, 100)` returns exactly the 7 rows that kept JSON, oldest first, and `latest_snapshot()` is the newest row — RED on current code (an error), GREEN after. Fix (Bun): the two `SELECT`s in `src/history-store.ts` (`latestSnapshot` :206, `readHistory` :230) filter `snapshot_json IS NOT NULL` — compose it correctly with `readHistory`'s `${where}` whether `where` is empty or not; `MetricSampleRow.snapshot_json` stays `string` (the filter guarantees it). Bun test (new file, `bun:test`, a temp-dir DB via the store's own factory): insert two snapshots through the store, `UPDATE metric_samples SET snapshot_json = NULL` on the older one with a raw statement, then `readHistory()` returns 1 and `latestSnapshot()` is the newer. Run `bun test tests/history-store-null-json.test.ts` RED first (today `JSON.parse(null)` → `null` and the caller throws), then GREEN, then `bun test` whole.

**F2 (P1) — Windows path prefix.** `disk.rs:15` `canonicalize()` returns verbatim paths (`\\?\C:\…`) on Windows while `sysinfo` mount points are `C:\`; `Path::starts_with` compares prefix components, so no mount matches and every populated-v0 migration on Windows refuses with `NotFound`. Fix: canonicalise each mount point too (`disk.mount_point().canonicalize()`, skipping mounts whose canonicalisation fails) and match canonical against canonical; keep `free_bytes_from_mounts` pure. Document in the fn doc that `path` must exist (the migration passes the DB's parent directory) and that both sides are canonicalised for exactly this reason (we cannot produce `\\?\` on Linux — the fix is by construction; say so). Add a unit test proving component matching: mounts `/home` and `/`; path `/homeless/x/db` must select `/`, not `/home`.

**F3 (P1 coverage) — the v0 fixture is not v0.** `tests/migration_v1.rs` :19 and :47 create only `metric_samples`, so `add_rollup_1m_columns` (the ALTER path) and the index recreation are never exercised — and that is exactly the path the live database takes. Fix: build the v0 fixture from the COMPLETE v0 DDL — `git show 07d3fcc:agent/crates/tinytop-store/src/lib.rs` lines 1022-1130 (every `CREATE TABLE`/`CREATE INDEX` of v0: `metric_samples`, `metric_rollups_1m` WITHOUT the `min_*`/`max_root` columns, `app_settings`, `app_events`, their indexes), `user_version` 0. Seed one `metric_rollups_1m` row and one `app_events` row so the ALTER runs on a populated legacy table. Assert after migration: all six additive columns exist on `metric_rollups_1m` (name each), the seeded rollup row survives with its existing values intact and the new columns NULL, `idx_metric_samples_captured_at` and `idx_metric_samples_runtime_captured_at` exist in `sqlite_master`, and every index `migration.rs` declares exists. If these assertions are already GREEN on the current migration code, say so — this item is coverage, not a bug — the assertions are still required.

**F4 (P2) — the crash window after commit.** `migration.rs` ~:426-438: schema commit → `VACUUM` → audit record are three separate steps. A crash after the commit leaves a v1 database that is never vacuumed (the live file would stay 4 GB) with no `schemaMigration` record, and the `SCHEMA_VERSION =>` branch of `ensure_schema` then skips both forever. Fix: write the `schemaMigration` record INSIDE the schema transaction with the fields known at that point (`from`, `to`, `preImagePath`, `samplesKept`, `jsonRowsKept`, `bytesBefore`, `startedAtMs`) and `vacuumedAtMs: null`, `bytesAfter: null`, `durationMs: null`; after the `VACUUM`, update the record with those three. In the `SCHEMA_VERSION =>` branch: read `schemaMigration`; if it exists with `vacuumedAtMs == null`, run the `VACUUM` and complete the record (idempotent completion — the `MigrationReport` type may gain `Option`s for the three late fields; keep the plan's public names). Test: migrate the fixture; with raw SQL set the record's `vacuumedAtMs` to null and create+drop a 1 MiB padding table (freelist pages); `connect` again → the record is completed and `PRAGMA freelist_count` == 0. RED first.

**F5 (P2 test) — refusal must leave the pre-image untouched.** `tests/migration_v1.rs` ~:165: record the pre-existing pre-image's byte length and mtime before `connect`; after the refusal assert both unchanged and `user_version` still 0.

**F6 (P3) — `migration.rs:394`:** the undeterminable-free-space `reason` must name the database byte count and the required bytes, as the low-space case does.

**F7 (P3 docs) — `CHANGELOG.md`:** "Startup now requires 1.2× free space" is broader than the code (only the populated-v0 migration checks it) — say what the code does; move the whole T1 entry under a `## 0.2.7 - 2026-08-28` heading (Fable merges T1 as 0.2.7; 0.3.0 is the phase close) and add one bullet per fix above. `docs/sqlite-history-architecture.md`: add the read-path rule (raw reads return JSON rows only; horizon = the JSON keep window; both runtimes), the idempotent VACUUM completion, and the operational rule that all writers are stopped before the migration runs (there is deliberately no lock between `VACUUM INTO` and the schema transaction; document, do not code).

**Do NOT change:** the padding-table fixture in the shrink test — reviewed and kept: it is a valid detector of a skipped `VACUUM`, and the JSON nulling is asserted separately; add a one-line comment saying exactly that. Do not touch `tinytop-collectors`, `writer.rs`, or any file outside the allowed list.

Gate to run and paste in full: `bun run check` (both runtimes: `check:bun` + `check:rust`). Known sandbox limit: the 10 `serve_contract` tests in `tinytop-agent` bind a local port your sandbox denies (`PermissionDenied: should reserve a local port`). If those 10 are the ONLY failures, say so verbatim and treat the gate as passed-for-you — Fable re-runs it outside the sandbox before merge. Any other failure is yours.

Report (your final message), in this order: files changed with line ranges · per item F1–F7: test names, RED output, GREEN output · full gate output · `git diff --stat` · docs updated · anything this brief was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
