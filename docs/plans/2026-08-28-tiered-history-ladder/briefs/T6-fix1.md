You are hexe lane **T6-fix1** for tinytop: a FIX ROUND on Task 6 ("CLI + docs + Phase 1 close-out material") after its blind review (ari-luna run 566, validated by Fable). Your branch starts from the T6 lane branch (`tinytop/ladder-t6-cli-docs` @ `dcfa4c7`, everything below already exists). Do ONLY F1–F5. No other lane runs beside you.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §7 (the pre-image: taken before the first migration, never overwritten, **never deleted automatically**; `remove` refuses unless `user_version ≥ 1` and the main DB passes `integrity_check`) and §14.
2. The plan's **Global Constraints**, **Task 6**.
3. Code (line numbers = `dcfa4c7`): `agent/crates/tinytop-store/src/lib.rs` — `connect_for_inspection` :552-567 (**:555-556 falls back to `connect()` when the file is missing — `connect` CREATES a v1 database: that is F1**), `database_path` :569, `user_version` :573, `HistoryCoverage` :264-286 (**it already carries `sample_count`, `oldest_captured_at_ms`, `newest_captured_at_ms` — the three `StoreStats` fields**), `history_coverage` :1381 (**:1386 calls `self.stats()`**), `StoreStats` :35; `agent/crates/tinytop-store/src/migration.rs` — `migrate_populated_v0` :353 (**:360-370 canonicalises the db path, then `pre_image_path(&canonical_db_path)`**), `pre_image_path` :521, `refuse_existing_pre_image` :527; `agent/crates/tinytop-agent/src/main.rs` — `db()` (**:188 `let mut sqlite_url = default_sqlite_url()?;` BEFORE the flag loop — and the same pattern at the `serve`/`collect` site: `grep -n "default_sqlite_url()"`**), `stats` arm :208-225 (**:210 `store.stats()` — a second scan**), `db_pre_image` :262-320 (`store.database_path()` = the RAW url path — never canonicalised), `pre_image_remove_allowed` :~330, `normalize_sqlite_url` :482-492 (**:488-490 `create_dir_all(parent)` — runs for the DEFAULT path even when `--sqlite` is given**); `agent/crates/tinytop-agent/tests/cli_db.rs` (the black-box pattern; `set_sqlite_user_version` :85 shows how the fixture removes its OWN `-wal`/`-shm`).

The two defects that matter (luna 566, confirmed by Fable at source):
- **P0.** With only `<db>.pre-v0.sqlite` present — the main database lost, the pre-image the ONLY copy — `db pre-image remove --yes` today CREATES an empty v1 `<db>` (user_version 1, integrity "ok"), passes every check, and deletes the pre-image.
- **P1.** The migration derives the pre-image from the CANONICAL database path; `status`/`remove` derive it from the raw URL path. Through a symlinked path, `status` answers `exists:false` while the pre-image sits at the canonical location, and `remove` targets the wrong path. ONE path function fed TWO different inputs.

Fixes (each: failing test first, paste RED, implement, paste GREEN):
- **F1 — inspection NEVER creates a database (P0).** `connect_for_inspection` must return an error when the file does not exist — never call `connect()`, never `create_if_missing(true)`. Add `pub fn inspect_database_path(database_url: &str) -> Result<Option<PathBuf>, StoreError>` to `tinytop-store` (`lib.rs`): parses the URL exactly like `connect` does, returns `Ok(None)` when the file does not exist, `Ok(Some(canonical_path))` otherwise (see F2 — it MUST use the same canonicalisation as the migration). `db_pre_image` calls it FIRST: `None` → `status` prints `{ "status": "ok", "value": { "path": "<raw db path>.pre-v0.sqlite", "exists": <bool>, "bytes": <n|null>, "databaseExists": false, "userVersion": null, "integrityCheck": null } }` (exit 0 — the recovery scenario needs this answer), `remove` → refused, exit 1, reason names the check and the database path (e.g. `database <path> does not exist; the pre-image may be the only copy — restore or recreate the database before removing it`). `Some(path)` → today's flow, plus `"databaseExists": true` in the status envelope (additive). The refusal is decided in the ONE predicate: extend `pre_image_remove_allowed` so the database's `(user_version, integrity_check)` is an `Option` (`None` = the database file does not exist) — order: `--yes` → pre-image exists → database exists → `user_version ≥ 1` → `integrity_check == "ok"`; unit-test all FIVE refusals and the allowed case in `main.rs`'s `#[cfg(test)]`. Black-box test in `tests/cli_db.rs` (`pre_image_remove_refuses_when_database_is_missing`): create the DB (`db check`), touch the pre-image, delete the fixture's own `<db>`, `<db>-wal`, `<db>-shm` → `remove --yes` → exit 1, `status:"refused"`, reason contains `does not exist` and the db path; the pre-image still exists; **assert `<db>` was NOT created**; then `status` → exit 0, `databaseExists:false`, `exists:true`, `userVersion:null`, `integrityCheck:null`. RED on `dcfa4c7` = exit 0 and the pre-image gone.
- **F2 — ONE canonical database path (P1).** Add `pub fn canonical_database_path(db_path: &Path) -> Result<PathBuf, StoreError>` in `migration.rs` (the `canonicalize` + `StoreError::Migration { reason, remedy }` mapping that :360-369 does today) and call it from BOTH `migrate_populated_v0` (:360) and `inspect_database_path`/`connect_for_inspection`; `SqliteHistoryStore::database_path()` returns the canonical path for an inspected store. `pre_image_path` stays the one suffix function, now always fed the canonical path (except the missing-database `status` above, which reports the raw path and says `databaseExists:false`). Black-box test, `#[cfg(unix)]` (`pre_image_status_follows_a_symlinked_database_path`): create `<tmp>/real/h.sqlite` (`db check`), `std::os::unix::fs::symlink(<tmp>/real, <tmp>/alias)`, touch `<tmp>/real/h.sqlite.pre-v0.sqlite`, run `status --sqlite sqlite://<tmp>/alias/h.sqlite` → `exists:true` and `path` == `<tmp>/real/h.sqlite.pre-v0.sqlite` after `canonicalize()` of the expected path (macOS maps `/tmp` → `/private/tmp`); `remove --yes` through the alias → exit 0, the canonical file gone, nothing at `<tmp>/alias/h.sqlite.pre-v0.sqlite` was ever created. RED on `dcfa4c7` = `exists:false`. Report the two call sites of `canonical_database_path`.
- **F3 — `stats()` once per `db stats` (P2).** `HistoryCoverage` already carries the three `StoreStats` fields; `db stats` must call ONLY `history_coverage` and build `DbStatsOutput.stats` from `coverage.sample_count / oldest_captured_at_ms / newest_captured_at_ms` (`StoreStats { … }` constructed from coverage — the printed JSON is byte-identical to today: same keys, same order). Remove the `store.stats()` call at :210. No black-box change is observable — say so; the proof is the diff (report the `stats()` call sites left in `main.rs`: expected zero).
- **F4 — lazy default database path (P2, pre-existing, exercised by T6's tests).** At BOTH call sites the default URL (and its `create_dir_all`) must be resolved ONLY when `--sqlite` is absent: `let mut sqlite_url: Option<String> = None;` … after the flag loop `let sqlite_url = match sqlite_url { Some(u) => u, None => default_sqlite_url()? };`. Black-box test (`explicit_sqlite_url_never_touches_the_default_state_directory`): spawn `db stats --sqlite sqlite://<tmp>/h.sqlite` with `.env("HOME", <tmp>/home).env("USERPROFILE", <tmp>/home).env("LOCALAPPDATA", <tmp>/home).env("XDG_STATE_HOME", <tmp>/home).env_remove("TINYTOP_HISTORY_DB")` where `<tmp>/home` does NOT exist beforehand → exit 0 and **`<tmp>/home` still does not exist** afterwards. RED on `dcfa4c7` = `<tmp>/home/.local/share/tinytop` created.
- **F5 — docs.** `CHANGELOG.md`: APPEND one line per fix under the existing `## Unreleased` — do NOT rewrite or reorder T6's entries (the T4/T5 fix lanes replaced theirs; that was wrong). `INSTALL.md` (the pre-image paragraph): one sentence — if the main database is missing, `db pre-image status` reports `databaseExists: false` and `remove` refuses: the pre-image may be your only copy. `README.md` CLI table, the `remove` row: refuses unless confirmed, the database exists, schema v1, and integrity ok.

NOT yours (recorded for the Phase-1 deep review): usage errors (`unknown db option`) print to stderr with exit 1 — the pre-existing CLI contract, not a defect; every `db …` subcommand exits without closing the sqlx pool (WAL left un-checkpointed) — a design question, not a fix here.

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only; every file a test deletes is one the test created.
- No new dependencies. No edits outside: `agent/crates/tinytop-agent/src/main.rs`, `agent/crates/tinytop-agent/tests/cli_db.rs`, `agent/crates/tinytop-store/src/lib.rs`, `agent/crates/tinytop-store/src/migration.rs`, `CHANGELOG.md`, `INSTALL.md`, `README.md`. Do not touch `writer.rs`, `disk.rs`, `ladder.rs`, `maintenance.rs`, `retention_ladder.rs`, `agent/assets/`, Bun's `src/`/`tests/`.
- Every refusal names the check and the observed value; refusals go to stdout as `{ "status": "refused", "reason": … }` with exit 1; nothing is deleted before every check has passed; the only deletion is `std::fs::remove_file` on the exact canonical pre-image path.

Gate to run and paste in full: `bun run check` (from the repo root). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests bind a local port your sandbox denies (`PermissionDenied: should reserve a local port`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours. Do not ESCALATE on a sandbox-denied bind.

Report (your final message), in this order: files changed with line ranges · tests added/rewritten (names) · RED output per fix · GREEN output · full gate output · `git diff --stat` · docs updated · the call sites of `canonical_database_path` and the remaining `stats()` call sites in `main.rs` · anything the brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
