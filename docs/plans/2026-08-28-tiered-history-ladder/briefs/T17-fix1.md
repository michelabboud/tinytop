# Brief — lane T17-fix1: three validated findings from luna's blind review

You are hexe lane **T17-fix1**. Base: the tip of `tinytop/phase5-t17-thermals` (`git rev-parse HEAD` first and quote it). The T17 gate is GREEN (27 suites, 367 passed, 0 failed) and must stay green. Governing: **ADR 0026 including both amendments**, brief `T17.md` including AMENDMENTS 1–5. Fix ONLY the three findings below — luna raised seven, and the orchestrator validated each at source; the other four need no code (rulings recorded in the review file). Build ONLY these.

**Files you may edit:** `agent/crates/tinytop-store/src/lib.rs`; `agent/crates/tinytop-store/src/thermal_settings.rs`; `agent/crates/tinytop-store/src/settings_transfer.rs`; `agent/crates/tinytop-collectors/src/thermal.rs` (F3's test only); `agent/crates/tinytop-store/tests/*.rs`; `docs/guides/API.md`; `README.md`. NOTHING else. **No new dependency; `Cargo.lock` must not change.** **Git is read-only — no commit, no branch, no bare `cargo fmt`.** Never touch `~/.local/share/tinytop/` or read `/sys/class/hwmon` in a test. No subprocess (`Command::new`/`process::Command`); `std::process::id()` is allowed.

**F1 — `db stats` and import planning must not explode on an un-migrated v4 database (luna finding 5; CONFIRMED at source by the orchestrator).** `StoreStats::stats()` (`lib.rs`, the `pub async fn stats` query) unconditionally runs `(SELECT COUNT(*) FROM sensor_dim)` and `(SELECT COUNT(*) FROM sensor_samples)`, but `connect_for_inspection` is **documented as opening an existing database WITHOUT migrating it** (`lib.rs`, its doc comment says exactly that, so `db stats` on a v4 file hits `no such table: sensor_dim`). The same hazard exists in `settings_transfer.rs`'s `would_delete` sensor-row count. **Fix:** detect the tables' presence once (`SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name IN ('sensor_dim','sensor_samples')` — or the store's existing table-presence helper if one exists; `grep` for it first and REUSE it rather than adding a second idiom) and, when absent, report `sensor_count`/`sensor_sample_count` as **0** and `wouldDelete.sensorSampleRows` as **0** — never an error, never a missing field. **Do the same audit for `gpu_adapters`/`gpu_samples`**: if `db stats` on a v3 file has the identical bug, fix it the same way in the same place and say so in your report (it is the same defect class, and shipping the fix for only the newer table would be dishonest). Tests, RED first: `db_stats_on_an_unmigrated_v4_file_reports_zero_sensors` (build a v4 fixture with `CREATE_SCHEMA_V4_SQL`, open via `connect_for_inspection`, assert `stats()` succeeds with zeroes — **not** an error); the same for the import dry-run path; and confirm a v5 file still reports true counts.

**F2 — `extraChips` must not readmit a chip that is already reported elsewhere (luna finding 3; VALID — it defeats ADR 0026 decision 5's promise).** `thermal_settings.rs`'s `validate` accepts any syntactically-valid name, so `extraChips = ["amdgpu"]` puts the GPU's temperature into `sensors[]` a second time, next to the `gpus[].temperatureC` that has shipped since 0.5.4. **Fix:** reject a reserved name with `StoreError::Validation("thermal.extraChips must not name a chip already reported elsewhere: amdgpu, i915, nvme")`. Define the reserved list as a `pub const RESERVED_EXTRA_CHIPS: [&str; 3] = ["amdgpu", "i915", "nvme"]` beside the allow-list so the two are read together, with a comment naming WHY each is reserved (amdgpu/i915 = `gpus[].temperatureC`, ADR 0022/0025; nvme = disk temps, the parent sensors plan's later slice). Tests: each reserved name rejected with its message; a normal name (`cpu_thermal`) still accepted; the existing `extra_chips_opts_a_chip_in` test still passes.

**F3 — one end-to-end sensorless proof (luna finding 6; fair).** The WSL2 behaviour is currently proven in three separate places (missing root → empty scan; wire omission; empty store). Add ONE test that walks the whole path: a collector pointed at a **non-existent** hwmon root with `thermal_enabled = true` → collect → assert the snapshot's `sensors` is empty **and** that `serde_json::to_value(&snapshot)` has **no `sensors` key at all** → insert that snapshot into a fresh store → assert `sensor_dim` and `sensor_samples` both hold **zero rows**. Name it `sensorless_host_end_to_end_writes_nothing`. This is the WSL2 case and it runs natively on your host.

**Gate:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green. **Baseline to beat: 27 suites, 367 passed, 0 failed — paste every `test result:` line, sum them yourself, and the suite count must NOT drop.** `git diff --stat agent/Cargo.lock` prints nothing. `grep -rnE 'Command::new|process::Command' agent/crates/tinytop-collectors/src agent/crates/tinytop-store/src` prints nothing.

**Report:** per finding the RED line(s) → GREEN; whether the GPU-table variant of F1 was also present (yes/no, with the evidence); the reserved-chip messages verbatim; your own census sum. Anything else under `## Outside this lane` — never fixed silently. **Default:** anything not covered here — STOP and reply `ESCALATE: <what, what you tried, where you stopped>`.
