You are hexe lane **T7-fix2** for tinytop. Your base is branch `tinytop/ladder-t7-queryable-archive` at `ae409ab` (T7 run 573 + T7-fix1 run 575); you are on `tinytop/ladder-t7-queryable-archive-fix2`. A blind review of `ae409ab` (validated in Fabulous `docs/fleet/tinytop/2026-08-29-ari-luna-t7-review.md`) found one P1, three P2 and four P3 defects in the archive move; the decisions are **ADR 0019** (`docs/adr/0019-archive-move-keyset-verify-fullrow-delete-fsynced-copy.md`, on `main` — read it FIRST and in full; it supersedes ADR 0018's Decision steps 3–5 and is the contract for F1–F6). No redesign beyond ADR 0019; escalate if it cannot be implemented as written.

Read, in this order: (1) ADR 0019, then ADR 0018 for the two-phase order it keeps; (2) `agent/crates/tinytop-store/src/archive.rs` end-to-end (this tree; anchors below are its line numbers); (3) `agent/crates/tinytop-store/src/lib.rs` `history_state_set` `:626-648`, `apply_pragmas` `:1974-1982`, `now_ms()`; (4) `agent/crates/tinytop-store/tests/archive.rs`; (5) `CLAUDE.md` at the repo root.

**F1 — Verify by key set (P1: livelock).** `archive.rs:477-493` counts `archive` rows in `[min, max]`; after any partial batch the archive holds keys inside the interval that `main` no longer has, so the count exceeds `row_count` forever and every later call fails at `verify`. Replace with: `SELECT COUNT(*) FROM main.metric_rollups_1h AS m WHERE m.bucket_start_ms BETWEEN ?1 AND ?2 AND EXISTS (SELECT 1 FROM archive.metric_rollups_1h AS a WHERE a.bucket_start_ms = m.bucket_start_ms)` must equal `row_count` (step `verify`, message names both numbers). **Regression test** `partial_batch_does_not_livelock_the_next_move` in `tests/archive.rs`: seed expired `{0, 1h, 2h}`; `copy_expired_l4_batch(…, batch 2)` → archive `{0, 1h}`; then `DELETE FROM metric_rollups_1h WHERE bucket_start_ms = 3600000` on `main` through the fixture pool (the state a partial phase B leaves: main `{0, 2h}`, archive `{0, 1h}`); `move_expired_l4(…, batch 10)` must return `2`, main empty, archive exactly `{0, 1h, 2h}`, watermark `3h`. RED on this tree at `verify` ("3 did not equal 2") — paste it — then GREEN.

**F2 — Delete by full-row equality (P2: stale copy).** `archive.rs:511-521` matches only `sample_count` and `newest_captured_at_ms`. Write ONE `const ARCHIVE_ROW_MATCH: &str` = the 18 non-key column comparisons between `archive.metric_rollups_1h AS a` and `metric_rollups_1h` (`=` for the 15 `NOT NULL` columns, `IS` for `avg_root_used_percent`, `min_root_used_percent`, `max_root_used_percent`) and use it in phase B's `DELETE … AND EXISTS (SELECT 1 FROM archive.metric_rollups_1h AS a WHERE a.bucket_start_ms = metric_rollups_1h.bucket_start_ms AND <ARCHIVE_ROW_MATCH>)`. Test: extend `archive_copy_is_committed_before_main_delete` with a second mutation between the phases that changes ONLY `avg_cpu_usage_percent` (to `99.0`) on bucket `0` (count and newest unchanged) and assert the archived copy after the full move carries `99.0` — that proves the refresh; the predicate itself is reviewed by reading (a change *within* one call cannot be injected without a hook — say so in the report, do not fake it).

**F3 — Schema DDL in one transaction (P2).** `ARCHIVE_SCHEMA_SQL` `:15-51` runs three `CREATE`s and the `PRAGMA` as separate autocommit statements; a kill after the first leaves `user_version 0` with one object, which F4's refusal (`:111-114`) then rejects forever. Wrap the constant in `BEGIN; … COMMIT;` (SQLite DDL is transactional; `PRAGMA user_version` inside the transaction is fine). No deterministic test exists without fault injection — say so; the existing `archive_directory_setting_relocates_the_file` object/version assertions still pass.

**F4 — Fsync the archive commit (P2 durability, ADR 0019).** In `acquire_attached` `:360-381`, immediately after a successful `ATTACH`: `PRAGMA archive.synchronous = FULL` (step `attach`; on failure `close_on_drop` + `Err` exactly like a failed ATTACH). `main` stays at NORMAL (`apply_pragmas`). Then read `PRAGMA archive.synchronous` back on the same connection and, if it is not `2`, `close_on_drop` and return `Err(StoreError::Archive { step: "attach", source: ArchiveErrorSource::Io(InvalidData "archive synchronous is N, expected 2 (FULL)") })` — production code, not a test hook: a SQLite build that cannot honour the pragma refuses to move rather than moving unsafely. No new test seam; every existing move test exercises this path.

**F5 — Watermark inside phase B (P3, ADR 0019).** Factor `history_state_set` `:626-648` into `pub(crate) async fn history_state_set_on(connection: &mut SqliteConnection, key, value, now_ms)` holding the SQL, with `history_state_set` acquiring a pool connection and calling it (ONE implementation). In `delete_attached_batch` `:503-532`, after the `DELETE` and before `COMMIT`: if `rows_affected == batch.row_count`, call `history_state_set_on(connection, "archiveMovedUntilMs", &(batch.max_ms + Tier::L4.resolution_ms()), crate::now_ms())` (step `watermark`; failure → `ROLLBACK` + `Err`, nothing deleted). Delete the post-DETACH watermark block in `move_expired_l4` `:238-247`; `move_expired_l4` returns `deleted`. `archive_remedy("watermark")` `:75-87` loses its special text (a watermark failure now rolls phase B back). Keep test 1's and F7's watermark assertions; `archive_error_remedy_is_step_aware` updates its `watermark` case to the "between the commits" text of F6.

**F6 — Remedy by phase (P3).** `archive_remedy(step)` returns three texts: steps up to and including `commit copy` (`create directory`, `open`, `schema`, `close`, `acquire`, `attach`, `begin copy`, `select`, `select decode`, `insert`, `commit copy`) → "nothing was written to history-archive.sqlite and nothing was deleted from the main database; check the archive directory is writable and retry"; `verify`, `begin delete`, `delete`, `watermark`, `commit delete` → "the batch's copy is committed in history-archive.sqlite and is refreshed on retry; nothing was deleted from the main database — retrying is safe"; `detach` → today's post-delete text. Encode the phase as a match on the step name in ONE place (a `const` list per phase or a `fn phase_of(step)`), unit-tested for one step of each phase.

**F7 — Idle-connection assertions on the store's own pool (P3, test gap).** The store pool has `max_connections(1)` (`lib.rs:528-529`); `tests/archive.rs` checks `PRAGMA database_list` on a SEPARATE pool (`:196-238`, `:527-554`), which proves nothing about the connection moves use. Add `#[doc(hidden)] pub async fn attached_database_names(&self) -> Result<Vec<String>, StoreError>` on `SqliteHistoryStore` (acquire from `self.pool`, `PRAGMA database_list`, collect `name`). In test 1 assert `store.attached_database_names().await == ["main"]` right after the failed (chmod) move AND after the converging third move; in `archive_is_never_attached_while_idle` assert it after each of the two moves. Keep the separate-pool checks if you like; they are not the proof.

**F8 — README endpoint row (P3).** `README.md:404` (`GET /api/history/points` row) still says "Archive is an empty unavailable page until the archive phase" — replace that sentence with: "`archive` returns hourly points with `available:true` when `retentionLadder.archive.queryable` is enabled; an explicit archive request while it is disabled is an empty `available:false` page."

**F9 — `batch == 0` (P3).** `move_expired_l4` `:226` and `copy_expired_l4_batch` `:255` turn `batch = 0` into one row (`.max(1)`). `batch == 0` → return `Ok(0)` / `Ok(None)` before acquiring a connection (nothing was requested). One assertion in `archive_is_never_attached_while_idle`.

**Docs (F10).** `docs/sqlite-history-architecture.md` `## Archive` (`:444-480`): the verify sentence → key-set existence; the delete sentence → full-row equality; add the `PRAGMA archive.synchronous = FULL` sentence and the power-cut line of the crash matrix; the watermark sentence → inside phase B; name ADR 0019 beside 0018. Spec §9 `:177`: "verify the committed count" → "verify every selected key is in the committed archive"; append "; archive commit fsynced (`archive.synchronous = FULL`); watermark inside the delete transaction — ADR 0019". `CHANGELOG.md` `## Unreleased`: amend the ADR 0018 bullet to name ADR 0019 and the key-set/full-row/fsync/watermark changes; one bullet for the transactional schema DDL. Rejected review items (do NOT act on them): "ADR 0018 / T7-fix1 brief deleted" (a branch-base artefact of `git diff main..HEAD`); `pub(crate)` for the `archive.rs` functions (the plan's own Task 7 tests are integration tests and require `pub`; the plan is inconsistent — note it in your report, change nothing).

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`, `branch`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check`; if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only; leave nothing under `/tmp` outside fixtures your tests remove.
- No new dependencies (`Cargo.lock` unchanged). No edits outside: `archive.rs`, `lib.rs` (`history_state_set` split + the new seam only), `tests/archive.rs`, `README.md:404`, `docs/sqlite-history-architecture.md`, the spec §9 line, `CHANGELOG.md`. Not `maintenance.rs`, `writer.rs`, `main.rs`, `migration.rs`, Bun, assets.
- Additive only, names unchanged (spec §13); the `archive.rs` public surface stays (`copy_expired_l4_batch`/`ArchiveBatch`/`archive_remedy` included) plus `attached_database_names` and `history_state_set_on` from this brief.

Gate to run and paste in full: `bun run check` (repo root). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests in `tinytop-agent` bind a local port (`PermissionDenied`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. One more KNOWN flake, not yours: `cli_db::db_stats_refuses_a_v0_database` can fail once with "database disk image is malformed" (its fixture deletes the `-wal`; T8 fixes it) — re-run that test alone and paste the re-run. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours.

Report (your final message), in this order: files changed with line ranges · F1's RED (the `verify` mismatch) before the change and GREEN after · the F2 extension's assertion · full gate output · `git diff --stat` · docs updated · anything ADR 0019 or this brief was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
