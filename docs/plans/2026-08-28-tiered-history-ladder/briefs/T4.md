You are hexe lane **T4** for tinytop. Execute ONLY **"Task 4: Read API — four-tier `auto`, coverage, detail endpoints"** from
`docs/plans/2026-08-28-tiered-history-ladder-plan.md`. Lane **T5** (dashboard JS/HTML/CSS) runs CONCURRENTLY on another branch — do not touch `agent/assets/`, `GUIDE.md`, or Bun's `src/`/`tests/`.

Read, in this order, before touching anything:
1. `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` — the whole file; **§10 (read API: the `auto` rule (a)(b)(c) verbatim, the coverage shape, raw history only with JSON, the two detail endpoints with `limit` clamped 1–10,000)** is your spec; §4/§5/§6 tell you the tiers, the settings (T3 landed `RetentionLadder`) and the detail tables' columns; §13 (two-runtime invariant).
2. The plan's **Global Constraints** and **Context & Rationale** sections.
3. **Task 4** in the plan — its Files, Steps 1–5, Acceptance. Tasks 1–3 tell you what already exists.
4. `CLAUDE.md` at the repo root (the two-runtime invariant and the gates).
5. `agent/crates/tinytop-store/src/lib.rs`: `HistoryCoverage` :236, `HistoryQuery` :265, `HistoryPointMode` + `FromStr` :288-318, `HistoryPointsQuery` :311, `HistoryPoint` :320, `get_settings` :406, `history_state_get/set` :435/:450, `read_tier_buckets` :686, `read_history` :980, `read_history_points` :1010, `history_coverage` :1109, `resolve_history_point_source` :1300; `src/retention_ladder.rs` (`RetentionLadder` :13, `to_ladder_config` :296); `src/ladder.rs` `Tier` :2-46 (`resolution_ms`/`table`/`finer`/`coarser`); `src/maintenance.rs` `LadderConfig` :10; `src/disk.rs` (`free_bytes_at`, ADR 0017). `agent/crates/tinytop-agent/src/writer.rs`: `AppState` :46, `router` :194-215, `history` :298, `history_coverage` :306, `history_points` :312, `history_markers` :323, `history_query` :512, `history_points_query` :534, the `#[cfg(test)] mod tests` :822 (your HTTP tests go HERE — see the test rule below).

Rules (non-negotiable):
- You are in a git worktree on your own branch. **git is READ-ONLY for you**: no `git add`, `commit`, `push`, `checkout`, `stash`. Leave the tree dirty; report `git diff --stat`.
- Never run bare `cargo fmt`. Use `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` (the `--all` is required — without it cargo reports "Failed to find targets"); if it flags a file you changed, run `rustfmt --edition 2024 <that file>` only.
- **Never open, read, copy, or write `~/.local/share/tinytop/` or any file under it.** Tests use `std::env::temp_dir()` fixtures only.
- **HTTP tests (plan correction, 2026-08-28):** `tinytop-agent` is a bin-only crate (`main.rs` + `writer.rs`; `router` and `AppState` are private), so the plan's `tests/history_api.rs` cannot reach the router. Put the Task 4 tests in `writer.rs`'s existing `#[cfg(test)] mod tests` (:822): build `router(state)` over a temp-dir `SqliteHistoryStore` (a private `fn test_state(...) -> AppState` builder is allowed) and drive it with `tower::ServiceExt::oneshot` — no sockets, no ports. Dev-dependencies to add to `agent/crates/tinytop-agent/Cargo.toml` (both are ALREADY in `agent/Cargo.lock` as transitive deps of axum, so pin exactly these — no new resolution, no network): `tower = { version = "=0.5.3", features = ["util"] }`, `http-body-util = "=0.1.3"`. Record both in your report. No other new dependencies.
- No edits outside Task 4's Files list (+ the `Cargo.toml` dev-deps above). Do not touch `tinytop-collectors`, `migration.rs`, `disk.rs`, `ladder.rs`, `maintenance.rs`, `retention_ladder.rs`, `agent/assets/`, or Bun's `src/`. Read ADR 0009 (additive API pattern) and ADR 0013 before Step 3.
- TDD as the steps say: write the failing tests first, run them, paste the failing output, then implement, then paste the passing output. The `auto` table in Step 1 is one test with every row from the plan; each row asserts `source` AND `resolutionMs`.
- **Additive only, names unchanged (spec §13):** `"rollup"` keeps meaning 1 m; every existing coverage field keeps its name; `GET /api/history` returns only rows with `snapshot_json IS NOT NULL`. `"archive"` parses and returns an empty page with `"source":"archive","available":false` until T7. The resolver takes `(&RetentionLadder, now_ms, query)` and is a pure function you can table-test without a database.
- CHANGELOG: put Task 4's entries under a new `## Unreleased` heading at the top (Fable assigns the version at merge — T5 merges beside you).

Gate to run and paste in full: `bun run check` (both runtimes; Task 4's acceptance names `check:rust`) (from the repo root; `cargo fmt --all -- --check` + `cargo test --workspace` under `agent/`). **Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures:** (1) the 10 `serve_contract` tests in `tinytop-agent` bind a local port, which your sandbox denies (`PermissionDenied: should reserve a local port`); (2) Bun's `tests/server.test.ts` fails the same way on `Bun.serve({ port: 0 })`; (3) Bun's `tests/tinytop-script.test.ts` needs a writable runtime dir — run the Bun suite with `XDG_RUNTIME_DIR=/tmp/<random>` and it passes. Fable re-runs the full gate outside the sandbox before merge. Any other failure is yours. **Acceptance's `curl` transcript:** `cargo run -p tinytop-agent -- serve` needs a port your sandbox will refuse — if it does, say so verbatim; Fable runs the transcript outside. Do not ESCALATE on a sandbox-denied bind.

Report (your final message), in this order: files changed with line ranges · tests added (names) · the RED output of your new tests before implementation · the GREEN output after · full gate output · `git diff --stat` · docs updated · anything the plan was silent on, listed — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
