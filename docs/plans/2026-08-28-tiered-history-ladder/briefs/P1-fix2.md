You are hexe lane **P1-fix2** for tinytop: the DASHBOARD + DOCS fix round after the Phase 1 deep dual-blind review (ari-sol-deep run 568 + ari-luna run 569, validated by Fable — Fabulous `docs/fleet/tinytop/2026-08-29-ari-dual-blind-phase1-review.md`). Your branch starts from `main` @ `9c1b8ab`. Do ONLY F1, F2, F3, F3b, F4, F5. Lane **P1-fix1** (Rust) runs CONCURRENTLY — do not touch anything under `agent/crates/`, `agent/Cargo.*`, or Bun's `src/`.

Read first: spec `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md` §7, §9, §11, §13; `agent/assets/dashboard/app.js` (`fetchSnapshot` `:2928-2941` → `fetchHistoryCoverage` `:3093`; state `:208-216`; settings load `:2790-2810`; the shrink confirm `:2840-2870`), `agent/assets/dashboard/ladder-rules.js`, `tests/dashboard-assets.test.ts` and the Bun test that imports `ladder-rules.js`.

Fixes (each: failing test first where a pure helper exists, paste RED/GREEN; otherwise a manual checklist line in the report):
- **F1 — coverage polling coalesced and throttled (P2).** `fetchSnapshot` fires `fetchHistoryCoverage()` unawaited every 1.5 s (`:2940`); since T4 each call runs ~16 statements. Add to `ladder-rules.js` a pure `shouldFetchCoverage({ lastFetchedAtMs, nowMs, inFlight, force }, minIntervalMs = 15000)` → boolean (in flight ⇒ false; force ⇒ true unless in flight; else `nowMs − lastFetchedAtMs ≥ minIntervalMs`), Bun-tested (4 cases). Wire it: `state.coverageInFlight`, `state.coverageFetchedAtMs`; the snapshot poll calls it without `force`; a history-preset change, a settings save, and the confirm dialog's estimate call with `{ force: true }`. Report the call sites.
- **F2 — ladder capability fails CLOSED (P2).** `state.retentionLadderAvailable` starts `true` (`:212`) and the settings-fetch `catch` (`:2803-2806`) leaves it so — Rust-only controls are shown and `retentionLadder` submitted against Bun when the first settings request fails. Add a pure `ladderCapabilityFrom(settingsOrNull)` in `ladder-rules.js` (null/failed ⇒ `false`; a document with `retentionLadder` ⇒ `true`), Bun-tested; initial state `false`; the `catch` sets it from `null` and renders the group as unavailable ("settings unavailable — ladder controls hidden"); a save never includes `retentionLadder` while unavailable.
- **F3 — the disable-tier confirm tells the truth (P3).** `:2855-2858`: disabling L3/L4 currently predicts "all N buckets may be deleted"; §9 leaves disabled tables untouched (rows are pruned by the tier's own horizon only when re-enabled). Change the dialog line for the disable case to say the table is kept and reads fall through to the next tier, with the count shown as "kept", not "deleted"; the shrink cases stay as they are.
- **F3b — unknown disk state is not `0 B` (P3 — luna 569).** `app.js` `renderDiskCoverage` `:3015-3027`: when `disk.freeBytes` is `null`/absent (no check has run yet — `lastCheckMs` null) render "History disk check: not measured yet; minimum <minFreeBytes>" instead of `0 B free`; a pure `describeDiskCoverage(disk)` in `ladder-rules.js` returning the string, Bun-tested for the null case and a normal case.
- **F5 — the wrapper scripts stop hard-coding the version (P2 — luna 569, pre-existing release blocker).** `tinytop:4` (`TINYTOP_VERSION="0.2.0"`, used by `rust_release_download_url` `:164-166`) and `tinytop.ps1:4` (`$TinyTopVersion = "0.2.0"`, `Get-TinyTopReleaseUrl` `:71-73`): read the repo's `VERSION` file next to the script when it exists (`"$(dirname "$0")/VERSION"` / `Join-Path $PSScriptRoot VERSION`), trimmed; fall back to the embedded constant only when the file is absent (a copied script), and set that constant to the CURRENT `VERSION` (0.2.11 today — Fable bumps it to 0.3.0 at release). Test: `tests/tinytop-script.test.ts` asserts the script's printed version (find the existing `version`/`--version` path; add one if none — report it) equals the trimmed `VERSION` file. `tinytop.ps1` is `#[cfg]`-free text — keep it ASCII (a BOM-less UTF-8 `.ps1` with a smart quote breaks PS 5.1), no other change.
- **F4 — docs tell the landed truth (P2).** Fix each: `ARCHITECTURE.md:250-252` (samples keep JSON only for `snapshotJsonKeepMinutes`; retention is the four-tier ladder — `retentionHours`/`rollupRetentionDays` are DERIVED mirrors; point at §Tiered History Ladder); `INSTALL.md:251-255` (retention bullets → the ladder, legacy keys derived); `PROGRESS.md` `## Known Limitations` and any "not yet implemented" line that T1–T6 implemented; `docs/sqlite-history-architecture.md:281-283` (the `db pre-image` commands HAVE landed — name them); `docs/adr/0013-tiered-history-ladder.md:3` **Status → `Accepted (2026-08-28, Michel's go 17:20Z) — implemented in Phase 1 (0.3.0: 0.2.7–0.2.11 + T6)`** (append to the status line; never rewrite the decision text); spec §9 wording "idempotent" → "convergent and bounded (a tick promotes ≤ 50 buckets; two ticks at one `now` may promote different batches)"; spec §7 one sentence: the journal-mode/synchronous PRAGMAs run before the pre-image — a header write, no row; §9 one sentence on the replay limit (a replayed sample whose raw row was pruned merges as a late write — collectors never replay); `docs/sqlite-history-architecture.md` a short "WAL facts (measured 2026-08-29)" paragraph: source WAL frames enter the `VACUUM INTO` pre-image; the pre-image is rollback-journal with no WAL sidecar; inspection replays a killed writer's WAL; the CLI leaves the WAL un-checkpointed on exit and SQLite recovers it on the next open; `GUIDE.md` one sentence: browser path validation is advisory, the server (host-native paths) is authoritative. **Luna's wider stale-line list (each must be fixed or shown current):** `ARCHITECTURE.md:39-45,104-120,180-205`; `README.md:20,204,239-240`; `INSTALL.md:251-257`; `PROGRESS.md:375-389`; `docs/guides/API.md:255,291-303,361-386` (the ten dashboard windows, the three detail endpoints, the coverage shape). **Two operator paragraphs for INSTALL.md:** (i) if the first start fails AFTER the pre-image was written but BEFORE the schema commit, the database is still v0 and the pre-image is PROTECTED (`db pre-image remove` refuses while `user_version < 1` — by design: it is your data at that moment); verify the pre-image, move it aside by hand, then retry; (ii) a crash after the post-migration `VACUUM` but before the audit commit makes the next start run `VACUUM` once more — safe, costs minutes. **Spec §8 one sentence:** `root_used` folds are weighted by bucket `sample_count`, exact whenever the root mount is present on every sample of the bucket (the production case); a per-bucket root observation count would be a schema change and is out of scope. **CHANGELOG.md:** APPEND one line per fix under `## Unreleased` — never rewrite earlier entries.

Rules (non-negotiable): worktree only; **git is READ-ONLY**; never open `~/.local/share/tinytop/`; NO new dependencies; no edits outside `agent/assets/dashboard/{app.js,ladder-rules.js,index.html,styles.css}`, `tests/` (Bun tests), `tinytop`, `tinytop.ps1`, `docs/guides/API.md`, `ARCHITECTURE.md`, `INSTALL.md`, `PROGRESS.md`, `GUIDE.md`, `README.md`, `docs/sqlite-history-architecture.md`, `docs/superpowers/specs/2026-08-28-tiered-history-ladder-design.md`, `docs/adr/0013-tiered-history-ladder.md`, `CHANGELOG.md`. Rebuild the Rust agent after asset edits (`cargo build --manifest-path agent/Cargo.toml -p tinytop-agent`) so the embedded assets match; say so.

Gate to run and paste in full: `bun run check` (repo root). Known sandbox limits — say so verbatim and treat the gate as passed-for-you if these are the ONLY failures: the 10 `serve_contract` port binds; Bun's `tests/server.test.ts` bind; `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`. Fable re-runs the gate outside the sandbox and a Playwright checklist before merge. Do not ESCALATE on a sandbox-denied bind.

Report (final message), in order: files changed with line ranges · tests added (names) · RED/GREEN per pure helper · full gate · `git diff --stat` · every doc line you changed (file:line, old → new, one line each) · anything the brief was silent on — do NOT improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
