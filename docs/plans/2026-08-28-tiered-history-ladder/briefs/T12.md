You are hexe lane **T12** for tinytop — the first lane of Phase 5 (plan `docs/plans/2026-08-29-cadence-classes-and-gpu-plan.md`, Task 12; ADR 0021 Accepted). Base: `main` at the commit your worktree was created from (`git rev-parse HEAD` first and quote it; it is the docs-only commit that added this brief on top of `f78ff0e` = v0.5.0 `ce848e6` + the Phase-5 plan/ADR commits). Every file:line below was read at `f78ff0e`. Build ONLY the findings below. Files you may edit are named per finding; NOTHING else. **Git is read-only for you — no commit, no branch, no stash, no `cargo fmt` on the tree (run `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` only and fix by hand); the orchestrator commits.** No edit to `agent/Cargo.toml`, `agent/Cargo.lock`, any `crates/*/Cargo.toml`, `src/`, `legacy/`, `VERSION`, `CHANGELOG.md` (Fable writes the line at merge), `docs/adr/*`, the plan, the spec, `agent/crates/tinytop-store/src/migration.rs` (no schema change — T13 owns v2), `agent/crates/tinytop-store/src/lib.rs` (the store is untouched: `latest_snapshot()` at `lib.rs:1370-1384` STAYS — T14 deletes it), `otel.rs`, `settings_transfer.rs`, `maintenance.rs`, `main.rs`, `vendor/`. Never open, read, stat or write `~/.local/share/tinytop/`, `~/.config/systemd/user/`; never send anything to `127.0.0.1:4274`, `:4317`, `:4318`; `std::env::set_var` is forbidden (child-process `Command::env` is the only way to set a variable in a test). No subprocess in any collector (ADR 0012/0022; `tests/linux_collector.rs:197` enforces it). **Known sandbox limits — not failures:** every `serve_*` test binds a port your sandbox denies (`cargo test -p tinytop-agent -- --skip serve_`); Bun's `tests/server.test.ts` likewise; `tests/tinytop-script.test.ts` needs `XDG_RUNTIME_DIR=/tmp/<random>`; there is no crates.io in the sandbox — everything you need is in the lock and the registry cache (`--offline` works, proven for the two cross-target checks in the gate). If a contract below is wrong against the code you read, or you would be guessing, STOP and reply `ESCALATE: <what, what you tried, where you stopped>` — escalating early beats a wrong build.

**Read first:** plan §2 (the measured facts and decisions 1, 2, 5 — each names the alternative you must NOT reintroduce) and Task 12; ADR 0021 (`docs/adr/0021-cadence-classes-and-typed-history.md`); `agent/crates/tinytop-types/src/lib.rs:62-85` (`CpuTimes`, `CpuSnapshot`) and `:172-184` (`SystemSnapshot`); `agent/crates/tinytop-collectors/src/lib.rs` (the `Collector` trait `:19-21`, the unsupported-platform `NativeCollector` `:32-62`); `linux.rs:19-42` (`LinuxSnapshotSources`), `:59-91` (`LinuxCollector`, `collect`), `:93-147` (`build_linux_snapshot_from_sources`), `:149-195` (`collect_sources` — the 120 ms first-call CPU warm-up at `:161-164` and the `Disks::new_with_refreshed_list_specifics` call at `:166-168` that walks every mount with `statvfs`), `:531-534` (`parse_processes` — `take(10)`), `:590-602` (`refresh_system`), `:713-757` (`sysinfo_process_text` — `take(10)` at `:743`); `common.rs` (all 220 lines: `SysinfoCollector`, `native_filesystems` `:127-149`, `native_processes` `:151-181` — `truncate(10)` at `:179`); `macos.rs`, `windows.rs` (the 25-line wrappers); `tests/linux_collector.rs` (the fixture `:10`, the live tests `:136-148` and `:172-194`, the no-subprocess test `:197-212`); `agent/crates/tinytop-agent/src/writer.rs:104-113` (`AppState`), `:276-323` (`serve` — it reads settings at `:278`, collects once at `:290` BEFORE binding, spawns the loop at `:304`), `:325-345` (`router`), `:442-454` (`latest_snapshot` — reads the store, collects on demand; `collect_snapshot`), `:670-686` (`collect_and_store` — the tick order collect → publish `:675-677` → insert → `get_settings` `:683` → maintain), `:856-867` (`spawn_collection_loop`), `:1308-1314` (`no_store`), `:1339-1398` (`ServeError`, `status_code`, `IntoResponse` → `{"error": …}`), `:1492-1528` (`tests::test_state`), `:1552` (`request_json(app, uri)`), `:2168-2182` (the watch test); `agent/crates/tinytop-store/src/lib.rs:64,114,213-214,253` (`top_process_count`: default 8, validated 1..=50, read by NOTHING today — `grep -rn top_process_count agent/crates` proves it) and `:825-828` (`close`); `retention_ladder.rs:23,74,219-224` (`detail_interval_sec`: default 60, validated 15..=3600); `agent/assets/dashboard/index.html:293-306` (the Filesystem panel heading), `app.js:266-320` (`elements`), `:884-887` (`snapshotCapturedAtMs`), `:1888-1927` (`renderFilesystems`), `:2354` (its call site), `ladder-rules.js:325-347` (`formatOtelTime`, `describeOtelCoverage`); `tests/dashboard-ladder.test.ts:1-22`, `tests/dashboard-process-filesystem.test.ts:1-40`; `README.md:310-315` (Implementation notes) and `:387-404` (`## Persistence` table, row `:400`), `GUIDE.md:87-93` and `:207-219`, `ARCHITECTURE.md:40-57` (Data Flow, steps 10-12) and `:93-95`, `docs/guides/API.md:122-132` and `:498-500`. Work RED → GREEN per finding and paste each RED line and GREEN summary in your final report.

**F1 — `cpu.times` becomes optional (types + producers + fixtures; additive serde).** `CpuSnapshot.times: CpuTimes` (`tinytop-types/src/lib.rs:84`) becomes `pub times: Option<CpuTimes>` with `#[serde(default, skip_serializing_if = "Option::is_none")]`. Meaning (decision 2): `Some` on a live Linux snapshot, `None` where no source exists; T14 will store `None` in history. Producers: `linux.rs:124` → `Some(current_cpu)`; `common.rs:69` → `None` with a one-line comment (sysinfo exposes no tick breakdown; the previous `CpuTimes::default()` was twelve fake zeros). Fixtures (mechanical `Some(CpuTimes::default())`, nothing else in those files): `tinytop-store/tests/ladder_maintenance.rs:1150`, `migration_v1.rs:643`, `sqlite_history_store.rs:54`, `history_json_contract.rs:41`, `retention_settings.rs:1159`. `otel.rs`'s fixture is JSON (`otel.rs:528`) and reads only `cpu.usage_percent` (`:390`) — expected: no edit there; if the compiler forces one, make the minimal fixture change and report it. Test (`tinytop-types/src/lib.rs`, a new `#[cfg(test)] mod tests` — `serde_json` is already a dev-dependency): `cpu_times_and_filesystems_captured_at_are_optional_and_additive` — a `SystemSnapshot` JSON document WITHOUT `cpu.times` and WITHOUT `filesystemsCapturedAtMs` deserializes (`times == None`, `filesystems_captured_at_ms == None`) and re-serializes without either key; the same document WITH both round-trips byte-for-byte through `serde_json::Value` equality. RED today: the document without `cpu.times` fails with `missing field \`times\`` — paste it.

**F2 — `filesystemsCapturedAtMs` on the snapshot (types).** `SystemSnapshot` (`tinytop-types/src/lib.rs:174-184`) gains `pub filesystems_captured_at_ms: Option<i64>` (camelCase → `filesystemsCapturedAtMs`), `#[serde(default, skip_serializing_if = "Option::is_none")]`: the Unix time in milliseconds at which the `filesystems` list was last enumerated by the collector (F3). `None` only from a producer that never enumerated (the Bun runtime, JSON written before this change). `SystemSnapshot` derives no `Default`, so every struct literal gains the field: `linux.rs:110` and `common.rs:49` (the real value from F3), and the fixtures `ladder_maintenance.rs:1132`, `migration_v1.rs:625`, `history_json_contract.rs:23`, `retention_settings.rs:1141`, `sqlite_history_store.rs:36` (`None`; `snapshot_with_runtime` at `:528` derives from `snapshot` and needs nothing). Covered by F1's test.

**F3 — `CollectorConfig`, `configure()`, and the fast / slow / static split (collectors).** `tinytop-collectors/src/lib.rs`: `#[derive(Debug, Clone, PartialEq, Eq)] pub struct CollectorConfig { pub top_process_count: usize, pub filesystems_interval: Duration }` with `impl Default` = `{ 8, Duration::from_secs(60) }` (doc-comment: these mirror the store's `topProcessCount` default `lib.rs:114` and `detailIntervalSec` default `retention_ladder.rs:74`; the daemon always calls `configure` from settings before its first collection (F5), so the defaults govern only `tinytop-agent collect --json` (`main.rs:229`) and tests); the trait becomes `pub trait Collector { fn configure(&mut self, config: CollectorConfig); fn collect(&mut self) -> CollectorResult<SystemSnapshot>; }` — `collect` keeps its signature; the unsupported-platform `NativeCollector` (`:45-62`) gets a no-op `configure`. **Contract of `configure`:** it stores the config; a `top_process_count` change applies from the next `collect`; a `filesystems_interval` change never resets the slow cache or its `taken_at` — the next slow tick is due when `now − taken_at ≥ new interval` (a shorter interval can make it due at once); calling it with an equal config is a no-op. **`linux.rs`:** `LinuxCollector` gains `config: CollectorConfig`, a slow cache (`Option<{ taken_at: Instant, captured_at_ms: i64, sources: LinuxSlowSources }>`), an injectable monotonic clock `Box<dyn FnMut() -> Instant + Send>` (`Default` uses `Instant::now`; add `#[doc(hidden)] pub fn with_clock(clock: impl FnMut() -> Instant + Send + 'static) -> Self` for tests) and a `#[doc(hidden)] pub fn slow_enumerations(&self) -> u64` counter incremented on every slow enumeration (the strace-free proof the tests and the orchestrator's acceptance rely on). Split `collect_sources` (`:149-195`) into `pub fn collect_fast_sources(system: &mut System, previous_proc_stat_text: Option<&str>, top_process_count: usize) -> CollectorResult<LinuxFastSources>` (timestamp, `cpu_count`, `uptime_text`, `meminfo_text`, `loadavg_text`, both proc-stat texts incl. the first-call 120 ms warm-up, the three pressure texts, `ps_text` with `top_process_count` — the fast class = identity's uptime, cpu, memory, swap, load, pressure, processes) and `pub fn collect_slow_sources() -> CollectorResult<LinuxSlowSources>` (`captured_at_ms` = Unix ms at enumeration, `hostname`, `platform`, `arch`, `os_release_text`, `proc_version`, `kernel_release`, `wsl_distro_name`, `wsl_interop`, `df_blocks_text`, `df_inodes_text` — the `Disks::new_with_refreshed_list_specifics` call moves here and becomes the ONLY `statvfs` site; the static class = identity, re-read on the slow tick). Keep `LinuxSnapshotSources` as the merged struct (+ `pub filesystems_captured_at_ms: i64`) so `build_linux_snapshot_from_sources(sources)` keeps its signature and `tests/linux_collector.rs:10`'s fixture needs only the new field; keep `pub fn collect_sources(system, previous)` as the one-shot convenience that calls both and merges (used by `tests/linux_collector.rs:178`; it enumerates every call by design — say so in its doc-comment). `LinuxCollector::collect`: `let now = (self.clock)();` slow is due when the cache is `None` (first call ALWAYS due — contingency: never add a warm-up sleep) or `now.duration_since(taken_at) >= self.config.filesystems_interval`; when due, enumerate and replace the cache; then fast sources → merge (cloning the cached slow sources) → build; `filesystems_captured_at_ms: Some(cache.captured_at_ms)`, `times: Some(current_cpu)`. `top_process_count` replaces BOTH hard-coded tens — `sysinfo_process_text(system, total_memory, top_process_count)` (`:743`) and `parse_processes` (`:534`; drop its cap or pass the count — the text already holds ≤ N lines); clamp the count `.max(1)` (the store validates 1..=50; the clamp is the belt-and-braces invariant, one comment). **`common.rs`:** the same config + slow cache for `SysinfoCollector` (`configure`, `taken_at`, `captured_at_ms`, the cached `Vec<FilesystemSnapshot>` from `native_filesystems` and the cached static identity `hostname`/`distro`/`kernel`; `uptime` stays fast) and `native_processes(system, top_process_count)` replacing `truncate(10)` (`:179`); the `Disks::new_with_refreshed_list_specifics` call (`:39-41`) runs only on the slow tick; `filesystems_captured_at_ms: Some(...)`. `macos.rs` / `windows.rs`: `configure` forwards to `inner`; `collect` unchanged. **Tests (`tests/linux_collector.rs`, guarded `if std::env::consts::OS != "linux" { return; }` like `:136`):** (a) `filesystems_are_served_from_cache_between_slow_ticks` — `with_clock` driven by `let base = Instant::now(); let offset = Arc<std::sync::Mutex<Duration>>` (`base + *offset`), default config; collect #1 → `filesystems_captured_at_ms == Some(t1)`, `slow_enumerations() == 1`; advance 30 s; collect #2 → `filesystems == #1.filesystems` (`FilesystemSnapshot: PartialEq`), `filesystems_captured_at_ms == Some(t1)`, `slow_enumerations() == 1`, and `timestamp != #1.timestamp` (nanosecond RFC 3339 — the fast class was re-read); (b) `slow_tick_re_enumerates_after_the_interval` — advance to 61 s → `slow_enumerations() == 2`, `filesystems_captured_at_ms > Some(t1)`; (c) `configure_changes_the_interval_without_resetting_the_cache` — after collect #1, `configure(same)` + collect at 30 s → still 1; `configure(interval 10 s)` + collect at 30 s → 2 (due from the cached `taken_at`, not from the configure call); (d) `top_process_count_is_honoured` — `configure(1)` → `processes.len() == 1`; `configure(8)` → `<= 8`; `configure(50)` → `<= 50` (the sandbox has more than one process; if a bound cannot hold there, say why). RED = the compile error on `configure` / `with_clock` / `slow_enumerations` (paste the first `E0599`/`E0425` line), GREEN after. Cross-target proof of `common.rs` (the sandbox cannot run it; both checks pass offline on the orchestrator's box in 16 s and 4 s): `cargo check --manifest-path agent/Cargo.toml -p tinytop-collectors --no-default-features --features windows-collector --target x86_64-pc-windows-msvc --offline` and `… --features macos-collector --target aarch64-apple-darwin --offline` — paste both `Finished` lines.

**F4 — `/api/snapshot` answers from memory (agent).** `latest_snapshot` (`writer.rs:442-449`) reads `state.store.latest_snapshot()` and collects on demand when the table is empty — a JSON round-trip per dashboard poll for data the daemon already holds in `state.latest_snapshot` (`:112`, published at `:675-677`). Contract (decision 5): `let snapshot = state.latest_snapshot.borrow().clone();` (clone the `Arc` out of the `watch` `Ref` before anything else, as the exporter does at `:754`); `Some(snapshot)` → `no_store(Json(snapshot.as_ref()))` (serde has no `rc` feature in this workspace — serialize through a reference or clone; no `Cargo.toml` edit); `None` → `Err(ServeError::NoSnapshotYet)`: a new unit variant, `status_code` → `StatusCode::SERVICE_UNAVAILABLE`, `Display` exactly `no snapshot yet` (the existing envelope yields `{"error":"no snapshot yet"}`), `source` → `None`. The handler no longer touches the store and no longer collects; both routes bound to it (`/snapshot/latest` `:328`, `/api/snapshot` `:336`) change together; `/snapshot/collect` (`:329`, `collect_snapshot`) keeps collecting on demand; `read_history_with_backfill` (`:653-664`) is out of scope (T14). The store's `latest_snapshot()` stays (T14) — after this change `grep -n 'latest_snapshot()' agent/crates/tinytop-agent/src/*.rs` must show no production hit; say so. Tests (`writer.rs` `cfg(test)`, `test_state` + `request_json(router(state.clone()), …)`): (a) `snapshot_route_answers_503_before_the_first_collection` — a fresh state: `/api/snapshot` → `503`, body `== json!({"error":"no snapshot yet"})`; RED today: the handler collects on demand and answers 200 — paste; (b) `snapshot_route_answers_from_memory_with_the_store_closed` — `collect_and_store` once (keep the returned sample), then `state.store.clone().close().await` (sqlx closes the shared pool for every clone), prove the store is really down (`/api/history/coverage` → 500), then `/api/snapshot` → 200 with `timestamp == sample.snapshot.timestamp` and `filesystemsCapturedAtMs` a number, and `/snapshot/latest` → the same body; RED today: 500 from the closed store — paste. The daemon's startup order makes the 503 practically unreachable in production (`serve` collects at `:290` before binding at `:312`) — state that in the handler's comment.

**F5 — `collect_and_store` re-configures the collector only when the settings changed (agent).** `AppState` (`:105-113`) gains `collector_config: Arc<Mutex<Option<CollectorConfig>>>` (tokio `Mutex`, like `otel`), constructed `None` at `serve` (`:280-288`) and `test_state` (`:1499-1526`). Free function `fn collector_config_from(settings: &DashboardSettings) -> CollectorConfig` in `writer.rs`: `top_process_count = usize::try_from(settings.top_process_count).unwrap_or(1).max(1)`, `filesystems_interval = Duration::from_secs(u64::try_from(settings.retention_ladder.detail_interval_sec).unwrap_or(60).max(1))` — the store validates both ranges on every read (`lib.rs:253`, `retention_ladder.rs:219-224`); the clamps are the belt-and-braces invariant, one comment in the style of the OTel interval clamp. `async fn configure_collector_if_changed(state: &AppState, settings: &DashboardSettings)`: lock `collector_config`; if `*applied == Some(desired)` return; else lock `collector` (lock order config → collector; `collect_and_store` locks `collector` alone, so no cycle), call `configure(desired.clone())`, store `Some(desired)`; comment the lock order at the function. Call sites: `serve`, right before the first `collect_and_store` at `:290`, with the `settings` already read at `:278` (so the first collection already honours `topProcessCount` and the filesystem interval); `collect_and_store`, right after `let settings = state.store.get_settings().await?;` at `:683` (pass `&settings` to `maintain_history` as today). **The tick order stays collect → publish → insert → settings → maintain → configure:** the memory-backed `/api/snapshot` (F4) and the exporter must keep receiving fresh snapshots while the store fails (ARCHITECTURE:95's "never delays collection"), so the settings read stays behind the publish, and a settings change reaches the collector one tick (≤ `pollIntervalMs`) later — F6 documents that. Also add `#[doc(hidden)] pub fn configure_calls(&self) -> u64` to `LinuxCollector` (F3's file; the same counter pattern) — the instrument for test (b). Tests (`writer.rs` `cfg(test)`, Linux): (a) `collector_is_configured_from_settings_before_the_first_collection` — `put_settings` with `topProcessCount: 3` (read `put_settings`'s signature at `lib.rs:870` and pass what it needs), `configure_collector_if_changed(&state, &settings)`, `collect_and_store` → `sample.snapshot.processes.len() == 3` (the Linux sandbox always has more than three processes; if not, say so and assert `<=`); (b) `collect_and_store_reconfigures_only_when_the_settings_changed` — default settings; `configure_collector_if_changed` once (the `serve` step) → `configure_calls() == 1`; two `collect_and_store` ticks → still 1 (no blind per-tick call); `put_settings` changing only `topProcessCount` to 3; tick 3 → `configure_calls() == 2` and tick 3's snapshot still has the default count (collected before the settings read); tick 4's snapshot has exactly 3 — the "next tick" contract proven. RED = the compile error on `collector_config` / `configure_collector_if_changed` (paste), GREEN after.

**F6 — docs (Rust doc-comment + four documents; no behaviour).** `retention_ladder.rs:23` gets a `///` doc-comment: the filesystem check interval in seconds — the collector's slow cadence class: mounts are re-enumerated (one `statvfs` per mount) and `filesystemsCapturedAtMs` re-stamped once per interval, the cached list is served between checks — and, until the typed-history tasks (T13/T14) land, also the cadence of the typed filesystem/process detail rows; 15..=3600; the validation message at `:220` is unchanged. `README.md`: row `:400` → `Filesystem check interval (collector slow class; also the typed detail-row cadence until the typed-history migration); 15–3,600 seconds`; under `Implementation notes` (`:310-315`) three bullets — the three cadence classes (fast = CPU, memory, swap, load, pressure, processes, uptime on every `pollIntervalMs` tick; slow = filesystems every `retentionLadder.detailIntervalSec`, served from cache between checks and stamped `filesystemsCapturedAtMs`; static = hostname/kernel/distro re-read on the slow tick), `/api/snapshot` answered from the daemon's memory (`503 {"error":"no snapshot yet"}` only before the first collection; the daemon collects once before it binds), and `topProcessCount` now effective (default 8; the previous hard-coded 10 is gone; `collect --json` honours the default; a change applies on the next collection tick). `docs/guides/API.md` `:122-132` (`GET /api/snapshot`): the memory rule and the 503, `filesystemsCapturedAtMs` (Unix ms; older than `timestamp` between filesystem checks), `cpu.times` optional (present on the Linux collector, absent on the sysinfo-based macOS/Windows collectors), `processes.length ≤ topProcessCount`; `:498-500` (`/snapshot/latest`): same handler, same rule (the "collects and stores one before responding" sentence is no longer true — replace it); the settings fields near `:208` (`topProcessCount`: 1–50, effective on the next tick) and `:406` (`detailIntervalSec`: the filesystem check interval). `GUIDE.md:93` (the ladder sentence): `typed detail cadence` → `the filesystem check interval (`Disk check every`, also the typed detail cadence)`; `:207-219` (`## Refresh Behavior`): after step 6 one sentence — the daemon's own collection tick is `pollIntervalMs`; filesystems are re-checked every `detailIntervalSec` (default 60 s) and the Filesystem panel shows `as of hh:mm:ss` when its rows are older than one poll; and one sentence that a saved `Top processes` count applies on the daemon's next collection tick. `ARCHITECTURE.md:52` → `tinytop-agent serve` answers `/api/snapshot` from the latest snapshot published by the collection task (503 before the first collection); `:93` (the collector paragraph): the three classes, the cache, `filesystemsCapturedAtMs`, and the consequence — `statvfs` runs once per mount per `detailIntervalSec` instead of once per mount per tick; `:95`: `collect_and_store` re-configures the collector when `topProcessCount` or `detailIntervalSec` changed. No CHANGELOG (Fable writes the line at merge), no ADR, no spec edit (T17 appends §17).

**F7 — dashboard: the Filesystem panel says "as of hh:mm:ss" when its rows are older than one poll (Bun-neutral).** `index.html:304`: after `#filesystem-count` add `<span class="mini-stat" id="filesystem-captured-at" hidden></span>`. `ladder-rules.js`: `export function describeFilesystemFreshness(snapshot, pollMs)` → `null` unless `Number.isFinite(snapshot?.filesystemsCapturedAtMs)` and `Date.parse(snapshot.timestamp)` is finite and `Date.parse(snapshot.timestamp) − snapshot.filesystemsCapturedAtMs > pollMs` (strictly more than one poll); else the string `as of ${formatOtelTime(snapshot.filesystemsCapturedAtMs)}` — reuse the module-private `formatOtelTime` (`:325`), do not rename it. `app.js`: `filesystemCapturedAt: document.querySelector("#filesystem-captured-at")` in `elements` (`:266-320`, next to `filesystemCount` `:315`); a new `function renderFilesystemFreshness(snapshot)` that sets the text and toggles `hidden` (hidden when `null`); called right after `renderFilesystems(snapshot.filesystems)` at `:2354` so both the live poll and the history replay (which passes `/api/history` `.snapshot` objects to the same renderers) carry it; the Bun runtime never sets the field → hidden. Tests: `tests/dashboard-ladder.test.ts` — import `describeFilesystemFreshness`; four cases: field absent → `null`; equal to the snapshot time → `null`; exactly `pollMs` older → `null` (the boundary is strict); `pollMs + 1` older → `as of ` + the expectation computed with the SAME formatter the production code uses (`new Date(ms).toLocaleTimeString([], { hour: "2-digit", minute: "2-digit", second: "2-digit", hour12: false })` — the locale rule from P4-fix1 F9); `tests/dashboard-process-filesystem.test.ts` — `html` contains `id="filesystem-captured-at"`, `app` contains `filesystemCapturedAt: document.querySelector("#filesystem-captured-at")` and `function renderFilesystemFreshness`. RED = the failing import / `toContain` (paste one of each), GREEN after.

**Gate before you report:** `cargo fmt --manifest-path agent/Cargo.toml --all -- --check` clean; `cargo clippy --manifest-path agent/Cargo.toml --workspace --all-targets -- -D warnings` clean; `cargo test --manifest-path agent/Cargo.toml -p tinytop-types -p tinytop-collectors -p tinytop-store` and `cargo test --manifest-path agent/Cargo.toml -p tinytop-agent -- --skip serve_` green (paste every `test result:` line; the 0.5.0 release gate counted 252 passing across 21 suites — expect the same plus F1's one, F3's four, F4's two, F5's two); the two cross-target `cargo check … --offline` `Finished` lines (F3); `bun test tests/dashboard-ladder.test.ts tests/dashboard-process-filesystem.test.ts` green (paste); `grep -n 'take(10)\|truncate(10)' agent/crates/tinytop-collectors/src/*.rs` empty; `grep -n 'latest_snapshot()' agent/crates/tinytop-agent/src/*.rs` shows no production hit; `git status --short` and `git diff --stat` pasted (files ⊆ the F-lists: `tinytop-types/src/lib.rs`; `tinytop-collectors/src/{lib,linux,common,macos,windows}.rs` + `tests/linux_collector.rs`; `tinytop-agent/src/writer.rs`; `tinytop-store/src/retention_ladder.rs` (doc-comment only) + the five store test files (fixture lines only); `agent/assets/dashboard/{index.html,app.js,ladder-rules.js}`; `tests/dashboard-ladder.test.ts`, `tests/dashboard-process-filesystem.test.ts`; `README.md`, `GUIDE.md`, `ARCHITECTURE.md`, `docs/guides/API.md`). Final report: per finding RED line(s) → GREEN summary; the nine new test names; the two cross-target `Finished` lines; the base commit you quoted; anything you could not do with the exact reason. The orchestrator's own acceptance (outside your sandbox, on a temp daemon on `:4290` with a scratch DB): `filesystemsCapturedAtMs` changes only every 60 s while `timestamp` changes every 1.5 s, `topProcessCount 3` yields three rows on the next tick, and the per-mount `statvfs` count over 60 s is ≈ 27 × 2, not 27 × 40.
