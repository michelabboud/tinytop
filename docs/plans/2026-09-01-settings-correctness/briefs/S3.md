You are hexe lane **S3** for tinytop. Your base is `main` at `f573f7e` (= `v0.7.1`). You implement **D3 (frontend)**, **M1** and **M2** of `docs/plans/2026-09-01-settings-correctness-plan.md`.

Every file:line below was **re-verified at `f573f7e` by Fable before this brief was written**, and several claims in the plan and in `docs/reports/2026-08-31-settings-defect-inventory.md` are now **out of date** — T18b changed this file after they were written. Where they disagree with this brief, this brief is right. If a line does not say what this brief claims, STOP and report it.

Read first: `CLAUDE.md`; `docs/plans/2026-09-01-settings-correctness-plan.md` §D3; ADR 0026 (decision 4, the threshold band); ADR 0027 (the tabbed settings shell you are working inside).

Scope rules: edits ONLY in `agent/assets/dashboard/ladder-rules.js`, the Bun test files under `tests/`, and `CHANGELOG.md`. **Untouchable:** `agent/crates/**` (that is lane S1, running concurrently — the backend is the authority here and must not be "aligned" toward the client), `agent/assets/dashboard/app.js`, `index.html`, `styles.css`, `PROGRESS.md`, `docs/adr/**`.

## The rule you are implementing

**The backend is authoritative.** It is the validator that can actually refuse a write, so the client mirrors it exactly — same rule firing, same words. A client that disagrees with the server means the message an operator sees depends on which validator ran first.

## F1 — the client must evaluate the rules in the backend's order

This is the part the plan missed, so read it carefully.

The backend (`agent/crates/tinytop-store/src/thermal_settings.rs:25-58`) checks the count once, then loops **per chip, in array order**, and returns on the first offending *element*: pattern (`:34-43`) → reserved (`:44-49`) → duplicate (`:50-54`).

The client (`ladder-rules.js:371-379`) checks each rule across the **whole array** in turn: count → `pattern.some` → `reserved.some` → duplicate.

These are not equivalent. For `["cpu_a", "cpu_a", "amdgpu"]` the backend reports **duplicate** (reached on the second element) and the client reports **reserved** (found by scanning the whole array for rule 3). Aligning only the strings would leave this in place.

**Restructure the client validator to walk the array once, per element, mirroring the backend**: count first, then for each chip in order — pattern, reserved, duplicate — returning the first failure. Keep returning an array of messages so the call sites are unchanged.

## F2 — adopt the backend's exact strings

Three of the four already-existing constants diverge. The pattern message (`ladder-rules.js:363`) **already matches** the backend (`thermal_settings.rs:41`) — leave it exactly as it is.

| constant | current client text | replace with (backend, verbatim) | backend site |
|---|---|---|---|
| `THERMAL_CHIP_COUNT_ERROR` (`:364`) | `thermal.extraChips must hold at most 16 entries` | `thermal.extraChips accepts at most 16 chip names` | `:28` |
| `THERMAL_CHIP_DUPLICATE_ERROR` (`:365`) | `thermal.extraChips must not contain duplicates` | `thermal.extraChips contains duplicate chip name "<chip>"` | `:52` |
| `THERMAL_RESERVED_*` (`:367`) | `thermal.extraChips must not contain reserved non-CPU chips: amdgpu, i915, nvme` | `thermal.extraChips must not name a chip already reported elsewhere: amdgpu, i915, nvme` | `:46` |

The duplicate message **names the offending chip** — the backend interpolates it (`format!`), so the client must too, with the same `"` quoting. It therefore stops being a plain constant; make it a function of the chip name. This is a real improvement, not a cosmetic swap: the operator learns *which* entry is duplicated.

Take the strings from the Rust source, not from this table, and paste what you copied into your report so the match can be checked character by character.

## F3 — M1: mirror the whole threshold band

`usableSensorThreshold` (`ladder-rules.js:127-129`) is `Number.isFinite(value) && value > 0`. ADR 0026 decision 4 defines the usable band as `0 < t <= 200 °C`, and the collector filters on it before serialising. The client mirrors only the lower half.

Add the upper bound. Consequence to preserve deliberately: an out-of-band ceiling makes `sensorBarPercent` return `null` (no bar) rather than scaling every reading against a nonsense maximum — which is what a `temp2_max` of `65261850` (observed on sheep's nvme) would otherwise do, rendering a permanently-empty bar with no explanation.

Unreachable from our own backend today; this is defence against any other producer of the same shape.

## F4 — M2: a reading with no chip must not head a group called `undefined`

`groupSensorsByChip` (`ladder-rules.js:143-152`) does `const chip = sensor?.chip` at `:147` with no guard and uses it directly as a `Map` key, so a reading without `chip` produces a group whose heading renders as the literal string `undefined`.

**Group such readings under `"unknown"`** — a real label. Do **not** silently drop them: losing a reading is worse than labelling it, and an operator seeing `unknown` can act on it. Treat any non-string or empty-string `chip` the same way. Unreachable from our own backend (chips with an unreadable `name` are skipped), so this is defensive only.

## F5 — tests

`tests/dashboard-thermal.test.ts` and `tests/dashboard-settings.test.ts` already import from `ladder-rules.js`; add to whichever fits each case.

- **F1 ordering** — `["cpu_a", "cpu_a", "amdgpu"]` yields the **duplicate** message (naming `cpu_a`), not the reserved one. This is the test that fails before your change; paste RED and GREEN.
- **F2 parity** — one case per rule (count with 17 entries, pattern with `AMDGPU`, reserved with `amdgpu`, duplicate with a repeat) asserting the exact backend strings.
- **F3** — a threshold of `65261850` is unusable: `sensorBarPercent` returns `null` and `sensorSeverity` returns `"normal"`. A threshold of `95` still works.
- **F4** — a sensor with no `chip`, and one with `chip: ""`, both land in a single `"unknown"` group and no group key is the string `undefined`.

## Rules (non-negotiable)

- git **READ-ONLY** — no add/commit/push/checkout/stash/branch. Leave the tree dirty; report `git diff --stat`. Fable commits.
- Never bind a socket, start a daemon, or reach the network. Never open `~/.local/share/tinytop/`.
- Do not add a lint script. There is deliberately no dashboard linter; do not introduce one.
- Do not edit the Rust validator to meet the client halfway. The backend is the authority; if you believe a backend string is wrong, report it, do not change it.

## Gate

**`bun test` only.** Do **not** run `bun run check:bun` or `bun run check` — they start a server and will fail inside containment (this is exactly what made lane T18b escalate; it is a limitation of the sandbox, not of your work).

**Baseline to beat, measured by Fable on `f573f7e` immediately before this dispatch: 261 pass / 0 fail across 22 files.** Your run must show 261 + your new tests and 0 fail. Report the file count too, and if it is not 22 say so.

## Report (final message, in order)

Files changed with line ranges · the RED and GREEN transcripts for F1 · the strings you copied out of `thermal_settings.rs`, verbatim · full `bun test` output with pass/fail/file counts · `git diff --stat` · anything this brief was silent on — do **not** improvise around it, report it.

**Note:** lane S1 is running concurrently and also adds a `## Unreleased` bullet to `CHANGELOG.md`. A conflict there is expected and Fable resolves it at merge by keeping both. Add your bullet and do not attempt to coordinate.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
