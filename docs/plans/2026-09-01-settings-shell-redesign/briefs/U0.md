You are hexe lane **U0** for tinytop. Your base is `main` at `85e7086` (= `v0.8.1`). You implement **ADR 0032** — `docs/adr/0032-one-switch-implementation.md`.

**Read ADR 0032 first and in full.** It is the contract. This is a small, surgical CSS change with a visible blast radius: **every switch in the settings dialog changes appearance.** Do not extend the scope.

Read also: `CLAUDE.md`; ADR 0028 (the decision being amended — **do not edit it**); ADR 0029 decision 4 (why competing selectors keep winning in this file).

Scope rules: edits ONLY in `agent/assets/dashboard/styles.css` and `CHANGELOG.md`. **Untouchable:** `index.html`, `app.js`, `ladder-rules.js`, every Rust crate, `docs/adr/**`, `PROGRESS.md`, `VERSION`, `Cargo.*`.

## The defect

`styles.css` has two switch implementations that do **not** override each other, because they draw the knob with different pseudo-elements — so every toggle renders **two knobs** travelling different distances:

- ADR 0028's `.settings-dialog input[type="checkbox"]` (`:1429`), specificity (0,1,1) — knob via **`::after`** (`:1445`), `0.8rem`, `translate: calc(0.99rem - 2px)`, track `2.15rem × 1.2rem`, on-state `--cyan`.
- Legacy `.settings-group .toggle-row input[type="checkbox"]` (`:1528`), specificity **(0,2,1) — wins** — knob via **`::before`** (`:1544`), `16px` at `left: 3px`, `transform: translateX(18px)`, track `42px × 24px`, on-state `--amber`.

## F1 — delete the legacy block

Remove **`:1528`–`:1572`**: the `.settings-group .toggle-row input[type="checkbox"]` rule and its `::before`, `:checked`, `:checked::before` and `:focus-visible` companions. Verify the boundaries before cutting — the block ends with the `:focus-visible` rule immediately before `#effective-settings-readout`.

**These must SURVIVE — they style the row, not the control:**
- `:1491` `.settings-group .toggle-row { display: grid; … }`
- `:1502` `.history-ladder-settings-group .toggle-row { … }`

If you delete either, the toggle rows lose their layout. Confirm both are still present in your diff.

## F2 — the surviving rule adopts the legacy geometry

ADR 0032 decision 2: the single remaining rule (`:1429` + `:1445` + `:1465`) takes the **legacy dimensions**, because that is the size the user has been looking at since v0.1.31 and 24 px is a better pointer target than 19 px:

- track `42px × 24px` (replacing `2.15rem × 1.2rem`)
- knob `16px × 16px` (replacing `0.8rem`)
- knob offset `3px` from the inline start (replacing `0.18rem`)
- checked travel **`18px`** (replacing `calc(0.99rem - 2px)`)

**Keep the `::after` pseudo-element, the `translate` property, and the vertical centring** (`inset-block-start: 50%` + the `-50%` in `translate`) — do not port the legacy's `top: 3px` / `transform` approach. One knob, ADR 0028's mechanism, legacy's numbers.

**Keep ADR 0028's colours**: on-state `--cyan` background with a `--surface` knob, `--control-active-border` border. **Do not** carry `--amber` across — ADR 0032 decision 3 explains why (`--cyan` is remapped per theme; `--amber` pins one hue).

Preserve the existing `:hover`, `:focus-visible`, `:disabled` and `prefers-reduced-motion` rules on the surviving selector.

## F3 — leave a marker for the next contributor

Add one short comment above the surviving rule saying it is the **only** switch implementation, that a more specific rule added elsewhere will silently win, and citing ADR 0032. This file has now produced that failure three times; the comment is cheap insurance.

## F4 — `CHANGELOG.md`

One bullet under a new `## Unreleased`: what was wrong (two implementations, different pseudo-elements, two knobs on all 13 switches), what changed (one rule, legacy geometry, ADR 0028's colour), and that the on-state hue now follows the theme accent.

## Rules (non-negotiable)

- git **READ-ONLY** — no add/commit/push/checkout/stash/branch. Leave the tree dirty; report `git diff --stat`. The orchestrator commits.
- Do not bump `VERSION` or any crate version.
- Never bind a socket, start a daemon, or reach the network. Never open `~/.local/share/tinytop/`.
- Do not add a lint script. There is deliberately no dashboard linter.

## Gate

**`bun test` only** — do **not** run `bun run check:bun` or `bun run check`; they start a server and cannot bind inside containment.

Known sandbox limits, state them verbatim and treat as passed-for-you if they are the ONLY failures:
1. `tests/server.test.ts` — `Bun.serve({ port: 0 })` → `EADDRINUSE`.
2. `tests/tinytop-script.test.ts` — needs `XDG_RUNTIME_DIR`.

Baselines: **outside containment 264 pass / 0 fail across 22 files**; **inside containment 263 pass / 1 fail across 22 files** (limit 1 above). Report pass/fail/file counts explicitly and name every failing test. If the file count is not 22, say so.

**Expect no test-count change.** This is CSS; no Bun test asserts on these rules. If you find one that does, say so rather than editing it to suit.

## What you cannot verify, and must not claim

A unit test cannot see two knobs. **You cannot prove this fix works** — the acceptance is a rendered check of every switch in both themes, on and off, which the orchestrator runs outside the sandbox. Report what you changed and what the gate says; **do not assert the visual result.**

## Report (final message, in order)

Files changed with line ranges · confirmation that `:1491` and `:1502` survive · the surviving rule's final declarations, pasted · full `bun test` output with pass/fail/file counts · `git diff --stat` · anything this brief was silent on — do **not** improvise around it.

You are on the lowest model expected to handle this. If the task exceeds you — stuck after a real attempt, looping, or about to guess — stop and reply `ESCALATE: <what is beyond you, and what you tried>`. Escalating early is cheaper than a wrong answer.
